# Project Status

- Last verified main commit: `af40ff3` (merge of PR #166, `claude/next-level-xm8lpi` — the backfill for PR #165, docs-only; `ADR-0030`/`ADR-0031` and their design docs accepted as proposed, docs-only; `SERVER-MTLS-CLASS` / `SERVER-001` v0.21.0 / FR-031 implemented, all three bundled into one PR since the branch stayed open across them). Immediately preceding checkpoints, same session, newest first: `6424fea` (merge of PR #165, same branch — `SERVER-ACCESS-LOG-DESIGN` / `ADR-0031` proposed, docs-only); `6e50891` (merge of PR #164, same branch — backfill for PR #163, docs-only); `24690db` (merge of PR #163, same branch — `SERVER-SESSION-VALIDATE-ON-STAGE` / `SERVER-001` v0.20.0 / FR-030 implemented); `2cbdeda` (merge of PR #162, same branch — backfill for PR #161, docs-only); `5ff5521` (merge of PR #161, same branch — `SERVER-AUTH-RATE-LIMIT-DESIGN` / `ADR-0030` proposed, docs-only); `fce0762` (merge of PR #160, same branch — backfill for PR #159, docs-only); `0f317e3` (merge of PR #159, same branch — `SERVER-AUTH-AUDIT` / `SERVER-001` v0.19.0 / FR-029 implemented); `4034f67` (merge of PR #158, same branch — backfill for PR #157, docs-only); `ab4e251` (merge of PR #157, same branch — `SERVER-SESSION-READ-YOUR-WRITES` / `SERVER-001` v0.18.0 / FR-028 implemented); `c66abc5` (merge of PR #156, same branch — backfill for PR #155, docs-only); `a73316f` (merge of PR #155, same branch — `SERVER-JOURNAL-GROUP-COMMIT` / `SERVER-001` v0.17.0 / FR-027 implemented); `efd650f` (merge of PR #154, same branch — backfill for PR #153, docs-only); `b56041f` (merge of PR #153, same branch — `ADR-0026`–`ADR-0029` and their design docs accepted as proposed, docs-only); `861dbf0` (merge of PR #152, same branch — backfill for PR #151, docs-only); `99ea6e3` (merge of PR #151, same branch — `SERVER-AUTH-AUDIT-DESIGN` / `ADR-0029` proposed, docs-only); `c8ab774` (merge of PR #150, same branch — backfill for PR #149, docs-only); `0489b5e` (merge of PR #149, same branch — `SERVER-MTLS-CLASS-DESIGN` / `ADR-0028` proposed, docs-only); `d51cf57` (merge of PR #148, same branch — backfill for PR #147, docs-only); `3fe7722` (merge of PR #147, same branch — `SERVER-SESSION-READ-YOUR-WRITES-DESIGN` / `ADR-0027` proposed, docs-only); `97da28c` (merge of PR #146, same branch — backfill for PR #145, docs-only); `8edaf7e` (merge of PR #145, same branch — `SERVER-JOURNAL-GROUP-COMMIT-DESIGN` / `ADR-0026` proposed, docs-only); `3987f9c` (merge of PR #144, same branch — backfill for PR #143, docs-only); `170ebd8` (merge of PR #143, same branch — `SERVER-CLIENT-HELLO-FALLBACK` / `SERVER-001` v0.16.0 / FR-026 implemented); `ab56852` (merge of PR #142, same branch — backfill for PR #141, docs-only); `8fc9987` (merge of PR #141, same branch — `SERVER-TRANSACTION-JOURNAL` / `SERVER-001` v0.15.0 / FR-025 implemented); `351c653` (merge of PR #140, same branch — backfill for PR #139, docs-only); `25d3a27` (merge of PR #139, same branch — `SERVER-TRANSACTION-SESSION` / `SERVER-001` v0.14.0 / FR-024 implemented, plus the backfill for PR #138); `dd0142b` (merge of PR #138, same branch — `SERVER-TRANSACTION-SESSION-DESIGN` / `ADR-0024` / `ADR-0025` accepted as proposed, docs-only, plus the backfill for PR #137); `cba147d` (merge of PR #137, same branch — `SERVER-TRANSACTION-SESSION-DESIGN` / `ADR-0024` / `ADR-0025` proposed, docs-only); `371b421` (merge of PR #136, same branch — backfill for PR #135, docs-only); `f0bfbbb` (merge of PR #135, same branch — `SERVER-MTLS` / `SERVER-001` v0.13.0 / FR-023 implemented, plus the backfill for PR #134); `ca45a3c` (merge of PR #134, same branch — `SERVER-MTLS-DESIGN` / `ADR-0023` accepted as proposed, docs-only, plus the backfill for PR #133); `dbb532a` (merge of PR #133, same branch — `SERVER-MTLS-DESIGN` / `ADR-0023` proposed, docs-only, plus the backfill for PR #132); `f836020` (merge of PR #132, same branch — the legacy "this PR" Evidence backfill, docs-only); `5f82061` (merge of PR #131, same branch — backfill for PR #130, docs-only); `64333df` (merge of PR #130, same branch — `SERVER-CLIENT-TLS` / `SERVER-001` v0.12.0 / FR-022 implemented); `21e46a7` (merge of PR #129, same branch — backfill for PR #128, docs-only); `f7e8ca0` (merge of PR #128, same branch — `SERVER-CLIENT-AUTH` / `SERVER-001` v0.11.0 / FR-021 implemented); `8b4ea6a` (merge of PR #127, same branch — backfill for PR #126, docs-only); `1c2f9a1` (merge of PR #126, same branch — `SERVER-PROTOCOL-VERSION` / `SERVER-001` v0.10.0 / FR-020 implemented); `fc2e36a` (merge of PR #125, same branch — `SERVER-PROTOCOL-VERSION-DESIGN` / `ADR-0022` accepted as proposed, plus the backfill for PR #124, docs-only); `382700d` (merge of PR #124, same branch — `SERVER-PROTOCOL-VERSION-DESIGN` / `ADR-0022` proposed, docs-only); `835d7a9` (merge of PR #123, same branch — backfill for PR #122, docs-only); `ad87dd1` (merge of PR #122, same branch — `STORAGE-018` v0.1.1, `DOGBLOB\0` fingerprint-before-decode closed as not warranted, docs-only); `1883670` (merge of PR #121, same branch — backfill for PR #120, docs-only); `c605f4d` (merge of PR #120, same branch — `BINCODE-ENCODING-STABILITY` / `STORAGE-018` v0.1.0 implemented: `src/codec.rs`, all `bincode` sites routed, golden vectors); `ecb0d7d` (merge of PR #119, same branch — backfill for PR #118, docs-only); `80b2ad4` (merge of PR #118, same branch — `BINCODE-ENCODING-STABILITY-DESIGN` / `ADR-0021` accepted as proposed by the owner, docs-only); `fbba7d1` (merge of PR #117, same branch — backfill for PR #116, docs-only); `b03fdce` (merge of PR #116, same branch — `BINCODE-ENCODING-STABILITY-DESIGN` / `ADR-0021` proposed, docs-only); `b30af2f` (merge of PR #115, same branch — backfill for PR #114, docs-only); `ed1ff48` (merge of PR #114, same branch — `MULTI-FIELD-MMAP-DURABILITY` / `STORAGE-017` v0.1.0 implemented); `0bf3000` (merge of PR #113, same branch — backfill for PR #112, docs-only); `ee5defb` (merge of PR #112, same branch — `MULTI-FIELD-MMAP-DURABILITY-DESIGN` / `ADR-0020` accepted as proposed by the owner, docs-only); `7571a71` (merge of PR #111, same branch — mechanical placeholder fix for PR #110, docs-only); `39e366e` (merge of PR #110, same branch — `MULTI-FIELD-MMAP-DURABILITY-DESIGN` / `ADR-0020` proposed, docs-only); `c689e50` (merge of PR #109, same branch — mechanical placeholder fix for PR #108, docs-only); `f2d89c8` (merge of PR #108, same branch — `BLOB-SCHEMA-TAG` implemented as `STORAGE-015` v0.2.0 / `STORAGE-016` v0.2.0); `5bce5ce` (merge of PR #107, same branch — mechanical placeholder fix for PR #106, docs-only); `ff2d2d3` (merge of PR #106, same branch — `BLOB-SCHEMA-TAG-DESIGN` / `ADR-0019` accepted, docs-only); `772ed7c` (merge of PR #105, same branch — mechanical placeholder fix for PR #104, docs-only); `a3c10ae` (merge of PR #104, same branch — `BLOB-SCHEMA-TAG-DESIGN` / `ADR-0019` proposed, docs-only); `f9b5560` (merge of PR #103, same branch — mechanical placeholder fix for PR #102, docs-only); `68341bb` (merge of PR #102, same branch — `SYMMETRIC-EDGE-PORTABILITY` implemented as `STORAGE-016` v0.1.0); `cd4b3a5` (merge of PR #101, same branch — mechanical placeholder fix for PR #100, docs-only); `d57c791` (merge of PR #100, same branch — `SYMMETRIC-EDGE-PORTABILITY-DESIGN`/`ADR-0018` accepted as proposed by the owner, docs-only); `adc98d4` (merge of PR #99, same branch — mechanical placeholder fix for PR #98, docs-only); `4258818` (merge of PR #98, same branch — `Symmetric` edge-list portability design proposed, `SYMMETRIC-EDGE-PORTABILITY-DESIGN`/`ADR-0018`, Proposed, no code changes); `f50c3dd` (merge of PR #97, same branch — mechanical placeholder fix for PR #96, docs-only); `9afff3b` (merge of PR #96, same branch — unchanged-code Criterion rerun recorded under the `STORAGE-015` follow-up as harness-drift calibration, docs-only); `eb2b4af` (merge of PR #95, same branch — mechanical placeholder fix for PR #94, docs-only); `842fca9` (merge of PR #94, same branch — `STORAGE-015`'s trait-method fingerprint fallback measured and closed as not warranted, `GENERIC-STORE-FINGERPRINT-MEASUREMENT`, docs-only); `0eb09f7` (merge of PR #93, same branch — mechanical placeholder fix for PR #92, docs-only); `18e8bf9` (merge of PR #92, same branch — `GenericMmapStore` file portability implemented, `GENERIC-STORE-PORTABILITY`, `STORAGE-015` v0.1.0); `0eaf6bb` (merge of PR #91, same branch — mechanical placeholder fix for PR #90, docs-only); `33558ea` (merge of PR #90, same branch — `GenericMmapStore` file portability design accepted, `ADR-0017`/`GENERIC-STORE-PORTABILITY-DESIGN`, no code changes); `aea33e5` (merge of PR #89, same branch — mechanical placeholder fix for PR #88, docs-only); `393f5a5` (merge of PR #88, same branch — `GenericMmapStore` file portability design proposed, `GENERIC-STORE-PORTABILITY-DESIGN`/`ADR-0017`, Proposed, no code changes); `01a6d5a` (merge of PR #87, same branch — mechanical placeholder fix for PR #86, docs-only); `711dfbc` (merge of PR #86, same branch — record-blob header fingerprint, `RECORD-BLOB-FINGERPRINT`, `STORAGE-014` v0.2.0, blob version 2); `07f708c` (merge of PR #85, same branch — mechanical placeholder fix for PR #84, docs-only); `00e9295` (merge of PR #84, same branch — `ProductionStore` file portability implemented, `PRODUCTION-STORE-PORTABILITY`, `STORAGE-014` v0.1.0); `3cd8750` (merge of PR #83, same branch — mechanical placeholder fix for PR #82, docs-only); `2ef3f53` (merge of PR #82, same branch — `ProductionStore` file portability design accepted, `ADR-0016`/`PRODUCTION-STORE-PORTABILITY-DESIGN`, no code changes); `57531d1` (merge of PR #80, same branch — `ProductionStore` file portability design proposed, `PRODUCTION-STORE-PORTABILITY-DESIGN`/`ADR-0016`, Proposed, no code changes); `a579644` (merge of PR #79, same branch — mechanical placeholder fix for PR #78, docs-only); `6a51f8b` (merge of PR #78, same branch — five-hop `littermate_of` traversal added to the external-DB benchmark comparison, `FIVE-HOP-EXTERNAL-DB-BENCHMARK`, `STORAGE-013` v0.5.0); `c6db6c2` (merge of PR #77, same branch — mechanical placeholder fix for PR #76, docs-only); `1139804` (merge of PR #76, same branch — four-hop `littermate_of` traversal added to the external-DB benchmark comparison, `FOUR-HOP-EXTERNAL-DB-BENCHMARK`, `STORAGE-013` v0.4.0); `769edc4` (merge of PR #75, same branch — mechanical placeholder fix for PR #74, docs-only); `e4dd3b1` (merge of PR #74, same branch — three-hop `littermate_of` traversal added to the external-DB benchmark comparison, `THREE-HOP-EXTERNAL-DB-BENCHMARK`, `STORAGE-013` v0.3.0); `256c1d5` (merge of PR #73, same branch — mechanical placeholder fix for PR #72, docs-only); `c2b87ad` (merge of PR #72, same branch — two-hop `littermate_of` traversal added to the external-DB benchmark comparison, `TWO-HOP-EXTERNAL-DB-BENCHMARK`, `STORAGE-013` v0.2.0); `dbcbdb0` (merge of PR #71, same branch — mechanical placeholder fix for PR #70, docs-only); `77717dd` (merge of PR #70, same branch — CI build cache added to stop rebuilding DuckDB from scratch on every run, `CI-CACHE`, workflow-only); `c61dcc5` (merge of PR #69, same branch — mechanical placeholder fix for PR #68, docs-only); `063099a` (merge of PR #68, same branch — `MmapAgeStore` record-identity-keying fix, columnar redesign); `ed785eb` (merge of PR #66, same branch — external database benchmark comparison, `benches/external_db.rs`, ADR-0015, `STORAGE-013` v0.1.0); `bebf3fa` (merge of PR #65, same branch — mechanical placeholder fix for PR #64, docs-only); `be1067f` (merge of PR #64, same branch — native transport encryption implemented, `TlsConfig`, `SERVER-001` v0.9.0, ADR-0014). Older checkpoints, same session, newest first: `8adf478` (merge of PR #63, same branch — ADR-0014 server transport encryption design accepted as revised, docs-only, no implementation yet at that point); `8ee7311` (merge of PR #62, same branch — ADR-0014 revised to depend on `rusty_tls` instead of `rustls` directly, docs-only); `d885e92` (merge of PR #59, same branch — transaction benchmark coverage, `benches/server.rs`, `SERVER-001` v0.8.0). Older checkpoints, same session, newest first: `857417d` (merge of PR #57, same branch — atomic multi-operation transactions implemented, `SERVER-001` v0.7.0, ADR-0013); `1c97ff7` (merge of PR #56, same branch — mechanical placeholder fix for PR #55, docs-only); `144f747` (merge of PR #55, same branch — ADR-0013 server transaction proposal accepted, docs-only, no implementation yet at that point); `1923c3e` (merge of PR #54, same branch — ADR-0013 server transaction design proposal, docs-only, not yet accepted at that point); `b76f90c` (merge of PR #53, same branch — mechanical placeholder fix for PR #52, docs-only); `0528b39` (merge of PR #52, same branch — server authentication/authorization implemented, `SERVER-001` v0.6.0, ADR-0012); `416b491` (merge of PR #51, same branch — mechanical placeholder fix for PR #50, docs-only); `75aa3e3` (merge of PR #50, same branch — ADR-0012 auth/encryption design accepted, docs-only, no implementation yet at that point); `aaf07c4` (merge of PR #48, same branch — ADR-0012 auth/encryption design proposal for the server layer, docs-only, not yet accepted at that point); `e6ccc75` (merge of PR #46, same branch — schema-driven client library, `server::client::SchemaDrivenClient`, `SERVER-001` v0.5.0); `8047a28` (merge of PR #44, `claude/server-bench-baileyai-real-hardware` — second real-hardware follow-up, `benches/server.rs`'s `THREAD_COUNTS` retuned to `[1, 4, 32, 64]` for `baileyai`); `23995ca` (merge of PR #43, `claude/server-bench-real-hardware-25146` — first real-hardware follow-up, `THREAD_COUNTS` retuned to `[1, 4, 24, 48]` for `Beast`); `0a07938` (merge of PR #41, `claude/next-level-xm8lpi` — throughput/latency benchmark, `benches/server.rs`, `SERVER-001` v0.4.0); `6bacb43` (merge of PR #40, same branch — mechanical placeholder fix, docs-only); `d53e7fe` (merge of PR #39, same branch — third validation domain `Employee`, `SERVER-001` v0.3.0, `STORAGE-012` v0.2.0); `318caf5` (merge of PR #37, same branch — schema discovery, ADR-0011, `SERVER-001` v0.2.0); `2bbfa10` (merge of PR #35, same branch — `SERVER-QUERY-LAYER` implemented: `src/server/**`, `SERVER-001` v0.1.0); `24a507d` (merge of PR #34, same branch — a mechanical placeholder fix, docs-only); `76a07e9` (merge of PR #33, same branch — `SERVER-QUERY-LAYER-DESIGN`/ADR-0010 accepted as proposed, docs-only); `a64cf43` (merge of PR #32, same branch — this file's own 13-PR documentation-sync catch-up, docs-only). Older checkpoints below, from `176f5d0` (merge of PR #31, `claude/future-growth-doc` — adds `docs/FUTURE-GROWTH.md` and links it from the README index) back through `GENERIC-SCHEMA-WRITE-THROUGH-FIX` (`5b4300a`, PR #18), newest first: `4000565` (PR #30, `claude/readme-and-rustdoc` — `README-AND-RUSTDOC`); `e490a40` (PR #29, `claude/fix-append-slot-race` — `GENERIC-MMAP-APPEND-SLOT-RACE-FIX`); `320b2b3` (PR #28, `claude/mmap-multiprocess-diagnosis` — `GENERIC-MMAP-MULTIPROCESS-DIAGNOSIS`, diagnosis only); `e2d779b` (PR #27, `claude/narrow-public-api` — `PUBLIC-API-NARROWING`); `cba64a9` (PR #26, `claude/mmap-torn-write-fix` — `GENERIC-MMAP-TORN-WRITE-FIX`); `95144bc` (PR #25, `claude/mmap-crash-safety-diagnosis` — `GENERIC-MMAP-CRASH-SAFETY-DIAGNOSIS`, diagnosis only); `7a8f71c` (PR #24, `claude/mmap-schema-version-header` — `GENERIC-MMAP-SCHEMA-VERSION-HEADER`); `5905c9e` (PR #23, `claude/mmap-record-identity-keying` — `GENERIC-MMAP-RECORD-IDENTITY-FIX`); `446f582` (PR #22, `claude/req-traceability-schema` — `REQ-TRACEABILITY-SCHEMA`, spike); `09e662c` (PR #21, `claude/pr-12-ci-failure-mwkbuf` — restored `Parent::parent`'s not-found/no-parent distinction); `151a063` (PR #20, `claude/childof-optional-parent-fix` — `CHILDOF-OPTIONAL-PARENT-FIX`); `399c298` (PR #19, `claude/rule-trace-spike` — `RULE-TRACE-SPIKE`). See the `## Completed` section below for the detail behind each. Checkpoints since `PRODUCTION-DEFAULT` up to `GENERIC-SCHEMA-WRITE-THROUGH-FIX`: `2edf1cd` (PR #15, `claude/generic-schema-macro-forwarding` — the ambiguity-fix and macro-forwarding spike rounds); `f9d6e25` (PR #14, `claude/generic-schema-spike` — the Dog-overhead spike round); `fce10e4` (PR #12, `claude/generic-schema-design` — `GENERIC-SCHEMA-DESIGN.md`/ADR-0009's original proposal, rebased onto `main` before merge); `3951155` (PR #13, a CI-flake fix for a pre-existing bookkeeping race in `run_concurrency_stress_test`/`run_contention_phase`, unrelated to PR #12's own docs-only diff — merged separately, then PR #12 rebased onto it); `8ea5f2e` (PR #16, the fourth and final `GENERIC-SCHEMA-SPIKES` round); `fa0787a` (PR #17, `GENERIC-SCHEMA-LIBRARY`); `1dfb902` (PR #18, `GENERIC-SCHEMA-WRITE-THROUGH-FIX`). Prior checkpoints before that: `fd83b8e` (PR #10, `CONCURRENCY-PROTOTYPES` plus its real multi-core follow-up passes — a container run, a substituted Windows-desktop run, and finally a genuine `baileyai` run from a session executing directly on that machine); `0f9937e` (PR #9, gating the durability reopen-path parallelization on a measured size threshold); `251975d` (PR #8, `DURABILITY-TIER1`/`DURABILITY-TIER2`); `89160d7` (PR #7, `MIXED-WORKLOAD`); `b336845` (PR #6, `GRAPH-TRAVERSAL`); `5da8380` (PR #5, `scan_ages` 100K crossover closed out with real `baileyai` L1D data); `fb17f09` (PR #4, crossover investigation narrowed); `ec67ba3` (PR #3, cache-miss counts from `baileyai`); `d1d9169` (PR #1, `HYBRID-BACKEND`); `ae9a0d0` (PR #2, `scan_ages` wall-clock noise diagnosis); `5bfc8c5` (bootstrap, first pass) — `main` and the feature branch were identical at that commit (GitHub auto-set `main` to the first push into an otherwise-empty repo), so the owner elected to leave `main` as-is rather than force-reset it for a formal PR; PR #1 is the first real PR/merge in this repo's history.
- Verified at: 2026-09-03
- Current milestone: `SERVER-ACCESS-LOG-DESIGN` (**Proposed** in PR #165; design doc `docs/design/SERVER-ACCESS-LOG-DESIGN.md` + `ADR-0031`) — the last of the owner's next four picks ("1, 2, 3, 4": land the upstream `rusty_tls` accessor, the rate-limit/lockout design round, stage-time validation, this round), answering `ADR-0029`'s second revisit trigger and `SERVER-AUTH-AUDIT-DESIGN`'s own access-logging non-goal. The real choice: a second, independent sink family (`AccessSink`/`AccessEvent` in a new `src/server/access.rs`) rather than folding into `AuditKind`, so turning on the audit log never implies accepting per-request log volume and vice versa. `AccessEvent` carries a timestamp, peer, class, `RequestKind` (reused from `audit.rs`), and an `Outcome` of `Ok` or `Err(ErrorCode)` — kind and outcome shape only, never a record id, field, or value; its own line format, distinct from the audit log's; `NoAccessLog`/`StderrAccessLog`/`FileAccessLog` mirroring the audit sinks' fail-open shapes; hung on `AuthConfig::with_access_log`, `serve` unchanged; fires once per dispatched request, never for `Hello`/`Authenticate`/a gate refusal, so the two streams are disjoint by construction; `SERVER_ACCESS_LOG` in `dog_server`, independent of `SERVER_AUDIT_LOG`. Out of scope, named: payloads, timing, sampling, session/transaction correlation, a per-record access trail. Options: (a) accept as proposed (recommended), (b) fold into `AuditKind` instead — one switch, coupled volume, (c) close as not warranted. With this the owner's four-item round is fully proposed/implemented: `ADR-0030` (rate limiting) awaits acceptance, `ADR-0031` (this design) awaits acceptance, stage-time validation shipped, and the upstream `rusty_tls` PR #148 awaits the owner's merge. Before this, `SERVER-SESSION-VALIDATE-ON-STAGE` (`SERVER-001` v0.20.0 / FR-030, **Implemented** in PR #163) — the third of the owner's next four picks, `ADR-0024`'s second revisit trigger and `ADR-0027`'s stage-time trigger taken together as the bounded additive completion both decisions described (no new ADR): `BeginWith` learns `SESSION_VALIDATE_ON_STAGE` at protocol version 6 with no new variant — a flag bit is introduced at a version like a variant, recorded against `ADR-0022` — and `ConnectionStore` gains `validate_op`, each adapter's `validate_batch` on one operation; a validating session refuses a bad write at its own round trip with the code `Commit` would have given and stages nothing; the two bits compose; `SessionOptions` / `begin_with` / `Session::validate_on_stage` in the client. One unit test and three integration tests (357 lib; transaction suite 17/17, client suite 7/7). No `Response`/`ErrorCode`, store, `serve`, or `Cargo.toml` change. Next: the access-logging design round; `ADR-0030` awaits acceptance; the upstream `rusty_tls` PR #148 awaits the owner's merge. Before this, `SERVER-AUTH-RATE-LIMIT-DESIGN` (**Proposed** in PR #161; design doc `docs/design/SERVER-AUTH-RATE-LIMIT-DESIGN.md` + `ADR-0030`) — the second of the owner's next four picks ("1, 2, 3, 4": land the upstream `rusty_tls` accessor, this design round, stage-time validation in sessions, an access-logging design round), answering the remaining two thirds of `SERVER-AUTH-DESIGN`'s named gap and `ADR-0029`'s first revisit trigger. Two deliberately unequal mechanisms: a per-connection lockout after `MAX_AUTH_FAILURES = 5` failed `Authenticate`s — on by default, a constant, because five wrong tokens on one connection is a bug or an attack and a reconnect costs a legitimate client nothing — and an opt-in per-peer failure budget over a fixed window in a table bounded by `MAX_TRACKED_PEERS`, an over-budget peer refused before any comparison. Nothing on the wire changes (a throttled or locked-out attempt is `Unauthenticated`, indistinguishable from a wrong token by design), no delays, state on `AuthConfig`, both recorded on the audit log as `LockedOut`/`Throttled`, `SERVER_AUTH_RATE_LIMIT` in `dog_server`; `AuditKind`/`RequestKind` marked `#[non_exhaustive]` as a clarification of `ADR-0029`. Out of scope, named: connection and handshake floods, distributed guessing, NAT false positives (why the budget is opt-in). Options: (a) accept as proposed (recommended), (b) both opt-in with a typed `ErrorCode::RateLimited` at protocol 6, (c) close as not warranted. The first pick — the upstream accessor — could not be done from this session directly (cross-owner repository attach is unsupported here); a child session was started on `Rusty-Mill/rusty_mill` with the design's patch and told to open the PR without merging; its outcome is recorded when it reports. Before this, `SERVER-AUTH-AUDIT` (`SERVER-001` v0.19.0 / FR-029, **Implemented** in PR #159) — the accepted `SERVER-AUTH-AUDIT-DESIGN` / `ADR-0029` implemented exactly as designed, with no deviation: `src/server/audit.rs` (public) with the closed `AuditKind` set — `Admitted`, `HandshakeFailed`, `Authenticated`, `AuthenticationFailed`, `Refused`, `Disconnected` — never carrying a token, certificate, id, or value; `AuditSink` with `NoAudit` (default, free), `StderrAudit`, and `FileAudit` (append-only, no `fsync`) writing the documented `key=value` line built in one place; the sink on `AuthConfig::with_audit` so `serve` keeps its four parameters; `handle_connection` taking the peer once, completing the TLS handshake eagerly (a refused admission is `HandshakeFailed` with the TLS error's text — the mechanism `ADR-0028` also needs, landed here first), recording at its existing gates after the decision and before the response outside every lock, and emitting exactly one `Disconnected` per admitted connection through a drop guard; fail-open with one stderr notice per process; `dog_server` reading `SERVER_AUDIT_LOG`. Three unit tests, one binary test, four integration tests (356 lib; auth suite 10/10, TLS suite 13/13); every acceptance criterion 1–7 recorded as holding in `SERVER-001`. With this, three of the four accepted designs are implemented; `ADR-0028`'s crate-side unit still waits on the upstream `rusty_tls` accessor. Before this, `SERVER-SESSION-READ-YOUR-WRITES` (`SERVER-001` v0.18.0 / FR-028, **Implemented** in PR #157) — the accepted `SERVER-SESSION-READ-YOUR-WRITES-DESIGN` / `ADR-0027` implemented: `Request::BeginWith { flags }` at index 14 and protocol version 5 (`SESSION_READ_YOUR_WRITES = 1`, unknown bits `Malformed`, `flags: 0` exactly `Begin`, `Malformed` below 5), the connection's own `GetById` overlaid by a pure `overlay_staged` — last staged write per field wins, only for fields the record carries, only when the kind matches and the schema marks the field updatable, so a write `Commit` would refuse is never shown — with set reads, plain sessions, older clients, and other connections unchanged; `SchemaDrivenClient::begin_read_your_writes`, `Session::get` on every session, `Session::read_your_writes`. One clarification recorded against the design: read-only fields are not overlaid (the kind rule alone would have shown a staged `Dog::breed`). No new `Response`/`ErrorCode`, no store, adapter, `ConnectionStore`, or `Cargo.toml` change. One unit test and four integration tests (353 lib; transaction suite 15/15, client suite 6/6); every acceptance criterion 1–7 recorded as holding in `SERVER-001`. Next: `ADR-0029` (audit log); `ADR-0028` waits upstream. Before this, `SERVER-JOURNAL-GROUP-COMMIT` (`SERVER-001` v0.17.0 / FR-027, **Implemented** in PR #155) — the accepted `SERVER-JOURNAL-GROUP-COMMIT-DESIGN` / `ADR-0026` implemented exactly as designed, with no deviation: `CommitGroup` in `src/server/journal.rs` takes the journal's `fsync` out of the store's exclusive section — append under the journal's own mutex taking a sequence, a leader/follower `fsync` (the first waiter with no sync in flight leads and syncs once for everything appended before its `sync_data`; no timer, delay, or thread), a turn gate so applies happen in journal order, a checkpoint offered only to the last-appended batch and re-checked under the lock after the flush, v0.15.0's failure semantics per group, a turn guard on unwind — and the three adapters' journaled `apply_transaction` becomes validate-by-per-call-reads then `commit` with `with_exclusive` only inside the apply closure; the unjournaled path is untouched. Measured on this container: `dog-jrnl-txn` latency 304.3 µs against v0.15.0's 320.7, and throughput 3,169 / 5,906 / 14,276 / 15,322 batches/s at 1 / 4 / 32 / 64 connections against v0.15.0's flat 3,182 / 3,843 / 3,395 / 3,337 — the `fsync` scales with concurrency instead of serializing it. Five unit tests and one integration test; every acceptance criterion 1–8 recorded as holding in `SERVER-001`. Next: `ADR-0027` (read-your-writes), then `ADR-0029` (audit log); `ADR-0028` waits upstream. Before this, the four-design acceptance — `ADR-0026` (journal group commit, PR #145), `ADR-0027` (session read-your-writes, PR #147), `ADR-0028` (class from certificate, PR #149), and `ADR-0029` (authentication audit log, PR #151), each **Accepted** as proposed in PR #153 ("a, a, a, a"; every (b) and (c) declined; no changes requested). Implementation order: `ADR-0026`, then `ADR-0027`, then `ADR-0029`, each as its own `SERVER-001` minor / FR unit; `ADR-0028`'s crate-side unit waits on the upstream `rusty_tls` accessor (`CLS-FR-001`), which this session cannot land. Before this, `SERVER-AUTH-AUDIT-DESIGN` (**Proposed** in PR #151, **Accepted** in PR #153; design doc `docs/design/SERVER-AUTH-AUDIT-DESIGN.md` + `ADR-0029`) — the last of the owner's four picks ("1, 2, 3, 4"), answering the audit-log third of the gap `SERVER-AUTH-DESIGN` named and every security round since carried forward unchanged. An audit record for every decision the server's three gates take — admission (the handshake), authentication (`Authenticate`), authorization (every `Unauthenticated`/`Unauthorized` refusal) — plus `Disconnected`, and nothing else: no successful request, no token or certificate or any part of one, no record id or value; the peer address and a Unix-seconds timestamp are the identifiers. `AuditSink` with `NoAudit` (the default, free), `StderrAudit`, `FileAudit` (append-only, one documented `key=value` line per event); hung on `AuthConfig::with_audit` so `serve` does not change; called at the existing gates after the decision and before the response, outside every lock; fail-open with one stderr notice per process, so the audit path can never take the server down; the eager server-side handshake shared with `ADR-0028` so a rejected admission has a typed reason to record; `dog_server` reads `SERVER_AUDIT_LOG`. Rate limiting and lockout — the other two thirds — stay named, now with the record they would be tuned from. No `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, or `serve`-signature change. Options: (a) accept as proposed (recommended), (b) fail-closed (a sink write failure ends the connection), (c) close as not warranted. With this, all four picks are proposed and await the owner's acceptance: `ADR-0026` (group commit), `ADR-0027` (read-your-writes), `ADR-0028` (class from certificate), `ADR-0029` (audit log). Before this, `SERVER-MTLS-CLASS-DESIGN` (**Proposed** in PR #149; design doc `docs/design/SERVER-MTLS-CLASS-DESIGN.md` + `ADR-0028`) — the third of the owner's four picks ("1, 2, 3, 4"), answering `ADR-0023`'s first revisit trigger and `SERVER-MTLS-DESIGN`'s "Class from certificate" open question. Both halves the trigger named are decided. The upstream accessor is `rusty_tls::TlsServerStream::peer_certificate_der()`, a mirror of the accessor the pinned `rusty_tls` already has on the client stream; its eight-line patch is in the design verbatim and was *run* against this crate through a temporary local `[patch]` (discarded): after an eager `complete_handshake()` the server sees exactly the client's leaf DER, and a client without a certificate fails that handshake with a typed `NoCertificatesPresented`. The X.509 reading decision is answered by not reading X.509: `AuthConfig` gains a certificate class map matched by exact DER bytes of the presented leaf (from DER, from PEM files, from two `dog_server` variables), `handle_connection` completes the handshake eagerly before the frame loop and seeds the connection's class from the classed leaf — a valid token later replaces it; an admitted leaf not in the map behaves exactly as today; `dog_server` refuses classes without a client CA. No parser, no new dependency (the one `Cargo.toml` change is the `rusty_tls` rev bump), no wire or `TokenClass` change. One constraint recorded plainly: the upstream repository could not be attached from this session (refused by the session's permission policy), so the implementation unit's first step — the `rusty_tls` PR — needs that permission or the owner applying the patch. Options: (a) accept as proposed (recommended), (b) with SPKI pinning (the first X.509 code in this crate, for rotation convenience), (c) close as not warranted. Before this, `SERVER-SESSION-READ-YOUR-WRITES-DESIGN` (**Proposed** in PR #147; design doc `docs/design/SERVER-SESSION-READ-YOUR-WRITES-DESIGN.md` + `ADR-0027`) — the second of the owner's four picks ("1, 2, 3, 4"), answering `ADR-0024`'s first revisit trigger. Offered as a bounded change when the options were listed; on reading `ADR-0024` ("a real design with a cost on every read") and `SESS-FR-007` (a numbered requirement whose meaning would change), written up as a design round instead, so the versioning and the opt-in are decided rather than assumed. The design: an appended `Request::BeginWith { flags: u32 }` at protocol version 5 (`SESSION_READ_YOUR_WRITES = 1`, unknown bits `Malformed`, `flags: 0` exactly `Begin`); while such a session is open, the connection's own `GetById` is overlaid by a pure function in `handle_connection` — last staged write per `(id, field)` wins, only for fields the record carries, only when the kinds match, so a write that would fail at `Commit` never produces a misleading read; set reads (`ScanField` is by position with no ids, `FilterEq` by value), plain sessions, older clients, and every other connection keep `SESS-FR-007` unchanged; cost linear in the buffer, only on the sessions and reads that asked. Client: `begin_read_your_writes()` gated on ≥ 5 and `Session::get` on every session — found while reading: a `Session` borrows the client mutably, so today no read at all is possible through the library while one is open, which any read-your-writes design must fix or be unreachable. No new `Response`/`ErrorCode`, no store, adapter, `ConnectionStore`, or `Cargo.toml` change. Options: (a) accept as proposed (recommended), (b) with `FilterEq` adjusted by the buffer too, (c) close as not warranted. Before this, `SERVER-JOURNAL-GROUP-COMMIT-DESIGN` (**Proposed** in PR #145; design doc `docs/design/SERVER-JOURNAL-GROUP-COMMIT-DESIGN.md` + `ADR-0026`) — the first of the owner's next four picks ("1, 2, 3, 4": this design round, read-your-writes in sessions, class-from-certificate for mTLS, an auth audit-log design round), answering `ADR-0025`'s third revisit trigger the moment `RESULTS.md`'s FR-025 rows fired it: journaled throughput is flat at ~3.3k batches/s from 1 to 64 connections because the journal's `fsync` runs inside the store's exclusive section, so every other connection's batch, read, and single write queues behind one `fsync` at a time. The design takes the `fsync` out of that section and shares it: validate with per-call reads (sound by the no-runtime-deletion invariant), append under the journal's own mutex taking a sequence, a leader/follower group commit with no timer, delay, or thread (a lone connection pays exactly today's one `fsync`), apply under `with_exclusive` in sequence order so replay order equals apply order (a free gate — durability is monotone), and checkpoint only when nothing later is appended. A throwaway probe run before proposing (recorded in the design doc, never committed) shows per-batch `fsync` flat at ~4.6k/s at every thread count and group commit at 29k/s with an average group of 16 at 64 threads, the lone thread unchanged; a 100 µs commit delay buys group size, not throughput, and halves the lone thread — so the delay is offered as option (b) and recommended against. No format, store, wire, `PROTOCOL_VERSION`, lock-type, `Cargo.toml`, or unjournaled-path change. Options: (a) accept as proposed (recommended), (b) accept with a fixed commit delay, (c) close as not warranted. Before this, `SERVER-CLIENT-HELLO-FALLBACK` (`SERVER-001` v0.16.0 / FR-026, **Implemented** in PR #143) — `ADR-0022`'s third revisit trigger, the reconnect-without-hello fallback, the last of the owner's four picks ("1, 2, 3, then 4"). Offered as (1) close as not warranted — recommended, matching the design's own "not while none is deployed" — (2) opt-in, or (3) default-on; the owner chose (3), and that is recorded here as the owner's decision. `SchemaDrivenClient::connect_with` now treats the peer closing the connection under the `Hello` with no reply (an end-of-stream/reset/abort-class I/O error, the one shape a pre-hello server produces) as such a server: one silent second connection with no `Hello`, `server_protocol_version()` reported as 1, the version-3 `begin` refused with `Unsupported`. The heuristic is bounded on purpose — at most once per connect, only under the `Hello`, only on that error class, never on a reply (`Malformed` keeps its meaning), and a second failure is returned as is, so a server that genuinely dies under any first frame is still an error after one extra connect. `ConnectOptions::require_hello()` restores the pre-v0.16.0 EOF error for a caller that would rather see it. Connection setup (TCP, `TCP_NODELAY`, the TLS wrap and identity) is factored into one `open_transport` so both attempts are identical. One test in `tests/server_protocol_version.rs` with a pre-hello fake server built on the public `dispatch` — the v0.9.1 shape emulated rather than checked out — covering the fallback, the opt-out, and the bounded retry. No wire, server, `Cargo.toml`, or `PROTOCOL_VERSION` change; a bounded, additive client change, no new ADR. With this, every follow-up item 70 surfaced and every trigger `ADR-0022` named except the major-break one is closed. Before this, `SERVER-TRANSACTION-JOURNAL` (`SERVER-001` v0.15.0 / FR-025, **Implemented** in PR #141) — the accepted `SERVER-TRANSACTION-SESSION-DESIGN` Part B / `ADR-0025` implemented: an opt-in redo journal at the domain adapter that makes a batch answered `Ok` survive a crash of the server process whole. `src/server/journal.rs` (new) holds `BatchJournal` — `TXNJRNL\0`, `u32` version 1, entries `[u32 len][crate::codec(Vec<TransactionOp>)]`; `open` replays every complete entry and drops a torn tail, `append` writes and `fsync`s, `truncate` after a checkpoint past `JOURNAL_CHECKPOINT_BYTES` (1 MiB) — plus `CheckpointFlush`, a server-layer trait for exactly the three exclusive store types (so no storage module was touched), and `JournalError`. `with_journal(store, path)` on `DogConnectionStore`/`OrderConnectionStore`/`EmployeeConnectionStore` replays, flushes, and truncates on open; inside `apply_transaction`'s existing `with_exclusive` closure the batch is validated, appended and `fsync`'d *before* its first write, applied, and checkpointed when the journal has grown past the threshold (flush first, then truncate). `validate_batch`/`apply_batch` are factored out so replay reuses the apply step; `dog_server` reads `SERVER_TXN_JOURNAL_PATH`. Redo suffices because every operation is an idempotent overwrite keyed by an undeletable id, and the adapter is the only layer that both sees a batch and can replay one operation. Opt-in: every unjournaled adapter is exactly v0.14.0. The one wire addition, `ErrorCode::Journal` (9, a failed append with nothing applied), is an appended variant, so rule 2 bumps `PROTOCOL_VERSION` to 4 and rule 3 gets its first "nearest older shape" use — `downgrade_for_version` rewrites it to `Unsupported` on a connection below 4; `ADR-0025` named the index and the gating but not the bump, recorded as a clarification, not a deviation. Seven unit tests (format, torn tail, foreign file, checkpoint; the file-copy replay pair — pre-batch files gain the batch, post-batch files are a no-op, a foreign journal refused; the adapter-level checkpoint; the downgrade) and one integration test (a `Transaction` and a session `Commit` each journaled before `Ok`; staging, a rejected batch, and a single `UpdateField` journal nothing). `benches/server.rs` gained `dog-jrnl-txn` rows, run on this session's container and recorded in `RESULTS.md` beside `dog-txn`: the `fsync` per batch is the whole cost, and it is large — see `RESULTS.md`'s numbers. Every Part B acceptance criterion 1–6 recorded as holding in `SERVER-001`. Before this, `SERVER-TRANSACTION-SESSION` (`SERVER-001` v0.14.0 / FR-024, **Implemented** in PR #139) — the accepted `SERVER-TRANSACTION-SESSION-DESIGN` Part A / `ADR-0024` implemented exactly as designed, with no deviation, and with it the first protocol-version-3 change `ADR-0022` named as its own trigger: `PROTOCOL_VERSION = 3`; `Request::Begin`/`Commit`/`Rollback` (11–13), `Response::Staged { index }` (11), `ErrorCode::NoSession`/`SessionOpen`/`SessionFull` (6–8), `MAX_STAGED_OPS = 4096`, the table's row 3 and seven golden vectors with every older vector unchanged. `handle_connection` now keeps the negotiated version per connection (1 for a silent client) and a per-connection staged buffer: an admitted `UpdateField` inside a session is pushed and answered `Staged { index }` — nothing applied, no lock taken, no validation — `Commit` runs `apply_transaction` on the buffer exactly as one `Transaction` and closes the session either way, `Rollback` or a disconnect discards, the three requests are `Malformed` below version 3 (compatibility rule 3, its first real branch), and `Commit` joins the `ReadOnly` gate; `dispatch` maps them to `Unsupported`. `SchemaDrivenClient::begin()` → `Session` (`update` returning the staged index after the usual client-side capability checks, `commit`, `rollback`, `Drop` rolling back best-effort) is the library's first version-gated API (rule 4), with `ClientError::TransactionFailed` naming the staged index. No adapter, store, `ConnectionStore`, framing, codec, or `Cargo.toml` change; no benchmark (staging is a `Vec` push, commit is FR-018's measured path). Nine tests across three suites — including a concurrent observer proving no lock is held between round trips and the first rule-3/rule-4 gating test with silent, `Hello { 2 }`, and `Hello { 3 }` clients side by side — plus two unit tests; the unknown-index test moved from 11 to 14, as its own doc predicted. Every Part A acceptance criterion 1–8 recorded as holding in `SERVER-001`. Part B (`ADR-0025`, the redo journal) is the next unit, as `SERVER-001` v0.15.0 / FR-025. Before this, `SERVER-TRANSACTION-SESSION-DESIGN` (**Proposed** in PR #137, both parts **Accepted** as proposed in PR #138; design doc `docs/design/SERVER-TRANSACTION-SESSION-DESIGN.md` + `ADR-0024` + `ADR-0025`) — `ADR-0013`'s two revisit triggers taken as one design round with two independently acceptable decisions, the owner's third pick ("1, 2, 3, then 4"). **Part A, `ADR-0024` — a buffered transaction session.** The literal reading of the trigger (hold the exclusive lock across client round trips) is rejected again for `ADR-0013`'s own liveness reason; what a caller actually wants — accumulate writes over several round trips, commit atomically — needs no lock: `Begin` opens a per-connection `Vec<TransactionOp>`, an admitted `UpdateField` is staged and answered `Staged { index }` (nothing applied, no lock, no validation), `Commit` runs `apply_transaction` on the buffer exactly as one `Transaction` (`Ok` or `TransactionFailed` naming the staged index), `Rollback` or a disconnect discards; `NoSession`/`SessionOpen`/`SessionFull` for misuse with the connection open, `MAX_STAGED_OPS` (4096) as the cap; reads inside a session see committed state only (no read-your-writes, named). These are the first version-3 variants — `PROTOCOL_VERSION = 3`, `handle_connection` keeps the negotiated version at last, `Begin`/`Commit`/`Rollback` on a connection below 3 are `Malformed`, `SchemaDrivenClient::begin()` → `Session` is the library's first version-gated API — exactly the trigger `ADR-0022` named. **Part B, `ADR-0025` — crash-atomic batches.** `TXN-FR-007`'s gap (a crash between two of a batch's slot writes reaching disk leaves a partial batch durable) closes with a redo journal, not the transaction manager `FUTURE-GROWTH` predicted, because every operation is an idempotent overwrite of a slot keyed by an undeletable id: the domain adapter, opt-in via `with_journal(store, path)`, appends the validated batch and `fsync`s it *before* the first write inside the same `with_exclusive` closure, replays every complete entry on the next open (torn tail discarded), and checkpoints (`flush` then truncate) by `JOURNAL_CHECKPOINT_BYTES` and at open — `hybrid.rs`'s discipline with the batch as the unit and the `.mmap` files as the snapshot. Placement is the real decision: only the adapter both sees a batch and can replay one operation; the store knows neither `TransactionOp` nor `FieldRef`. No store, format, lock, or `Cargo.toml` change; one appended `ErrorCode::Journal`; one `fsync` per batch, paid only by journaled adapters, measured by a new bench row. Acceptance options were `ADR-0024` (a) as proposed (recommended), (b) lock-held session with idle timeout and forced abort, (c) close as not warranted; `ADR-0025` (a) as proposed, opt-in (recommended), (b) always-on, (c) close as not warranted; the owner chose (a) for both ("1, 1"). Implementation is `SERVER-001` v0.14.0 / FR-024 (Part A) then v0.15.0 / FR-025 (Part B), as separate units, next. Before this, `SERVER-MTLS` (`SERVER-001` v0.13.0 / FR-023, **Implemented** in PR #135) — the accepted `SERVER-MTLS-DESIGN` / `ADR-0023` implemented exactly as designed, with no deviation: `TlsConfig::new_with_client_auth` / `from_pem_files_with_client_ca` / `from_env` honoring an optional `SERVER_TLS_CLIENT_CA_PATH` (set without the chain/key pair is `Some(Err(Io(NotFound)))` naming the variables — the table factored as `from_env_values` for a hermetic unit test) / `requires_client_certificate()`; `ClientTlsConfig::with_identity` / `with_identity_pem_files` / `has_identity()` with a hand-written `Debug` that never prints the key; `dog_server` names the variable and reports the mode. Admission only, layered under `AuthConfig`: `handle_connection`, `serve`, `AuthConfig`, `TokenClass`, the wire, `PROTOCOL_VERSION`, and `Cargo.toml` are unchanged — the acceptor carries the policy. Four tests appended to `tests/server_tls_integration.rs` on a throwaway `rcgen` CA and one unit test in `src/server/mod.rs`; every `SERVER-MTLS-DESIGN` acceptance criterion 1–10 recorded as holding in `SERVER-001`. Before this, `SERVER-MTLS-DESIGN` (**Proposed** in PR #133, **Accepted** as proposed in PR #134; design doc `docs/design/SERVER-MTLS-DESIGN.md` + `ADR-0023`) — `ADR-0014`'s first revisit trigger taken as a design round, the owner's second pick ("1, 2, 3, then 4") after the legacy Evidence backfill (PR #132). The question the design actually settles is what a verified client certificate *means*: `SERVER-AUTH-DESIGN` left open whether mTLS would replace the token scheme or layer alongside it, and the answer proposed is **layer** — the certificate is an *admission* gate (a client presenting none, or one not chaining to the operator's own CA roots, fails the handshake before any framed byte, `Authenticate` included, is read), the token still decides *class*. Proposed shape is one optional input on each end: `TlsConfig::new_with_client_auth` / `from_pem_files_with_client_ca` / `from_env` reading an optional `SERVER_TLS_CLIENT_CA_PATH` (set without chain/key is a startup error, never a silent downgrade), on `rusty_tls::TlsAcceptor::new_with_client_auth`; `ClientTlsConfig::with_identity` / `with_identity_pem_files` on `rusty_tls::TlsStream::new_with_client_identity`. `handle_connection` does not change — the acceptor carries the policy and the lazy handshake already fails a rejected client on the first `read_message` (`TLS-FR-003`) — nor do `serve`, `AuthConfig`, `TokenClass`, the wire, `PROTOCOL_VERSION`, or `Cargo.toml` (`rcgen` already issues a CA and a signed leaf). Rejected: class derived from the certificate — the pinned `rusty_tls` exposes no peer-certificate accessor and this crate has no X.509 parser, so it is named as the future revisit with admission as its strictly compatible base; certificate replacing tokens (discards the `ReadOnly`/`ReadWrite` split for nothing); optional client certificates (not exposed, and not an admission check). Out of scope, named: revocation (client-side only in `rusty_tls`), issuance/distribution, rotation. Every `rusty_tls` and `rcgen` claim verified against the pinned/vendored source. Acceptance options were (a) accept as proposed (recommended), (b) hold for class-from-certificate (open the upstream accessor first), (c) close as not warranted; the owner chose (a). Before acceptance the design's throwaway probe (never committed) confirmed the mechanism end to end: an `rcgen` CA-signed `ClientAuth` leaf round-trips through `TlsAcceptor::new_with_client_auth`; no identity fails at the client's first write with `AlertReceived(CertificateRequired)`; a different CA's leaf fails with `BadSignature`; an empty root set is `InvalidClientCaRoots` at construction; no hangs. Implementation is `SERVER-001` v0.13.0 / FR-023 (`MTLS-FR-001`–`008`), the next unit. Before this, `SERVER-CLIENT-TLS` (`SERVER-001` v0.12.0 / FR-022, **Implemented** in PR #130) — `SchemaDrivenClient` catching up with the server's own transport encryption, the owner's pick ("1") of the three follow-ups `SERVER-CLIENT-AUTH` closed on. `connect_with(addr, ConnectOptions)` is now the general constructor (`connect` and `connect_authenticated` delegate to it); `ConnectOptions::tls(ClientTlsConfig)` wraps the TCP connection in a `rusty_tls::TlsStream` — the client-side half of the same wrapper `TlsConfig` uses, so this crate still never touches `rustls` directly — before the `Hello`, so the `Hello`, any token, the schema fetch, and every later request travel encrypted. `ClientTlsConfig::new(server_name, TrustPolicy)` is the explicit client-side trust configuration `ADR-0014`'s Consequences said a self-signed certificate would need; `rusty_tls::TrustPolicy` is re-exported from `server::client` so a caller need not depend on `rusty_tls`. `rusty_tls::TlsStream::new` performs no I/O, so a server name or policy it rejects is the new `ClientError::Tls` before anything is sent, while a rejected certificate or a non-TLS server surfaces under the `Hello` as `Frame(Io(..))` — and a plaintext `connect` against a TLS server fails there too, before any token is written. Named, not mitigated: a TLS client against a *plaintext* server can block rather than fail (the server reads the ClientHello as a frame length and waits), true of any `rusty_tls` client. Internally the client holds one stream (a `TlsStream` cannot be split by `try_clone`, the server's own v0.9.0 finding) and assembles each frame before a single `write_all`, so the plaintext path still does one write per request; `ClientError` gaining `Tls` is the one shape change to an existing type. No wire, protocol-version, server, or `Cargo.toml` change; three tests appended to `tests/server_tls_integration.rs`, beside the raw `rusty_tls::TlsStream` coverage the client must match. A bounded, additive completion of FR-015 (the client) and FR-019 (transport encryption) — no new ADR or design document, per the v0.8.0/v0.11.0 precedent. Before this, `SERVER-CLIENT-AUTH` (`SERVER-001` v0.11.0 / FR-021, **Implemented** in PR #128) — `SchemaDrivenClient` catching up with the server's own authentication gate, the owner's pick ("1") of the two independent items `SERVER-PROTOCOL-VERSION` surfaced and left out of scope. `connect_authenticated(addr, token)` sends `Request::Authenticate` between the `Hello` and the `DescribeSchema`; the token has to reach the *constructor* because `AUTH-FR-002` gates the schema fetch itself, so plain `connect` cannot succeed on an `AuthConfig`-configured server — it now fails with the server's own `Unauthenticated` rather than `UnexpectedResponse("Schema")`, the one change to an existing path. `authenticate(&mut self, token)` re-presents a token mid-connection and changes the class in both directions; a rejected token changes nothing, exactly the raw protocol's `AUTH-FR-001`. Neither learns the granted `TokenClass` — the server answers `Authenticate` with a bare `Ok`. Both are no-ops on a server with no tokens configured (`AUTH-FR-007`). Plaintext only, as the client has always been: the plaintext `Hello` fails a TLS server's handshake before any token is written. No wire, protocol-version, server, or `Cargo.toml` change (`Authenticate` is a version-1 variant); two tests appended to `tests/server_auth_integration.rs`, beside the raw-protocol coverage the client library must match. A bounded, additive completion of FR-015 (the client) and FR-016 (authentication), taken from `ADR-0022`'s named revisit trigger — no new ADR or design document, per the v0.8.0 precedent. The other item — a reconnect-without-hello fallback for a pre-hello server — stays as `ADR-0022` left it: none deployed, named, not mitigated. Before this, `SERVER-PROTOCOL-VERSION` (`SERVER-001` v0.10.0 / FR-020, **Implemented** in PR #126) — the implementation unit for `SERVER-PROTOCOL-VERSION-DESIGN` / `ADR-0022` (proposed in PR #124, accepted as proposed in PR #125), the owner's pick ("1") from the ranked open questions `STORAGE-018` left with a trigger and no driver: `SERVER-001` v0.9.1's *"frames carry no protocol version and there is no hello handshake, so a `Request`/`Response` change is a named incompatibility, not a negotiated one — revisit trigger: a second deployed client build"*, taken ahead of the trigger because the handshake a second build must speak is cheapest to settle while there is one build. What two builds would break today, stated concretely: a request an older server does not know is a `bincode` decode error on the variant index and `handle_connection` closes the connection with no reply (the client sees `FrameError::Io`, an EOF); a newer server has no way to learn which `Response`/`ErrorCode`/`ScanValue`/`ValueKind` variants an older client can decode. Proposed: `pub const PROTOCOL_VERSION: u32 = 2` (version 1 is retroactively the v0.9.1 shape — what a client that never says hello speaks); `Request::Hello { protocol_version }` / `Response::Hello { protocol_version }` appended at index 10 on both enums, optional, first frame if sent, answered by `handle_connection` before the `Authenticate` intercept and the auth gate (the `ADR-0012` precedent) with `min(client, server)` — version 0 or a non-first `Hello` is `Malformed`, `dispatch` maps it to `Unsupported` as it does `Authenticate`; four compatibility rules written into `protocol.rs` and `SERVER-001` (append-only, never reorder/insert/remove/retype; every variant records its introducing version in a per-version table; a server sends a variant introduced at *N* only on a connection negotiated at ≥ *N*, nearest older shape otherwise; a client sends a request introduced at *N* only after negotiating ≥ *N*); `SchemaDrivenClient::connect` says `Hello` before `DescribeSchema` and exposes `server_protocol_version()`. Rejected, each with its reason in the design: a per-frame version in the length prefix's top byte (zero wire cost, but it versions the transport for a per-connection property, caps at 255, changes `FR-002` and the 16 MiB edge), a field on `DomainSchema` (a format change to `Response::Schema` for every existing client, and unreachable before authentication), a feature bitmap (the version integer *is* the capability set under append-only), `min_supported` (always 1 until a major break is designed), renegotiation, and building the gating branch now (no variant to gate at version 2 — dead code behind a public-signature change to `dispatch`). Every v0.9.1 golden vector unchanged; `framing.rs`, `MAX_FRAME_BYTES`, the codec, `benches/server.rs`, and every integration suite untouched (they are version 1's regression test); no dependency. **Acceptance question, three options, first recommended — resolved: (a) accepted as proposed, (b) and (c) declined, no changes requested**: (a) as proposed; (b) hello *required* (a non-`Hello` first frame is `Malformed` — breaks every existing client for no gain today); (c) rule only, no `Hello` (the server still cannot know its peer's version, so no new `ErrorCode` could ever answer an old request, and an old server still drops a new client silently). Named limitation: a pre-hello server drops a hello-sending client with no reply — none is deployed, so named, not mitigated. **What landed in PR #126** (`SERVER-001` v0.10.0 / FR-020, per `PROTO-FR-001`–`008`): `src/server/protocol.rs` — `pub const PROTOCOL_VERSION: u32 = 2`, `Request::Hello`/`Response::Hello` appended at index 10, the per-version variant table and the four compatibility rules in the module docs, golden vectors for both `Hello { 2 }` (`0a 00 00 00 02 00 00 00`) appended to the pinned-bytes tests, and a test asserting the constant against the table; `src/server/mod.rs` — `handle_connection` reads its first frame with one `bool` of state and answers a first-frame `Hello` itself, before the `Authenticate` intercept and the auth gate, with `min(client, PROTOCOL_VERSION)`, answers version 0 or a non-first `Hello` with `Malformed` and keeps the connection open, and `dispatch` maps `Hello` to `Unsupported` as it does `Authenticate`; `src/server/client.rs` — `SchemaDrivenClient::connect` sends `Hello { 2 }` before `DescribeSchema`, treats any other reply as `UnexpectedResponse`, and exposes `server_protocol_version()`; `tests/server_protocol_version.rs` (new, `required-features = ["server", "research"]`, all three domains) — the design's acceptance criteria 3, 4, 7 and 8 over real sockets; criteria 1–2 and 5 are unit tests in `protocol.rs`/`mod.rs`, criterion 6 is every pre-existing suite unchanged. Every v0.9.1 golden vector, `framing.rs`, `MAX_FRAME_BYTES`, the codec, `benches/server.rs`, and every existing suite untouched; no dependency. One recorded deviation from `PROTO-FR-008`/criterion 10 ("`Cargo.toml` unchanged"): the new suite's `[[test]]` registration — one line, the same line every server suite has, so it compiles under default features. The pointer set is in place: `SERVER-001`'s wire-shape open question resolved, `STORAGE-018`'s non-goal and open question, `ADR-0010`, `ADR-0021`, `SERVER-QUERY-LAYER-DESIGN`, and item 33 below. Sweep: `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 337 lib tests (333 prior + 4: `protocol.rs`'s `protocol_version_is_the_one_the_table_names` and both pinned-bytes tests extended with the `Hello` golden vectors; `mod.rs`'s `dispatch_never_routes_hello_to_a_store`, `hello_is_answered_unauthenticated_with_the_min_version`, `hello_version_zero_and_a_second_hello_are_malformed_but_not_fatal`) plus every integration suite including the new `tests/server_protocol_version.rs` (4/4: pre-auth `min` negotiation, version 0 / late hello `Malformed` with the connection open and a silent client served, `SchemaDrivenClient` negotiating 2 on all three domains, an unknown request index closing the connection with no reply); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`; three intra-doc links in the new `src/server/mod.rs` section were rewritten to `crate::server::protocol` paths so they resolve). Next: nothing queued from this round — two independent items it surfaced, neither in scope, are listed at item 70. Before this, `BINCODE-ENCODING-STABILITY` (`STORAGE-018` v0.1.0, **Implemented** in PR #120) — the implementation unit for the accepted `ADR-0021` / `BINCODE-ENCODING-STABILITY-DESIGN` round, the fourth and last of the owner's queued follow-ups ("1, 2, 3, then 4"), resolving `ADR-0010`'s named limitation, `SERVER-001`'s open question, item 33, and `ADR-0019`'s trigger note by pointer. What landed, in three commits: (1) the **capture** commit (`4ae86bd`) — golden byte vectors pinned against the pre-codec code, before any call site moved: `src/test_support.rs`'s `hex_literal`/`assert_golden`/`assert_golden_eq` helpers (which print the actual bytes as a paste-ready literal on drift), primitive/`Option`/enum/struct/`Uuid` shapes in a tests-only `src/codec.rs`, every `Request`/`Response` variant in `src/server/protocol.rs`, and one body each of `DOGBLOB\0`/`GENBLOB\0`/`GENEDGE\0` in the three blob modules (+12 tests, 316 → 328 under `--all-features`); (2) the **routing** commit (`049e4f6`) — `src/codec.rs` becomes real: `pub(crate)` `encode`/`encode_into`/`decode` over one `options()` (`DefaultOptions::new().with_fixint_encoding().with_little_endian().with_no_limit().reject_trailing_bytes()`), all 23 production call sites plus the test helpers routed so `grep -rn "bincode::" src` finds only `codec.rs` and the two error-type positions (`FrameError::Encoding`, `DurabilityError::Serde`), every golden literal from (1) passing unchanged, module docs recording the configuration, the concrete format, the 1.x stability promise, and the evolution rules (+5 tests, 328 → 333: trailing bytes rejected while the free functions' bytes equal ours, `encode_into` ≡ `encode`, a framed `Request` with two junk bytes → `FrameError::Encoding`, a `GENBLOB\0` body with trailing junk → still a fingerprint mismatch, a `DOGBLOB\0` body with trailing junk → `RecordBlobUnreadable`); (3) this docs commit — `STORAGE-018` v0.1.0 written and registered, `SERVER-001` v0.9.1 (the wire encoding is now the codec's; its open question resolved by pointer, wire-*shape* evolution stays open with a named trigger), `ADR-0010`/`ADR-0019`/`ADR-0021`/the design doc/`ROADMAP`/this file pointed. **One implementation-time finding**, recorded in the spec's Traceability, the ADR's implemented entry, and the design doc's change history: `durability::record_blob::RecordBlob::read` decodes the body *before* fingerprinting (its fingerprint is over the decoded records), so a `DOGBLOB\0` body with trailing junk was silently accepted before this unit and is now `RecordBlobUnreadable` with a "body does not decode" cause — the one place `reject_trailing_bytes()` changed an observable outcome, exactly the narrowing the owner chose option (a) for; the two generic blobs hash bytes first and were already a fingerprint mismatch. No format bump, no dependency, no public API change; writer bytes identical everywhere (the golden vectors are the proof). Revisit triggers (`STORAGE-018` open questions): a second deployed client build (wire-shape evolution); a `bincode` 2 migration (`config::legacy()` against the golden vectors is its acceptance test); reordering `DOGBLOB\0`'s read to fingerprint-before-decode was deferred and is now **closed as not warranted** (`STORAGE-018` v0.1.1, docs-only, PR #122; the owner's call from three ranked options) — it is a `BLOB_VERSION` 2 → 3 format change, since the `Dog` blob's fingerprint is over decoded, age-free content precisely so `ProductionStore::open` does not rewrite after `update_age` and a byte hash would include the ages; the payoff would be a "fingerprint mismatch" cause in place of a "body does not decode" cause on files no conforming writer produces. Re-arms only on a second `DOGBLOB\0` writer or a measured read-path cost. Before this, the design round `BINCODE-ENCODING-STABILITY-DESIGN` (**Accepted** — the owner approved the design and `ADR-0021` as proposed, option (a) with `reject_trailing_bytes()`, spec `STORAGE-018`; proposed in PR #116; docs-only, no code) — the fourth and last of the owner's queued follow-ups ("1, 2, 3, then 4"), scoped by item 68 as *"verify and document what `bincode`'s default configuration pins today, and decide whether to pin it explicitly"*, and the limitation `ADR-0010` named when the server layer was proposed (*"`bincode`'s wire-format stability across crate versions is unverified for this new use"*), carried by `SERVER-001` as an open question and by item 33 since. Per the `ADR-0016`–`0020` precedent this round wrote `docs/design/BINCODE-ENCODING-STABILITY-DESIGN.md` + `ADR-0021` first, no code. **Verified, not recalled** — from the vendored `bincode` 1.3.3 source, `uuid` 1.25.0's `Serialize` impl, and an executed throwaway probe: every `bincode` byte this crate writes comes from the crate's free functions (`serialize`/`deserialize`/`serialize_into`), which are `DefaultOptions::new().with_fixint_encoding().allow_trailing_bytes()` — fixed-width integers at their Rust width, little-endian, no size limit, trailing bytes accepted on read. That is **not** `bincode::options()`/`DefaultOptions::new()`, which are varint and reject trailing bytes (`bincode`'s own docs carry a table to warn about the difference): a `Uuid` costs 24 bytes (a `u64` LE length of 16, then 16 bytes) rather than 17, and one probed enum value 22 bytes rather than 5. The 1.x readme promises the format is *"stable across minor revisions, provided the same configuration is used"*; `Cargo.toml`'s `bincode = "1"` keeps `cargo update` on the promise's side of 2.x; but the "same configuration" condition is satisfied by convention only — no configuration is named anywhere in `src/`, and no test pins a byte (every existing test is a same-build round trip, which passes under any self-consistent encoding). 23 production call sites, all free functions: 11 front-door (`server/framing.rs` ×2, `durability/record_blob.rs` ×3, `generic/record_blob.rs` ×3, `generic/edge_blob.rs` ×3 — the owner's "wire protocol and the three blob formats" exactly) and 12 `research`-gated or research-only-reached (`durability/mod.rs`, `hybrid.rs`, `lsm_store.rs`, `snapshot_rebuild.rs`). Everything around the bodies (frame length prefix, the 20-/28-byte blob headers, WAL entry lengths) is already explicit hand-written LE. **Proposed**: a `pub(crate)` `src/codec.rs` naming the configuration once (`DefaultOptions::new().with_fixint_encoding().with_little_endian().with_no_limit()` on both sides, `reject_trailing_bytes()` on decode) behind `encode`/`encode_into`/`decode` returning `bincode::Error` so every `?`/`map_err` compiles unchanged; all 23 sites routed so `bincode::` in production `src/` is a `grep` (`codec.rs` and the two error-type positions only); golden byte vectors — primitives and composites in `codec.rs`, every `Request`/`Response` variant in `protocol.rs`, one body each of `DOGBLOB\0`/`GENBLOB\0`/`GENEDGE\0` in their modules — captured on the pre-change code in a commit that precedes `codec.rs` and checked in as hex, so the routing is proven byte-identical by the commit history and any later change to `bincode`, `serde`, `uuid`'s impl, the configuration, or a pinned type's derive fails a named test; the evolution rules documented (appending an enum variant keeps existing bytes; reordering/inserting variants, changing struct fields or integer widths is a format change requiring the owning format's version bump); the size limit left in `read_message` (`MAX_FRAME_BYTES` before allocation) where it is; writer bytes unchanged, no format bump, no dependency change, no public API change. The one behavioral change offered is the acceptance question: (a) reject trailing bytes — a frame whose payload is a valid message plus junk becomes `FrameError::Encoding` instead of a silent discard; the blobs' fingerprint already refuses such a body and research reads are exact-length — (b) allow, a purely no-behavior-change pin, or (c) document only (rejected as the whole answer: a documented-but-unnamed configuration is one `bincode::options()` away from silently becoming a different one with every test green). `ADR-0019`'s question to this unit — whether the record *layout* needs recording in the blob headers — is answered no for now (serde exposes no layout description to hash; the change it would guard against is a version bump by the evolution rules), trigger left armed. Non-goals and revisit triggers named: `bincode` 2.x (`config::legacy()` against the golden vectors is the migration's acceptance test; `standard()` is varint, a format change), a wire-protocol version field or handshake (a second deployed client build), a `[u8; 16]` `Uuid` newtype (a third of every id, a format change), cross-language clients (item 38 — a pinned format is the precondition, not the client). Eight requirements `BINENC-FR-001`–`-008` and eight acceptance criteria for a spec at implementation — `STORAGE-018` with `SERVER-001` amended to cite it, approved by the owner over a new category. Accepted; acceptance authorizes the design only, and the next unit registers `STORAGE-018` and implements. Before this, `MULTI-FIELD-MMAP-DURABILITY` (`STORAGE-017` v0.1.0, **Implemented** in PR #114) — the implementation unit for the accepted `ADR-0020` / `MULTI-FIELD-MMAP-DURABILITY-DESIGN` round, closing `ADR-0006`'s named revisit trigger by composition rather than by widening `GenericMmapStore`. What landed: the slot-file engine `GenericMmapStore` already had (header check, `[id][value][COMMITTED]` slots, the `chunks_exact` scan fast path's byte layout, `msync`) is extracted into a `pub(crate)` `SlotFile<Id, V>` in `src/generic/slot_file.rs`, and `GenericMmapStore` is refactored onto it with all 16 of its tests textually unchanged; a new public `MmapScanned<S, R, Marker>` layer (`src/generic/mmap_scanned.rs`) owns a second `SlotFile` for one more `MmapFieldValue` field and forwards everything else to its inner store, so `Scanned`'s in-memory per-field layer now has a durable twin with the same shape; `forward_scannable_pairs!` gained `for Scanned;` / `for MmapScanned;` arms so a domain forwards its `ScannableField` pairs through either layer with one invocation each; `open` reconciles the second file against the inner store's live ids exactly as `GenericMmapStore::open` reconciles the `.mmap` against the blob (append a slot for a record the file never saw, ignore a stale slot, re-append an uncommitted one); a new `DurabilityError::SlotWidthMismatch { path, body_len, slot_width }` refuses a file whose body is not a whole number of slots (the layer refuses trailing partial bytes; the store stays permissive, as before); `OrderProductionStack` now has `DiscountCents` mutable-and-durable alongside `Amount` (`<path>.discount_cents.mmap` next to `<path>.records` / `<path>.mmap`, `discount_cents_path` names it), and `open_order_production_stack` rebuilds the stack from the three files alone; `CreatedAt` stays in-memory-only and updating it through the durable stack is a compile-time refusal. Verification: 8 new research-gated tests in `mmap_scanned.rs` plus 1 new and 1 extended in `order_customer.rs` (and 2 compile-time pair checks) pin acceptance criteria 1–8 of the spec; `cargo test --all-features` 316 lib tests, `cargo test` 124 lib + 2 integration, fmt/clippy/doc clean; two new Criterion groups (`generic_production_scan_layer`, `generic_production_update_layer`) and the before/after table in `RESULTS.md` show the core's `scan`/`update` within noise (the extraction is free), `get`/`parent` carrying the second layer's per-call cost (+22–28% at 1K/100K, +67% at 1M — the same per-layer price `Scanned`'s write-through fix measured, now on a slot-file read), and the two-file `create`/`open` cost (+15–42%). Before this, the design round `MULTI-FIELD-MMAP-DURABILITY-DESIGN` (**Accepted** as proposed by the owner in PR #112; proposed in PR #110; docs-only) — the third of the owner's four queued follow-ups ("1, 2, 3, then 4"), and `ADR-0006`'s own named revisit trigger: *"a future record shape needs more than one mutable field persisted."* `GenericMmapStore` persists exactly one `ScanMarker` field in its `.mmap`; `STORAGE-015`'s companion blob makes every other field durable but immutable (an immutable snapshot, rewritten only when `open` finds it stale or foreign, never on `update`), so `OrderProductionStack` has `Amount` mutable-and-durable and `CreatedAt`/`DiscountCents` in-memory only — item 22 below has carried this as "genuinely unscoped" since the generic library landed. Per the `ADR-0016`–`0019` precedent this round wrote `docs/design/MULTI-FIELD-MMAP-DURABILITY-DESIGN.md` + `ADR-0020` first, no code. The problem is narrower than `ADR-0006`'s phrasing: `GenericMmapStore` already settled the fixed-layout half by construction (a durable field is whatever implements `MmapFieldValue`), and the blob settled durability-of-the-rest, so the gap is precisely "mutable *and* durable for more than one fixed-width field." The design weighs the three shapes item 68 named. (1) A multi-slot `.mmap` layout — `[id][v1]…[vN][COMMITTED]`, `SCHEMA_VERSION` 3, a per-domain layout type with macro-generated `DurableSlot<R, M> { OFFSET }` impls (the same `E0119` coherence wall `forward_scannable_pairs!` exists for): one file, one reconciliation, one `msync`, but it refuses every existing `.mmap` (detection not migration, and `open` cannot heal one value into N), makes every slot helper, `create`, `open`, and the benchmark-pinned `chunks_exact` `scan` fast path layout-generic, and walks a 41-byte stride to read 8 bytes per record for `Order` — the AoS shape `ADR-0001` measured as bad for single-column scans; the most invasive form of the "real redesign" `ADR-0006` predicted. (2) **Per-field `.mmap` files via a composable layer — proposed**: `MmapScanned<S, R, Marker>`, the durable twin of `Scanned`, owning one file per marker in exactly the existing `GMMAPST\0` version-2 format, stacked over any inner store (an in-memory `BaseStore` included), `ScanField`/`UpdateField` for its marker (the gapless fast path or the sorted-positions fallback, `write_value` in place), `GetById` patching its field on the way up via `set_scannable_value` so a stack of layers patches each in turn, `Flush` own-`msync`-then-inner, generic forwards for `FilterEq`/`Neighbors`/`Children`, cross-marker `ScanField`/`UpdateField` from `forward_scannable_pairs!` generalized to take the layer type; no blob of its own (the record set is the inner store's concern); the slot/header/commit-marker/reconcile machinery extracted from `GenericMmapStore` into a `pub(crate)` `SlotFile<Id, V>` both use, with `GenericMmapStore` delegating and its tests and benchmarks the guard (duplication the fallback if the extraction moves the fast path beyond noise); caller-supplied path per layer (`STORAGE-016`'s precedent) with the domain constructor deriving siblings from one base (`<path>`, `<path>.records`, `<path>.discount_cents.mmap`); `R: SchemaTag` on the file constructors, a slot-width check on `open` as the weak foreign-file guard and the tagged blob read first on the portable path as the strong one (a `.mmap` tag would be `GMMAPST\0` version 3 and refuse every existing file — deferred). This is the shape `GENERIC-SCHEMA-DESIGN` §4.2 sketched without designing ("N separate flat mmap-backed arrays, one per field, same shape as §4.1's multiple-`Scanned`-layers stacking"), and the library's own rule (`ADR-0009`: one capability, one layer). Costs: N + 1 files and a naming convention, N reconciliation passes at `open`, N `msync`s per `flush`, O(N²) generated forwards per domain. (3) Widening the blob's role — rewrite or append the `.records` blob on `update`: a full `bincode` encode + fingerprint + write per update (milliseconds against the nanoseconds `ADR-0006` called the standout result), or a second WAL beside `STORAGE-008`'s; rejected for fixed-width fields, kept as the named fallback for a variable-width mutable field. `OrderProductionStack` would become `Reversed<MmapScanned<GenericMmapStore<Order, Status, Amount>, Order, DiscountCents>, Customer, Order, BelongsToCustomer>` — `DiscountCents` (the domain's refund-adjustment case) as the in-crate proof, `CreatedAt` deliberately left in-memory so "durable is opt-in per field" stays visible. Non-goals named: variable-width fields, atomic multi-field updates (two `update`s are two independent writes, as today — `ADR-0013`'s territory, not storage's), any format change, a mutable `IndexedField`, `redb`/LSM, `ProductionStore`, the stack manifest. Nine requirements `MFMD-FR-001`–`-009` and eight acceptance criteria (two-field round trip; per-file reconciliation and torn-slot repair; two-file `flush`; portable open after updates with no blob rewrite; slot-width refusal; every existing `GenericMmapStore` test and benchmark unchanged through the extraction; macro-generated forwards only; an in-memory inner) plus one Criterion row for a `STORAGE-017` v0.1.0 at implementation. Revisit triggers: a variable-width mutable field; a `.mmap` opened without a tagged blob; enough files that one naming rule stops being adequate; the extraction making `GenericMmapStore`-as-`MmapScanned` obviously cheap; a workload needing several fields of one record read together at scan speed (option 1 as an additional store). Preceding milestone: `BLOB-SCHEMA-TAG` (**Implemented** — `STORAGE-015` v0.2.0 / `STORAGE-016` v0.2.0, PR #108: the `SchemaTag` trait, both generic blobs at version 2 with the 8-byte tag hash at offset 20, foreign-`R` and version-1 blobs refused by name on the read-only paths and rewritten by `open`, 12 new tests) closing `BLOB-SCHEMA-TAG-DESIGN` (**Accepted** — the owner approved it as proposed) — the second of the owner's four queued follow-ups ("1, 2, 3, then 4"), and the one open question `STORAGE-015` and `STORAGE-016` both hold and agreed to resolve together: neither generic companion blob records which `R` it holds, so a `.records` blob written for `Employee` handed to `GenericMmapStore::<Order, _, _>::open_portable` passes magic, version, and fingerprint (the fingerprint covers the body, whichever `R` encoded it) and fails — if at all — inside `bincode`, while every `Uuid`-keyed domain's `.edges` blob decodes as every other's. Per the `ADR-0016`/`0017`/`0018` precedent this round wrote `docs/design/BLOB-SCHEMA-TAG-DESIGN.md` + `ADR-0019` first, no code. The design: a new opt-in `SchemaTag { const SCHEMA_TAG: &'static str }` trait in `src/generic/traits.rs` — a stable, domain-chosen name, not a supertrait of `Record` (`std::any::type_name` rejected as documented-unstable across compiler versions and path-embedding; a required const on `Record` rejected as touching all nine impls, most in-memory-only); an 8-byte `u64` LE FNV-1a 64 hash of the tag at offset 20, after the unchanged 20-byte shared header, so `TAGGED_HEADER_LEN` is 28 and `durability::record_blob`'s `parse_header`/`encode_image` and `DOGBLOB\0` are untouched (new `pub(crate)` helpers in `generic::record_blob`, imported by `generic::edge_blob`); `GENBLOB\0` and `GENEDGE\0` `BLOB_VERSION` 1 → 2 — `STORAGE-015` v0.2.0 and `STORAGE-016` v0.2.0 at implementation; check order magic → version → tag → fingerprint → decode, a tag mismatch a `RecordBlobUnreadable { path, cause }` whose cause names the expected tag string and both hashes (no new variant); `is_current_at` reads 28 bytes and requires all four to match, so `GenericMmapStore::open`/`Symmetric::open` heal a version-1 or foreign-`R` blob by rewriting it (the `DOGBLOB\0` 1 → 2 story) while `read_portable_*`/`open_portable` refuse it; the `R: SchemaTag` bound on exactly the blob-touching functions — `GenericMmapStore`'s `create`/`open`/`read_portable_records`/`open_portable` moved into their own `impl` block (the `STORAGE-016` shape; the all-blocks `STORAGE-015` precedent rejected as six blocks for four functions), and `Symmetric`'s `STORAGE-016` block — with the private slot helpers, every trait impl, `Symmetric::new`, `Reversed`, and the in-memory stacks unbounded; `Order` (`"order_customer::Order"`), `Employee` (`"employee::Employee"`), and the `GenericProductionStore` doctest's `Widget` the whole in-crate impl set; the edge blob tagged with `R::SCHEMA_TAG` passed as a value (`EdgeBlob` is generic over `Id`, not `R`; a `Marker`-level tag on `SymmetricRelation` rejected — two relations over one `R` would share a tag, a semantic ambiguity named and aligned with `ADR-0018`'s revisit trigger). Named breaking twice — on disk (no build reads the other version's body) and in the API (every `R` used with the constructors implements `SchemaTag`). Offered at acceptance as the alternative: a readable fixed-width 32-byte NUL-padded tag string (a 40-byte header, a 32-character cap, a new create-time failure). Deferred: tagging the `.mmap` file, distinct tags for two `Symmetric` relations over one `R`, the stack manifest. Before this, `SYMMETRIC-EDGE-PORTABILITY` — the accepted `SYMMETRIC-EDGE-PORTABILITY-DESIGN` became real code, registered as `STORAGE-016` v0.1.0, completing the first of the owner's four queued follow-ups ("1, 2, 3, then 4"). `src/generic/edge_blob.rs` (new, `pub(crate)`) holds the `Symmetric` layer's companion edge blob: `GENEDGE\0` magic, blob version 1, the same 20-byte magic/version/FNV-1a header as `STORAGE-014` v0.2.0 via the already-`pub(crate)` `encode_image`/`parse_header`/`Fnv1a64`/`EncodedRecordBlob` (their third call site, nothing duplicated), body the bincode `Vec<(Id, Id)>` in caller order — the edge list, not the adjacency map; `EdgeBlob<'a, Id>` borrows the slice and fingerprints it by streaming into `Fnv1a64`; `read(path)` verifies magic, version, and body-vs-header fingerprint, every failure a `RecordBlobUnreadable { path, cause }` naming the edge blob; `edges_path(path) -> <path>.edges`. `src/generic/store.rs` gains exactly one `impl` block, bounded `R::Id: Serialize + DeserializeOwned`: `Symmetric::create(inner, edges, edges_path)` (write, then `new`), `open(inner, edges, edges_path)` (20-byte header check, rewrite only when stale/missing/unreadable, then `new` from the caller's edges), `read_portable_edges(edges_path)` (blob only, never writes), `open_portable(inner, edges_path)` (= `new(inner, &read_portable_edges(edges_path)?)`); `Symmetric::new`, `Neighbors`, and every forwarding impl untouched, no existing call site changed. The file also gains its first `#[cfg(test)] mod tests` (6 tests over an in-memory `BaseStore` inner). `src/generic_spike/employee_impl.rs` (research-gated): `create_employee_production_stack`/`open_employee_production_stack` keep their signatures and switch to `Symmetric::create`/`open` at `<path>.edges`; new `open_employee_production_stack_portable(path)` rebuilds the whole `Reversed`-over-`Symmetric`-over-`GenericMmapStore` stack from `<path>`, `<path>.records`, and `<path>.edges` — the record blob read once (for the core store and for `Reversed`), the edge blob once. `RecordBlobUnreadable`'s doc comment gains one sentence naming the edge blob; no new error variant. 23 new tests (12 in `edge_blob.rs`, 6 in `store.rs`, 5 in `employee_impl.rs`): every acceptance criterion the design named — identical `get`/`filter_eq`/`scan`/`update`/`parent`/`children`/`neighbors` (order included) after a portable reopen with no arguments; three files copied elsewhere reopen; a missing `.edges` is a typed error the existing `open` heals; `open` rewrites only when the edge list changed, a reordered list counts as changed; a `GENBLOB\0`/`DOGBLOB\0` file at the edge path is a magic error, not a decode attempt. One deliberate departure from the design's sketch, recorded in the spec: `EdgeBlob::fingerprint` returns `Result<u64, DurabilityError>` (serialization can fail and `expect` is forbidden outside tests), and `is_current_at` treats that failure as "not current" so `open` surfaces it through the rewrite. Cost not measured, per the design (the mechanism is the one `STORAGE-015` measured at ~4% of `open` for records; an edge is 32 bytes to a record's ~76; no published Criterion group times the `Employee` stack). `GenericMmapStore`, `.mmap`, `.records`, `Reversed`, `dog_impl.rs`, and `build_employee_generic_store` are untouched; `STORAGE-015` stays v0.1.0. Before this, `SYMMETRIC-EDGE-PORTABILITY-DESIGN` (**Accepted** — the owner approved the design and `ADR-0018` as proposed, no changes requested; docs-only, no code) — the first of the four follow-ups the owner queued after `GENERIC-STORE-FINGERPRINT-MEASUREMENT` ("1, 2, 3, then 4"), and the first open question `GENERIC-STORE-PORTABILITY-DESIGN` left: `Symmetric`'s external edge list, the one relation-layer state not derivable from records, so `Employee`'s durable stack (`Reversed` over `Symmetric` over `GenericMmapStore`, research-gated) still needs the caller to hold `collaboration_edges` and got no portable helper under `STORAGE-015`. Per the `ADR-0016`/`ADR-0017` precedent this round wrote `docs/design/SYMMETRIC-EDGE-PORTABILITY-DESIGN.md` + `ADR-0018` first, no code. The design: a `Symmetric`-level companion blob at a caller-supplied `edges_path` (`GENEDGE\0`, version 1, the `STORAGE-014` v0.2.0 20-byte header via the already-`pub(crate)` `encode_image`/`parse_header`/`Fnv1a64`/`EncodedRecordBlob`, body a bincode `Vec<(R::Id, R::Id)>` in the order given — the edge list, not the adjacency map, so the bytes are deterministic and half the size; order is part of the fingerprint since it is observable through `neighbors`); a parallel `create(inner, edges, edges_path)`/`open(inner, edges, edges_path)`/`open_portable(inner, edges_path)` triple plus `read_portable_edges(edges_path)` in a separately bounded `impl` block (`R::Id: Serialize + DeserializeOwned`, that block only), `Symmetric::new` and every existing impl untouched — the parallel-constructor shape `ADR-0017` rejected for `GenericMmapStore` is right here because `new` has no path to refresh a blob from, and the residual by-convention gap (a durable stack assembled with `new` writes no blob) is named in the design's non-goals; `edges_path(path) -> <path>.edges` as the single-relation convention (the path is caller-supplied because a stack may hold two `Symmetric` layers); `RecordBlobUnreadable` reused with `path` naming the edge blob rather than a breaking new variant; `create_employee_production_stack`/`open_employee_production_stack` keep their signatures and switch to `Symmetric::create`/`open`; new `open_employee_production_stack_portable(path)` builds the whole stack from `<path>`, `<path>.records`, and `<path>.edges`. Out of scope, named: any change to `GenericMmapStore`/`.mmap`/`.records` (`STORAGE-015` stays v0.1.0), `Reversed`, `dog_impl.rs`, a stack manifest, a schema tag (deferred with `STORAGE-015`'s, follow-up 2). Accepted; acceptance authorizes the design only, and the next unit registers `STORAGE-016` and implements. Before this, `GENERIC-STORE-FINGERPRINT-MEASUREMENT` — a docs-only round that measured `STORAGE-015`'s named fallback before building it, and closed it. The owner asked for the trait-method fingerprint (the design's answer if `open`'s streamed-serialization fingerprint cost landed nearer `STORAGE-014` v0.1.0's +27% than v0.2.0's +0.3–4% at 1M, which the Criterion pair appeared to say). Measured in place (the same binary with the fingerprint stubbed out), the shipped fingerprint is ~52 ms of a 1,222 ms `open` at 1M — 4%; the Criterion group re-run on unchanged code drifted +8.8% against itself, so its +24–27% was drift. In isolation a whole-record hand-walk saves 7 ms per million `Order`s (72 vs. 79 ms — same bytes hashed); only hashing fewer fields goes lower (42 / 21 ms), at the cost of a reopen with a changed non-hashed field keeping the blob silently stale, the gap `ADR-0016` rejected. Offered the three-way choice (build whole-record for ~7 ms; build id-only for ~50 ms at that cost; close), the owner chose **close, not warranted**: the streamed fingerprint stays, `BLOB_VERSION` stays 1, no record-trait API is added, `STORAGE-015` stays v0.1.0. `RESULTS.md` gains `#### Follow-up: the trait-method fingerprint, measured and not built` and its earlier "Criterion is the trustworthy one" verdict is marked superseded; the design doc, ADR-0017, and the spec's open question record the closure. Before this, `GENERIC-STORE-PORTABILITY` — the accepted `GENERIC-STORE-PORTABILITY-DESIGN` became real code, registered as `STORAGE-015` v0.1.0, closing follow-up (b) of the owner's "1 then 2". `src/generic/record_blob.rs` (new) holds the generic companion blob: `GENBLOB\0` magic, blob version 1, the same 20-byte magic/version/FNV-1a header as `STORAGE-014` v0.2.0, then the bincode `Vec<R>`; `GenericRecordBlob<'a, R>` borrows the record slice and fingerprints it by streaming `bincode::serialize_into` a `Fnv1a64` that `impl`s `io::Write` (no per-type hand-walking, no allocation). `STORAGE-014`'s `HEADER_LEN`/`Fnv1a64`/`parse_header`/`encode_image`/`companion_path`/`EncodedRecordBlob` became `pub(crate)` at their second call site; `RecordBlob`'s 12 tests are unchanged. `GenericMmapStore::create` now also writes the blob (encoded before the records are consumed, written after the `.mmap` flush); `open` reads the 20-byte header, re-encodes only when the fingerprint is stale, and writes after the existing open (which also heals a pre-`STORAGE-015` directory holding only the `.mmap` file); two new additive functions, `read_portable_records(path) -> Vec<R>` (persisted order preserved, so a `Reversed` layer rebuilt from it is deterministic) and `open_portable(path)` (= `open(read_portable_records(path)?, path)`, no second code path); `open_order_production_stack_portable(path)` rebuilds the whole `Order`/`Customer` stack, `Reversed` included, from the two files alone. The one API change the design named: `R: Serialize + DeserializeOwned` joins the store's bounds, so `Order`/`OrderStatus`/`Customer` and `Employee`/`Department` gain serde derives. The `.mmap` slot format is untouched — the diff removes only the six bound lines — and `GenericProductionStore` is unchanged. 16 new tests (9 in `record_blob.rs`, 6 in `mmap_store.rs`, 1 stack test in `order_customer.rs`) plus the `production.rs` doctest reopening via `open_portable`. Two deliberate departures from the design's sketch, recorded in the spec: no bound on `R::Id` (only `Vec<R>` is serialized), and no `Employee` portable helper (its `Symmetric` edge list stays the deferred decision). **Cost, measured, and the one finding that is not clean**: `create` roughly doubles at every size (the blob write, ~76 B/record, 3× the `.mmap` file). `open` pays the streamed-serialization fingerprint on every call: a ~0.1 ms floor at 1K, +19–33% at 100K (10–17 ms of encoding on a ~52 ms call), and at 1M — the cell the design's fallback trigger names — the throwaway example's three-sample medians say inside noise (−4% to +2%) while the published 20-sample Criterion `generic_production_open` group, run twice against a `git stash`-isolated before-run, says +24–27% (about 300 ms on 1.25 s). `RESULTS.md` records both and treats the Criterion figure as the trustworthy one, which trips the trigger: the design's named fallback — a per-type trait-method fingerprint over immutable fields only, no serialization pass — is the next step, the owner's call, not built here (it adds to the record traits' API). Unlike `STORAGE-014`, two published Criterion groups (`generic_production_create`/`generic_production_open`) time these constructors, so their numbers move; `RESULTS.md`'s new `### GenericMmapStore file portability (STORAGE-015)` subsection carries the throwaway table, the Criterion before/after pair, and the verdict. Before this, `GENERIC-STORE-PORTABILITY-DESIGN` (**Accepted** — the owner approved the design and `ADR-0017` as proposed, no changes requested; docs-only, no code) — follow-up (b) of the two the portability implementation named, taken on the owner's "1 then 2" once (a) merged: the identical file-portability treatment for `GenericMmapStore`/`GenericProductionStore`, written up as `docs/design/GENERIC-STORE-PORTABILITY-DESIGN.md` + `ADR-0017` first, per the `PRODUCTION-STORE-PORTABILITY-DESIGN`/`ADR-0016` precedent. The gap is the same one-durable-field wall `GENERIC-SCHEMA-DESIGN.md` §4.2 hit with `Order`: `GenericMmapStore` persists one fixed-width `(id, scan value)` slot per record, so `create`/`open` both need the caller's full `Vec<R>` and the file alone can't reconstruct a store. The design ports `STORAGE-014` v0.2.0 as-is where it can — a `<path>.records` companion blob behind the same 20-byte magic/version/FNV-1a header (`GENBLOB\0`, version 1), the same temp-then-rename write, the same `RecordBlobUnreadable` error — and makes the choices the generic shape forces where it can't: the fingerprint is FNV-1a over the streamed `bincode` encoding of `Vec<R>` (no per-type hand-walking; it includes the mmap-backed field, an accepted spurious-rewrite cost with a trait-method fingerprint named as fallback); `create`/`open`'s existing bounds gain `Serialize + DeserializeOwned` rather than adding a parallel constructor pair that would leave a plain `open`'s blob silently stale (the one breaking change, named; every in-crate record type is one derive line away); two new additive functions, `read_portable_records(path) -> Vec<R>` (persisted order preserved, so `Reversed`'s child order stays deterministic) and `open_portable(path)`; and `open_order_production_stack_portable(path)` as the stack-level helper. Relation layers are not in the blob: `Reversed` derives everything from the records; a `Symmetric` edge list (only `Employee`'s research-gated spike uses one durably) is explicitly out of scope, a separate later decision. `GenericMmapStore`'s own `.mmap` format and `GenericProductionStore` are unchanged. Accepted; the next unit registers `STORAGE-015` and implements. Before this, `RECORD-BLOB-FINGERPRINT` — `STORAGE-014` v0.2.0, blob version 2: a `u64` FNV-1a fingerprint of the record set's immutable content (`id`/`breed`/edges, never `age`) now follows the version in the record blob's 20-byte header, so `ProductionStore::open`'s steady-state check is a 20-byte read and a fingerprint compare instead of serializing and byte-comparing the whole record set. Re-measured: `open` +0.3-4% at 1M (was +27%); version-1 blobs are upgraded in place on the first `open`. This is follow-up (a) of the two the portability implementation named, taken on the owner's "1 then 2"; (b), the same treatment for `GenericMmapStore`/`GenericProductionStore`, is next and gets its own design doc + ADR first. Before this, `PRODUCTION-STORE-PORTABILITY` — the accepted `PRODUCTION-STORE-PORTABILITY-DESIGN` became real code, registered as `STORAGE-014` v0.1.0 (the owner's "go now" after accepting the design). `src/durability/record_blob.rs` (new) holds the companion record blob — `RecordBlob { records, edges }` bincode-serialized behind a `DOGBLOB\0` magic + `u32` version header, written to `<path>.records` via `MmapAgeStore`'s own write-to-temp-then-atomic-rename pattern. `ProductionStore::create` now also writes it; `ProductionStore::open` reads and byte-compares it against the caller's record set and rewrites only on a mismatch (which also heals a pre-`STORAGE-014` directory holding only the ages file); the new additive `ProductionStore::open_portable(path)` reconstructs the whole store from the path alone — copy `dogs.mmap` + `dogs.mmap.records` together and the pair is a self-contained database, the way SQLite's/DuckDB's own files are. A new `DurabilityError::RecordBlobUnreadable { path, cause }` covers missing/short/wrong-magic/wrong-version/truncated blobs, distinct from the ages file's own `InvalidMagic`/`SchemaVersionMismatch`. `src/durability/mmap_store.rs` is untouched (empty diff, verified) and its existing suite passes unmodified. 9 new unit tests in `record_blob.rs`, 5 new tests in `production.rs`. **One implementation-time finding the design did not predict, measured rather than assumed**: the design's open question expected `open`'s common-case cost to be zero (the blob rewrite riding on `MmapAgeStore`'s own rewrite decision) — but that decision is private to `MmapAgeStore`, which this round deliberately didn't touch, so `open` serializes and compares on every call. Release build, median of 7 samples (3 at 1M): `create` +15%/+68%/+78% and `open` +27%/+30%/+27% at 1K/100K/1M records; `open_portable` between the old and new `open`; blob ~5.6× the ages file (~118 B/record). No benchmarked hot path is affected (nothing times `create`/`open` inside `b.iter()`). Full table in `RESULTS.md`'s new `### ProductionStore file portability (STORAGE-014)` subsection; `ADR-0016` and the design doc each carry an implementation addendum recording the divergence and the named follow-up (a header content hash, a `BLOB_VERSION` bump, not built with no caller asking). Before this, `PRODUCTION-STORE-PORTABILITY-DESIGN` — the owner asked to pursue `ProductionStore` file portability, a standing open question since `MMAP-AGE-STORE-IDENTITY-FIX` (*"persists only `age`, not the full record"*). Research first, not code: `MmapAgeStore`'s `.mmap` file persists only `id`+`age`; `breed` and `littermate_of` edges are never written to disk, and `create`/`open` both require the caller to already hold the full `Vec<DogRecord>`/`Vec<(Uuid, Uuid)>` — the file alone can't reconstruct a store, unlike SQLite's/DuckDB's own self-describing files. This is the exact redesign `ADR-0006` declined once (*"needs a string-heap file format... or capping breed's length"*) and `GENERIC-SCHEMA-DESIGN.md` §4.2 hit again with `Order` — given that history, this round wrote a design doc and ADR first rather than diving into code (the owner's own explicit choice when asked). **The key reframing**: ADR-0006's rejected string-heap design solves *mutable* variable-length storage; `breed`/`id`/edges are never mutated in this crate, only `age` is — so the harder problem doesn't need solving at all. `docs/design/PRODUCTION-STORE-PORTABILITY-DESIGN.md` (Proposed) and `ADR-0016` (Proposed) instead propose a small, additive companion file (bincode-serialized `id`/`breed`/edges, reusing `DogRecord`'s already-`Serialize`/`Deserialize` derive, `SnapshotFullStore`'s already-proven full-record bincode round trip, and `MmapAgeStore`'s already-proven write-to-temp-then-atomic-rename crash-safety pattern) plus a new, additive `ProductionStore::open_portable(path)` constructor — zero changes to `MmapAgeStore`'s own file format or to `create`/`open`'s existing signatures. **Accepted** — the owner reviewed and approved the design as proposed, no changes requested. Before this, `FIVE-HOP-EXTERNAL-DB-BENCHMARK` — the owner asked to extend the external-DB comparison one hop further still, to five-hop `littermate_of` traversal (`STORAGE-013` v0.5.0, no new ADR — same idiom, `WHERE hop.depth < 5` / `WHERE depth = 5`, against a new private `five_hop_neighbors` composed one more round from `four_hop_neighbors`). `ProductionStore` still wins every cell (20.3×-228.3×). **The headline: the four-hop Postgres/SQLite crossover did not hold** — SQLite is closest again at 1,000,000 records (29.8× vs. Postgres's 32.4×), while a *different* crossover appears at 100,000 records for the first time (Postgres 30.9× vs. SQLite's 34.6×). Investigated directly, not smoothed over: a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass at all three sizes found Postgres's query plan completely unchanged since v0.2.0 (`Seq Scan`+Hash Join at 1K, indexed Nested Loop at 100K/1M) — the volatility is in how much that plan's buffer I/O grows per added hop at a given size, not in which plan runs, and that growth rate isn't itself a stable, predictable function of table size. The one part of this five-round arc that *has* stayed a clean, monotonic trend: DuckDB's own margin against `ProductionStore` narrows at every size, every hop. Full tables, verdicts, and the volatility account are in `RESULTS.md`'s new `### Graph traversal, five-hop` subsection. Before this, `FOUR-HOP-EXTERNAL-DB-BENCHMARK` — the owner asked to extend the external-DB comparison one hop further yet again, to four-hop `littermate_of` traversal (`STORAGE-013` v0.4.0, no new ADR — same recursive-CTE idiom, `WHERE hop.depth < 4` / `WHERE depth = 4`, against a new private `four_hop_neighbors` composed one more round from `three_hop_neighbors`). Unlike the prior two additions, this one surfaced a genuinely new *result*, not just a bigger number on an already-established trend: the "SQLite's margin over `ProductionStore` narrows every hop" pattern from hops 1-3 (161× → 27.2× → 17.9× at 1,000 records) broke down at hop 4 — essentially flat (17.8×) rather than continuing to narrow. **The real headline: at 1,000,000 records, Postgres overtakes SQLite as the closest external engine to `ProductionStore` on graph traversal for the first time** (26.9× vs. SQLite's 34.5×) — driven by Postgres's own four-hop cost growing more slowly (1.97×) than SQLite's (2.92×) specifically at that size, the same join-strategy mechanism (`Seq Scan`+Hash Join at 1K vs. indexed Nested Loop at 100K/1M) confirmed via a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass, reproducing identically for the third time. `ProductionStore` still wins every cell (17.8×-424.6×). Full tables, verdicts, and the crossover account are in `RESULTS.md`'s new `### Graph traversal, four-hop` subsection. Before this, `THREE-HOP-EXTERNAL-DB-BENCHMARK` extended the external-DB comparison one hop further still, to three-hop `littermate_of` traversal (`STORAGE-013` v0.3.0, no new ADR — same recursive-CTE idiom, `WHERE hop.depth < 3` / `WHERE depth = 3`). This shape has **no in-repo counterpart**: `bench_support` composes at most two hops (`two_hop_neighbors`, ADR-0004), so `ProductionStore`'s comparison point is a new, private, bench-target-local `three_hop_neighbors` added directly to `benches/external_db.rs` — deliberately not promoted into `bench_support`, since this is (so far) its only call site (this project's own "no abstraction before two real call sites" convention). `ProductionStore` still wins every cell, but the margin is visibly narrower than any shallower workload — 17.9×-917.8× — because `ProductionStore`'s own per-hop cost multiplier (roughly 12-14× one-hop→two-hop, then 3.9-4.8× two-hop→three-hop) grows faster than SQLite's (roughly 2.2-2.7×, then 2.6-3.0×), so SQLite (still the strongest external engine) keeps closing the gap: 161× behind at one hop, 27× at two, 18× at three, all at 1,000 records. **The two-hop Postgres finding reproduced exactly, confirming it's real and depth-independent, not a one-off**: the same `Seq Scan`+Hash Join (1K) vs. indexed Nested Loop (100K/1M) join-strategy split shows up again at three hops, via a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass against each size's own schema — at 1M, the base case also now switches from a `Bitmap Heap Scan` to a plain `Index Scan`, a further, consistent refinement of the same size-dependent planning behavior. Full tables, verdicts, and the join-strategy account are in `RESULTS.md`'s new `### Graph traversal, three-hop` subsection. Before this, `TWO-HOP-EXTERNAL-DB-BENCHMARK` extended the same comparison to two hops (`STORAGE-013` v0.2.0) — `ProductionStore` won every cell (27×-2,822×), and the same Postgres join-strategy flip (then unconfirmed as reproducible) was first found there, via the identical `EXPLAIN (ANALYZE, BUFFERS)` method: `ANALYZE` already current at every size, no plan crossing `jit_above_cost`, ruling out a repeat of the earlier missing-statistics/JIT bug. See `RESULTS.md`'s `### Graph traversal, two-hop` subsection for that round's own full account. Before this, `MMAP-AGE-STORE-IDENTITY-FIX` — a real, latent correctness bug in `MmapAgeStore` (`ProductionStore`'s durable storage layer), found while the owner was asking how `ProductionStore`'s own file compares to SQLite's/DuckDB's portable ones, is fixed: `open` used to trust that array position N in the file still held position N's age from `create` time, with nothing on disk recording which record an age actually belonged to — a caller supplying `records` in a different order between `create` and a later `open` would have silently misattributed ages, no error, no panic (never triggered by this crate's own test/bench suite, but never ruled out by `ProductionStore`'s public contract either). Fixed by porting `crate::generic::mmap_store::GenericMmapStore`'s own already-proven identity-keying + schema-version-header design — reconciliation by id, not position. **A first, row-oriented (interleaved-slot) port regressed `scan_ages` 2.2-6.1× (299.5 µs → 1.838 ms at 1M) and reversed this session's own just-published external-database headline finding** (`ProductionStore` losing to DuckDB on `scan_ages` at 1M, not narrowly beating it) — caught via detailed before/after benchmarking before merging, not discovered after. **A second, columnar redesign (ids/ages/markers in three separate contiguous regions instead of interleaved) recovered nearly all of it**: `scan_ages` now only 1.1-1.4× slower than pre-fix (338.6 µs at 1M), and the external-database headline finding is restored almost exactly (DuckDB 3.7× behind at 1M, was 3.3×). The real cost moved to a rare, never-benchmarked path instead: a columnar layout can't append a new record cheaply in place, so that case now does a full, crash-safe rewrite (write-to-temp-then-atomic-rename) rather than the row-oriented design's cheap `O_APPEND`. See `RESULTS.md`'s `## Production recommendation` → `### MmapAgeStore identity-keying fix` for the complete before/after account. Before this, `CI-CACHE` added `Swatinem/rust-cache@v2` to `.github/workflows/ci.yml`, resolving `EXTERNAL-DB-BENCHMARK`'s own open-questions entry naming DuckDB's `bundled`-feature ~20-minute CI rebuild cost — workflow-only, no code or test impact. Before this, `EXTERNAL-DB-BENCHMARK` — the first comparison against real, external, general-purpose databases (SQLite, Postgres, DuckDB) on the same three fixed access patterns this crate has always benchmarked (`get`, `scan_ages`, one-hop `littermate_of` traversal) — is done: `benches/external_db.rs` (new), `external-db-bench` Cargo feature, ADR-0015 (Accepted), `STORAGE-013` v0.1.0. `ProductionStore` wins every cell (expected — a purpose-built in-memory store has no query-engine/serialization overhead to pay), but DuckDB closes to within 3.3× on `scan_ages` at 1M records — exactly the columnar-scan strength the task predicted — while being the *worst* of the three external engines on both `get` and graph traversal. A real methodology bug was found and fixed mid-round, not glossed over: Postgres's initial graph-traversal numbers showed a ~1,400× cliff between 1K and 100K records, traced to a missing `ANALYZE` after the bulk `COPY` load driving Postgres's recursive-CTE cost estimate high enough to trigger LLVM JIT compilation on every execution (confirmed directly via `EXPLAIN ANALYZE`: 53 ms before the fix, 0.196 ms after, same query, same data). See `RESULTS.md`'s `## External database comparison` section for the full account. Before this, `SERVER-TLS` (the real implementation of the accepted `SERVER-TLS-DESIGN`) was done: `TlsConfig`/`TlsConfigError` (`src/server/mod.rs`), a new `ReadHalf`/`WriteHalf` enum pair, `src/server/pem.rs` (new, small hand-written PEM/base64 decoder), `serve`'s new `tls: Option<TlsConfig>` parameter; `SERVER-001` bumped to v0.9.0 (`SERVER-001-FR-019`). This closes the last remaining half of the "no auth, no encryption" gap ADR-0010 named at acceptance — the second of the two options the owner picked after "what's next," and the transport-encryption revisit ADR-0012's own "Validation and revisit triggers" named directly. Depends on `rusty_tls` (`Rusty-Mill/rusty_mill`, this owner's own ecosystem-wide `rustls` wrapper — this crate's first git dependency, pinned to a specific commit), not `rustls` directly, per the design's own "Ecosystem check." **The one real implementation-time finding this round surfaced**: the existing plaintext path splits a connection into independent read/write halves via `TcpStream::try_clone` (two real OS-level socket handles), but `rusty_tls::TlsServerStream`'s `rustls::ServerConnection` state can't be split that way — resolved with a `Rc<RefCell<TlsServerStream<TcpStream>>>` shared between a new `ReadHalf`/`WriteHalf` enum pair (single-threaded, since each connection is served by exactly one OS thread), while the `Plain` variant keeps the existing `try_clone` split completely unchanged — verified by every pre-existing plaintext test passing unmodified. `tests/server_tls_integration.rs` (5 new tests) covers every functional acceptance criterion over a real socket via `rusty_tls::TlsStream` (this ecosystem crate's own client-side half); its flagship test captures the real bytes a TLS client sends on the wire (a byte-recording `TcpStream` wrapper) and asserts the plaintext `Authenticate` token is genuinely absent — the direct evidence the design's whole purpose depends on. **Explicitly still does not deliver mTLS** — client identity remains `AuthConfig`'s existing shared-secret token scheme, now traveling encrypted. Before this, `SERVER-TRANSACTION-BENCHMARK` (`SERVER-001` v0.8.0) extended `benches/server.rs` to cover `Request::Transaction`, finding no meaningful cost relative to `GetById`; `SERVER-TRANSACTION` (`SERVER-001` v0.7.0) added atomic multi-operation transactions; `SERVER-AUTH` (`SERVER-001` v0.6.0) added authentication/authorization; `SERVER-QUERY-LAYER` v0.5.0 added the schema-driven client library — see `## Completed` below for all of it.
- Health: green (no blockers; process gap flagged this pass, see Risks: PR #27's `research`-feature-flag public-API narrowing — a consequential, crate-wide design decision — shipped without an ADR, against `AGENTS.md`'s own default-to-writing-one guidance for active major development. New open questions from this arc, not yet owner-decided: the multi-process append fix covers slot creation only, not broader multi-writer coordination; its `O_APPEND` atomicity guarantee is local-filesystem-only, excluding NFS — both named explicitly in `docs/FUTURE-GROWTH.md`, not hidden gaps. Older open questions — the `scan_ages` 100K cache-miss crossover (investigated to the practical limit of available tooling), `GRAPH-TRAVERSAL`'s/`MIXED-WORKLOAD`'s open questions, the remaining durability open questions (LSM compaction, `run_load`'s uncovered "true first start" path for WAL/Hybrid, Tier 2's ages-only scope as a ceiling), the concurrency open questions (`ShardedStore`'s shard count unswept, `ActorStore`'s single-owner ceiling, bursty/skewed access patterns, and sharded's advantage over global `RwLock` not tracking real core count monotonically across the container/Windows/`baileyai` passes), and the `PRODUCTION-DEFAULT` open questions (the physical `src/comparisons/` reorganization, explicitly deferred in favor of a docs-only lead; `MmapAgeStore::get`'s modest ~1.5× unoptimized cost; re-running the four-backend suite on `baileyai` for a true apples-to-apples comparison — see ADR-0008) — all tracked as open questions, not blockers; write-heavy-workload behavior is no longer an open question, see `MIXED-WORKLOAD` below; `HybridStore`'s checkpoint clone cost and the reopen-parallelization break-even point are no longer open questions either — both fixed, see `DURABILITY-TIER1`/`DURABILITY-TIER2` below; "which concurrency strategy paired with which durability variant" is no longer fully open either — one specific pairing (global `RwLock` + mmap) is now `PRODUCTION-DEFAULT`'s `ProductionStore`, though every other pairing remains unexplored; `ProductionStore`'s 100K concurrency tax is no longer an open question — diagnosed and fixed, see `PRODUCTION-DEFAULT` below)

## Completed

- `BOOTSTRAP` — charter, architecture, ADR-0001, ADR-0002, spec tree (`STORAGE-001..004`), roadmap, traceability, `AGENTS.md`, `WORKFLOW.md`, Cargo scaffolding, CI. On `main` at `5bfc8c5`.
- `GENERATOR` — `DogRecord` + seeded, configurable dataset generator (`src/record.rs`, `src/generator.rs`); 8 unit tests, all passing. On `main`.
- `BACKENDS` — `DogStore` trait + `AosStore`/`SoaStore`/`CanonicalStore` (`src/store/**`); 18 backend unit tests + 4 cross-backend equivalence tests, all passing. On `main`.
- `BENCH-SUITE` — Criterion suite (`benches/workloads.rs`), 4 workloads × 3 sizes × 3 backends = 36 cases, all run successfully. On `main`.
- `CACHE-MISS` — `perf-events`-gated target (`benches/cache_events.rs`), builds and links on Linux; the bootstrap session's own environment lacked hardware performance-counter access (confirmed via both `perf stat` and running the built binary), so real numbers were deferred at the time (see ADR-0002) — resolved by `CACHE-MISS-BAILEYAI` below. On `main`.
- `RESULTS` — `RESULTS.md` published with real `cargo bench` numbers, a verdict per workload, explicit canonical-store win/loss call-outs, and an open-questions section (first pass, 3 backends). On `main`.
- `HYBRID-BACKEND` — `CanonicalCachedStore` (`src/store/canonical_cached.rs`): `CanonicalStore`'s map + breed index, plus a packed `Vec<u32>` age cache kept in sync by eager write-through (ADR-0003). 8 unit tests (staleness test highest-priority) + 5 cross-backend tests, all passing. Wired into both `benches/workloads.rs` and `benches/cache_events.rs`. `RESULTS.md` revised to a 4-way comparison: `scan_ages` gap closed (from losing to both baselines to beating AoS by ~17.7× and landing within ~14% of SoA); `update_age` write-through costs ~1.5× at every size (well under the ~10× check-in threshold, so this proceeded without pausing) while remaining 4–5 orders of magnitude faster than AoS/SoA. Merged via PR #1 (`fa80a74` → merge commit `d1d9169`), CI green (`fmt, clippy, test`) before merge.
- `SCAN-AGES-NOISE-CHECK` — diagnosed `scan_ages`'s reported ~14% wall-clock gap to SoA at 1M: re-running at higher rigor (50 then 100 samples, up from 20) flipped the gap's *sign* twice (14% slower → 12% faster → 9% faster), which settled it as measurement noise from the shared/virtualized session environment, not a real cost. No code changed — `Vec::with_capacity`/`.clone()`-memcpy were already confirmed clean. `RESULTS.md` updated with the diagnostic paragraph. Merged via PR #2 (`3ab260d` → merge commit `ae9a0d0`).
- `CACHE-MISS-BAILEYAI` — real hardware cache-miss/cache-reference counts obtained on `baileyai` (bare-metal, real PMU access — the first run this repo has had with actual counter access). Required dropping `perf_event_paranoid` from 2 to 1 (session-only sysctl, not persisted). `benches/cache_events.rs` extended to cover all four workloads (previously only `get`/`same_breed`, on untested theories about what would dominate `scan_ages`/`update_age`'s cost). **Headline finding, and a correction to `SCAN-AGES-NOISE-CHECK`'s scope**: at 1M records, `scan_ages` on Canonical+cache has ~3.36× fewer cache misses than SoA (7,098 vs. 23,817) and a much lower miss rate (2.84% vs. 9.46%) despite near-identical reference counts — a real structural advantage that wall-clock timing (correctly found to be noisy/tied) couldn't see. New open question: an unexplained crossover at 100K where SoA has fewer misses instead, despite identical code paths at every size. `get`/`update_age`/`same_breed` cache-miss numbers all corroborate the existing wall-clock verdicts. Merged via PR #3 (`fe59233` → merge commit `ec67ba3`) from a separate session working directly on `baileyai`.
- `SCAN-AGES-CROSSOVER-INVESTIGATION` (PR #4) — narrowed the `scan_ages` 100K cache-miss crossover using real memory-footprint data (`examples/memory_footprint.rs`, no PMU needed): `CanonicalCachedStore` is ~87% larger than `SoaStore` at 100K but ~156% larger at 1M, ruling out the simplest "bigger co-resident working set → more misses" hypothesis without confirming a replacement. Also closed most of the separate "memory overhead" open question. Merged.
- `SCAN-AGES-CROSSOVER-L1D` — finished the investigation `benches/scan_ages_crossover.rs` started. First real run on `baileyai` found a bug in the bench itself (all four measurement groups shared one Criterion benchmark-group name, so the `change: ...%` lines compared different counter types against each other — fixed by giving each measurement type its own group name). The real, corrected numbers: **L1-data-cache miss rate is nearly identical between backends at both sizes** (60.25% vs. 59.40% at 100K, 57.93% vs. 58.29% at 1M — under 1.5 percentage points either way, vs. ~3× on the generic deeper-cache counters), ruling out L1 as the crossover's location. The last-level-cache half of the bench doesn't run on `baileyai`'s AMD chip (`ENOENT` — AMD's PMU doesn't implement the generic last-level-cache descriptor this crate's API uses; would need raw, model-specific perf events). **Closing this out as "investigated to the practical limit of available tooling," not pursuing raw AMD PMU events** — the decision this data needs to support doesn't depend on it. `RESULTS.md` updated. Merged via PR #5 (`c8de807` → merge commit `5da8380`).
- `GRAPH-TRAVERSAL` — the previously-untested third leg of the original row/column/graph hypothesis: a real edge relationship (`littermate_of`, 0-3 edges/dog, seeded and configurable via a new `littermate_avg_degree` generator parameter) rather than `same_breed`'s shared-attribute stand-in. Added one trait method, `DogStore::neighbors` (one-hop only — ADR-0004 explains why multi-hop is deliberately kept out of the trait and instead built generically as `bench_support::two_hop_neighbors`, composed from two rounds of `neighbors` calls). AoS/SoA implement it as a linear scan of a stored edge list; `CanonicalStore`/`CanonicalCachedStore` implement it via a `HashMap<Uuid, Vec<Uuid>>` adjacency index built the same way as the existing breed index. 8 new generator unit tests + 9 new per-backend `neighbors` unit tests + 2 new `bench_support::two_hop_neighbors` unit tests + 2 new cross-backend consistency tests (`all_backends_agree_on_neighbors_one_hop` — the edge-list-vs-adjacency-index consistency check, highest priority for this unit, equivalent in role to `HYBRID-BACKEND`'s stale-cache test — and `all_backends_agree_on_neighbors_two_hop`), all passing. Wired into both `benches/workloads.rs` and `benches/cache_events.rs` as `neighbors_one_hop`/`neighbors_two_hop` groups. `RESULTS.md` gains a new `## Graph traversal` section (kept separate from, and not altering, the closed row/column verdict): `neighbors` matches `same_breed`'s result shape (Canonical/Canonical+cache tied for the win, ~103,000-113,000× faster than AoS/SoA at 1M — a larger multiple than `same_breed`'s because the edge list is proportionally larger than the average same-breed group under this dataset's cardinality); the generic 2-hop composition preserves the win (~28,600-33,200× at 1M) with no sign of the baselines closing the gap as hop count grows. Cache-miss counters not producible in this session's environment (same PMU gap as every prior pass, confirmed directly); `benches/cache_events.rs`'s new groups are ready to run on `baileyai` as a follow-up, same as the existing four workloads once were. Merged via PR #6 (`d422e71` → merge commit `b336845`), CI green (`fmt, clippy, test`) before merge.
- `MIXED-WORKLOAD` — directly tests whether `HYBRID-BACKEND`'s eager write-through tax (ADR-0003, ~1.5× on `update_age` in isolation) changes the recommendation once reads and writes are actually blended, rather than benchmarked in isolation like every prior workload. `MixedWorkloadDriver` (`src/bench_support.rs`) issues a seeded, blended `get`/`update_age`/`scan_ages` sequence — `write_ratio` chance of `update_age`, remainder split evenly between `get`/`scan_ages` — reusing the existing `RoundRobin` sample-ID infrastructure rather than building new ID-sampling logic. Swept at write ratios 10%/50%/90%. 10 new `bench_support` unit tests (bounds validation incl. `NaN`, determinism, statistical ratio-distribution check, never-errors-against-its-own-dataset highest-priority, write-only-actually-mutates end-to-end check), all passing. Wired into both `benches/workloads.rs` and `benches/cache_events.rs` as `mixed_workload_write{10,50,90}` groups (36 wall-clock cases: 3 ratios × 3 sizes × 4 backends). `RESULTS.md` gains a new `## Mixed read/write workload` section, per-configuration verdict format, directly answering the motivating question: **`CanonicalCachedStore` wins all 9 tested configurations outright — it never loses to `CanonicalStore` or `SoaStore` at any tested write ratio, including 90%.** Margin ranges from 1.8× (closest, 90% writes/1K records) to 178.6× (widest, 90% writes/1M vs. AoS); the `write90/canonical/1000000` case came back noisy on the first pass (±20% CI) and was re-run at higher rigor per this document's established practice, moving the number *further* from `CanonicalCachedStore`, reinforcing rather than undermining the verdict. No ADR revision follows — the result confirms the existing recommendation, it doesn't change it (per the task's own explicit constraint: an ADR is warranted only if the recommendation changes). No `src/store/**`/`src/generator.rs` changes — benchmark-harness-only, per `STORAGE-007`. Cache-miss counters not producible in this session's environment (confirmed directly, same as prior passes); ready to run on `baileyai`. Merged via PR #7 (`9b927e7` → merge commit `89160d7`), CI green (`fmt, clippy, test`) before merge.

- `DURABILITY-TIER1`/`DURABILITY-TIER2` — the first persistence work in this crate: everything above is purely in-memory. Scoped to `CanonicalCachedStore` only (ADR-0005's Context — the other three backends are baselines nobody would deploy). A new `src/durability/` module hosts a shared, rebuilt-from-scratch `CanonicalCachedState` core (so all eight variants' `get`/`scan_ages`/`same_breed`/`neighbors` are written once, not eight times) without touching the closed `src/store/canonical_cached.rs`. **Tier 1** (`STORAGE-008`, ADR-0005, full rigor): WAL fsync-per-write, WAL buffered, snapshot canonical-only with rebuild-on-load, snapshot save-as-is, and a hybrid periodic-snapshot-plus-WAL design with cutoff-sequence-number-based partial replay. **Tier 2** (`STORAGE-009`, ADR-0006, lighter proof-of-concept): mmap-backed ages (`memmap2`), an LSM-tree-style memtable+WAL+flushed-SSTs design with no compaction (explicitly flagged, per the task's own instruction to scope down rather than let this balloon), and a `redb`-backed embedded-engine variant (chosen over `sled` for its ACID-from-the-start design and lack of `sled`'s crash-safety caveats). New dependencies, flagged explicitly in `Cargo.toml` (first added since this crate's inception): `serde`+`bincode` (all 8 variants), `memmap2` (mmap variant), `redb` (embedded-engine variant). 34 new unit tests across `src/durability/*.rs` (108 total unit+integration tests, up from 74), each variant's highest-priority correctness test matching the rigor `HYBRID-BACKEND`'s stale-cache test set (WAL reconstruction-from-log for variants 1/2; the snapshot-plus-partial-replay-across-the-cutoff test for hybrid; newest-flushed-generation-wins for LSM). `benches/durability.rs` (new `[[bench]]` target) measures per-write, checkpoint/flush, and load/replay/startup cost for all 8 variants at 1K/100K/1M records (`redb` excluded from the checkpoint metric at compile time — no separate checkpoint operation exists). `RESULTS.md` gains a new `## Durability` section: per-metric tables for all 8 variants, a non-Criterion worked comparison of how long a 1,000-write batch takes to become guaranteed-recoverable under each design (continuous small cost for WAL-based approaches vs. burst checkpoint cost with a real data-loss window for snapshot-alone approaches), and an explicit recommendation (`redb` over hand-rolled WAL-fsync for zero-loss-window correctness with the least code; WAL-buffered over Hybrid for this crate's specific write volume; mmap as the standout result — cheaper per write than the non-durable baseline itself, and the cheapest explicit durability step by 2-3 orders of magnitude; snapshot-rebuild over snapshot-full if a WAL-less design is chosen; LSM not recommended beyond proof-of-concept without compaction).

**Two follow-up fixes landed before merge, both found by re-checking the results rather than accepting them at face value:**
- **`HybridStore::checkpoint`'s extra `state.clone()`**: identified as the cause of Hybrid's checkpoint cost being the one 3× outlier in the whole durability sweep (1.17 s at 1M vs. 289–393 ms for its full-state-persisting peers). Fixed by serializing directly from `&self.state` through a borrowed mirror struct (`HybridSnapshotRef`) instead of an owned `HybridSnapshot` — same on-disk bytes, `open`'s deserialization path unchanged. Dropped checkpoint cost 25–61% across all three sizes with zero change to correctness (all 4 hybrid unit tests, including the cutoff-boundary replay test, pass unchanged) or to `update_age`'s cost.
- **Reopen-cost profiling found the prior round's "shared bottleneck" framing was right about the symptom, wrong about the mechanism.** Real profiling (not assumption) traced which code path each variant's benchmarked `open` actually takes: `benches/durability.rs`'s own `run_load` calls `finalize()`/`checkpoint()` before timing load, so `wal_fsync`/`wal_buffered`/`hybrid`/`snapshot_full` all measure "deserialize an existing snapshot," not "rebuild indexes from records." `snapshot_full` itself has no bug — it genuinely deserializes its persisted indexes, and deserializing a `HashMap`-heavy structure costs about the same as rebuilding one (1.275 s vs. 1.274 s at 1M, after correcting for cold-start measurement bias in this shared environment). Only `snapshot_rebuild` and the three Tier 2 variants genuinely rebuild indexes under this benchmark. Fixed by parallelizing the two independent construction phases in `CanonicalCachedState::new` and the three Tier 2 `build_indexes` functions via `std::thread::scope` (stdlib, no new dependency) — `snapshot_rebuild`/`mmap`/`lsm`/`redb` reopen 25–47% faster at 100K/1M; the other four move by noise only, exactly as predicted. Honest tradeoff reported, not hidden: at 1,000 records the "improved" four actually regress 17–26% (thread-spawn overhead exceeds the tiny workload) — not engineered around with a size threshold.

Both fixes verified via the full existing test suite (108/108, unchanged, re-run 3× after the reopen fix to rule out a scoped-thread race) — no new tests needed since these were performance fixes to already-tested code, not new behavior. Merged via PR #8 (`9b6f301` → merge commit `251975d`), CI green (`fmt, clippy, test`) before merge, no unresolved review comments.

**Third follow-up fix, landed via a second PR after #8 merged**: the reopen-path parallelization's own 1,000-record regression (17–26% slower — reported honestly, not hidden, in the pass that introduced it) turned out to have a real, measurable crossover point rather than needing a guessed round number. Two independent, warm-up-corrected 9-sample runs comparing sequential vs. parallel construction at 500/800/1,000/1,200/1,500/1,800/2,000/3,000/5,000 records found ≤800 reliably favors sequential, ≥1,500 reliably favors parallel, and 1,000–1,200 is a genuine coin-flip between runs — not a clean number. `PARALLEL_CONSTRUCTION_THRESHOLD = 1,500` (`src/durability/mod.rs`, one constant, four gated sites: `CanonicalCachedState::new` and the three Tier 2 `build_indexes` functions) falls back to the original, pre-parallelization sequential code below it, erring toward the known-safe side of the ambiguous band. Result: the 1K regression is gone (`snapshot_rebuild`/`mmap`/`lsm`/`redb` land at or below their original pre-parallelization numbers); 100K/1M gains unchanged (the gate is a no-op there — both sizes are far above threshold). The rebenchmark run also surfaced an honest confound worth naming: a broadly faster machine state (~15–30% across all eight variants at 100K/1M, including the four whose exercised code the gate never touches) — session/environment drift, called out explicitly in `RESULTS.md` rather than folded into the fix's credit. Verified via the full test suite (108/108, unchanged — the tiny test fixtures now exercise the new sequential path directly). Since branch `claude/durability-prototypes`' first PR (#8) was already merged, this follow-up restarted that branch from `main` per this session's merged-PR convention and opened a fresh PR. Merged via PR #9 (`d7d2b04` → merge commit `0f9937e`), CI green before merge, no unresolved review comments.

Evidence: `cargo test --all-features` / `cargo bench` output referenced in `RESULTS.md`; PR #1/#2/#3/#4/#5/#6/#7/#8/#9 diffs and CI runs; `examples/memory_footprint.rs` output; real `baileyai` terminal output for `SCAN-AGES-CROSSOVER-L1D`; this session's own `cargo bench --bench workloads -- neighbors` run for `GRAPH-TRAVERSAL`'s numbers, `cargo bench --bench workloads -- mixed_workload` run for `MIXED-WORKLOAD`'s numbers, and this session's own `cargo bench --bench durability` runs (initial + two follow-up re-benchmarks) for `DURABILITY-TIER1`/`DURABILITY-TIER2`'s numbers.

- `CONCURRENCY-PROTOTYPES` — the first reader/writer-safety work in this crate: everything above assumes single-threaded access. Scoped to `CanonicalCachedStore` only, same "don't build for backends nobody would deploy" reasoning as `DURABILITY-TIER1`/`DURABILITY-TIER2`, and deliberately **not** paired with any specific durability variant — the motivating task named that combination as a distinct future round. A new `src/concurrency/` module hosts a new `ConcurrentStore` trait (`&self` throughout, including `update_age` — unlike `DogStore`'s `&mut self`, needed because these types are shared across threads via `Arc`). **Tier 1** (`STORAGE-010`, ADR-0007, full rigor): global `RwLock` (`GlobalRwLockStore`, the whole store behind one `std::sync::RwLock`), sharded locking (`ShardedStore`, 64 independently-`RwLock`-guarded shards routed by UUID hash, scoped to `get`/`update_age`/`scan_ages` only — sharding `same_breed`/`neighbors` too was judged a materially bigger design problem, avoided rather than discovered mid-pass), and `dashmap` (`DashMapStore`, the canonical map swapped for `dashmap::DashMap`). **Tier 2** (lighter proof-of-concept): actor/single-writer-thread (`ActorStore`, one detached worker thread owns a plain `CanonicalCachedStore`, every other thread talks to it over an `std::sync::mpsc` channel guarded by a `Mutex` for `Sync`). **New dependency, flagged explicitly**: `dashmap = "6"`, the one addition the task anticipated needing — no other new dependency. **Correctness (highest priority, same weight as `HYBRID-BACKEND`'s original stale-cache test)**: a flagship stress test (`run_concurrency_stress_test`, `src/concurrency/mod.rs`) — 16 threads × 2,000 iterations each, randomly interleaved `get`/`update_age` against a 20-id contended pool drawn from a 500-record dataset — replays the real recorded write-completion order sequentially against a fresh reference store and checks the concurrent result matches exactly (no lost updates), plus a secondary torn-write membership check. **All four variants pass outright.** 13 new unit tests across `src/concurrency/*.rs` (121 total unit+integration tests, up from 108), including the 4 flagship stress tests. `MixedWorkloadDriver` (`src/bench_support.rs`) gains a new, additive `run_one_concurrent` method (`run_one` and every benchmark using it unchanged) reused by a new custom (non-Criterion) throughput harness, `benches/concurrency.rs`, sweeping dataset size (1,000/100,000 — 1M dropped to keep the new four-axis sweep tractable in one pass), write ratio (10%/50%/90%, matching `MIXED-WORKLOAD`'s sweep), and thread count (1/4/8/16, used exactly as specified against this session's measured 4 real cores — 8/16-thread rows deliberately measure honest oversubscription, not scaled down). `RESULTS.md` gains a new `## Concurrency` section: **global `RwLock` wins 5 of 6 (size × write-ratio) configurations outright, including every 100,000-record case** (its single lock's overhead turns out to matter far less than `CanonicalCachedStore`'s already-efficient packed data structures, and `scan_ages`'s O(n) cost — the same driver quirk `MIXED-WORKLOAD` already documented — dominates absolute throughput at scale far more than any locking scheme does); **sharded locking wins only at 1,000 records / 90% writes / ≥4 threads** (2.4–2.7× ahead), the one tested configuration where its actual thesis (writers to different shards proceed in parallel) pays off; **`dashmap` never wins a single configuration**; **the actor pattern plateaus at a measured serial-processing ceiling of roughly 65,000–105,000 ops/sec**, unmoved by adding client threads past about 4. No overall winner — a recommendation per configuration, matching `## Durability`'s established reporting standard. No `src/store/**`/`src/durability/**`/`src/generator.rs` changes — new surface confined to `src/concurrency/**`, the additive `bench_support.rs` method, `benches/concurrency.rs`, and `Cargo.toml`. Merged via PR #10 (`1d3c6d0`/`bbd0b73` → merge commit `fd83b8e`), together with two real multi-core follow-up passes on the same branch:

- **Windows-machine follow-up**: `baileyai` was unreachable over SSH from that session, so — at the user's redirection — the sweep re-ran on the owner's Windows dev machine instead (24 logical/12 physical cores, AMD Ryzen 9 7900X3D). `THREAD_COUNTS` changed from the container's `[1, 4, 8, 16]` to `[1, 4, 24, 48]`. Headline: sharded's one container win (1K/90% writes) widened sharply (2.4–2.9× → 9.5–10.1×) and newly appeared at 1K/10%/50% writes too; global `RwLock`'s 100K win held at every cell but its margin shrank toward parity (down to 1.2× at 90% writes/48 threads).
- **`baileyai` follow-up**: this session executed directly on `baileyai` (32 logical/16 physical cores, AMD Ryzen AI MAX+ 395 — confirmed via `hostname`/`whoami`, no SSH substitution needed), re-running the same sweep at `THREAD_COUNTS = [1, 4, 32, 64]`. **Headline, and a correction to the Windows-machine pass's own framing: "sharded's advantage widens with more real cores" does not hold monotonically across real machines.** The 100K/90%-writes margin did keep shrinking as predicted (down to 1.03–1.05×, the closest yet to a flip). But `baileyai`'s extra 8 real cores over the Windows machine *reversed* the 1K/10%-writes crossover outright (global `RwLock` wins again at 32/64 threads) and *narrowed* the 1K/90%-writes sharded margin (7.3–7.9× vs. the Windows machine's 9.5–10.1×) rather than widening it — evidence the two real, non-container machines differ in more than core count alone (microarchitecture, memory subsystem), not just a cleaner data point on the same trend line. `RESULTS.md`'s Recommendation section was revised accordingly: sharded is confirmed only for 1K, write-heavy (≥50%), multi-thread deployments across all three real machines — not generalized to "many-core hardware" broadly, which `baileyai`'s data contradicts directly at 10% writes.

- `PRODUCTION-DEFAULT` — consolidates all six prior rounds' converged recommendation (`CanonicalCachedStore` architecture + mmap durability + global `RwLock` concurrency) into one new type, `ProductionStore` (`src/production.rs`), rather than leaving it as three separately-documented, separately-benchmarked picks a reader had to synthesize themselves. `RwLock<MmapAgeStore>` — reuses `MmapAgeStore` (`STORAGE-009`) directly rather than building a second, competing mmap wrapper around `CanonicalCachedStore`, since `MmapAgeStore` already *is* that architecture made durable (see ADR-0008's Considered options for why "three literally nested types" was rejected). Implements both `DogStore` (drop-in for existing single-threaded code — gets the full surface, including `same_breed`/`neighbors`, for free, unlike `ShardedStore`/`DashMapStore`'s narrower `ConcurrentStore`-only scope) and `ConcurrentStore` (genuine multi-threaded sharing, reusing the existing flagship stress test and throughput harness with zero new infrastructure). No changes to `src/store/canonical_cached.rs` or `src/concurrency/global_rwlock.rs` (`src/durability/mmap_store.rs` was touched by a follow-up fix — see below — but not as part of this initial consolidation). **Flagship integration test** (`tests/production_integration.rs`, highest priority this round): two phases of 16 threads × 2,000 iterations each of concurrent `get`/`update_age` contention, separated by a genuine `drop` + reopen from disk (not just an in-process flush), verified via sequential-replay linearizability across both phases' combined write log *and* a third, post-drop reopen proving genuine persistence — the first test exercising mmap durability and `RwLock` concurrency together; each was previously verified only in isolation. `ProductionStore` wired into `benches/workloads.rs` (listed first in all seven existing groups) and `benches/concurrency.rs`'s existing sweep, rather than a new bench target — reuses every existing generic runner unchanged. 7 new unit tests (`src/production.rs`) + 1 new flagship integration test (128 total, up from 121). Documentation reorganized to lead with `ProductionStore`: `src/lib.rs`'s crate doc comment, `README.md`, `docs/architecture/SYSTEM-ARCHITECTURE.md` all now introduce it first and frame `store`/`durability`/`concurrency` as benchmarked alternatives. New ADR-0008 and `STORAGE-011`; `RESULTS.md` gains a `## Production recommendation` section tying all six prior rounds together. **Checked in on explicitly before proceeding**: the physical file move (`src/{store,durability,concurrency}` → `src/comparisons/`) the task offered as an alternative reorganization was presented to the user, who chose the docs-only lead instead — no file moves, no import-path churn across the crate; see ADR-0008's Considered options.

**Follow-up diagnosis-and-fix pass, same branch, before merge**: `ProductionStore`'s own concurrency-throughput numbers showed a large (6–32×), thread-count-invariant tax at 100K records — investigated directly rather than left as a guess. Leading hypothesis (synchronous `msync` on every write) refuted by reading the write path and confirmed via an isolated, same-machine measurement: `update_age` is still cheaper than the in-memory baseline, exactly as the original durability round found. The real cause: `MmapAgeStore::scan_ages` (`src/durability/mmap_store.rs`, a previously-closed durability-round module) read one mapped position at a time via four individually-bounds-checked byte indices, 25–32× slower than `CanonicalCachedStore`'s packed-`Vec` clone — never benchmarked before this round, since the original durability benchmark only measured per-write/checkpoint/load. **Checked in before touching code outside `ProductionStore`'s own files** (per this round's own working-style instruction), then fixed via a safe `chunks_exact(4)` bulk read — no `unsafe`, no new dependency, 12–17× faster. Re-benchmarked: the concurrency-throughput tax is gone (every tested cell now within 0.77–1.46× of the plain in-memory baseline, most at or ahead of parity). `RESULTS.md`'s `## Production recommendation` and ADR-0006/ADR-0008 updated with the diagnosis and corrected numbers. 128/128 tests still pass, unchanged. Merged via PR #11 (`64a858c`/`d514562` → merge commit `4d8bf0d`), CI green before merge, PR description updated post-fix so it doesn't read as if the tax were still open.

- `GENERIC-SCHEMA-DESIGN` — **design only; no source file in `src/`, `benches/`, or `tests/` was touched.** The task named this "the biggest architectural decision in the project's history" (the schema/query abstraction would become the crate's public API surface) and explicitly required stopping after a design document, before any implementation, for review. Per this project's own standing rule ("no abstraction before two real call sites"), the design was validated against a second domain deliberately picked to be structurally *unlike* `Dog`: `Order`/`Customer` — a directed, one-to-many relation (`Order belongs_to Customer`) instead of `littermate_of`'s symmetric edges; a currency-like numeric field (`amount`) instead of a bare count; an enum categorical field (`status`) instead of a string; a timestamp field (`created_at`) `Dog` never had. Delivered: `docs/design/GENERIC-SCHEMA-DESIGN.md` (four schema traits — `Record`/`IndexedField`/`ScannableField`/`SymmetricRelation`/`ChildOf` — seven query traits, two generic hop-composition functions, four composable store wrapper layers) and ADR-0009. Every trait/struct in the design document was written and compiled in a standalone scratch crate outside this repository before being transcribed, specifically to prove the shapes are real, compiling Rust rather than pseudocode — including two genuine design mistakes the compiler caught mid-build and that are reported directly in the doc rather than quietly fixed: an ambiguous-associated-type error in a naive forwarding impl, and a `Reversed` wrapper that initially (and wrongly) tried to forward `GetById` for the *parent* record type from a store that only ever held *child* records. **Headline findings** (§4 of the design doc): the packed-`Vec`/position-index trick that makes `scan_ages` fast generalizes mechanically but its *magnitude* is expected to shrink for wider field types; mmap durability's generalization to more than one mutable field is confirmed to hit `ADR-0006`'s own predicted revisit trigger immediately; the symmetric-relation adjacency-index pattern does *not* generalize to directed relations at all; composing capability layers by wrapping carries a real, quantified forwarding-boilerplate tax. No new dependency added; `rust_decimal`/`chrono` named and explicitly not added. **Superseded by the four validation spikes and `GENERIC-SCHEMA-LIBRARY` below** — every finding here has since been individually resolved with real data, and ADR-0009 is now Accepted.

- `GENERIC-SCHEMA-SPIKES` (four rounds, `src/generic_spike/`, kept as historical record) — validated `GENERIC-SCHEMA-DESIGN`'s proposal against real code and real benchmarks before any of it was treated as accepted, per the design doc's own "stops here for review" framing. **Round 1** (Dog overhead): genericizing `Dog`'s schema/query surface costs negligibly relative to `CanonicalCachedStore`, but surfaced a real ambiguous-associated-type compile error (`R::Value` ambiguous between `IndexedField`/`ScannableField`). **Round 2** (ambiguity diagnosis, `Order`/`Customer`): the ambiguity is worse than round 1 suggested — a same-trait multi-marker case (`Order`'s `Amount`/`CreatedAt`) hits a real Rust coherence limit (`E0119`), not just a naming collision; fixed the cross-trait case by renaming `IndexedField`/`ScannableField`'s associated types to `IndexValue`/`ScanValue`. **Round 3** (macro-generated forwarding): `forward_scannable_pairs!` (a `macro_rules!` using the "rotating accumulator" pattern) generates the O(pairs) concrete forwarding impls the coherence limit requires, from a field list, validated by adding a third scannable field (`DiscountCents`) and confirming it costs one macro-invocation entry, not hand-written impls. **Round 4** (directed-relation generalization): measured whether the adjacency-index pattern that made `littermate_of` traversal ~113,000× faster than a linear scan generalizes to a *directed* relation (`Order belongs_to Customer`) — yes, same order of magnitude (`Children` 17.3×–~58,000×, `Parent` 7.5×–~37,000× across 1K–1M), with a measured, structural explanation for why the multiple is smaller than the symmetric case's (the naive baseline's own cost asymmetry, not adjacency-index underperformance). Each round shipped as its own PR (`claude/generic-schema-spike`, `claude/generic-schema-ambiguity-fix` + `claude/generic-schema-macro-forwarding`, `claude/directed-relation-spike`), CI green, merged, `main` synced between rounds. No `GENERIC-SCHEMA-DESIGN.md`/ADR-0009 changes during any spike round, per each round's own explicit instruction — that update is `GENERIC-SCHEMA-LIBRARY` below.

- `GENERIC-SCHEMA-LIBRARY` — promotes the validated design into `crate::generic` (`src/generic/`), a real, public library, and builds the piece none of the four spikes attempted: a generic equivalent of `ProductionStore`. **Promoted unchanged**: `traits.rs`/`query.rs`/`store.rs` (the `IndexValue`/`ScanValue` rename, `forward_scannable_pairs!`, the `Parent`/`Children`/`Reversed` directed-relation split — all exactly as the spikes validated them) and `order_customer.rs` (`Order`/`Customer`, promoted from `generic_spike/order_impl.rs` to this library's real reference implementation). `Dog` (`generic_spike/dog_impl.rs`) stays historical reference, not promoted further — a benchmark fixture, not a target domain. **New this round, beyond every spike**: `GenericMmapStore<R, IndexMarker, ScanMarker>` (`mmap_store.rs`) — the generic, hand-fused analogue of `MmapAgeStore`, keeping its exact one-durable-field scope generically (`ADR-0009` §4.2's own finding: generalizing to more than one mutable field needs a real redesign, not attempted here); `GenericProductionStore<S>` (`production.rs`) — wraps any composed generic store in one `RwLock`, exposing inherent `&self` methods generic over whatever capability trait `S` implements (not trait impls, since the query traits are single-owner-shaped — see that module's own doc comment for why); a new `Flush` capability trait forwarded through every wrapper layer; a new `ScannableField::set_scannable_value` method. **A genuine finding, not anticipated by any spike**: building `GenericMmapStore::get` required write-through consistency with `UpdateField::update` (matching every hand-written backend in this crate), which surfaced that the *in-memory* `Scanned`/`BaseStore` composition (unchanged since the spikes) has never had this property — `GetById::get` on `DogGenericStore`/`OrderGenericStore` can return a stale value after an `update`. Not fixed this round — reported here, in ADR-0009, `RESULTS.md`, and `crate::generic`'s own module docs, not silently worked around. **Resolved in the very next round — see `GENERIC-SCHEMA-WRITE-THROUGH-FIX` below; the root cause turned out to be structurally different from (and simpler than) the O(N²) marker-pair treatment this bullet speculated it would need.** **Flagship test** (`tests/generic_production_integration.rs`, highest priority): two phases of 16 threads × 2,000 iterations each of concurrent `get`/`update` contention against `GenericProductionStore<OrderProductionStack>`, separated by a genuine drop + reopen from disk — the same bar `PRODUCTION-DEFAULT` set for `Dog`, now met for a directed-relation domain too. Verified via sequential-replay linearizability against a plain `HashMap` reference (not `OrderGenericStore` — its write-through gap would make it an untrustworthy reference) and a third, post-drop reopen for persistence; run 5× consecutively with no flakiness. 12 new unit tests + 1 new flagship integration test (151 total, up from 128). **Benchmark suite** (`benches/generic_production.rs`, own Criterion group names): `get`/`scan`/`filter_eq`/`parent`/`children` at 1K/100K/1M through the real `RwLock`+mmap-backed store — `parent`/`children` (48–64 ns) land in the same ballpark as the `directed-relation-spike` round's own adjacency-index numbers (44–53 ns; the small increase is the added `RwLock` acquisition and one extra generic-dispatch layer, not a regression in the unchanged underlying mechanism); `get`/`scan` land in the same ballpark as `ProductionStore`'s own post-fix `Dog` numbers, using the `chunks_exact` bulk-read technique from the start rather than needing the same fix rediscovered. No new dependency (`memmap2` already present from `DURABILITY-TIER2`). ADR-0009 moved from Proposed to Accepted; `docs/design/GENERIC-SCHEMA-DESIGN.md` gained an "Implementation status" addendum; new `STORAGE-012` spec; `SPEC-REGISTRY.md`/`TRACEABILITY.md`/`ROADMAP.md` updated. `src/lib.rs`/`README.md` updated to describe `crate::generic` alongside the existing `Dog`/`ProductionStore` story — both real, neither shadowing the other. `Dog`/`ProductionStore`/`src/store/**`/`src/durability/**`/`src/concurrency/**` completely untouched.

- `GENERIC-SCHEMA-WRITE-THROUGH-FIX` — closes the one gap `GENERIC-SCHEMA-LIBRARY` reported rather than fixed: the in-memory `Scanned`/`BaseStore` composition's `GetById::get` not reflecting a prior `UpdateField::update` write. **Diagnosed first, per this round's own instruction not to assume the durable core's fix transplants directly — and it doesn't.** `GenericMmapStore` (the durable core) is one hand-fused struct that merges two views it owns directly; `Scanned` is a separate struct layered on top of whatever owns the record (`BaseStore`, potentially several layers down) with no way to reach down and mutate that owner's storage — the root cause is structurally different, not the same mechanism. **Fix**: `Scanned`'s `GetById` forwarding impl (`src/generic/store.rs`) now patches the record it gets back from its inner store with its own live cached value (reusing `set_scannable_value`, already added for the durable core) before returning it, rather than forwarding the possibly-stale record unmodified. Stacked `Scanned` layers self-compose correctly with no further changes: each layer patches only its own field as `get` unwinds back up through the stack. **New regression test** (`order_customer.rs`'s `get_reflects_a_prior_update_through_every_layer_of_the_in_memory_stack`): writes through both the innermost (`Amount`) and outermost (`DiscountCents`) `Scanned` layers on `Order`/`Customer` and confirms `GetById::get` reflects each write immediately, while untouched fields on the same record stay correct — exactly the "write, then immediately read" case neither prior spike exercised. **Verified, not assumed, that the fix is cheap**: measured directly, same-session and back-to-back (stash the fix / benchmark / restore the fix / benchmark again, to isolate the change from this shared environment's own session-to-session drift) — `get`'s cost on the in-memory path (`benches/generic_spike.rs`'s `generic_get`, `Dog`'s single-`Scanned`-layer stack) rose from 35.8/54.3/55.4 ns to 67.5/77.7/80.9 ns at 1K/100K/1M, a real ~43–88% cost, not noise (confirmed by measuring `scan_ages` the identical way and finding it flat, since that code path is untouched). Reported honestly rather than assumed negligible, per this round's own explicit instruction to check rather than assume. 1 new unit test (152 total unit+integration tests, up from 151). `crate::generic`'s module docs, ADR-0009, `RESULTS.md`, and this document's own `GENERIC-SCHEMA-LIBRARY` bullet above updated to remove the "known follow-up work" framing — the gap is closed, at a documented, measured cost, not still open. No `GenericMmapStore`, `Dog`/`ProductionStore`, or anything outside `crate::generic`'s in-memory composition touched. No new dependency.

- `RULE-TRACE-SPIKE` — a narrow slice of an external requirements-traceability spec-tree design (`Rule`/`RuleRelation` only, not the full five-entity schema), spike-only in `src/generic_spike/` — the first real, non-toy domain to stress `crate::generic` beyond `Dog`/`Order`-`Customer`, testing recursive parent-chain traversal and multiple distinct relation kinds between the same record type. **Finding 1**: parent-chain traversal composes cleanly via a plain loop of repeated `Parent` calls (the same "compose the primitive N times" shape as `two_hop_neighbors`), but `Rule`'s optional parent (root has none) means `ChildOf::ParentId` needs to be `Option<Uuid>`, not `Uuid` — a real, concrete trait-bound conflict for `Children`'s exact-type-equality bound, not attempted in this round (resolved next, see `CHILDOF-OPTIONAL-PARENT-FIX`). **Finding 2**: multiple relation kinds (`requires`, `implements`) hit the identical `E0119` coherence conflict multiple `ScannableField`s did — confirmed by actually compiling the naive attempt first, not assumed by analogy; the same `forward_scannable_pairs!` rotating-accumulator macro pattern extends directly (`forward_related_to_pairs!`, spike-local only). Two benchmarks against naive baselines: parent-chain traversal at three depths found a genuine crossover (naive beats indexed at shallow depths 10/100; indexed wins ~3x by depth 1000, matching its O(depth) vs. naive's O(depth²) asymptotic advantage becoming visible); relation-kind-filtered lookup matched every prior directed-relation spike's order-of-magnitude speedup (~26x/~6,200x/~53,000x at 1K/100K/1M). No changes to `crate::generic`, `GENERIC-SCHEMA-DESIGN.md`, or ADR-0009. Merged via PR #19 (`a3557bf`/`eaf39ac` → merge commit `399c298`).

- `CHILDOF-OPTIONAL-PARENT-FIX` — fixes the exact bound conflict `RULE-TRACE-SPIKE` found genuinely unimplementable: `ChildOf::ParentId` stays the bare id type (so it can still equal `P::Id`, satisfying `Children`'s bound), and optionality moves onto the method instead — `fn parent_id(&self) -> Option<Self::ParentId>`. `Order`'s mandatory case becomes "the method never returns `None`," not a separate code path. Two call-site updates in `crate::generic::store` (the blanket `Parent` impl, `Reversed::new`), both no-ops for `Order`. One real, non-free consequence, caught and reported rather than hidden: `Parent::parent`'s return collapsed to a single-level `Option`, losing the "child not found" vs. "child found, no parent" distinction (fixed in the very next PR, see below). Implemented and benchmarked `Children` for `Rule` (self-referential `Reversed<_, Rule, Rule, ParentOf>`) against a naive linear scan — indexed wins outright at every tested size (2.7x-3.9x), unlike the parent-chain benchmark's shallow-depth crossover. Full suite 156/156 (up from 151), no regression to `Order`/`Customer`. Merged via PR #20 (`0c28b1d` → merge commit `151a063`).

- Restored `Parent::parent`'s not-found/no-parent distinction — the one real cost `CHILDOF-OPTIONAL-PARENT-FIX` introduced. `Parent::parent` now returns `Result<Option<C::ParentId>, NotFound<C::Id>>`, reusing the existing `NotFound` error type: `Err` means the child doesn't exist, `Ok(None)` means it exists with no parent, `Ok(Some(parent))` means it exists with one. `Rule`'s `chain_to_root` simplifies — no longer needs its own extra `GetById<Rule>` bound or upfront existence check, since `Parent::parent`'s `Err` case now covers that directly. `Order`/`Customer` confirmed unaffected (full suite green, benchmark within noise). Real, measured cost on `chain_to_root`'s indexed backend: ~25-35% slower (git-stash isolated, 100-sample runs) from checking existence on every node instead of once upfront — documented in the function's own doc comment as a genuine cost, not an equivalent rewrite. Merged via PR #21 (`60564fe` → merge commit `09e662c`).

- `REQ-TRACEABILITY-SCHEMA` — sixth spike round, completing the remaining three pieces of the five-entity requirements-traceability schema (`Source`/`Rule`/`RuleRelation`/`RuleDerivation`/`SelectionGroup`) in `src/generic_spike/`, following `RULE-TRACE-SPIKE`'s conventions. `Source` reuses the `ChildOf`/`Parent` optional-nested-parent-chain pattern for `Source`-under-`Source` nesting (`effective_domain_tags` walks to the root, tracking only the last node rather than accumulating a `Vec`). `RuleDerivation` is a completely separate `DerivationRelation`/`DerivesFrom`/`Derived` trait triad sharing no supertrait or marker with `RuleRelation`'s own — proven independent by the compiler, not just convention, via a `compile_fail` doctest (verified against a positive control) plus a runtime test showing the two edge sets are fully independent. `SelectionGroup` models cardinality constraints (`SelectExactlyOne`/`SelectAtLeastN`) over member `Rule`s as `Rule` being a `ChildOf` a `SelectionGroup`, reusing `Reversed`/`Children` — required adding one field to the existing `Rule` struct (`selection_group_id`), a real, acknowledged tension with this round's own "don't touch existing `Rule` code" constraint that `ChildOf`'s foreign-key-on-the-record shape left no other way to satisfy. New benchmarks confirm the same shallow-vs-deep crossover `RULE-TRACE-SPIKE` found for `source_root_lookup`/`derivation_chain_lookup`, and indexed winning at every size for `selection_group_members_lookup` (matching `Order`'s own `Children` shape). No changes to `Dog`, `ProductionStore`, `Order`/`Customer`, `GENERIC-SCHEMA-DESIGN.md`, or ADR-0009. Merged via PR #22 (`71341ad` → merge commit `446f582`).

- `GENERIC-MMAP-RECORD-IDENTITY-FIX` — fixes a real production bug in `GenericMmapStore`: each persisted slot held only a scannable value's raw bytes, addressed by array position with nothing recorded about which record it belonged to, so reordering records between `create()` and a later `open()` silently misattributed position N's persisted value to whatever record now occupied position N. Fix: every slot now carries `(id, value)`; `open()` reads every persisted pair up front and reconciles by id, not position. Two mismatch cases handled explicitly rather than left ambiguous: a persisted id with no matching current record is simply never surfaced (the file can only grow, a stated cost, not a silent one); a current record with no persisted entry is seeded fresh and appended. `scan()` gained a fallback path (sorted-position reads) for when slots can outlive their record, keeping the bulk fast path for the common all-live case. New `tests/mmap_record_identity_keying.rs` (4 tests: the exact reorder scenario, removed-record invisibility, new-record durability, both mismatches in one reopen). A same-PR follow-up commit measured the real, git-checkout-isolated cost against the pre-fix numbers: `get`/`update`/`create` negligible-to-modest (0-15%), `parent` 9-18% (one extra offset computation), `scan` 1.6-4.4x slower (24-byte id+value slots vs. the old 8-byte value-only slots), `open` 1.4-1.9x slower (the reconciliation pass the fix's correctness guarantee requires) — reported in `RESULTS.md`, with the prior "no regression from spike to real implementation" verdict marked superseded, not deleted, per this document's established layering convention. No changes to `Dog`, `ProductionStore`, or the durability crate's separate serde/bincode-based path; no changes to `GENERIC-SCHEMA-DESIGN.md`/ADR-0009. Merged via PR #23 (`cf6fc51`/`3ef4aec` → merge commit `5905c9e`).

- `GENERIC-MMAP-SCHEMA-VERSION-HEADER` — `GenericMmapStore`'s on-disk format had no marker distinguishing "not a file this store wrote" from "a file this store wrote, but at an older schema version," confirmed by reading the layout directly rather than assumed. Adds a 12-byte header (8-byte magic + little-endian `u32 SCHEMA_VERSION`) validated in `open()` before touching any record data: a bad/missing magic fails with a new `DurabilityError::InvalidMagic`; a version mismatch fails with `SchemaVersionMismatch { found, expected }`; a match proceeds as before. Detection only, no migration logic, per scope. Tests cover the baseline round-trip, a simulated version mismatch, a corrupted magic number, and a too-short file, each asserting the specific error variant. Measured the header's cost with a controlled before/after rather than assuming it's free: a second baseline-vs-baseline control run showed swings of the same magnitude as the apparent header cost, settling it as host-level session noise, not a real regression. `Dog`/`ProductionStore` untouched; `DurabilityError`'s existing catch-all `From` conversion keeps the two new variants non-breaking for `Dog`. Merged via PR #24 (`393498d` → merge commit `7a8f71c`).

- `GENERIC-MMAP-CRASH-SAFETY-DIAGNOSIS` — every durability test in this crate up to this point was a graceful drop+reopen; nothing exercised a real crash (no unwind, no cleanup) against `GenericMmapStore`. New subprocess-based crash harness (`src/bin/crash_writer.rs`, spawned and `SIGKILL`ed by `src/bin/crash_safety_harness.rs`), 8 trials × 2 runs per question, zero variance. **Findings**: (1) unflushed writes the child had already completed survive 100% of the time, every trial — process-crash-safe, but explicitly documented as *not* proof of power-loss/physical-durability safety, since a `SIGKILL` doesn't unmap the page cache, which stays coherent across process death; (2) `Flush`-called-then-immediately-killed survives 100% — `msync`-backed `Flush` is a real durability guarantee against process crash; (3) torn writes reproduced in all 8 trials by killing in the gap between a new slot's id write and its value write, confirmed distinct from the other two findings via an uninterrupted control run — motivating the fix in the next PR. Diagnosis only, matching the schema-evolution round's own discipline: two new diagnostic binaries under `src/bin/`, no production code touched. Merged via PR #25 (`3fba1ce` → merge commit `95144bc`).

- `GENERIC-MMAP-TORN-WRITE-FIX` — fixes the torn write `GENERIC-MMAP-CRASH-SAFETY-DIAGNOSIS` reproduced 8/8 times. Every slot gains a trailing `COMMITTED` byte, written strictly last, only once both id and value are fully in place; a single new `write_slot_into` function performs all three writes in order and is now the only path either slot-creating call site (`create`'s per-record loop, `open`'s new-slot path) can go through, closing the drift the original two-step id/value split allowed. `open()`'s reconciliation skips any slot whose marker isn't set, treating it as never-persisted — the torn bytes stay inert, the same already-accepted "no compaction" cost every other stale slot has. `SCHEMA_VERSION` bumps 1→2 for the wider slot layout, so an old file correctly fails as `SchemaVersionMismatch` rather than being misread. Separately diagnosed, not assumed, that `UpdateField::update`'s in-place overwrite path needed no fix: a new rapid, unsynchronized crash-writer mode found 16/16 trials always read back exactly one of the two known patterns, never a torn mix — accepted as a narrower, explicit residual assumption (depends on codegen, not proven at the language level) rather than given a second commit-marker mechanism, since closing it for certain would turn every in-place update into unbounded log-structured growth. Verified through the real `GenericMmapStore::open` API, not raw bytes: 32/32 trials across both gaps always read back a fresh reseed value, never the interrupted write's attempted value. Measured cost (git-stash-isolated, same-session methodology, since a stale cross-session baseline had shown 70-100%+ apparent regressions that were pure host noise): only `open` at larger sizes shows a small, consistently-directional single-digit-percent increase, roughly matching the +4.2% larger per-slot byte volume; every other operation's deltas flipped direction between repeated runs. Merged via PR #26 (`e9d4d00` → merge commit `cba64a9`).

- `PUBLIC-API-NARROWING` — narrows the crate's actual compiled public surface to a real "front door" via a new Cargo feature (`research`, off by default) gating the benchmarked-alternative/research bucket behind `#[cfg(feature = "research")]`, superseding an earlier, broken `pub(crate)` attempt (`tests/`, `benches/`, `examples/`, `src/bin/*.rs` each compile as separate crates linking this library externally, so `pub(crate)` items were invisible to nearly the entire bench/test/example suite that needed them). **Front door** (unconditional): `ProductionStore`, `GenericProductionStore`, the `Record`/`IndexedField`/`ScannableField`/`SymmetricRelation`/`ChildOf` traits and the composable query/store layer traits, `DogRecord` (structurally required by `DogStore`'s own public signature despite reading like research material — documented explicitly), and every error type. **Gated behind `research`**: the other 3 `Dog` storage backends, the 7 non-mmap durability variants, the 3 non-`RwLock` concurrency strategies (plus `GlobalRwLockStore`, also gateable since `ProductionStore` rolls its own bare `RwLock`), `bench_support`, `generator`, `generic_spike`, and `generic::order_customer`. New always-on `test_support` module (`fresh_temp_dir`), since `ProductionStore`'s own infallible constructors need it unconditionally, not just tests/benches; several other items needed the same "gated for research, but also available under plain `#[cfg(test)]`" treatment. Verified from both sides: default `cargo test` passes with 61 tests and the previously-internal modules absent; `--all-features --release` still passes the full 183; a throwaway example confirmed the narrowing is real, not cosmetic (`store::AosStore` fails to resolve without the feature). No behavior changed, nothing deleted — compilation-gating only. **No ADR was written for this PR despite it being a consequential, crate-wide public-API design decision** — flagged explicitly in Risks below, not silently treated as sufficient. Merged via PR #27 (`8257bb5` → merge commit `e2d779b`).

- `GENERIC-MMAP-MULTIPROCESS-DIAGNOSIS` — first test of two separate OS processes (not just threads within one process) opening the same `GenericMmapStore` file concurrently. New two-process harness (`src/bin/multiprocess_writer.rs`/`multiprocess_harness.rs` — both processes stay alive and race concurrently, unlike the crash harness's kill-one-of-them design), 8 trials per scenario across up to 4 full runs. **Findings**: (1) no OS-level locking exists — confirmed by reading `memmap2`'s own source (no `flock`/`fcntl`/`lockf`/`O_EXCL`) and empirically, every trial; (2) concurrent `create()` with genuinely different datasets resolved cleanly 24/24 trials but is explicitly reported as "safe by accident of current timing," not a designed guarantee (`create()` has no lock, and `.truncate(true)` can shrink a file another process's live mmap still references); the "next free slot" race is real and reproducible, the most severe finding — two processes independently computing the same `existing_slot_count` both write to the same on-disk position, silently overwriting one process's entire slot including its commit marker, and the losing process's own subsequent reads on "its own record" transparently keep operating on the winner's data until a later, independent reopen accidentally heals it; the update-path commit-marker invariant holds even under two genuinely concurrent live writers (32/32 trials); (3) readers never observed a torn value across a concurrent writer (32/32 trials). Diagnosis only, matching the schema-evolution and crash-safety rounds' own discipline: two new diagnostic binaries under `src/bin/`, gated behind `research`, no production code touched. Merged via PR #28 (`bb6daef` → merge commit `320b2b3`).

- `GENERIC-MMAP-APPEND-SLOT-RACE-FIX` — fixes the "next free slot" race `GENERIC-MMAP-MULTIPROCESS-DIAGNOSIS` found. Evaluated a cross-process file lock (`fs2`/`fd-lock`) against `O_APPEND` per instruction to check both rather than default to precedent; chose `O_APPEND` — POSIX guarantees the reposition-to-EOF-and-write is atomic across separate open file descriptions on local filesystems (NFS is the one documented, accepted, non-target exception), needs no new dependency, and doesn't over-serialize concurrent appends against each other. `open()`'s new-slot path now appends each missing record's fully-formed slot (id+value+commit marker, one buffer, one `write_all` call) through a dedicated `O_APPEND` handle and reads back its real position from that write's own resulting file offset — private to the open file description that wrote it, needing no coordination to query safely. Verified against the real two-process harness (assertions updated from "who wins the collision" to "both records must be present at distinct positions"): 24/24 trials across 3 full runs, zero collisions, both processes' records always intact. Cost: a new benchmark isolating the append path found ~0.84us/record extra at a 1K-record base, statistically indistinguishable from noise at 100K/1M (dwarfed by `open()`'s existing O(n) reconciliation cost); `create`/gapless-`open` unaffected. No `SCHEMA_VERSION` bump — on-disk bytes are unchanged, only how a new slot's position is chosen. Merged via PR #29 (`f629e58` → merge commit `e490a40`).

- `README-AND-RUSTDOC` — rewrites the README around a real getting-started path (how to depend on this repo by git/path since it stays internal, a complete copy-paste-run `ProductionStore` example verified to compile and run in a separate crate depending on this one by path, a pointer to `GenericProductionStore` for a custom record type, what `research` unlocks), replacing an undifferentiated research-log wall of prose with a "where to go deeper" index — one line per doc file (charter, system architecture, `GENERIC-SCHEMA-DESIGN.md`, all nine ADRs, spec registry/tree, roadmap, traceability, this document, `RESULTS.md`, `AGENTS.md`/`WORKFLOW.md`). Adds real rustdoc `# Examples` doctests to `ProductionStore` and `GenericProductionStore` (the latter implements a minimal custom domain from scratch, unconditionally available with no `research` feature needed, demonstrating the actual "build your own domain" path a consumer would take), cross-references trait doc comments back to it rather than duplicating, adds the one missing doc comment (`GetById`). Documentation/doc-comment changes only — no behavior changed anywhere; both new doctests verified passing under default and `--all-features` builds; `cargo doc`'s pre-existing warning count (53 default / 62 `--all-features`) confirmed unchanged by the new intra-doc links. Merged via PR #30 (`c45bf2a` → merge commit `4000565`).

- `FUTURE-GROWTH-DOC` — adds `docs/FUTURE-GROWTH.md` (pre-written, pre-reviewed verbatim, no content changes in this PR) naming candidate future directions with an explicit "nothing here is planned or scheduled" framing: a server/query layer genuinely additive on top of the existing storage API (concurrency across client connections collapses back to the already-solved in-process `RwLock` case if one server process owns the file) versus SQLite/DuckDB-tier parity work (SQL, transactions, arbitrary joins — each named as a standalone, multi-year effort, explicitly not a natural extension of this project). Links it from the README's doc index added in `README-AND-RUSTDOC`. Merged via PR #31 (`d6914bd` → merge commit `176f5d0`).

- `SERVER-QUERY-LAYER-DESIGN` — the owner chose the server/query-layer direction over SQLite/DuckDB-tier parity when asked which of `docs/FUTURE-GROWTH.md`'s two named directions to pursue next. `docs/design/SERVER-QUERY-LAYER-DESIGN.md` proposes a `Request`/`Response` wire protocol (`GetById`/`FilterEq`/`ScanField`/`UpdateField`/`Parent`/`Children`/`Neighbors`) over length-prefixed `bincode` framing (`bincode` already a dependency, from the durability work — no new one added), thread-per-connection dispatch reusing `ProductionStore`/`GenericProductionStore`'s existing `RwLock`-shared-store concurrency, and integer field tags (not a schema-description sub-protocol) for v1. The proposed `Request`/`Response`/`ConnectionStore`/framing shapes were compiled — not executed — in a standalone, dependency-free scratch probe outside this repository (`rustc --edition 2021 --crate-type lib`), the same "prove signatures compile" discipline `GENERIC-SCHEMA-DESIGN` used. **Explicitly, deliberately out of scope**, matching `docs/FUTURE-GROWTH.md`'s own split: authentication/authorization, transport encryption, transaction semantics, and any query language beyond fixed field-tag addressing — the design document and ADR-0010 both state plainly that this design does not authorize deploying a server binary beyond a trusted, localhost/development context. `ADR-0010` records the decision (protocol/framing, concurrency model, and field-addressing alternatives considered and rejected, with reasoning). `docs/charter/CHARTER.md`'s "Product shape" section gained a note: "no server, no network surface" is, as of ADR-0010, amended (pending this acceptance — see below); "no persistence"/"no concurrency control" were already superseded by earlier ADRs without the charter text itself being rewritten at the time, named explicitly now rather than left as an unremarked contradiction. **The owner reviewed and accepted the design as proposed** — ADR-0010 and the design document are now Accepted, no changes requested. No existing source file was modified, no new dependency added, and no server implementation was written by this unit or authorized by its acceptance — a separate, not-yet-started implementation unit will register `SERVER-001` and a real planning packet first, matching how `STORAGE-012` followed `GENERIC-SCHEMA-DESIGN`'s own acceptance. Merged via PR #33 (`c5ee286`/`35b6c67` → merge commit `76a07e9`).

- `SERVER-QUERY-LAYER` — the real implementation of `SERVER-QUERY-LAYER-DESIGN`/ADR-0010, registering `SERVER-001` (`docs/specifications/server/SERVER-001-query-layer.md`). New `src/server/` module (`protocol.rs`, `framing.rs`, `mod.rs`'s `ConnectionStore` trait + `dispatch` + `serve`, `dog.rs`, `order.rs`), gated behind a new `server` Cargo feature — off by default, deliberately kept separate from `research` (new, additive capability, not a benchmarked alternative). No new dependency (reuses `bincode`/`serde`/`uuid`, all already present); no existing source file outside `src/server/**`/`src/bin/dog_server.rs`/`Cargo.toml`'s `[features]`/`[[bin]]`/`[[test]]` entries touched — verified by diff.

  **Implementation-time completions beyond the design's "Proposed shape"** (documented in `protocol.rs`'s own doc comment, none reopening a decision ADR-0010 recorded): `ScanValue` gains a `Str(String)` variant (needed for `Dog::breed`'s `GetById` response — no `ScannableField` anywhere in this crate is a `String`, so the design's numeric/bool-only shape never needed to represent one); `Response` gains `Id`/`ScanValues` variants (`Parent`'s "found, has a parent" case and `ScanField`'s result, neither worked out in the design's own dispatch sketch); `ErrorCode` replaces a bare `code: u8` with a named three-variant enum (`UnknownField`/`Unsupported`/`Malformed`). `ParentLookup` (a three-way `Parent(id)`/`NoParent`/`ChildNotFound` enum) deliberately preserves the not-found/no-parent distinction this project's own PR #21 restored in-process, rather than collapsing it again at the wire boundary.

  **Validated against both domains** the design's Acceptance criteria named: `server::dog::DogConnectionStore` wraps `ProductionStore` (`Neighbors` — `Dog`'s real symmetric relation; `Parent`/`Children`/`FilterEq` report `ErrorCode::Unsupported`, since no directed relation or value-based equality filter exists for `Dog` in-process either); `server::order::OrderConnectionStore` (behind `research`, since `order_customer` itself is) wraps `GenericProductionStore<OrderProductionStack>` (`Parent`/`Children` via `BelongsToCustomer`, `FilterEq`/`ScanField`/`UpdateField` via `Status`/`Amount` — the only two fields the *durable* stack carries, `CreatedAt`/`DiscountCents` staying read-only-via-`GetById` since they were never part of `OrderProductionStack` to begin with; `Neighbors` reports `Unsupported`, since `Order`/`Customer` has no symmetric relation). A minimal, real server binary (`src/bin/dog_server.rs`, `required-features = ["server"]`) seeds `ProductionStore` from a small hand-written sample dataset (not `generator`, which is research-gated) so it builds under `server` alone.

  **Real end-to-end coverage over a genuine socket**, not just `dispatch`'s in-process logic: `tests/server_dog_integration.rs` (3 tests) and `tests/server_order_integration.rs` (1 test) drive a real `TcpListener`/`TcpStream` pair (a background thread with a real socket, not a genuinely separate OS process — both files' own module docs say so plainly) through every request kind including the domain-appropriate `Unsupported` cases, plus a second independent connection observing the first's writes (proving the store, not connection-local state, is shared). **Flagship stress test** (`concurrent_clients_over_the_wire_match_a_sequential_replay`, highest priority): 8 real client connections × 200 interleaved `GetById`/`UpdateField` requests each against a 20-id contended pool, verified via sequential-replay linearizability — the same bar `run_concurrency_stress_test`/`production_integration.rs` set, now cleared over the wire protocol, not just in-process.

  **Two real issues found and fixed during implementation, neither anticipated by the design, reported honestly rather than silently absorbed** (full account in ADR-0010's own "Acceptance and implementation" section):
  - **A Nagle/delayed-ACK interaction**: the stress test above initially took ~36s (confirmed by direct measurement) — a synchronous request/response protocol pays a real ~40ms-per-round-trip cost with `TCP_NODELAY` left at its default on either side of the connection. Fixed by setting it server-side (`handle_connection`) and client-side (both integration tests' own `connect` helper) — the same test then ran in ~0.04s, confirmed by re-running it repeatedly. A real, measured cost of the chosen protocol shape, not a flaw in the protocol choice itself — the fix is a one-line socket option.
  - **A test-isolation bug, not a server bug**: two integration tests both deriving their mmap-backed temp-file path from the process id alone raced on the same file once `cargo test`'s default concurrent test execution ran them together — one test observed the other's writes, an intermittent false failure. Fixed by adding a per-call atomic counter to the shared temp-dir-naming helper in both test files.

  **Documentation**: `SERVER-001` registered (`docs/specifications/SPEC-REGISTRY.md`, `docs/traceability/TRACEABILITY.md`); `docs/roadmap/ROADMAP.md` gains a `SERVER-QUERY-LAYER` row (Implemented) and a sequencing note; ADR-0010 and `docs/design/SERVER-QUERY-LAYER-DESIGN.md` both gain "real validation, post-acceptance" sections; `README.md` gains a `server`-feature section (mirroring the existing `research`-feature one) and doc-index entries for `ADR-0010`/`SERVER-QUERY-LAYER-DESIGN.md`/`SERVER-0xx`; `src/lib.rs`'s top-level doc comment gains a "A third arc: a network server" section.

  **Deliberately not built this round, named rather than silently assumed unnecessary**: a throughput/latency benchmark (`benches/server.rs`) — this round's acceptance criteria are correctness, not performance; the thread-per-connection model's real connection-count ceiling remains unmeasured; `bincode`'s wire-format stability across crate versions (client/server version skew) remains unverified.

  Merged via PR #35 (`6c024da` → merge commit `2bbfa10`).

- `SERVER-QUERY-LAYER` v0.2.0: schema discovery — asked what to do next with the server layer, the owner picked "a query language / schema-description sub-protocol" from a short list of concrete options. `ADR-0011` (Accepted, same delivery cycle as its implementation — see ADR-0011's own Context for why this didn't get the separate design-only-first treatment `SERVER-QUERY-LAYER-DESIGN` did). Adds `Request::DescribeSchema` (no arguments — one server instance serves one domain) / `Response::Schema(DomainSchema)`; `DomainSchema { fields: Vec<FieldDescriptor>, relations: RelationCapabilities }`; `FieldDescriptor { tag, name, value_kind, capabilities: FieldCapabilities { filter_eq, scan, update } }`. `ConnectionStore` gains one new, infallible method, `describe(&self) -> DomainSchema`; both adapters implement it, reporting their real, honest capability shape — including fields that exist but support none of the three operations (`Order`'s `created_at_unix_ms`/`discount_cents`, never part of the durable stack the server wraps) rather than hiding them from the schema. Field *tags* are unchanged as the wire addressing scheme; this adds runtime discovery of what a compile-time client already knows, resolving the one item ADR-0010's own "Validation and revisit triggers" had explicitly left open (not rejected) as a future decision.

  **Verified as genuinely usable, not just descriptive** — the real bar this round set for itself: `tests/server_dog_integration.rs`'s `a_schema_driven_client_discovers_and_uses_the_age_field` and `tests/server_order_integration.rs`'s `a_schema_driven_client_discovers_and_uses_the_status_field` each drive a real client that starts with zero compile-time field-tag knowledge, calls `DescribeSchema`, finds a field by name (`"age"` / `"status"`), and completes a real `UpdateField`/`FilterEq` using only the discovered tag — not just asserting `Response::Schema`'s static content. 3 new unit tests (`server::tests::describe_schema_returns_the_fixture_store_own_shape`, `server::dog::tests::describe_names_both_fields_and_reports_neighbors_only`, `server::order::tests::describe_names_all_four_fields_and_reports_parent_children_only`) plus the 2 new integration tests — 225/225 passing (up from 220/220), all-features release, re-run 3× with no flakiness.

  No new dependency, no framing/transport change, no existing `Request`/`Response` variant's shape changed, no existing source file outside `src/server/**`/`tests/server_*_integration.rs` touched — verified by diff. `SERVER-001` bumped to v0.2.0 (new FR-010/FR-011, a `## Change history` section added); `docs/design/SERVER-QUERY-LAYER-DESIGN.md`'s own "schema-description RPC... deferred to a future decision" open question marked Resolved rather than rewritten; `docs/roadmap/ROADMAP.md`/`docs/traceability/TRACEABILITY.md`/`README.md` updated to match.

  Merged via PR #37 (`eb3a95d` → merge commit `318caf5`).

- `SERVER-QUERY-LAYER` v0.3.0: third validation domain — asked what to do next, the owner picked "a third domain, then a throughput benchmark" (`"3 then 2"`). `Employee` (`src/generic_spike/employee_impl.rs`) is purpose-built, not drawn from an external reference domain the way `Order`/`Customer` was: `reports_to` (self-referential `ChildOf`) and `collaborates_with` (self-referential `SymmetricRelation`) on the same record type, a combination ADR-0009's own "revisit if" bullet named as untested. Building it surfaced a real gap in `crate::generic` itself: `Reversed<S, ..>` never forwarded `Neighbors` (unlike `Indexed`/`Scanned`, which already forward every sibling capability), so stacking `Symmetric` beneath `Reversed` silently lost `Neighbors` access from outside the composed stack. No prior domain had tried — `Order`/`Customer` has `ChildOf` but no `SymmetricRelation`. Fixed with a `Neighbors` forwarding impl on `Reversed` (`src/generic/store.rs`) and a new `neighbors` method on `GenericProductionStore` (`src/generic/production.rs`, which had never needed one). Recorded as an addendum to already-Accepted `ADR-0009` (matching `GENERIC-SCHEMA-WRITE-THROUGH-FIX`'s own "completion, not a new decision" precedent, not a new ADR) and as `STORAGE-012` v0.2.0.

  `server::employee::EmployeeConnectionStore` (`src/server/employee.rs`) wraps `GenericProductionStore<EmployeeProductionStack>` and is the first `ConnectionStore` domain adapter where `get`/`filter_eq`(department)/`scan_field`+`update_field`(salary)/`parent`/`children`/`neighbors` are *all* real operations — no domain-shaped `ErrorCode::Unsupported` anywhere, unlike `Dog` (`Neighbors` only) or `Order`/`Customer` (`Parent`/`Children` only). Verified at both layers: `src/generic_spike/employee_impl.rs`'s test module (in-memory stack, durable production stack, and a genuine flush-then-reopen cycle, each exercising `Neighbors` alongside `Parent`/`Children`) and, over the real wire protocol, `tests/server_employee_integration.rs` — a full `GetById`/`FilterEq`/`ScanField`/`UpdateField`/`Parent`/`Children`/`Neighbors` round trip with everything succeeding, the empty-but-real cases (`Parent` on a record with no manager returns `Response::NoParent`, not an error; `Neighbors` on a record with no collaborators returns an empty list), and a schema-driven test confirming `DescribeSchema` reports both `RelationCapabilities` as `true` for the first time.

  `SERVER-001` bumped to v0.3.0 (new FR-012/FR-013, `## Change history` extended); `STORAGE-012` bumped to v0.2.0 (new FR-010, `## Change history` added). No existing source file outside `src/server/employee.rs` (new), `src/generic_spike/{mod,employee_impl}.rs`, `src/generic/{store,production}.rs`, `tests/server_employee_integration.rs` (new), and `Cargo.toml`'s feature-and-target entries was touched — verified by diff. No new dependency.

  Merged via PR #39 (`ca214f9` → merge commit `d53e7fe`).

- `SERVER-QUERY-LAYER` v0.4.0: throughput/latency benchmark — the "2" in the owner's "3 then 2." `benches/server.rs` closes the gap `SERVER-001`'s own "Open questions" named since v0.1.0 (correctness was that round's acceptance bar, not performance). A custom, non-Criterion harness, matching `benches/concurrency.rs`'s own reasoning and shape (`Barrier`-synchronized thread sweep, aggregate ops/sec from the slowest thread's wall-clock time — not what Criterion's `b.iter` model measures) — and the first benchmark in this crate to put a real `TcpListener`/`TcpStream` pair in its timed path, rather than measuring `dispatch` in-process (every existing unit test) or a real socket only for pass/fail correctness (every existing integration test).

  Measures, per domain (`Dog`, `Order`/`Customer`, `Employee`, a small fixed 20-id pool each — matching the flagship concurrent-client stress test's own contended-pool precedent, not this crate's usual 1K/100K/1M `SIZES` sweep): single-connection, zero-contention `GetById` round-trip latency (5,000 sequential requests, one client), and aggregate throughput under a 1/4/8/16-thread sweep (thread-per-connection, each thread its own real socket). This session's container reports `std::thread::available_parallelism() == 4`, so 1/4/8/16 (not `benches/concurrency.rs`'s `baileyai`-specific 1/4/32/64) — matching that suite's own documented "original container run" precedent.

  **Results** (full tables in `RESULTS.md`'s new `## Server / query layer` section): single-connection latency lands within a couple of microseconds across all three domains (~36–39 µs). Throughput scales from ~22–28K ops/sec at 1 thread to ~160–310K ops/sec at 4+ threads, then stays roughly flat (within this shared container's own run-to-run noise) from 4 through 16 — consistent with the container saturating at its 4 reported cores, not with any domain-specific or server-layer ceiling. **Headline finding**: at this record-count scale, one loopback round trip's network/framing cost so thoroughly dominates any per-domain in-process operation cost (already measured at tens-to-low-hundreds of nanoseconds elsewhere in `RESULTS.md`, two to three orders of magnitude smaller) that all three domains' numbers land in the same rough band — this benchmark's own question ("what does the network layer add on top of an already-measured operation") is confirmed by its own numbers, not just answered by construction.

  `SERVER-001` bumped to v0.4.0 (new FR-014, `## Change history` extended; the "no throughput/latency benchmark" item moved from "Open questions" to "Resolved"). No existing source file outside `benches/server.rs` (new) and `Cargo.toml`'s feature-and-target entries was touched — verified by diff. No new dependency (`benches/server.rs` reuses `criterion`'s harness plumbing only nominally, `harness = false`, same as `benches/concurrency.rs` — a plain `fn main()`, no Criterion API calls).

  **Open, named rather than silently assumed answered**: the thread-per-connection model's real connection-count ceiling remains unmeasured — this pass's sweep is bounded by the container's own 4 cores, not by any structural limit of the model itself; a `baileyai` (or other dedicated-hardware) follow-up, matching `## Concurrency`'s own established container-then-real-hardware precedent, would be needed for that.

  Merged via PR #41 (`d5c7d6f` → merge commit `0a07938`).

- `SERVER-001` real-hardware follow-up: the connection-count-ceiling open question closed. The prior round's own "Open, named rather than silently assumed answered" flagged that its 1/4/8/16 sweep was bounded by a 4-core container, not by any structural limit of the thread-per-connection model. This session runs directly on the owner's Windows dev machine (`Beast`, `baileyrd`, confirmed via `hostname`/`whoami`/`nproc` — real dedicated hardware, 12 physical / 24 logical AMD Ryzen 9 7900X3D cores, not a container). `benches/server.rs`'s `THREAD_COUNTS` retuned from `[1, 4, 8, 16]` to `[1, 4, 24, 48]`, the exact array `benches/concurrency.rs` already established for this same machine (real core count, plus a deliberate 2×-cores oversubscription point); module doc comment updated to match.

  **Results** (full tables in `RESULTS.md`'s new real-hardware subsection): latency lands in the same ~29–42 µs band as the container's ~36–39 µs. Throughput genuinely keeps climbing from 4 to 24 threads on real hardware — roughly doubling again, every domain, both runs (e.g. `Dog`: 85,727 → 191,066 ops/sec) — confirming the container's flat 4-through-16 plateau was that environment's own ceiling, not the model's. Past 24 threads, 48 (2× cores) is flat-to-negative and noticeably noisier, not a continuation of the climb — the same oversubscription signature `## Concurrency`'s own real-hardware passes already documented. **Headline finding**: the thread-per-connection model's real ceiling sits at or near this machine's actual core count; pushing past it costs (up to ~40% in the worst observed case) rather than helping. **One honest surprise, not smoothed over**: this real machine's peak throughput (147K–192K ops/sec) is lower than the 4-core container's own plateau (160K–310K ops/sec) — plausibly Windows loopback-TCP/scheduling overhead vs. the container's Linux stack, left as an unverified, separately-flagged open question rather than an asserted explanation.

  `SERVER-001`'s "Open questions" updated: the connection-count-ceiling item moves to resolved (with the peak-throughput surprise above named as a new, narrower open sub-question). No existing source file outside `benches/server.rs` and docs (`RESULTS.md`, `SERVER-001-query-layer.md`, this file) was touched — verified by diff. No new dependency, no spec version bump (matching `STORAGE-010`'s own precedent: retuning a bench's thread-count sweep for a new environment doesn't itself change the spec's implemented capability).

  Merged via PR #43 (`de2b1ad` → merge commit `23995ca`).

- `SERVER-001` second real-hardware follow-up: the connection-count-ceiling question re-measured a third time, this time on `baileyai` itself rather than a substitute machine. This session runs directly on `baileyai` (`baileyai`, `baileyrd`, confirmed via `hostname`/`whoami`/`nproc` — 32 logical / 16 physical AMD Ryzen AI MAX+ 395 cores, SMT enabled, real dedicated hardware, no SSH substitution needed). `benches/server.rs`'s `THREAD_COUNTS` retuned from `Beast`'s `[1, 4, 24, 48]` to `[1, 4, 32, 64]`, the exact array `benches/concurrency.rs` already established for this same machine; module doc comment updated to match.

  **Results** (full tables in `RESULTS.md`'s new second real-hardware subsection): latency drops to ~8–13 µs — a third to a quarter of both the container's ~37–39 µs and `Beast`'s ~29–42 µs. Throughput climbs strongly from 4 to 32 threads on real hardware — 3.3×–5.0× across domains/runs (e.g. `Dog`: 376,886 → 1,229,443 ops/sec run 1) — confirming again that the model's ceiling sits at or near real core count, not at some lower structural limit. Past 32 threads, 64 (2× cores) is flat-to-mildly-negative (−2% to −10%), milder than `Beast`'s worst-case −40%. **Headline finding, and the resolution of the prior round's own open surprise**: `baileyai`'s peak throughput (1.17M–1.33M ops/sec at 32 threads) is far higher than both the container's plateau (160K–310K) and `Beast`'s own peak (147K–192K) — roughly 4–8× the container, 7–9× `Beast`. Combined with the latency numbers, this settles the question `Beast`'s pass left open: dedicated Linux hardware is genuinely faster end to end than either environment, meaning `Beast`'s Windows loopback-TCP/scheduling stack — not the container — was the actual outlier.

  `SERVER-001`'s "Open questions" updated: the second real-hardware follow-up entry added, and the `Beast` pass's own peak-throughput surprise marked resolved rather than left open. No existing source file outside `benches/server.rs` and docs (`RESULTS.md`, `SERVER-001-query-layer.md`, `docs/roadmap/ROADMAP.md`, `docs/traceability/TRACEABILITY.md`, this file) was touched — verified by diff. No new dependency, no spec version bump (same precedent as the `Beast` pass: retuning a bench's thread-count sweep for a new environment doesn't itself change the spec's implemented capability).

  Validation: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --release --all-features` (×3, no flakiness), `cargo test --release` (default features, unaffected) all clean; `cargo bench --features server,research --bench server` run twice for run-to-run variance (numbers above).

  Merged via PR #44 (`71fcc88` → merge commit `8047a28`).

- `SERVER-QUERY-LAYER` v0.5.0: schema-driven client library — asked what to do next, the owner picked three directions at once (auth/encryption, session/transaction semantics, and a schema-driven client library). Sequenced by weight: this one first, since it's bounded/additive (no new `Request`/`Response` variant, no wire-format change — a pure client-side completion of ADR-0011's already-accepted schema-discovery decision, not a new one), while the other two — both flagged "genuinely new," not incremental, in `docs/FUTURE-GROWTH.md` — get a design-only-first ADR treatment before any implementation, matching how `SERVER-QUERY-LAYER-DESIGN` itself was handled.

  `server::client::SchemaDrivenClient` (`src/server/client.rs`, unconditional under `server` — no domain-specific code, so no `research` gate) promotes the one-off schema-driven logic each of `tests/server_{dog,order,employee}_integration.rs`'s own schema-driven tests (FR-011/FR-013) had reimplemented by hand into real, reusable library API. `connect` sends `Request::DescribeSchema` first and keeps the result; every subsequent method (`get`/`filter_eq`/`scan`/`update`/`parent`/`children`/`neighbors`) is addressed by field *name*, never a domain's own `FIELD_*` constant — `get` returns `(String, ScanValue)` pairs, not `(FieldRef, ScanValue)`, for the same reason a caller with no compile-time schema knowledge has nothing useful to do with a bare `u16` tag. `filter_eq`/`scan`/`update`/`parent`/`children`/`neighbors` each check the relevant `FieldCapabilities`/`RelationCapabilities` from the discovered schema before sending anything — `ClientError::Unsupported`/`ClientError::UnknownField` locally, no round trip, for something the schema already ruled out. This is an optimization, not a trust boundary: `dispatch` still enforces the identical rules independently on the server side, so a client that skipped this check would still get back a typed `Response::Err`, never undefined behavior.

  Verified end to end, not just at the unit level: `tests/server_schema_driven_client.rs` connects `SchemaDrivenClient` to all three domains (`Dog`, `Order`/`Customer`, `Employee`) and drives every request kind purely through discovered field names, plus the unknown-field-name client-side-error case — 4 new tests, all passing. The test file itself imports no `FIELD_*` constant from any domain adapter, proving the promotion didn't quietly reintroduce compile-time domain knowledge through the test's own back door.

  `SERVER-001` bumped to v0.5.0 (new FR-015, `## Change history` extended). No existing source file outside `src/server/{mod,client}.rs` (new), `tests/server_schema_driven_client.rs` (new), and `Cargo.toml`'s feature-and-target entries was touched — verified by diff. No new dependency (`server::client` reuses `framing`/`protocol` directly).

  Validation: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --release --all-features` (×3, no flakiness — 240/240 passing, up from 236/236), `cargo test --release` (default features, unaffected), `cargo check --all-targets --all-features`, `cargo doc --all-features --no-deps` (clean, no new warning class) all clean.

  Merged via PR #46 (`ed8587e` → merge commit `e6ccc75`).

- `SERVER-AUTH-DESIGN` — the owner picked authentication/encryption as the second of three next directions (alongside the schema-driven client library, done above, and session/transaction semantics, still ahead). `docs/FUTURE-GROWTH.md` names authentication as "genuinely new," not incremental, so this got the same design-first, stop-for-review treatment `SERVER-QUERY-LAYER-DESIGN`/ADR-0010 itself got before any server code existed — no implementation code, no new source file under `src/`, no test suite, no dependency actually added to `Cargo.toml`.

  `docs/design/SERVER-AUTH-DESIGN.md` proposes a new `Request::Authenticate { token }`, checked once per connection before any other request (including `DescribeSchema`); two static token classes (`ReadOnly`/`ReadWrite`, `ReadOnly` blocked from `UpdateField`), configured server-side via environment variables, never committed to the repo; constant-time token comparison via a new, narrow dependency (`subtle`) rather than a hand-rolled compare — the one deliberate departure from this project's usual "avoid new dependencies" posture, since defeating compiler optimizations that reintroduce timing side channels is a well-known place DIY security code silently regresses. Backward compatible by construction: a server started with no configured tokens behaves exactly as today's unauthenticated `SERVER-001` does.

  **Explicitly, deliberately does not close the transport-encryption half of the gap**: requires an external TLS-terminating proxy/tunnel for any non-localhost deployment, with native TLS (`rustls`) named as a real, larger follow-up rather than added by default — matching the same dependency-weight reasoning ADR-0010 used to defer `tokio`/gRPC. Accepting ADR-0012 does not itself authorize deploying a server binary beyond a trusted, localhost/development context.

  **The owner reviewed and accepted the design as proposed, no changes requested.** ADR-0012 and `docs/design/SERVER-AUTH-DESIGN.md` are now Accepted. No existing source file was modified, no dependency actually added, and no server implementation was written by this unit or authorized by its acceptance — a separate, not-yet-started implementation unit will extend `SERVER-001` with new FRs (or register a dedicated spec) before any server code lands, matching how `SERVER-QUERY-LAYER` followed `SERVER-QUERY-LAYER-DESIGN`'s own acceptance.

  `docs/charter/CHARTER.md`'s "Product shape" section also got a small fix in the proposal pass: a stale line still describing ADR-0010 as "Proposed" (it's been Accepted for several rounds) is corrected, with a cross-reference to this new decision added.

  Proposed via PR #48 (`6444201` → merge commit `aaf07c4`); accepted via PR #50 (`638ad9b` → merge commit `75aa3e3`).

- `SERVER-AUTH` — the real implementation of `SERVER-AUTH-DESIGN`, following the same acceptance-then-implementation sequencing `SERVER-QUERY-LAYER` used after ADR-0010: `SERVER-001` bumped to v0.6.0, `SERVER-001-FR-016` registered.

  `AuthConfig`/`TokenClass` (`src/server/mod.rs`) and `Request::Authenticate`/`ErrorCode::{Unauthenticated,Unauthorized}` (`src/server/protocol.rs`) implement the accepted design's own "Proposed shape" essentially unchanged — no completion beyond it was needed this round, unlike `SERVER-QUERY-LAYER`'s own implementation pass. `handle_connection` gates its own request loop on per-connection state (`Option<TokenClass>`), intercepting `Authenticate` before `dispatch` is ever reached for anything else; `dispatch` itself stays exhaustive (`Request::Authenticate { .. } => err_response(ErrorCode::Unsupported)`) in case that invariant is ever violated, verified by its own unit test. A connection with no successful `Authenticate` yet gets `ErrorCode::Unauthenticated` for every other request kind, including `DescribeSchema` — the design's own "uniform rule" resolution of its one open question. `ReadOnly` is further rejected from `Request::UpdateField` with `ErrorCode::Unauthorized`; every other request kind succeeds for both classes once authenticated, and a rejected request never closes the connection.

  `AuthConfig::check` compares the presented token against every configured token via `subtle::ConstantTimeEq`, never short-circuited on the first match. `AuthConfig::new` (direct construction, used by every test — `cargo test` runs many tests in parallel within one process, so real environment variables would race) and `AuthConfig::from_env` (reads `SERVER_AUTH_READ_ONLY_TOKEN`/`SERVER_AUTH_READ_WRITE_TOKEN` at real startup, used only by `src/bin/dog_server.rs`) resolve the design's own left-open "exact environment-variable naming" question. `AuthConfig::default()` (no tokens configured) leaves every connection pre-authenticated at `ReadWrite` — `AUTH-FR-007`'s exact bar. Every pre-existing `serve` call site (`tests/server_{dog,order,employee}_integration.rs`, `tests/server_schema_driven_client.rs`, `benches/server.rs`) now passes `AuthConfig::default()` and required no other change — direct evidence, not just an assertion, that this round changed nothing for an unconfigured server.

  `tests/server_auth_integration.rs` (new, 5 tests, `required-features = ["server"]`) exercises every functional acceptance criterion `SERVER-AUTH-DESIGN.md` names over a real socket: unauthenticated rejection for every request kind including `DescribeSchema`; a wrong token producing the exact same `Unauthenticated` error as never authenticating (deliberately indistinguishable); `ReadOnly` succeeding on every read and rejected specifically on `UpdateField`, staying open and authenticated afterward; `ReadWrite` succeeding on everything; no configured tokens reproducing the original unauthenticated behavior including `Authenticate` itself round-tripping as a no-op. The one acceptance criterion needing empirical rather than read-through evidence — that the comparison is actually constant-time — is `server::tests::token_comparison_time_does_not_depend_on_where_the_mismatch_is`: mean latency of `AuthConfig::check` over 20,000 iterations each for a 64-byte token differing at its first byte vs. one differing only at its last byte, asserted within a deliberately lenient ratio band (0.2–5.0×) to avoid flakiness while still catching a genuine early-exit regression. Measured directly against the comparison function, not over a real TCP round trip: network jitter (microseconds to milliseconds) would completely swamp a signal this small, so a network-level version of this test would pass regardless of whether the comparison were actually constant-time.

  `SERVER-001` bumped to v0.6.0 (new FR-016, `## Change history` extended, Non-goals/Security sections updated to reflect the authentication/authorization half of the gap as closed while transport encryption stays open). `Cargo.toml` gained one new dependency, `subtle = "2"` — the one place this round departs from "avoid new dependencies," matching ADR-0012's own explicit reasoning. No existing source file outside `src/server/{mod,protocol}.rs`, every `serve` call site (a one-argument addition each), `src/bin/dog_server.rs`, `tests/server_auth_integration.rs` (new), and `Cargo.toml` was touched — verified by diff.

  Validation: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --release --all-features` (×3, no flakiness — 251/251 passing, up from 240/240): lib unit tests up from 211 to 217 (+6 new `server::{protocol::tests::authenticate_request_and_new_error_codes_round_trip_through_bincode, tests::auth_config_default_is_unconfigured, tests::auth_config_check_maps_each_token_to_its_own_class, tests::auth_config_works_with_only_one_class_configured, tests::dispatch_never_routes_authenticate_to_a_store, tests::token_comparison_time_does_not_depend_on_where_the_mismatch_is}`) + `tests/server_auth_integration.rs` (new, 5 tests) + every other suite unchanged (7 `cross_backend` + 1 `generic_production_integration` + 4 `mmap_record_identity_keying` + 1 `production_integration` + 4 `server_dog_integration` + 3 `server_employee_integration` + 2 `server_order_integration` + 4 `server_schema_driven_client` + 3 doctests). `cargo test --release` (default features): 63/63, unaffected — confirms `server` still adds no default-build surface. `cargo check --all-targets --all-features`, `cargo doc --all-features --no-deps` (two new "links to a private item" warnings were introduced by an early draft and fixed before merge by dropping the intra-doc link brackets around `handle_connection` references, matching this codebase's existing convention of plain-text references to private items in doc comments) all clean.

  Merged via PR #52 (`65e73a3` → merge commit `0528b39`); mechanical follow-up merged via PR #53 (`43bab93` → merge commit `b76f90c`).

- `SERVER-TRANSACTION-DESIGN` — the owner's third and last "what's next" pick, session/transaction semantics. `docs/FUTURE-GROWTH.md` names this "genuinely new," not incremental, so this got the same design-first, stop-for-review treatment `SERVER-AUTH-DESIGN`/ADR-0012 itself got — no implementation code, no new source file under `src/`, no test suite, no dependency added.

  `docs/design/SERVER-TRANSACTION-DESIGN.md` proposes the smallest real slice of `docs/FUTURE-GROWTH.md`'s broader "session/transaction semantics across multiple requests" framing: a new `Request::Transaction { updates: Vec<TransactionOp> }`, each `TransactionOp` shaped exactly like `Request::UpdateField`'s own fields — a batch of writes, all known and submitted together in one request message. Every operation's precondition (record exists, field known and updatable, value type matches) is checked before any write is applied; if every check passes, every write is applied, otherwise none are (`Response::Ok` vs. `Response::TransactionFailed { index, code, message }`). Requires `TokenClass::ReadWrite`, extending `AUTH-FR-003`'s existing rule to the new request kind rather than reopening it. One new `ErrorCode::RecordNotFound` variant, reachable only through the new response shape — a single, non-transactional `UpdateField` keeps its existing `Response::NotFound` behavior unchanged.

  Validate-then-apply (not an undo log) is the chosen atomicity mechanism, exploiting a real invariant this crate already has everywhere: no domain ever deletes a record at runtime, so a precondition checked once stays true for the rest of a batch as long as every check and every write happen under one continuously-held lock acquisition — no time-of-check-to-time-of-use race to guard against, no rollback machinery needed.

  **The one real architectural finding this proposal surfaced**: today's `ConnectionStore` trait methods (`get`/`update_field`/etc.) each independently acquire and release whatever lock the underlying store manages, per call — there is no way to hold that lock across several logical operations using only the trait's existing shape. Delivering real atomicity therefore needs a new, minimal primitive on the storage layer itself (`ProductionStore`/`GenericProductionStore`), not just an addition at the server layer — a materially bigger footprint than `SERVER-AUTH`'s purely server-layer-additive implementation, and named explicitly in ADR-0013's own Decision section rather than left for an implementation round to discover. A server-layer-only second lock was considered and rejected: it would either not achieve real isolation (unless every write, transactional or not, is rerouted through it) or duplicate the store's own lock outright — either way a real violation of ADR-0010's own "no new lock at this layer" principle.

  **Explicitly, deliberately does not propose a multi-round-trip interactive session** (`BeginTransaction`/several separate requests/`Commit`, transaction state held open across them) — the literal "session" half of `docs/FUTURE-GROWTH.md`'s framing. Rejected specifically because holding a connection's exclusive lock open across an unbounded number of client round trips is a real liveness/denial-of-service risk this project has never accepted anywhere else in the server layer (a slow, stalled, or malicious client could block every other connection indefinitely, with no timeout/forced-abort/fairness story anywhere in this project's concurrency model). Named as a real, larger future design, not ruled out forever.

  **Explicitly, deliberately does not deliver crash-atomicity across a batch** — a process crash between two of a batch's writes landing on stable storage can leave a partial batch durably applied. This proposal delivers atomicity and isolation with respect to concurrent access only; a true ACID transaction would additionally need a combined-journal or two-phase-commit-style mechanism this proposal does not build.

  ADR-0013 records the decision, following the identical structure/rigor ADR-0012 established: Context, Decision drivers, Considered options (batch shape, rollback mechanism, locking mechanism — each with a chosen option and honest rejections), Decision, Consequences (positive and negative, the storage-layer cost named as a real tradeoff), Validation and revisit triggers (true interactive sessions, crash-atomicity, a second mutating operation kind — each named as a real, separate future decision).

  **The owner reviewed and accepted the design as proposed, no changes requested.** ADR-0013 and `docs/design/SERVER-TRANSACTION-DESIGN.md` are now Accepted. No existing source file was modified, no dependency actually added, and no server or storage-layer implementation was written by this unit or authorized by its acceptance — a separate, not-yet-started implementation unit will extend `SERVER-001` and `STORAGE-011`/`STORAGE-012` with new FRs (or register a dedicated spec) before any code lands, matching how `SERVER-AUTH` followed `SERVER-AUTH-DESIGN`'s own acceptance.

  Proposed via PR #54 (`d9d0cdb` → merge commit `1923c3e`); accepted via PR #55 (`99aa29f` → merge commit `144f747`).

- `SERVER-TRANSACTION` — the real implementation of `SERVER-TRANSACTION-DESIGN`, following the same acceptance-then-implementation sequencing `SERVER-AUTH` used after ADR-0012: `SERVER-001` bumped to v0.7.0, `SERVER-001-FR-017` registered; `STORAGE-011` bumped to v0.2.0 (`FR-008`), `STORAGE-012` bumped to v0.3.0 (`FR-011`).

  `Request::Transaction { updates: Vec<TransactionOp> }`, `Response::TransactionFailed { index, code, message }`, and `ErrorCode::RecordNotFound` (`src/server/protocol.rs`) implement the accepted design's own "Proposed shape" essentially unchanged. `ConnectionStore` gains `apply_transaction(&self, updates: &[TransactionOp]) -> Result<(), (usize, ErrorCode)>` (`src/server/mod.rs`); `dispatch`'s new arm maps `Ok(())` to `Response::Ok` and `Err((index, code))` to `Response::TransactionFailed`. `handle_connection`'s auth gating extends the existing `ReadOnly`-vs-`UpdateField` check to also cover `Transaction`, rejecting the whole request before any operation in the batch is evaluated.

  **The real architectural finding**: `ConnectionStore`'s existing methods (`get`/`update_field`/etc.) are each independently self-contained — every call acquires and releases whatever lock the underlying store manages, on its own. Naively calling `update_field` in a loop cannot deliver atomicity: another connection's write can interleave between any two calls, and a failure partway through leaves an unpredictable prefix applied with no way to undo it. Real atomicity needed a new, minimal storage-layer primitive, named up front in ADR-0013 rather than discovered mid-implementation: `crate::production::TransactionalStore` (a new trait, one associated type `Exclusive: DogStore`, one method `with_exclusive`, implemented only by `ProductionStore` — not every `ConcurrentStore` variant, since the other `src/concurrency/**` strategies are benchmarked historical alternatives never wrapped by the server layer in practice) and `crate::generic::production::GenericProductionStore::with_exclusive` (a plain inherent method — `OrderConnectionStore`/`EmployeeConnectionStore` are concretely typed over one specific `S`, unlike `server::dog::DogConnectionStore<S>`, so no generic caller needs a trait bound here). Both hold each store's existing internal lock across the closure's entire duration instead of once per call — the same lock, held longer, no new lock introduced anywhere (verified by diff: no existing `ProductionStore`/`GenericProductionStore` method's behavior changed).

  Each domain adapter's `apply_transaction` (`dog.rs`/`order.rs`/`employee.rs`) runs one `with_exclusive` closure that first validates every operation's precondition (record exists — checked via `GetById`/`DogStore::get`, never mutating; field known and updatable; value type matches, both pure matches against the request itself), then applies every operation (`UpdateField::update`/`DogStore::update_age`) only if every check passed. This is sound without an undo log specifically because this crate never deletes a record at runtime and every domain's schema is compile-time fixed: a precondition checked once under the lock stays true for the rest of the batch checked/applied under that same lock acquisition — no time-of-check-to-time-of-use race to guard against.

  `tests/server_transaction_integration.rs` (new, 6 tests, `required-features = ["server"]`) exercises every functional acceptance criterion: a fully valid batch applies every write; a batch failing at its first operation, or its last, applies nothing — including operations that would have succeeded on their own; a malformed value is rejected with the right index; a `ReadOnly` connection's `Transaction` request is rejected before any operation is evaluated (verified by including an operation that would itself fail differently, confirming the reported error is still `Unauthorized`). The flagship concurrent test (`concurrent_transactions_and_updates_match_a_sequential_replay`) drives 8 real client connections × 200 interleaved `Transaction`/`UpdateField` requests against a 20-id contended pool, verified via sequential-replay linearizability — the same established, proven-sound pattern `tests/server_dog_integration.rs`'s own flagship stress test uses.

  **A different, more direct "is a transaction ever observed half-written" test was tried first and deliberately dropped, not silently abandoned**: a reader connection reading two ids a fixed pair was always written to together, checking the two ages were always equal, produced ~5% "divergences" on a correct implementation — because two *independent*, un-synchronized client round trips (this protocol has no multi-field read or read-transaction) cannot distinguish "this specific transaction's write is genuinely torn" from "a different, later, fully-completed transaction already updated the second id again since the first was read." Both look identical from outside with the tools this protocol provides, so that comparison would have passed or failed at roughly the same rate regardless of whether the implementation were correct — not a real test. The deterministic, single-threaded before/after tests already prove batch atomicity directly with no such confound; the concurrent test's real job (proving no lost updates/corruption under contention) is exactly what sequential replay is the established, sound tool for. Recorded here explicitly as a real methodological finding from this round, not hidden in commit history only.

  No existing source file was modified outside `src/server/{mod,protocol,dog,order,employee}.rs`, `src/production.rs`, `src/generic/production.rs`, `tests/server_transaction_integration.rs` (new), and `Cargo.toml` (the new `[[test]]` entry only — no new dependency) — verified by diff.

  Validation: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --release --all-features` (×3, no flakiness — 260/260 passing, up from 251/251): lib unit tests up from 217 to 220 (+3: `server::protocol::tests::transaction_request_and_new_shapes_round_trip_through_bincode`, `server::tests::transaction_all_pass_reports_ok`, `server::tests::transaction_reports_the_first_failing_operations_index_and_code`) + `tests/server_transaction_integration.rs` (new, 6 tests) + every other suite unchanged. `cargo test --release` (default features): 63/63, unaffected — confirms `server` still adds no default-build surface. `cargo check --all-targets --all-features`, `cargo doc --all-features --no-deps` (73 warnings, unchanged from the prior checkpoint — no new warning class) all clean.

  Merged via PR #57 (`676c7d1` → merge commit `857417d`).

- `SERVER-TRANSACTION-BENCHMARK` — with all three of the owner's original next-direction picks done, the owner was asked "what's next" via `AskUserQuestion` and selected two of the offered options: transport encryption (design-first, not yet started) and this — a bounded, additive extension of `SERVER-QUERY-LAYER`'s own `benches/server.rs` (v0.4.0, FR-014) to cover `Request::Transaction`, the request kind `SERVER-TRANSACTION` (v0.7.0) just added. No ADR needed — same reasoning as v0.5.0's client library: completing already-accepted FR-014 scope, not opening a new decision.

  `benches/server.rs` gained a directly-comparable `{domain}-txn` row set: `measure_transaction_latency` (single-connection, sequential two-op `Request::Transaction` round trips, averaged over `LATENCY_ITERATIONS`, same as the existing `GetById` latency measurement), `run_transaction_throughput` (the same `Barrier`-synchronized, slowest-thread-wins throughput sweep as `run_throughput`, driving two-op `Transaction` batches instead), and `bench_transaction_domain` (prints a `"{name}-txn"` row set per domain, matching `bench_domain`'s exact format). Each transaction batch touches two distinct ids from the same 20-id pool, both set to the same new value — the smallest batch that exercises the multi-operation validate-then-apply path `SERVER-TRANSACTION` added. Same environment (`THREAD_COUNTS = [1, 4, 32, 64]`, `LATENCY_ITERATIONS = 5_000`, `OPS_PER_THREAD = 2_000`) as the benchmark's current default, unchanged.

  `SERVER-001` bumped to v0.8.0, `SERVER-001-FR-018` registered — the fifth version bump this spec has needed, following the same "every real addition gets an FR" discipline every prior round used.

  Run twice on this session's shared 4-core container (`std::thread::available_parallelism() == 4`, same environment as the original benchmark's own container pass) to characterize run-to-run variance, matching every prior `RESULTS.md` benchmark pass's own convention. **Finding: no meaningful latency or throughput cost was found for `Request::Transaction` relative to plain `GetById`, at this benchmark's scale.** Latency: both request kinds land in the same 67-77 µs band across all three domains and both runs — the few-µs differences either direction are inside this container's own already-documented run-to-run noise, not a directional effect. Throughput: `Transaction` and `GetById` sit in the same rough band at every thread count for every domain once past the noisiest single cell (this container's already-established 4-thread noise floor, e.g. `Order`/`Customer`'s original 282K-vs-161K swing) — the one outlier, `Employee` at 4 threads (68,549/110,399 for `Transaction` vs. 200,736/176,351 for `GetById`), is read as an instance of that same noise rather than a new domain-specific cost, though a `baileyai`-class real-hardware follow-up (unscheduled, same open-question shape as the original benchmark's own container-first pass) would be needed to rule out a genuine `Employee`-specific contention effect with confidence. This is consistent with the design's own prediction: the longer-held lock `SERVER-TRANSACTION-DESIGN`'s "Architecture" section named as a real cost up front is real in principle but swamped by the network/framing cost that already dominates this whole benchmark's numbers, per the original pass's own headline finding.

  `RESULTS.md`'s `## Server / query layer` section gains a new `### \`Request::Transaction\` follow-up: this session's container` subsection with the full latency/throughput tables and this reading. No existing source file was modified outside `benches/server.rs` — verified by diff. No new dependency, no new test file (this is a benchmark, not a test suite).

  Validation: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --release --all-features` (×3, no flakiness — **260/260 passing, unchanged** from the `SERVER-TRANSACTION` checkpoint, confirming this is genuinely bench-only with no test-suite impact). `cargo test --release` (default features): 63/63, unaffected. `cargo check --all-targets --all-features`, `cargo doc --all-features --no-deps` (73 warnings, unchanged — no new warning class) both clean.

  Merged via PR #59 (`a2e04db` → merge commit `d885e92`).

- `SERVER-TLS-DESIGN` — the owner picked transport encryption as the second of two options after "what's next" (alongside the transaction benchmark, done above). This is the last remaining half of the "no auth, no encryption" gap ADR-0010 named at acceptance; ADR-0012's own "Validation and revisit triggers" named exactly this proposal as the condition for revisiting native TLS "not deferred again by default" — this got the same design-first, stop-for-review treatment `SERVER-AUTH-DESIGN`/`SERVER-TRANSACTION-DESIGN` each got before any server code existed.

  `docs/design/SERVER-TLS-DESIGN.md` originally proposed an opt-in `TlsConfig` (certificate chain + private key, PEM, operator-supplied paths) accepted alongside the existing `AuthConfig`, with native TLS depending on `rustls` directly, terminated inside this crate's own server process — reversing `SERVER-AUTH-DESIGN`'s own original rejection of native TLS once the dependency-weight objection is checked on its actual merits: a synchronous, `Read`/`Write`-compatible TLS API composes with the existing thread-per-connection model with zero changes to `src/server/framing.rs` (already generic over `Read`/`Write`).

  **Revised before acceptance**: the owner asked whether this owner's own `rusty_*`/`Rusty-Mill` ecosystem already had a hand-rolled or wrapped solution for the dependency being proposed. It does — `Rusty-Mill/rusty_mill`'s `crates/rusty_tls`, built specifically so no consumer in that ecosystem depends on `rustls` directly ("consumers import `rusty_tls`, never `rustls`"). Its server-side `TlsAcceptor`/`TlsServerStream` API is a close-to-drop-in fit: `TlsAcceptor::new(cert_chain_der, private_key_der)` builds a config once, `Arc`-shared across connections; `TlsAcceptor::accept<S: Read + Write>(sock) -> TlsServerStream<S>` is exactly the "small stream wrapper implementing `Read`/`Write` by delegation" the design needed, tested and fuzzed already (wrong hostname, expired cert, untrusted root, a real OS-trust-anchor corpus test), with working TLS 1.3 session resumption and mTLS support (`new_with_client_auth`, not adopted, but changes a future revisit's cost) included. The design was revised in place to depend on `rusty_tls` (a pinned git dependency) instead of `rustls` directly — see the document's own "Ecosystem check" section for the full finding, including the one real new cost this introduces: `rusty_tls` would be this crate's first git dependency, on a sibling repository, a real precedent shift beyond just "which TLS crate," flagged for the owner's own separate judgment.

  **The owner reviewed and accepted the design as revised, no further changes requested.** ADR-0014 and `docs/design/SERVER-TLS-DESIGN.md` are now Accepted. No existing source file was modified, no dependency actually added, and no server implementation was written by this unit or authorized by its acceptance — a separate, not-yet-started implementation unit will extend `SERVER-001` with new FRs before any server code lands, matching how `SERVER-AUTH`/`SERVER-TRANSACTION` each followed their own design's acceptance.

  Proposed via PR #61; revised via PR #62; accepted via PR #63 (`36af2af` → merge commit `8adf478`).

- `SERVER-TLS` — the real implementation of `SERVER-TLS-DESIGN`, following the same acceptance-then-implementation sequencing `SERVER-AUTH`/`SERVER-TRANSACTION` each used after their own ADR's acceptance: `SERVER-001` bumped to v0.9.0, `SERVER-001-FR-019` registered.

  `TlsConfig`/`TlsConfigError` (`src/server/mod.rs`) implement the accepted design's own "Proposed shape" essentially unchanged: `TlsConfig::new` takes DER-encoded certificate chain + private key directly (`rusty_tls::TlsAcceptor::new`'s own shape); `TlsConfig::from_pem_files`/`TlsConfig::from_env` (the latter mirroring `AuthConfig::from_env`'s pattern, reading `SERVER_TLS_CERT_CHAIN_PATH`/`SERVER_TLS_PRIVATE_KEY_PATH`) decode the common PEM-file case via a new `src/server/pem.rs` — a small, hand-written PEM/base64 decoder, not a new dependency (base64 decoding is a fully-specified, deterministic transform with no invisible-to-testing correctness property, unlike `AuthConfig::check`'s constant-time comparison, which *is* a dependency for exactly that reason). `serve` gains a fourth parameter, `tls: Option<TlsConfig>`; every pre-existing call site (`src/bin/dog_server.rs`, every test, every benchmark) now passes it explicitly (`None` everywhere except `dog_server.rs`'s `TlsConfig::from_env()`) and required zero other change.

  **The real architectural finding this round surfaced**: `handle_connection`'s existing plaintext path splits a connection into independent read/write halves via `TcpStream::try_clone` (two real OS-level socket handles) — but `rusty_tls::TlsServerStream`'s `rustls::ServerConnection` state can't be split that way; a read and a write both need to reach through the *same* connection object. Resolved with a new `ReadHalf`/`WriteHalf` enum pair: the `Plain` variant keeps the existing `try_clone` split completely unchanged (zero behavior change — verified directly: every pre-existing plaintext test passes unmodified); the `Tls` variant shares one `Rc<RefCell<TlsServerStream<TcpStream>>>` between both halves — `Rc`/`RefCell`, not `Arc`/`Mutex`, since each connection is served by exactly one OS thread (thread-per-connection, unchanged), so a single-threaded runtime borrow check is sufficient and the `Rc` itself never needs to be `Send` (constructed entirely inside the already-spawned connection thread). This was the one piece the design document itself had explicitly left as an "implementation-time decision," for exactly this reason.

  `tests/server_tls_integration.rs` (new, 5 tests, `required-features = ["server"]`) exercises every functional acceptance criterion over a real socket, using `rusty_tls::TlsStream` (this ecosystem crate's own client-side half, not a hand-rolled test client) and `rcgen`-generated throwaway self-signed test certificates (a new dev-only dependency, matching `rusty_tls`'s own identical precedent for its own identical purpose — never shipped). The flagship test (`the_authenticate_token_is_not_observable_in_the_bytes_sent_on_the_wire`) is the direct evidence the design's whole purpose depends on: a byte-recording `TcpStream` wrapper captures every byte a real TLS client actually sends, and the test asserts the plaintext `Authenticate` token string is not present anywhere in that capture — not "we used `rusty_tls` so it must be fine."

  One new dependency landed exactly as the revised design specified: `rusty_tls` (`Rusty-Mill/rusty_mill`, `crates/rusty_tls`, pinned to commit `9fd2e27c9cfd5d1b21a5e58b55e368258a0a2779`) — this crate's first git dependency, `dep:`-gated behind the `server` feature. No existing source file was modified outside `src/server/mod.rs`, `src/bin/dog_server.rs`, every `serve` call site (a one-argument addition each), `tests/server_tls_integration.rs` (new), `src/server/pem.rs` (new), and `Cargo.toml`'s dependency/`[[test]]` entries — verified by diff.

  Validation: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test --release --all-features` (×3, no flakiness — 275/275 passing, up from 260/260): lib unit tests up from 220 to 230 (+10: `server::pem::tests::*`) + `tests/server_tls_integration.rs` (new, 5 tests) + every other suite unchanged. `cargo test --release` (default features): 63/63, unaffected — confirms `server` still adds no default-build surface. `cargo check --all-targets --all-features`, `cargo doc --all-features --no-deps` (76 warnings, up from 73 — three more instances of the same already-documented "bare-item link in a module's own `//!` header doesn't resolve" rustdoc quirk, not a new warning class) all clean.

  Implemented via PR #64 (`c973b83` → merge commit `be1067f`).

- `EXTERNAL-DB-BENCHMARK` — the first comparison against real, external, general-purpose databases: `ProductionStore` benchmarked against SQLite (`rusqlite`), Postgres (`postgres`, a locally-run server), and DuckDB (`duckdb`) on the same three access-pattern shapes this crate has always used (`get`, `scan_ages`, one-hop `littermate_of` traversal). `benches/external_db.rs` (new), gated behind a new `external-db-bench` Cargo feature (additionally requiring `research`); ADR-0015 (Accepted); `STORAGE-013` v0.1.0. Stays fully inside `docs/FUTURE-GROWTH.md`'s existing "Path to SQLite/DuckDB parity" scope line — no SQL parser, planner, or arbitrary joins built or implied.

  Same dataset/seed/sizes/sample pool as every other benchmark in this crate (`bench_support::build_dataset`). Schema creation and bulk load happen once per (engine, size), outside the timed portion; every query is prepared once and reused. `get`: an indexed primary-key lookup. `scan_ages`: a whole-table `AVG(age)` aggregate. Graph traversal: a `WITH RECURSIVE` query over a `littermates` adjacency table (both edge directions loaded, indexed on `dog_id`), depth-bounded to 1 hop.

  **`ProductionStore` wins every cell, at every size, on every workload — expected, not the interesting finding.** The interesting finding is which external engine gets close, and where: DuckDB closes to within **3.3×** of `ProductionStore` on `scan_ages` at 1M records (69.78 ms SQLite / 56.26 ms Postgres / 1.156 ms DuckDB / 347.4 µs production) — by far the closest any external engine gets to `ProductionStore` on any workload, and a direct confirmation of the task's own prediction that DuckDB's columnar design would be strong specifically on the scan/aggregate shape. The same engine is the *worst* of the three externals on `get` (430–530 µs, worst at every size) and on graph traversal (1.7–1.8 ms, worst at every size by a wide margin, essentially flat regardless of `n` — recursive-CTE traversal is close to the opposite of what a batch-columnar engine is built for). SQLite is the most consistently competitive external engine across all three shapes (never fastest on `scan_ages` at scale, never worst on anything). Postgres's `get`/graph-traversal numbers are flat across sizes (~90–110 µs), consistent with loopback round-trip cost dominating over the trivial in-database work; its `scan_ages` numbers scale with row count, as expected for a real sequential scan.

  **A real methodology bug was found and fixed mid-round, not glossed over.** The first `neighbors_one_hop` run showed Postgres jumping from 109.6 µs at 1,000 records to 152.9 ms at 100,000 — investigated directly via `EXPLAIN ANALYZE` rather than assumed to be noise, and traced to `pg_class.reltuples = -1` (COPY doesn't update planner statistics itself) driving a wildly wrong recursive-CTE cost estimate (~93,000 vs. ~260 real cost), high enough to cross Postgres's default `jit_above_cost` threshold and trigger LLVM JIT compilation on every single execution. A direct before/after check on the same table/query confirmed it: 53.1 ms before running `ANALYZE`, 0.196 ms after — a ~270× difference from one statement, no data or code change. `postgres_load` now runs `ANALYZE` immediately after every bulk load (any real Postgres deployment does the same). All Postgres numbers in `RESULTS.md` are from the corrected, post-fix run.

  Three new dependencies used only by this bench target, `optional = true` under `[dependencies]` and `dep:`-gated behind `external-db-bench` (not `[dev-dependencies]` — Cargo has no way to make a dev-dependency optional; an earlier version of this wiring put them under `[dev-dependencies]` without `optional = true` and a plain `cargo test --release` with zero features enabled still compiled all three, DuckDB's full bundled build included — caught, diagnosed, and fixed before landing, see ADR-0015's Consequences for the full account): `rusqlite`/`duckdb` (`bundled`, compiled from vendored source — DuckDB's build is a real, meaningfully heavier CI cost than anything else this crate has taken on as a dependency, flagged plainly in ADR-0015's Consequences, not minimized) and `postgres` (`with-uuid-1` feature, for native `Uuid` `ToSql`/`FromSql`). No `src/` changes. Verified after the fix: `cargo tree -e normal` shows none of the three without the feature enabled; a default `cargo check --release` finishes in ~6s.

  Validation: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo check --all-targets --all-features`, `cargo doc --all-features --no-deps` all clean. `cargo test --all-features`/`cargo test` both unaffected (bench-only change, verified by diff touching only `Cargo.toml`/`Cargo.lock`, `benches/external_db.rs` (new), and this doc set). A real `cargo bench --features research,external-db-bench --bench external_db` run (reduced sampling, `--warm-up-time 1 --measurement-time 2 --sample-size 20`, matching this document's own established container-run precedent) against a local Postgres 16 instance (`initdb`/`pg_ctl`, no Docker), all 36 cases (3 workloads × 3 sizes × 4 systems), no panics.

  Implemented via PR #66 (`032bece` → merge commit `ed785eb`).

- `MMAP-AGE-STORE-IDENTITY-FIX` — a real, latent correctness bug in `MmapAgeStore` (`src/durability/mmap_store.rs`, the storage layer `ProductionStore` directly wraps), found while the owner was asking how `ProductionStore`'s own persisted file compares to SQLite's/DuckDB's portable, self-contained ones. `MmapAgeStore::open` rebuilt its `Uuid -> array position` index fresh from whatever order the caller-supplied `records`/`edges` happened to arrive in on that specific call, and trusted that array position N in the file still held position N's age from `create` time — nothing on disk recorded which record an age actually belonged to. A caller supplying `records` in a different order between `create` and a later `open` would have silently misattributed ages between records: no error, no panic, just wrong data under a real id. No caller anywhere in this crate's own test/bench suite ever triggers this (every one supplies the identical generator output, identically ordered, at both `create` and every later `open`), but nothing about `ProductionStore`'s public contract ever ruled it out. `GenericMmapStore` (`src/generic/mmap_store.rs`) already had this identical bug diagnosed and fixed earlier — see `GENERIC-SCHEMA-LIBRARY`'s own completed entry — but the fix was never backported to `MmapAgeStore` until now.

  **First attempt: a straight port of `GenericMmapStore`'s already-proven fix** — a versioned header (magic number + schema version) plus one interleaved `[id: 16 bytes][age: 4 bytes][commit marker: 1 byte]` slot per record, reconciled by id (not position) on `open`. Correct, but it broke `scan_ages`'s existing `chunks_exact(4)` bulk-read fast path (this same section's own earlier "Diagnosis and fix," 12-17× faster than a naive per-position loop) — that optimization needs the mapped region to be *exactly* a flat, homogeneous `[u32]` array, which an interleaved slot isn't. Measured directly before merging anything, per the owner's own explicit instruction to understand the full impact first: **`scan_ages` regressed 2.2-6.1× (299.5 µs → 1.838 ms at 1M)**, and — because `ProductionStore`'s numbers are exactly what `## External database comparison` had just measured `scan_ages` against — this **reversed that section's own just-published headline finding**: `ProductionStore` (1.691 ms) became slower than DuckDB (1.156 ms, unchanged) at 1M, not narrowly beating it. Every other workload (`get`/`update_age`/`same_breed`/`neighbors_one_hop`/`neighbors_two_hop`) stayed flat, within normal run-to-run noise.

  **Second attempt, accepted: a columnar layout instead of row-oriented.** Three separate, contiguous regions after the header — every id, then every age, then every commit marker, each its own flat array in the same record order — so `scan_ages`'s fast path returns to a pure `chunks_exact(4)` over *only* the ages region. The real cost moved to a different, rare path instead: a columnar layout can't append one new record cheaply in place (it would need inserting bytes into three separate, non-adjacent regions), so `open` reconciling a genuinely new record now triggers a full rewrite — crash-safely, via write-to-temp-then-atomic-`rename`, never in-place truncation (a *stronger* safety property than the row-oriented design's own `O_APPEND` approach, not a weaker one: a crash mid-rewrite can never leave `path` holding a partial generation). This path is never exercised by this crate's own benchmark suite or any existing call site (every one supplies the same record set at every reopen), so it costs nothing in the numbers below. Result: **`scan_ages` only 1.1-1.4× slower than the pre-fix baseline** (338.6 µs at 1M, vs. the row-oriented attempt's 6.1×), and the external-database headline finding is restored almost exactly: DuckDB 3.7× behind `ProductionStore` at 1M (was 3.3× before this fix), 43× behind at 100K (was 41.8×). `## External database comparison`'s own tables are left as originally measured, per this document's established practice — see `## Production recommendation`'s new `### MmapAgeStore identity-keying fix` subsection for the full before/after account, including the exact tables.

  8 new tests (`src/durability/mmap_store.rs`'s own test module): the exact reorder-misattribution regression this fix closes, a stale-id (removed record) case, a new-record-since-last-write case (exercising the rewrite-via-rename path), both mismatches in one reopen, and four header-validation tests (clean baseline, schema-version mismatch, wrong magic number, a too-short file) — mirroring `GenericMmapStore`'s own identical test shape. A real bug in the fix's own first draft was caught by this suite (well, by direct code review before the suite ever ran red — see below) before it shipped: the "no rewrite happened" branch of `open` was initially building `position_index` from the *newly-supplied* `records`' own enumeration order rather than from the *persisted* file positions, which would have silently reintroduced the identical class of bug this whole fix exists to close, just for the no-rewrite case specifically. Caught and fixed before any test ever ran against it.

  Validation: `cargo fmt --all -- --check`, `cargo clippy --all-targets --all-features -- -D warnings` both clean. `cargo test --release --all-features`: **283/283 passing** (up from 275/275) — 238 lib unit (up from 230, +8 new `durability::mmap_store::tests::*`) + every pre-existing suite unchanged. `cargo test --release` (default, no features): **71/71 passing** (up from 63/63, +8, since `durability` is unconditionally compiled), unaffected otherwise. `cargo doc --all-features --no-deps`: clean, 77 warnings (up from 76) — one more instance of the same already-documented "bare-item link in a module's own `//!` header doesn't resolve" rustdoc quirk this codebase already carries dozens of, not a new warning class (confirmed: every genuinely new "links to private item" warning this round's first draft introduced was fixed by dropping the intra-doc link brackets, this codebase's own established convention). No changes to any file outside `src/durability/{mmap_store,mod}.rs` and this doc set — verified by diff. No new dependency.

  Implemented via PR #68 (`93308a5` → merge commit `063099a`).

- `CI-CACHE` — resolves the CI build-time cost `ADR-0015`'s Consequences and `RESULTS.md`'s own open questions named explicitly (DuckDB's `bundled` feature rebuilding its full C++ amalgamation from scratch on every CI run, since `.github/workflows/ci.yml` had no build cache — observed directly, repeatedly, over this session's own PR cycles: ~20 minutes per run). Fixed by adding `Swatinem/rust-cache@v2` to the one CI job, keyed on `Cargo.lock` — the standard, minimal-risk mitigation for this exact problem, not the more invasive alternatives ADR-0015 also named (a system-DuckDB feature variant, or excluding the bench from the default `--all-features` sweep). Workflow-only change, validated as syntactically valid YAML; the real effect (faster subsequent CI runs) is observable from this PR's own CI time onward, not locally reproducible in this session.

  Implemented via PR #70 (`061259e` → merge commit `77717dd`).

- `TWO-HOP-EXTERNAL-DB-BENCHMARK` — extends `EXTERNAL-DB-BENCHMARK` to two-hop `littermate_of` traversal against all three external engines, closing that round's own "two-hop unmeasured" open question (`STORAGE-013` v0.2.0, `STORAGE-013-FR-008`; no new ADR — completes ADR-0015's already-accepted scope one hop deeper, same recursive-CTE idiom). `benches/external_db.rs` gains `run_neighbors_two_hop_{sqlite,duckdb,postgres,production}` and a `neighbors_two_hop_vs_external_db` Criterion group; the query is the one-hop recursive CTE extended one hop deeper (`WHERE hop.depth < 2` in the recursive term, `WHERE depth = 2` in the final `SELECT`), matching `bench_support::two_hop_neighbors`'s exact semantics against `ProductionStore`. `ProductionStore` wins every cell (27×-2,822×, depending on engine and size); SQLite stays the strongest external engine, DuckDB the weakest, matching the one-hop ranking. `ProductionStore`'s own two-hop cost runs 12.2-14.3× its one-hop cost — the same order of magnitude this crate's own in-repo `neighbors_two_hop` group already found for `Canonical`/`Canonical+cache` ("roughly 12-14×"), a genuine cross-check between the two independently-run benchmark passes. **A real, investigated finding, not glossed over**: Postgres's two-hop numbers are not flat or monotonic in `n` — 449.7 µs at 1,000 records, dropping to 138.1 µs at 100,000 and 133.3 µs at 1,000,000, backwards from every other size-vs-cost relationship in this document. `EXPLAIN (ANALYZE, BUFFERS)` against a prepared statement on each size's own already-`ANALYZE`d schema found a genuine, cost-based join-strategy flip: at 1,000 records the recursive term's join is planned as a `Seq Scan` + Hash Join over `littermates`; at 100,000/1,000,000 it's an indexed Nested Loop against `idx_littermates_dog_id` instead — the index-nested-loop plan is the faster of the two, so the smaller table ends up with the slower query. Confirmed as a real planner decision, not a repeat of the earlier missing-`ANALYZE`/JIT bug: statistics were already current at every size, and no plan at any size crosses `jit_above_cost` (no `JIT:` section in any plan). Full tables, verdicts, and the join-strategy account are in `RESULTS.md`'s new `### Graph traversal, two-hop` subsection.

  Implemented via PR #72 (`4cf43e7` → merge commit `c2b87ad`).

- `THREE-HOP-EXTERNAL-DB-BENCHMARK` — extends the external-DB comparison one hop further, to three-hop `littermate_of` traversal (`STORAGE-013` v0.3.0, `STORAGE-013-FR-009`; no new ADR — completes ADR-0015's already-accepted scope one hop deeper again, same recursive-CTE idiom as v0.2.0). Unlike `neighbors_one_hop`/`neighbors_two_hop`, this shape has **no in-repo counterpart** — `bench_support` composes at most two hops (`two_hop_neighbors`, ADR-0004) — so `benches/external_db.rs` gains a new, private, bench-target-local `three_hop_neighbors` (the deduplicated union of `neighbors(n)` for every `n` in `two_hop_neighbors(id)`) rather than a `bench_support` addition: this benchmark is (so far) the only call site, and this project's own "no abstraction before two real call sites" convention says not to generalize before a second one exists. `run_neighbors_three_hop_{sqlite,duckdb,postgres,production}` and a `neighbors_three_hop_vs_external_db` Criterion group round out the implementation. `ProductionStore` still wins every cell (17.9×-917.8×), but the margin is visibly narrower than any shallower workload in this comparison — `ProductionStore`'s own per-hop cost multiplier (12.2-14.3× then 3.9-4.8×) grows faster than SQLite's (2.2-2.7× then 2.6-3.0×), so the gap to SQLite (still the strongest external engine) keeps closing hop over hop: 161× → 27× → 18× at 1,000 records. **The v0.2.0 Postgres join-strategy finding reproduced exactly at this depth, confirming it's a real, repeatable Postgres planning behavior rather than a one-off**: a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass against each size's own schema found the identical `Seq Scan`+Hash Join (1K) vs. indexed Nested Loop (100K/1M) split; at 1M the base case's own plan also refines further, from a `Bitmap Heap Scan` to a plain `Index Scan` — one more instance of the same size-dependent planning behavior, not a new mechanism. Full tables, verdicts, and the join-strategy account are in `RESULTS.md`'s new `### Graph traversal, three-hop` subsection.

  Implemented via PR #74 (`f6da47d` → merge commit `e4dd3b1`).

- `FOUR-HOP-EXTERNAL-DB-BENCHMARK` — extends the external-DB comparison one hop further again, to four-hop `littermate_of` traversal (`STORAGE-013` v0.4.0, `STORAGE-013-FR-010`; no new ADR, same "completes ADR-0015's already-accepted scope" reasoning as the two prior additions). `benches/external_db.rs` gains a new private, bench-target-local `four_hop_neighbors` (the deduplicated union of `neighbors(n)` for every `n` in `three_hop_neighbors(id)`, same placement rationale as `three_hop_neighbors` itself), `run_neighbors_four_hop_{sqlite,duckdb,postgres,production}`, and a `neighbors_four_hop_vs_external_db` Criterion group. `ProductionStore` still wins every cell (17.8×-424.6×). **Unlike the two prior rounds, this one found a genuinely new result, not just a bigger number on an already-established trend**: SQLite's margin over `ProductionStore` had narrowed cleanly every hop through hop 3 (161× → 27.2× → 17.9× at 1,000 records) but goes essentially flat at hop 4 (17.8×) — a real example of an apparently-monotonic benchmark trend breaking down exactly where it was next measured, reported honestly rather than smoothed over. **The real headline: at 1,000,000 records, Postgres — not SQLite — is now the closest external engine to `ProductionStore` on graph traversal, for the first time in this comparison's history** (26.9× vs. SQLite's 34.5×), because Postgres's own four-hop cost grows more slowly (1.97×) than SQLite's (2.92×) specifically at that size. The mechanism is the same cost-based join-strategy split found at two hops (`Seq Scan`+Hash Join at 1K vs. indexed Nested Loop at 100K/1M) — reproducing for the third consecutive round, confirmed again (not assumed) via a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass on each size's own schema. Full tables, verdicts, and the crossover account are in `RESULTS.md`'s new `### Graph traversal, four-hop` subsection.

  Implemented via PR #76 (`c607a44` → merge commit `1139804`).

- `FIVE-HOP-EXTERNAL-DB-BENCHMARK` — extends the external-DB comparison one hop further still, to five-hop `littermate_of` traversal (`STORAGE-013` v0.5.0, `STORAGE-013-FR-011`; no new ADR, same "completes ADR-0015's already-accepted scope" reasoning as the three prior additions). `benches/external_db.rs` gains a new private, bench-target-local `five_hop_neighbors` (composed one more round from `four_hop_neighbors`, same placement rationale), `run_neighbors_five_hop_{sqlite,duckdb,postgres,production}`, and a `neighbors_five_hop_vs_external_db` Criterion group. `ProductionStore` still wins every cell (20.3×-228.3×). **The headline: the four-hop Postgres/SQLite crossover reversed rather than persisting.** At 1,000,000 records, SQLite is the closest external engine again (29.8× vs. Postgres's 32.4×) — the four-hop crossover did not hold. A *different* crossover then shows up at 100,000 records for the first time (Postgres 30.9× vs. SQLite's 34.6×). At 1,000 records SQLite has stayed closest at every depth measured so far. **Investigated directly rather than reported as noise**: a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass at all three sizes found Postgres's underlying query plan completely unchanged since v0.2.0 — the identical `Seq Scan`+Hash Join (1K) / indexed Nested Loop (100K/1M) split at every depth and size tested. The volatility is in how much that stable plan's buffer I/O grows per added hop at a given size (not a predictable function of table size on its own), not in which plan Postgres picks. The one part of this five-round arc that *has* stayed a clean, monotonic trend throughout: DuckDB's own margin against `ProductionStore` narrows at every size, every hop. Full tables, verdicts, and the volatility account are in `RESULTS.md`'s new `### Graph traversal, five-hop` subsection.

  Implemented via PR #78 (`b8816a4` → merge commit `6a51f8b`).

- `PRODUCTION-STORE-PORTABILITY` — the real implementation of the accepted `PRODUCTION-STORE-PORTABILITY-DESIGN`, registered as `STORAGE-014` v0.1.0 (`STORAGE-014-FR-001..005`). `src/durability/record_blob.rs` (new): `RecordBlob { records, edges }` / `EncodedRecordBlob`, `companion_path` (`<path>.records`), `DOGBLOB\0` magic + `u32` `BLOB_VERSION = 1` header, bincode body, crash-safe `write` (temp file → `write_all` → `sync_all` → rename, consuming any stale temp), `read` mapping every failure to the new `DurabilityError::RecordBlobUnreadable { path, cause }` with a distinguishing cause (`cannot read file`/`magic`/`version`/`decode`), `is_current_at` (one `fs::read` + byte compare — no decode, no clone). `ProductionStore::create` encodes the blob *before* moving `records`/`edges` into `MmapAgeStore::create` (no clone), then writes it after the ages file; `ProductionStore::open` encodes, opens the ages file, then compares and rewrites only when stale — ordered so an ages-file error never clobbers a valid blob, and so a pre-`STORAGE-014` ages-only directory is healed on first `open`; the new additive `ProductionStore::open_portable(path)` reads the blob and opens the ages file, never writing. `mmap_store.rs` untouched (empty diff). Tests: 9 in `record_blob.rs` (round trip incl. `breed`/edges; missing/short/wrong-magic/wrong-version/truncated all typed errors, never panics; whole-file replacement consuming a stale temp; `is_current_at`), 5 in `production.rs` (`open_portable` from the path alone asserting `breed`/`age`/`same_breed`/`neighbors`/`scan_ages`; both files copied to a fresh directory; missing blob → `RecordBlobUnreadable` naming the companion path while `open` still works; legacy ages-only directory healed; blob rewritten only when the record set changed — mtime and bytes unchanged otherwise). Cost measured in a release build and recorded in `RESULTS.md` (`create` +78%, `open` +27% at 1M; no benchmarked hot path touched) — the `open` number contradicts the design's "expected zero" and is recorded as such in `ADR-0016`'s and the design doc's implementation addenda. `SPEC-REGISTRY`/`TRACEABILITY`/`ROADMAP` rows added.
  Implemented via PR #84 (`76e2c66` → merge commit `00e9295`).
- `PRODUCTION-STORE-PORTABILITY-DESIGN` — a design doc (`docs/design/PRODUCTION-STORE-PORTABILITY-DESIGN.md`) and ADR (`ADR-0016`) closing out `ProductionStore` file portability, a standing open question since `MMAP-AGE-STORE-IDENTITY-FIX`. Researched first (via a dedicated investigation, not assumed): `MmapAgeStore`'s `.mmap` file persists only `id`+`age` in a fixed-width columnar layout; `breed`/`littermate_of` edges are never written to disk; `create`/`open` both require the caller to already hold the full dataset in memory. This is precisely the redesign `ADR-0006` declined once at `MmapAgeStore`'s original build (*"needs a string-heap file format... or capping breed's length... neither is minimal"*) and `GENERIC-SCHEMA-DESIGN.md` §4.2 confirmed is real, non-incremental work when `Order` hit the identical wall. **The design's key move**: ADR-0006's rejected string-heap format solves *mutable*, in-place, variable-length storage — but `breed`/`id`/`littermate_of` edges are never mutated anywhere in this crate (only `age` is), so that harder problem doesn't need solving. The proposal instead adds a small, purely additive companion file — bincode-serialized `id`/`breed`/edges, written once at `create()` (or when `open`'s existing reconciliation detects a genuinely changed record set) — reusing three already-validated pieces of this crate's own prior work: `DogRecord`'s existing `Serialize`/`Deserialize` derive, `SnapshotFullStore`'s already-proven full-record bincode round trip, and `MmapAgeStore`'s already-proven write-to-temp-then-atomic-rename crash-safety mechanism (from `MMAP-AGE-STORE-IDENTITY-FIX`'s own columnar redesign). A new `ProductionStore::open_portable(path)` constructor (needing no caller-supplied `records`/`edges`) is implemented purely in terms of the existing, completely unchanged `create`/`open` — zero modification to `MmapAgeStore`'s own file format, zero breaking change to `ProductionStore`'s existing public API, every current benchmark/test call site unaffected. Given this exact redesign was explicitly declined twice before, the owner chose (when asked) to review a design doc/ADR first rather than implement directly — matching this project's own established "design-first for consequential, hard-to-reverse decisions" convention (ADR-0009/ADR-0010's own precedent). Status: **Accepted** — the owner reviewed and approved the design as proposed, no changes requested. Acceptance authorizes the design; implementation still requires its own unit (registering `STORAGE-014` and a planning packet) before any code is written.

  Proposed via PR #80 (`34876c3` → merge commit `57531d1`); accepted via PR #82 (`9af1354` → merge commit `2ef3f53`).

- `RECORD-BLOB-FINGERPRINT` — the first of the two follow-ups `PRODUCTION-STORE-PORTABILITY` named, taken on the owner's "1 then 2": a content fingerprint in the record-blob header so `ProductionStore::open`'s steady-state cost stops being a full serialize-and-compare. `STORAGE-014` bumped to v0.2.0, blob version 2, no new ADR (`ADR-0016`'s own named follow-up; the ADR and design doc each carry an addendum). `src/durability/record_blob.rs`: the header grows from 12 to 20 bytes — `DOGBLOB\0` magic, `u32` `BLOB_VERSION = 2`, then a `u64` little-endian FNV-1a 64 fingerprint (`RecordBlob::fingerprint`) of the immutable content: record count, each record's `id` + `breed` length + `breed` bytes, edge count, each edge's two ids — `age` deliberately excluded, the ages file being its truth, so an age-only change never rewrites the blob. The hash is a hand-written 12-line `Fnv1a64` (no new dependency — the `src/server/pem.rs` precedent; `DefaultHasher` is explicitly unstable across Rust versions and this value lives on disk) and is a change detector, not an integrity guarantee — the companion is trusted exactly like the ages file. `RecordBlob::is_current_at` now opens the file, reads 20 bytes, and compares `parse_header`'s fingerprint against `self.fingerprint()` — no serialization, no full read; `read` verifies the body's fingerprint against the header's and reports a mismatch as `RecordBlobUnreadable`. `ProductionStore::open` reorders to header-check → encode-only-if-stale → open the ages file → write-if-stale; a version-1 blob reads as a version mismatch and counts as stale, so `open` upgrades it in place — while `open_portable` on a never-reopened version-1 blob is a typed error, the one documented cost of the bump (this crate's own tests/benches never persist a blob across builds). `EncodedRecordBlob::is_current_at` removed (nothing calls it). Tests: `record_blob.rs` 9 → 12 (a version-1 blob is a version mismatch and stale; a body not matching the header fingerprint is unreadable; the fingerprint pinned to `0xd98e_f572_3749_8d59` for the sample set — cross-checked against an independent implementation — order-sensitive, age-insensitive), `production.rs` 5 → 6 (`open` upgrades a version-1 blob in place; the rewrite-only-when-changed test also covers an age-only change leaving bytes and mtime untouched). **Re-measured, same throwaway release-build method, two runs**: `open` `MmapAgeStore` → `ProductionStore` +0.3% / +4% at 1M (was +27%), +4% / +11% at 100K (was +30%), +11% / +31% at 1K (was +27% — a ~0.2 ms floor of one extra file open on a 0.6 ms operation); `create` +36-45% at 100K/1M, the blob write itself, unchanged in kind; blob 8 bytes larger. Full table in `RESULTS.md`'s `#### Follow-up: header fingerprint` subsection. Follow-up (b) — the identical treatment for `GenericMmapStore`/`GenericProductionStore` — is next, design doc + ADR first.

  Implemented via PR #86 (`fe4f2ed` → merge commit `711dfbc`).

- `GENERIC-STORE-PORTABILITY-DESIGN` — a design doc (`docs/design/GENERIC-STORE-PORTABILITY-DESIGN.md`) and ADR (`ADR-0017`), both **Accepted** (the owner approved as proposed, no changes requested), for the second of the two follow-ups `PRODUCTION-STORE-PORTABILITY` named, taken on the owner's "1 then 2": the identical file-portability treatment for `GenericMmapStore`/`GenericProductionStore`, the gap `ADR-0016` twice named as its own deliberately-out-of-scope generic half and `GENERIC-SCHEMA-DESIGN.md` §4.2 first hit with `Order`. Research first, not code — `src/generic/mmap_store.rs`, `order_customer.rs`, `traits.rs`, `store.rs`, `generic_spike/employee_impl.rs`, and `record_blob.rs` read directly to confirm the shape: one fixed-width `(R::Id, R::ScanValue)` slot per record behind `GMMAPST\0`/schema-version 2, records held in a `HashMap` (nondeterministic order), relation layers (`Reversed`, `Symmetric`) stacked above the store and rebuilt from the caller's `Vec<R>` on every open, no serde derives on `Order`/`Employee` today. The design ports `STORAGE-014` v0.2.0 where it can (same `<path>.records` convention, same 20-byte header layout under a `GENBLOB\0` magic, same temp-then-rename write, same `RecordBlobUnreadable` error; `Fnv1a64`/`companion_path`/header code become `pub(crate)` at their second call site) and decides the two things the generic shape forces: **(1)** the blob cannot see inside `R`, so the fingerprint is FNV-1a over the streamed `bincode` encoding of `Vec<R>` — it includes the mmap-backed field, an accepted spurious-rewrite cost (no in-crate caller regenerates scan values between `create` and `open`), with a per-type trait-method fingerprint named as the fallback if `open`'s measured cost lands nearer v0.1.0's +27% than v0.2.0's +0.3–4%; **(2)** relations are layers, not constructor arguments, so the blob holds records only — `Reversed` derives everything from `ChildOf::parent_id`, and `open_portable` is paired with a public `read_portable_records(path) -> Vec<R>` (persisted order preserved) so `open_order_production_stack_portable(path)` can reuse the existing `open_order_production_stack(records, path)` with deterministic child order. `create`/`open`'s existing bounds gain `Serialize + DeserializeOwned` rather than a parallel `create_portable`/`open_portable` pair — the pair would let a plain `open` leave the blob silently stale, the exact gap `ADR-0016` rejected; the bound tightening is the one breaking change, named (`publish = false`, every in-crate record type one derive line away). Explicitly out of scope: `Symmetric`'s external edge list (only `Employee`'s research-gated spike uses one durably; a `Symmetric`-level companion is a separate later decision), any change to the `.mmap` format or to `GenericProductionStore`, multi-writer coordination beyond last-writer-wins. No source file changes; `ADR-0016` and `PRODUCTION-STORE-PORTABILITY-DESIGN.md` each gain a one-line pointer. Accepted; the next unit registers `STORAGE-015` and implements.

  Proposed via PR #88 (`8265a08` → merge commit `393f5a5`); accepted via PR #90 (`af48b6e` → merge commit `33558ea`).

- `GENERIC-STORE-FINGERPRINT-MEASUREMENT` — docs-only: `STORAGE-015`'s named fallback (a per-type trait-method fingerprint, `STORAGE-015` v0.2.0 had it been built) measured at the owner's request before any code was written, and closed as not warranted by the owner's choice. The measurement that decided it: an in-place A/B at 1M `Order` records (release build, 5-sample medians, one scratch example not kept) — `open` 1,222 ms with the shipped streamed-bincode fingerprint, 1,170 ms with `is_current_at`'s `fingerprint()` stubbed out, so the fingerprint is ~52 ms, **4% of `open`**, not the ~300 ms the two Criterion runs implied (the `generic_production_open`/1M group, re-run on unchanged code, reported +8.8% against its own previous run — drift larger than the fingerprint). Candidate fingerprints in isolation: streamed bincode 78.8 ms; FNV over the pre-serialized 76 MB body 94.9 ms; hand-walk every field 71.9 ms (**−7 ms**, the same ~76 B/record hashed); id + customer + status 42.3 ms; id only 21.4 ms — every cheaper row hashes less of the record, and a reopen with a changed non-hashed field would keep the blob silently stale (`open_portable` would then return the old values), the gap `ADR-0016` rejected. A whole-record trait walk also cannot exclude the mmap-backed field marker-independently (`Order` has three scannable fields; the store's `ScanMarker` picks one, which the record cannot know). Outcome: the streamed fingerprint stays, `BLOB_VERSION` stays 1, no record-trait method, `STORAGE-015` stays v0.1.0 (no requirement changed; a dated change-history line only). `RESULTS.md`'s `### GenericMmapStore file portability (STORAGE-015)` gains the `#### Follow-up` subsection and its earlier verdict is marked superseded in place; the design doc's considered option 1, "Implementation status", and open question, ADR-0017's considered option 2, revisit trigger, and "Acceptance and implementation", and the spec's acceptance criterion and open question all record the closure. Zero `src/` changes.

  Implemented via PR #94 (`d8de773` → merge commit `842fca9`).

- `GENERIC-STORE-PORTABILITY` — the real implementation of the accepted `GENERIC-STORE-PORTABILITY-DESIGN`, registered as `STORAGE-015` v0.1.0. `src/generic/record_blob.rs` (new): `GENBLOB\0`, blob version 1, `STORAGE-014` v0.2.0's 20-byte magic/version/FNV-1a header, the bincode `Vec<R>` body; `GenericRecordBlob<'a, R>` borrows `&[R]` and fingerprints by streaming `bincode::serialize_into` a `Fnv1a64` that `impl`s `io::Write`; `read(path) -> Vec<R>` verifies magic, version, and body-vs-header fingerprint, every failure a `RecordBlobUnreadable { path, cause }` naming the companion path. `STORAGE-014`'s helpers (`HEADER_LEN`, `Fnv1a64`, `parse_header`, `encode_image`, `companion_path`, `EncodedRecordBlob`) became `pub(crate)` and are shared — `RecordBlob`'s 12 tests unchanged. `GenericMmapStore::create` writes the blob; `open` refreshes it only when the header's fingerprint is stale; `read_portable_records(path)`/`open_portable(path)` added (additive); `open_order_production_stack_portable(path)` in `order_customer.rs`; `R: Serialize + DeserializeOwned` joins the store's bounds (serde derives on `Order`/`OrderStatus`/`Customer`/`Employee`/`Department`); the `.mmap` slot format untouched (the diff removes only the six bound lines); `GenericProductionStore` unchanged. 16 new tests plus the `production.rs` doctest; every pre-existing `mmap_store.rs`, `tests/mmap_record_identity_keying.rs`, `RecordBlob`, and `production.rs` portability test passes unmodified. Two departures from the design's sketch, on purpose and recorded: no bound on `R::Id`, no `Employee` portable helper. **Cost, measured, with one unclean finding**: `create` roughly doubles at every size (the blob write, ~76 B/record, 3× the `.mmap` file); `open` +0.1 ms at 1K, +19–33% at 100K, and at 1M the throwaway example (3 samples) says inside noise while the published 20-sample Criterion `generic_production_open` group, run twice against a `git stash`-isolated before-run, says +24–27% — both recorded in `RESULTS.md`'s `### GenericMmapStore file portability (STORAGE-015)` subsection, the Criterion figure treated as the trustworthy one. That appeared to trip the design's fallback trigger: the per-type trait-method fingerprint was recorded as the named next step, the owner's call (superseded — `GENERIC-STORE-FINGERPRINT-MEASUREMENT` above measured the fingerprint in place at ~4% and closed the fallback). `generic_production_create`/`generic_production_open` are published Criterion groups and their numbers move by roughly those deltas.

  Implemented via PR #92 (`a6d6eaf` → merge commit `18e8bf9`).

- `SYMMETRIC-EDGE-PORTABILITY-DESIGN` — a design doc (`docs/design/SYMMETRIC-EDGE-PORTABILITY-DESIGN.md`) and ADR (`ADR-0018`), both **Accepted** (the owner approved as proposed, no changes requested), for the first of the four follow-ups the owner queued after `GENERIC-STORE-FINGERPRINT-MEASUREMENT` ("1, 2, 3, then 4") and the first open question `GENERIC-STORE-PORTABILITY-DESIGN` left: `Symmetric`'s external edge list, the one relation-layer state not derivable from records. The design: a `Symmetric`-level companion blob at a caller-supplied `edges_path` (`GENEDGE\0`, version 1, the `STORAGE-014` v0.2.0 header via the shared helpers, body a bincode `Vec<(R::Id, R::Id)>` in the order given), a parallel `create`/`open`/`open_portable` triple plus `read_portable_edges` in a separately bounded `impl` block with `Symmetric::new` untouched, `edges_path(path) -> <path>.edges` as the single-relation convention, `RecordBlobUnreadable` reused, the `Employee` helpers switched to `create`/`open` and a new `open_employee_production_stack_portable(path)`. Out of scope, named: any change to `GenericMmapStore`/`.mmap`/`.records`, `Reversed`, `dog_impl.rs`, a stack manifest, a schema tag (deferred with `STORAGE-015`'s, follow-up 2).

  Proposed via PR #98 (`4258818`, merge commit); accepted via PR #100 (`d57c791`, merge commit).

- `SYMMETRIC-EDGE-PORTABILITY` — the real implementation of the accepted `SYMMETRIC-EDGE-PORTABILITY-DESIGN`, registered as `STORAGE-016` v0.1.0. `src/generic/edge_blob.rs` (new, `pub(crate)`): `GENEDGE\0`, blob version 1, the shared 20-byte magic/version/FNV-1a header (third call site of `STORAGE-014`'s `pub(crate)` helpers), the bincode `Vec<(Id, Id)>` body in caller order; `EdgeBlob<'a, Id>` borrows the slice and fingerprints by streaming into `Fnv1a64`; `read(path)` verifies magic, version, and body-vs-header fingerprint, every failure a `RecordBlobUnreadable { path, cause }` naming the edge blob; `edges_path(path) -> <path>.edges`. `src/generic/store.rs`: one added `impl` block bounded `R::Id: Serialize + DeserializeOwned` — `Symmetric::create`/`open`/`read_portable_edges`/`open_portable` — with `new`, `Neighbors`, and every forwarding impl untouched, plus the file's first test module. `src/generic_spike/employee_impl.rs`: the two durable helpers switch to `create`/`open` at `<path>.edges`, signatures unchanged; new `open_employee_production_stack_portable(path)` rebuilds the whole stack from the three files. `RecordBlobUnreadable`'s doc gains one sentence; no new variant. 23 new tests (12 + 6 + 5) covering every acceptance criterion the design named; every pre-existing test passes unmodified. One recorded departure from the design's sketch: `fingerprint` returns a `Result` (no `expect` outside tests). Cost not measured, per the design. `STORAGE-015` stays v0.1.0.

  Implemented via PR #102 (`bcf3c46` → merge commit `68341bb`).

- `SERVER-PROTOCOL-VERSION` — `SERVER-001` v0.10.0 / FR-020 (**Implemented** in PR #126): the accepted `SERVER-PROTOCOL-VERSION-DESIGN` / `ADR-0022` built. `PROTOCOL_VERSION = 2`; `Request::Hello`/`Response::Hello` at index 10 with golden vectors; `handle_connection` answering a first-frame `Hello` before authentication with `min(client, server)` and version 0 / a non-first `Hello` with `Malformed`, connection open; `dispatch` → `Unsupported`; the compatibility rules and version table in `protocol.rs`'s module docs; `SchemaDrivenClient` saying hello first and exposing `server_protocol_version()`; `tests/server_protocol_version.rs` (new). Every existing byte, suite, and bench unchanged; one `[[test]]` line in `Cargo.toml`, recorded as the deviation from the design's criterion 10. 337 lib tests (333 + 4), every integration suite green.

- `SERVER-CLIENT-AUTH` — `SERVER-001` v0.11.0 / FR-021 (**Implemented** in PR #128): `SchemaDrivenClient::connect_authenticated(addr, token)` (the `Authenticate` sent between `Hello` and `DescribeSchema`, because `AUTH-FR-002` gates the schema fetch itself) and `SchemaDrivenClient::authenticate(&mut self, token)` (re-presenting a token mid-connection; class changes in both directions; a rejected token changes nothing); `connect` on an auth-configured server now reports the server's own `Unauthenticated`. Two tests appended to `tests/server_auth_integration.rs` (7/7). No wire, protocol-version, server, or `Cargo.toml` change; `ADR-0022`'s revisit trigger, `SERVER-PROTOCOL-VERSION-DESIGN`'s open question, and `SERVER-001`'s open question all point here.
- `SERVER-CLIENT-TLS` — `SERVER-001` v0.12.0 / FR-022 (**Implemented** in PR #130): `SchemaDrivenClient::connect_with(addr, ConnectOptions)` with `ConnectOptions::{token, tls}` and `ClientTlsConfig::new(server_name, TrustPolicy)` (`TrustPolicy` re-exported), the TCP connection wrapped in a `rusty_tls::TlsStream` before the `Hello`; `ClientError::Tls` for a configuration `rusty_tls` rejects before any I/O, handshake failures under the `Hello` as `Frame(Io(..))`; one internal stream, each frame assembled before a single `write_all`. Three tests appended to `tests/server_tls_integration.rs` (8/8). No wire, protocol-version, server, or `Cargo.toml` change; `ADR-0014`'s client-side-trust consequence and `SERVER-001`'s v0.9.0 open question point here.
- `SERVER-ACCESS-LOG-DESIGN` — a design doc (`docs/design/SERVER-ACCESS-LOG-DESIGN.md`) and ADR (`ADR-0031`), proposed in PR #165 and both **Accepted** as proposed in PR #166, answering `ADR-0029`'s second revisit trigger: a second, independent sink family (`AccessSink`/`AccessEvent`), kind and outcome shape only, disjoint from the audit log by construction so the two switches never couple. Requirements `ACC-FR-001`–`008`; options (a) as proposed / (b) fold into `AuditKind` / (c) close.
- `SERVER-SESSION-VALIDATE-ON-STAGE` — `SERVER-001` v0.20.0 / FR-030 (**Implemented** in PR #163): stage-time validation in sessions — `SESSION_VALIDATE_ON_STAGE` at protocol 6 (no new variant), `ConnectionStore::validate_op`, a bad write refused when staged with `Commit`'s code and nothing staged, the two `BeginWith` bits composing, `SessionOptions`/`begin_with` in the client. One unit + three integration tests (357 lib). `ADR-0024`'s second trigger and `ADR-0027`'s stage-time trigger point here.
- `SERVER-AUTH-RATE-LIMIT-DESIGN` — a design doc (`docs/design/SERVER-AUTH-RATE-LIMIT-DESIGN.md`) and ADR (`ADR-0030`), proposed in PR #161 and both **Accepted** as proposed in PR #166, answering the lockout and rate-limit thirds of `SERVER-AUTH-DESIGN`'s named gap: a per-connection lockout after five failures (on by default, a constant) and an opt-in per-peer failure budget with bounded state, both `Unauthenticated` on the wire and both audited; no wire, `serve`, or `Cargo.toml` change. Requirements `RL-FR-001`–`008`; options (a) as proposed / (b) both opt-in with a typed error / (c) close.
- `SERVER-AUTH-AUDIT` — `SERVER-001` v0.19.0 / FR-029 (**Implemented** in PR #159): the accepted audit-log design, no deviation — decisions-only events on an `AuditSink` hung on `AuthConfig`, `NoAudit` by default, `StderrAudit`/`FileAudit`, the eager handshake, one `Disconnected` per connection, fail-open with one notice, `SERVER_AUDIT_LOG`. No `Cargo.toml`, wire, store, or `serve`-signature change. Three unit + one binary + four integration tests (356 lib). `SERVER-AUTH-DESIGN`'s gap line and `ADR-0012`'s consequence point here; rate limiting and lockout stay named.
- `SERVER-SESSION-READ-YOUR-WRITES` — `SERVER-001` v0.18.0 / FR-028 (**Implemented** in PR #157): the accepted read-your-writes design — `BeginWith { flags }` at protocol 5, the session's own `GetById` overlaid (last write per field; updatable fields of matching kind only), set reads and plain sessions unchanged, `Session::get` and `begin_read_your_writes` in the client. One clarification: read-only fields are not overlaid. No new `Response`/`ErrorCode`; no store, adapter, or `Cargo.toml` change. One unit test + four integration tests (353 lib). `ADR-0024`'s first trigger, `SESS-FR-007`, and the session design's open question point here.
- `SERVER-JOURNAL-GROUP-COMMIT` — `SERVER-001` v0.17.0 / FR-027 (**Implemented** in PR #155): the accepted group-commit design, no deviation — `CommitGroup` (append under the journal's mutex, leader/follower `fsync` outside the exclusive section, ordered apply through a turn gate, quiescent checkpoint, v0.15.0's failure semantics per group), the three adapters' journaled path restructured around it, the unjournaled path untouched. `dog-jrnl-txn`: 304.3 µs latency; 3,169 / 5,906 / 14,276 / 15,322 batches/s at 1 / 4 / 32 / 64 connections (v0.15.0: 320.7 µs; 3,182 / 3,843 / 3,395 / 3,337). Five unit tests + one integration test (352 lib; transaction suite 12/12). `ADR-0025`'s third trigger and Part B's lock-discipline invariant point here.
- `SERVER-AUTH-AUDIT-DESIGN` — a design doc (`docs/design/SERVER-AUTH-AUDIT-DESIGN.md`) and ADR (`ADR-0029`), proposed in PR #151 and both **Accepted** as proposed in PR #153, answering the audit-log third of `SERVER-AUTH-DESIGN`'s named gap: a decisions-only audit record (admission, authentication, authorization, disconnect — never a request, token, certificate, id, or value) on an `AuditSink` hung on `AuthConfig`, `NoAudit` by default, `StderrAudit`/`FileAudit`, fail-open with one notice, the eager handshake shared with `ADR-0028`. Requirements `AUD-FR-001`–`008`; options (a) as proposed / (b) fail-closed / (c) close.
- `SERVER-MTLS-CLASS-DESIGN` — a design doc (`docs/design/SERVER-MTLS-CLASS-DESIGN.md`) and ADR (`ADR-0028`), proposed in PR #149 and both **Accepted** as proposed in PR #153, answering `ADR-0023`'s first revisit trigger: an admitted client certificate may decide the connection's class by exact match of its DER bytes against certificates the operator pinned per class on `AuthConfig`, after an eager server-side handshake, on top of an upstream `rusty_tls` `TlsServerStream::peer_certificate_der` accessor whose patch is spelled out and was verified by a local probe. No parser, no new dependency, no wire change. Requirements `CLS-FR-001`–`008`; options (a) as proposed / (b) with SPKI pinning / (c) close. The upstream PR could not be opened from this session.
- `SERVER-SESSION-READ-YOUR-WRITES-DESIGN` — a design doc (`docs/design/SERVER-SESSION-READ-YOUR-WRITES-DESIGN.md`) and ADR (`ADR-0027`), proposed in PR #147 and both **Accepted** as proposed in PR #153, answering `ADR-0024`'s first revisit trigger: an opt-in session mode (`BeginWith { flags }`, protocol 5) in which the connection's own `GetById` sees its staged writes — last write per field wins, kinds must match — with set reads, plain sessions, older clients, and other connections unchanged; `Session::get` added to the client library, which today cannot read at all while a session is open. Requirements `RYW-FR-001`–`008`; options (a) as proposed / (b) with `FilterEq` adjusted / (c) close.
- `SERVER-JOURNAL-GROUP-COMMIT-DESIGN` — a design doc (`docs/design/SERVER-JOURNAL-GROUP-COMMIT-DESIGN.md`) and ADR (`ADR-0026`), proposed in PR #145 and both **Accepted** as proposed in PR #153, answering `ADR-0025`'s third revisit trigger after `RESULTS.md` fired it: the batch journal's `fsync` leaves the store's exclusive section and becomes a leader/follower group commit (no timer, delay, or thread), applies stay in journal order, checkpoints wait for a quiescent point. Probe evidence recorded in the doc (6.4× at 64 threads, lone connection unchanged, a commit delay not worth a knob). Requirements `GRP-FR-001`–`008`; options (a) as proposed / (b) with a fixed commit delay / (c) close.
- `SERVER-CLIENT-HELLO-FALLBACK` — `SERVER-001` v0.16.0 / FR-026 (**Implemented** in PR #143): the reconnect-without-hello fallback, default-on at the owner's call — `connect_with` reconnects once without a `Hello` when the peer closes under it with no reply, reports version 1, gates `begin`; `ConnectOptions::require_hello()` opts out; `open_transport` factored. One test with a pre-hello fake server on the public `dispatch` (protocol-version suite 6/6). No wire, server, `Cargo.toml`, or `PROTOCOL_VERSION` change. `ADR-0022`'s trigger and consequence, `SERVER-PROTOCOL-VERSION-DESIGN`'s open question and compatibility matrix, and `SERVER-001`'s v0.10.0 open question all point here.
- `SERVER-TRANSACTION-JOURNAL` — `SERVER-001` v0.15.0 / FR-025 (**Implemented** in PR #141): the accepted redo-journal design — `src/server/journal.rs` (`BatchJournal`, `CheckpointFlush`, `JournalError`), `with_journal` on all three adapters (append + `fsync` before the first write inside the existing exclusive closure; replay, flush, truncate on open; checkpoint by size), `SERVER_TXN_JOURNAL_PATH`, `ErrorCode::Journal` with `PROTOCOL_VERSION = 4` and the rule-3 downgrade, `dog-jrnl-txn` bench rows in `RESULTS.md`. Seven unit tests + one integration test (347 lib; transaction suite 11/11). No `Cargo.toml`, store, storage-format, or framing change. `ADR-0013`'s second trigger, `TXN-FR-007`, `MULTI-FIELD-MMAP-DURABILITY-DESIGN`'s atomic-multi-field non-goal, and `SERVER-001`'s v0.7.0 open question all point here.
- `SERVER-TRANSACTION-SESSION` — `SERVER-001` v0.14.0 / FR-024 (**Implemented** in PR #139): the accepted session design, no deviation — `PROTOCOL_VERSION = 3`, `Begin`/`Commit`/`Rollback`/`Staged` and three error codes with golden vectors, `handle_connection`'s negotiated version and staged buffer (no lock between round trips; `Malformed` below 3), `dispatch` → `Unsupported`, `SchemaDrivenClient::begin()` → `Session` gated on version 3, `ClientError::TransactionFailed`. Nine tests across three suites + two unit tests (340 lib). `ADR-0013`'s first trigger, `ADR-0022`'s first trigger, `SERVER-TRANSACTION-DESIGN`'s last open question, and `SERVER-001`'s v0.7.0/v0.10.0 open questions all point here.
- `SERVER-TRANSACTION-SESSION-DESIGN` — a design doc (`docs/design/SERVER-TRANSACTION-SESSION-DESIGN.md`) and two ADRs (`ADR-0024` session, `ADR-0025` crash-atomic batches), proposed in PR #137 and all **Accepted** as proposed in PR #138, answering `ADR-0013`'s two revisit triggers: a buffered session at protocol version 3 that holds no lock across round trips (`Begin`/`Commit`/`Rollback`/`Staged`, `MAX_STAGED_OPS`, the negotiated version finally kept, the client's first version-gated API), and an opt-in redo journal at the domain adapter (`fsync` before the first write, idempotent replay on open, checkpoint by size). Requirements `SESS-FR-001`–`009`, `JRN-FR-001`–`009`; acceptance options per ADR.
- `SERVER-MTLS` — `SERVER-001` v0.13.0 / FR-023 (**Implemented** in PR #135): the accepted mTLS design, no deviation — `TlsConfig::new_with_client_auth` / `from_pem_files_with_client_ca` / `from_env` (+ `SERVER_TLS_CLIENT_CA_PATH`, partial configuration an error) / `requires_client_certificate()`; `ClientTlsConfig::with_identity` / `with_identity_pem_files` / `has_identity()`, key-redacting `Debug`; `dog_server` reports the mode. `handle_connection`, the wire, `PROTOCOL_VERSION`, `Cargo.toml` unchanged. Four tests appended to `tests/server_tls_integration.rs` (12/12) + one unit test (338 lib). `ADR-0012`'s/`ADR-0014`'s mTLS triggers, `SERVER-AUTH-DESIGN`'s and `SERVER-TLS-DESIGN`'s open questions, and `SERVER-001`'s v0.9.0 open question all point here.
- `SERVER-MTLS-DESIGN` — a design doc (`docs/design/SERVER-MTLS-DESIGN.md`) and ADR (`ADR-0023`), both proposed in PR #133 and **Accepted** as proposed in PR #134, for mutual TLS: client certificates as an admission gate layered under `AuthConfig` (the token still decides class); `TlsConfig::new_with_client_auth` / `from_pem_files_with_client_ca` / `SERVER_TLS_CLIENT_CA_PATH` and `ClientTlsConfig::with_identity` / `with_identity_pem_files`, on `rusty_tls`'s existing mTLS constructors; `handle_connection`, the wire, and `Cargo.toml` unchanged. Class-from-certificate, revocation, and issuance named as out of scope with reasons. Requirements `MTLS-FR-001`–`008`; acceptance options (a) as proposed, (b) hold for class-from-certificate, (c) close as not warranted.
- `SERVER-PROTOCOL-VERSION-DESIGN` — a design doc (`docs/design/SERVER-PROTOCOL-VERSION-DESIGN.md`) and ADR (`ADR-0022`), both proposed in PR #124 and **Accepted** as proposed in PR #125, for `SERVER-001` v0.9.1's last open question (wire-*shape* evolution; trigger: a second deployed client build), taken ahead of the trigger at the owner's pick. `PROTOCOL_VERSION = 2` (1 = the v0.9.1 shape, a silent client's version); an appended, optional, first-frame `Request::Hello`/`Response::Hello` pair answered pre-auth with `min(client, server)`; four append-only compatibility rules (`PROTO-FR-005`); `SchemaDrivenClient` says hello first and exposes `server_protocol_version()`. Eight requirements (`PROTO-FR-001`–`008`), ten acceptance criteria, thirteen considered options (per-frame byte, `DomainSchema` field, feature bitmap, `min_supported`, required hello, renegotiation, gating state now, rule-only, wait — each rejected with its reason). No `src/` change, no dependency, no existing byte changed. Acceptance: option (a) as proposed; (b) hello required and (c) rule only declined.
- `BINCODE-ENCODING-STABILITY` — `STORAGE-018` v0.1.0 (**Implemented** in PR #120): the one-codec pin `ADR-0021` accepted. `src/codec.rs` (`pub(crate)`) names the crate's `bincode` configuration once — `DefaultOptions::new().with_fixint_encoding().with_little_endian().with_no_limit().reject_trailing_bytes()` — behind `encode`/`encode_into`/`decode`, and every production call site (`server/framing.rs`, the three blob modules, `durability/{mod,hybrid,lsm_store,snapshot_rebuild}.rs`) plus `test_support.rs`'s helpers go through it; `grep -rn "bincode::" src` is the criterion and finds only `codec.rs` and the two error-type positions. The bytes written are byte-for-byte what the free functions wrote (golden vectors captured in a commit that precedes the codec — primitives/`Option`/enums/structs/`Uuid` in `codec.rs`, every `Request`/`Response` variant in `protocol.rs`, one body each of `DOGBLOB\0`/`GENBLOB\0`/`GENEDGE\0` — and unchanged after routing); the one behavioral change is the accepted one, trailing bytes on decode are an error. Module docs record the format (fixint LE integers, `u64` length prefixes, `u32` enum indices, `Uuid` as a 24-byte length-prefixed byte string), the 1.x stability promise the configuration now satisfies by name rather than convention, and the evolution rules (appending an enum variant is compatible; reordering/inserting variants, changing struct fields or integer widths is a format change under the owning format's version — `BLOB_VERSION`, the slot-file header, or a `SERVER-001` amendment). Finding at implementation: `durability::record_blob` decodes before it fingerprints, so a `DOGBLOB\0` body with trailing junk was silently accepted and is now `RecordBlobUnreadable` ("body does not decode"); the generic blobs hash bytes first and were already a fingerprint mismatch. `SERVER-001` v0.9.1 cites the codec and closes its open question; `ADR-0010`, `ADR-0019`, `ADR-0021`, the design doc, and item 33 carry pointers. +17 tests (316 → 333 under `--all-features`; 124 → 136 default), no format bump, no dependency, no public API change.
- `BINCODE-ENCODING-STABILITY-DESIGN` — a design doc (`docs/design/BINCODE-ENCODING-STABILITY-DESIGN.md`) and ADR (`ADR-0021`), both **Accepted** (the owner approved them as proposed in PR #116 — option (a), `reject_trailing_bytes()`; spec `STORAGE-018`; no changes requested), for the limitation `ADR-0010` named at the server layer's proposal, `SERVER-001`'s open question, and item 33 — the fourth of the owner's four queued follow-ups. Verified from the vendored `bincode` 1.3.3 source, `uuid` 1.25.0's serde impl, and an executed probe: the crate's 23 production call sites all use `bincode`'s free functions, which pin fixint/little-endian/no-limit/allow-trailing (not `bincode::options()`'s varint/reject-trailing — a `Uuid` is 24 bytes, not 17); the 1.x promise holds *"provided the same configuration is used"*, and that configuration is named nowhere in `src/` and pinned by no test. Proposes one `pub(crate)` `src/codec.rs` naming it (`with_fixint_encoding().with_little_endian().with_no_limit()` + `reject_trailing_bytes()`) behind `encode`/`encode_into`/`decode`, every site routed, golden byte vectors (primitives, every `Request`/`Response` variant, one body each of the three blobs) captured on the pre-change code; no format bump, no dependency or API change; acceptance question reject-trailing (proposed, **chosen**) vs allow vs document-only; spec `STORAGE-018` at implementation (approved over a new category); `ADR-0019`'s layout-fingerprint question answered "not now", trigger armed. Zero `src/` changes. Accepted; the next unit registers `STORAGE-018` and implements.
- `MULTI-FIELD-MMAP-DURABILITY` — `STORAGE-017` v0.1.0 (**Implemented** in PR #114): the per-field durable layer `ADR-0020` accepted. `SlotFile<Id, V>` (`pub(crate)`, `src/generic/slot_file.rs`) is the one slot-file engine, extracted from `GenericMmapStore` without changing its on-disk format (`SCHEMA_VERSION` stays 2, every existing `.mmap` still opens) or any of its 16 tests; `MmapScanned<S, R, Marker>` (`src/generic/mmap_scanned.rs`) is the public layer — one more `MmapFieldValue` field in its own `<path>.<field>.mmap`, `ScanField` / `UpdateField` for that field, `GetById` / `Flush` / `FilterEq` / `Neighbors` / `Children` forwarded, `open` reconciled against the inner store's live ids, `SlotWidthMismatch` for a file whose body is not whole slots; `forward_scannable_pairs!` takes `for Scanned;` / `for MmapScanned;`; `OrderProductionStack` carries `Amount` and `DiscountCents` mutable-and-durable. Tests: 8 new in `mmap_scanned.rs`, 1 new + 1 extended in `order_customer.rs`, 2 compile-time pair checks; Criterion before/after in `RESULTS.md`.
- `MULTI-FIELD-MMAP-DURABILITY-DESIGN` — a design doc (`docs/design/MULTI-FIELD-MMAP-DURABILITY-DESIGN.md`) and ADR (`ADR-0020`), both **Accepted** (proposed in PR #110; accepted as proposed by the owner in PR #112 — the `MmapScanned` layer with the `SlotFile` extraction, over duplication, the multi-slot layout, and closing it), for `ADR-0006`'s named revisit trigger and the third of the owner's four queued follow-ups: more than one mutable durable field per record through the generic mmap store. Proposes per-field `.mmap` files via a composable `MmapScanned<S, R, Marker>` layer — the durable twin of `Scanned`, one file per marker in exactly the existing `GMMAPST\0` version-2 format, no format changes anywhere, the slot machinery extracted into a shared `pub(crate)` `SlotFile`, cross-marker forwards from a generalized `forward_scannable_pairs!`, `OrderProductionStack` gaining `DiscountCents` as the proof — over a multi-slot single-file layout (a `SCHEMA_VERSION` 3 that refuses every existing file and turns the store into the AoS shape `ADR-0001` measured against) and over widening the blob's role (a snapshot-per-write, or a second WAL; the fallback for a variable-width field). Non-goals: variable-width fields, atomic multi-field updates, `redb`/LSM, the stack manifest. Nine requirements and eight acceptance criteria for a `STORAGE-017` at implementation. No `src/` changes. See "Current milestone" above for the full shape.
- `BLOB-SCHEMA-TAG` — the accepted `BLOB-SCHEMA-TAG-DESIGN` implemented (PR #108) and registered as `STORAGE-015` v0.2.0 / `STORAGE-016` v0.2.0, closing the one open question both specs held. `pub trait SchemaTag { const SCHEMA_TAG: &'static str; }` in `src/generic/traits.rs` — public, not a supertrait of `Record`, documented as part of the on-disk format (renaming a tag is a format change). `src/generic/record_blob.rs`: `BLOB_VERSION` 1 → 2 and the `pub(crate)` tagged-header helpers — `TAG_OFFSET` (= `HEADER_LEN`, 20), `TAGGED_HEADER_LEN` (28), `tag_hash` (FNV-1a 64 over the tag's bytes, pinned to the published `""`/`"a"` test vectors), `encode_tagged_image`, `parse_tagged_header` (the shared `parse_header` first, then the tag, so a version-1 file is a `version` cause before the tag is looked at); `GenericRecordBlob` and `read` bounded `R: SchemaTag`. `src/generic/edge_blob.rs` imports those helpers: `EdgeBlob::new(edges, tag)` and `read(path, tag)` take the tag as a value (the blob stays generic over `Id` alone), `BLOB_VERSION` 1 → 2. Check order magic → version → tag → fingerprint → decode; a foreign-`R` blob is `RecordBlobUnreadable` whose cause reads `schema tag mismatch: this store expects \`<tag>\` (<hash>), file holds <hash>`, a file cut inside the tag bytes `file too short for a tagged header` — no new variant. `is_current_at` requires all of it, so `open` rewrites version-1 and foreign-`R` blobs the way it heals a missing one, no migration step (the `publish = false` compatibility call ADR-0019 made). The bound lands on exactly the blob-touching functions: `GenericMmapStore`'s `create`/`open`/`read_portable_records`/`open_portable` moved into their own `impl` block; `Symmetric`'s `STORAGE-016` block gains `R: SchemaTag` and passes `R::SCHEMA_TAG` at its three blob call sites. Impls: `Order` (`"order_customer::Order"`), `Employee` (`"employee::Employee"`), the `production.rs` doctest's `Widget` (`"doctest::Widget"`). `durability::record_blob`, `DOGBLOB\0`, the `.mmap` files, `Symmetric::new`, the query traits, and every in-memory stack untouched. 12 new tests: 5 in `record_blob.rs` (the FNV vectors; the tag at bytes 20..28 as the only difference between two same-shape types' images; same-shape other type a tag error, not a decode; version-1 image a version error; cuts inside the tag), 3 in `edge_blob.rs` (other tag; version-1; cuts inside the tag), 2 research-gated in `mmap_store.rs` (an `Employee` companion at an `Order` store's path refused by name from `read_portable_records`/`open_portable`, `is_current_at` true for the employees and false for the orders, `open(orders)` heals; a hand-built version-1 companion refused and rewritten byte-identical to the v2 image), 2 in `store.rs` (a second record type `Other` whose edge blob at a `Node` layer's path is a tag error healed by `open`; a version-1 edge blob likewise) — every one of the design's eight acceptance criteria pinned. `cargo fmt`, `clippy -D warnings`, `cargo test` (default and `--all-features`), `cargo doc --no-deps` at baseline.
- `BLOB-SCHEMA-TAG-DESIGN` — a design doc (`docs/design/BLOB-SCHEMA-TAG-DESIGN.md`) and ADR (`ADR-0019`), both **Accepted** (proposed via PR #104, accepted as proposed by the owner via PR #106 (`41ab96b` → merge commit `ff2d2d3`) — hashed 8-byte tag, readable 32-byte string declined), for the second of the four follow-ups the owner queued ("1, 2, 3, then 4") and the one open question `STORAGE-015` and `STORAGE-016` both hold and agreed to resolve together: neither generic companion blob records which `R` it holds. The design: an opt-in `SchemaTag { const SCHEMA_TAG: &'static str }` trait in `src/generic/traits.rs` (not a supertrait of `Record`; `type_name` and a required `Record` const both rejected), an 8-byte `u64` LE FNV-1a 64 hash of the tag at offset 20 after the unchanged 20-byte shared header (`TAGGED_HEADER_LEN` 28; `durability::record_blob` and `DOGBLOB\0` untouched; new `pub(crate)` helpers in `generic::record_blob` shared with `generic::edge_blob`), `GENBLOB\0`/`GENEDGE\0` `BLOB_VERSION` 1 → 2 (`STORAGE-015` v0.2.0, `STORAGE-016` v0.2.0 at implementation), check order magic → version → tag → fingerprint → decode with a tag mismatch a `RecordBlobUnreadable` naming the expected tag string (no new variant), `is_current_at` requiring all four so `open` heals version-1 and foreign-`R` blobs while `read_portable_*`/`open_portable` refuse them, the `R: SchemaTag` bound on exactly the blob-touching functions (`GenericMmapStore`'s four in their own `impl` block; `Symmetric`'s `STORAGE-016` block), `Order`/`Employee`/doc-example `Widget` the whole in-crate impl set, the edge blob tagged with `R::SCHEMA_TAG` passed as a value. Offered as the alternative at acceptance: a readable fixed-width 32-byte tag string. Deferred, named: the `.mmap` file's own tag, distinct tags for two `Symmetric` relations over one `R`, the stack manifest.

  Proposed via PR #104 (`76bf836` → merge commit `a3c10ae`).

## In progress

- (none)

## Blocked

- (none)

## Next

1. Whether a lazy-invalidation fifth backend/mode is worth pursuing is effectively settled by `MIXED-WORKLOAD`'s result (no crossover found even at 90% writes) — not planned unless the owner wants it for reasons beyond this benchmark's scope (see ADR-0003's revisit triggers).
2. Memory overhead is measured (see below) but the *decision* it feeds — is `CanonicalCachedStore`'s ~2.6× footprint over `SoaStore` at 1M acceptable for the workload — is still the owner's call, not made here.
3. The `scan_ages` 100K crossover's exact last-level-cache mechanism remains unconfirmed and is not planned as further work (see `SCAN-AGES-CROSSOVER-L1D` above) unless the owner explicitly wants to pursue raw AMD PMU events.
4. `GRAPH-TRAVERSAL`'s and `MIXED-WORKLOAD`'s cache-miss numbers still need a real `baileyai` run (build/feature is ready for both, per `RESULTS.md`'s respective cache-miss status notes).
5. Whether the graph-traversal numbers make a case for a real graph engine, a degree sweep, or an asymmetric relationship type is an owner's call — see `STORAGE-006`'s and ADR-0004's open questions, not decided here.
6. `MIXED-WORKLOAD`'s open questions (write ratios beyond 90%, bursty/correlated access patterns) are an owner's call — see `STORAGE-007`'s open questions, not decided here.
7. `DURABILITY-TIER1`/`DURABILITY-TIER2` merged via PR #8 (`251975d`) — done, including both follow-up fixes (Hybrid's checkpoint clone, reopen-path parallelization).
8. Whether LSM compaction is worth building, given `RESULTS.md`'s durability recommendation doesn't currently rely on `LsmStore` beyond proof-of-concept — owner's call, see ADR-0006's revisit triggers.
9. Durability cache-miss/hardware-counter numbers were not attempted this pass (the task's three specified metrics are all wall-clock/derived-comparison; no `benches/cache_events.rs` durability coverage was requested or built) — not an open gap against this pass's own scope, but worth noting alongside items 4/6 above if a future pass wants PMU-level durability numbers too.
10. The reopen-path parallelization's 1,000-record regression is fixed — `PARALLEL_CONSTRUCTION_THRESHOLD = 1,500` (measured, not guessed; see `RESULTS.md`). Merged via PR #9 (`0f9937e`) — done.
11. `benches/durability.rs`'s `run_load` doesn't currently measure WAL/Hybrid's true "first-ever start, no checkpoint yet" reopen path (it always checkpoints before timing load) — a real coverage gap surfaced during the reopen-parallelization diagnosis, not decided here whether it's worth a fourth benchmark group.
12. `CONCURRENCY-PROTOTYPES` merged via PR #10 (`fd83b8e`) — done, including both real multi-core follow-up passes (Windows machine, then genuine `baileyai`).
13. Concurrency + a specific durability variant, combined — one specific instance (global `RwLock` + mmap) is now `PRODUCTION-DEFAULT`'s `ProductionStore`; every *other* pairing (e.g. sharded + a WAL variant) remains an owner's call whether/when to pursue.
14. `ShardedStore`'s fixed `SHARD_COUNT = 64` was never swept against alternatives, and `ActorStore`'s measured single-owner throughput ceiling was not addressed by sharding the actor itself — both owner's calls, see ADR-0007's revisit triggers and `RESULTS.md`'s concurrency open questions.
15. The concurrency benchmark's bursty/skewed access patterns and beyond-100K dataset sizes are unmeasured — see `RESULTS.md`'s concurrency open questions, not decided here.
16. Real-hardware concurrency throughput was measured on three machines now (4-core container, 24-core Windows desktop, 32-core `baileyai`) and found *not* to track core count monotonically — sharded's margin over global `RwLock` widened on some configurations (100K/90% writes) but narrowed or reversed on others (1K/10%/90% writes) between the Windows machine and `baileyai`. Whether a fourth, still-larger real machine would clarify the pattern or confirm it's genuinely machine-specific (microarchitecture/memory subsystem, not just core count) is an owner's call — see `RESULTS.md`'s concurrency open questions.
17. `PRODUCTION-DEFAULT` merged via PR #11 (`4d8bf0d`) — done, including the concurrency-tax diagnosis and fix.
18. The physical `src/{store,durability,concurrency}` → `src/comparisons/` reorganization was explicitly considered and deferred in favor of a docs-only lead (per the user's own choice when checked in on) — remains available as a future follow-up if the docs-only lead proves insufficient; see ADR-0008's revisit triggers.
19. `MmapAgeStore::get`'s modest ~1.5× cost over `CanonicalCachedStore` (two `HashMap` lookups vs. one) was measured during the `scan_ages` diagnosis but not optimized — small enough not to warrant it, unlike `scan_ages`'s 25–32×. Owner's call whether it's worth closing further; see `RESULTS.md`'s `## Production recommendation` section.
20. **`GENERIC-SCHEMA-DESIGN` review is done** — ADR-0009 is Accepted, the design is implemented as `crate::generic` (`GENERIC-SCHEMA-LIBRARY` above). Done, not owner's-call anymore.
21. The forwarding-boilerplate tax (design doc §4.5) is resolved, not just measured — `forward_scannable_pairs!` generates the O(pairs) forwarding impls the coherence limit requires; the human-maintained surface is one macro invocation per record type. Not "unmeasured" anymore.
22. mmap durability's generalization to more than one mutable field (design doc §4.2, `ADR-0006`'s own predicted revisit trigger) was genuinely unscoped follow-up work from the generic library's landing until this round — `GenericMmapStore` deliberately keeps the original one-field scope. **Now built**: `MULTI-FIELD-MMAP-DURABILITY-DESIGN` / `ADR-0020` (Accepted as proposed) is implemented as `STORAGE-017` v0.1.0 in PR #114 — a per-field `MmapScanned` layer over the existing format, sharing one `SlotFile` engine with `GenericMmapStore`; see item 68 and "Current milestone". `ADR-0006`'s one-field scope stands for `GenericMmapStore` itself; the revisit trigger is answered by composition, not by widening it.
23. `bench_support.rs`/`GeneratorConfig`'s domain-specificity (design doc §4.7) remains unresolved — `crate::generic_spike::order_bench_support` is `Order`-specific and hand-written, not a generic dataset generator; still separate, unscoped follow-up work.
24. **Resolved** — the in-memory `Scanned`/`BaseStore` composition's write-through gap (`GetById::get` not reflecting `UpdateField::update`'s writes on `DogGenericStore`/`OrderGenericStore`) is fixed; see `GENERIC-SCHEMA-WRITE-THROUGH-FIX` above. Real, measured, accepted cost: `get` on the in-memory path is ~43–88% slower (one extra `HashMap` lookup per `Scanned` layer) — not an owner's-call item, a documented tradeoff of the correctness guarantee.
25. **New this pass**: only two domains (`Dog`, `Order`/`Customer`) have ever exercised `crate::generic`, and only `Order`/`Customer` has a real durable production-store benchmark — a third, structurally different domain (e.g. one needing `SymmetricRelation` *and* `ChildOf` together) would be the natural next stress test, an owner's call whether/when to pursue.
26. **Resolved since the prior checkpoint** — a third, structurally different domain did arrive: the requirements-traceability spike (`Rule`/`RuleRelation`/`Source`/`RuleDerivation`/`SelectionGroup`, PRs #19/#22) exercises recursive self-referential parent chains, multiple relation kinds between the same record type, and a `ChildOf`-modeled cardinality constraint — none of which `Dog`/`Order`-`Customer` had. It stays a spike (`src/generic_spike/`), not promoted into `crate::generic` itself, matching the existing `GENERIC-SCHEMA-SPIKES` precedent — promotion isn't planned unless the owner wants a real requirements-traceability product built on it.
27. `GenericMmapStore`'s production-hardening arc (record-identity keying, schema versioning, torn-write and multi-process-append fixes, PRs #23/#24/#26/#29) is done for the failure modes actually diagnosed. Two related gaps are named explicitly, not hidden, in `docs/FUTURE-GROWTH.md`: the multi-process fix covers slot *creation* only, not broader multi-writer coordination (e.g. concurrent updates to the same existing slot from two processes, never diagnosed); and `O_APPEND`'s atomicity guarantee is local-filesystem-only, excluding NFS. Both are owner's-call follow-ups, not committed to.
28. **New this pass, process gap**: PR #27 (`PUBLIC-API-NARROWING`) made a consequential, crate-wide public-API design decision — introducing the `research` feature flag as the compiled-surface boundary — without an ADR, despite `AGENTS.md`'s own "this project is in active bootstrap/major-development, so default to writing one" guidance. Flagged in Risks below; whether to backfill an ADR describing the decision as already made, or treat the gap as accepted and move on, is the owner's call, not decided in this documentation-sync pass.
29. This documentation-sync pass touched only `docs/PROJECT-STATUS.md` (this file) — `docs/roadmap/ROADMAP.md`, `docs/specifications/SPEC-REGISTRY.md`, and `docs/traceability/TRACEABILITY.md` were checked and deliberately left unchanged: none of PRs #18-#31 introduced a new spec-tracked capability unit (they are either spikes kept as historical record, fixes/diagnoses against the already-`Implemented`/`Verified` `STORAGE-012`, or docs-only), matching this project's own established precedent (e.g. `GENERIC-SCHEMA-WRITE-THROUGH-FIX`, the `DURABILITY-TIER1` follow-up fixes, and every `SCAN-AGES-*` diagnosis round also landed as `PROJECT-STATUS.md`-only entries with no new roadmap row).
30. **Resolved**: `SERVER-QUERY-LAYER` is implemented — `src/server/**`, validated against both `Dog` and `Order`/`Customer` over a real socket, including the flagship concurrent-client stress test. See `## Completed` above for the full account.
31. Authentication/authorization and transport encryption remain blocking gaps for any deployment beyond localhost/trusted-network use — not built by this implementation round either, and it does not authorize deploying a server binary beyond that trusted context. A real design pass for either would be needed first; owner's call on priority and shape, not decided here.
32. **New this pass**: no throughput/latency benchmark exists for the server layer (`benches/server.rs` was not built — this round's acceptance criteria were correctness, not performance). The thread-per-connection concurrency model's real connection-count ceiling remains unmeasured. Both are owner's-call follow-ups, not decided here.
33. Whether `bincode`'s existing encoding/version-stability guarantees are adequate for a wire protocol (client/server version skew, not just one process's own on-disk lifetime) remains unverified — named as an open question in `SERVER-001`, not resolved by this implementation round. **Verified and scoped, not built** — see item 69: `BINCODE-ENCODING-STABILITY-DESIGN`/`ADR-0021` (Accepted as proposed) records what the free functions pin and proposes naming it in one codec with golden vectors; `SERVER-001`'s open question is resolved by pointer at implementation. **Resolved in PR #120** — `STORAGE-018` v0.1.0 / `crate::codec` (see item 69): the configuration is named in one place, pinned by golden vectors, and `SERVER-001` v0.9.1 cites it; wire-*shape* evolution (a version field or a compatibility rule for `Request`/`Response`) stays open there, trigger: a second deployed client build. **Resolved in PR #126** — `SERVER-001` v0.10.0 / FR-020 (`ADR-0022`, see item 70): `PROTOCOL_VERSION = 2`, an optional first-frame `Hello`, append-only compatibility rules.
34. **New this pass**: only two domains (`Dog`, `Order`/`Customer`) have ever exercised the server layer — the same two `crate::generic` itself was validated against. A third, structurally different domain would be the natural next stress test for the protocol, matching this project's own "validate against a second, structurally different domain" discipline; owner's call whether/when to pursue.
35. **Resolved**: schema discovery (`DescribeSchema`) is implemented — `SERVER-QUERY-LAYER` v0.2.0, ADR-0011. See `## Completed` above for the full account.
36. **Resolved**: a schema-driven client *library* now exists — `server::client::SchemaDrivenClient`, `SERVER-QUERY-LAYER` v0.5.0. See `## Completed` above for the full account.
37. **New this pass**: `DomainSchema`'s own shape (the `FieldDescriptor`/`FieldCapabilities`/`RelationCapabilities` structs) is now part of the wire protocol's compatibility surface, same as every other `Request`/`Response` variant — a future change to it is a breaking change for a schema-driven client, and no version-negotiation story exists for any of this protocol yet (see item 33). Owner's call whether/when to address, not decided here.
38. Non-Rust or cross-language client support remains unaddressed — schema discovery solves field-naming/capability discovery, not serialization-format interoperability (still `bincode`-only). Named explicitly in `docs/design/SERVER-QUERY-LAYER-DESIGN.md`'s own updated "Open questions," not silently assumed solved by this round.
39. **Resolved**: a third, structurally different domain now exercises the server layer — `Employee` (item 34 above). It is also the first domain to close ADR-0009's own `SymmetricRelation`-plus-`ChildOf` "revisit if" trigger. See `## Completed` above for the full account.
40. **Resolved**: a throughput/latency benchmark for the server layer now exists — `benches/server.rs`, `SERVER-QUERY-LAYER` v0.4.0 (items 32/33's own gap). See `## Completed` above for the full account. Completes the owner's explicit "3 then 2" instruction — both items done.
41. **Resolved** — the thread-per-connection model's real connection-count ceiling is now measured on two real machines, not just the 4-core container: `Beast` (24 cores) and `baileyai` (32 cores) both show throughput climbing cleanly through real core count, then flattening-to-negative past it (oversubscription), confirming the container's flat 4-through-16 plateau was that environment's own ceiling, not the model's. See `## Completed` above for both real-hardware passes and the resolved `Beast`-peak-throughput surprise.
42. **Resolved**: ADR-0012's design review is done — the owner reviewed `docs/design/SERVER-AUTH-DESIGN.md` and accepted it as proposed, no changes requested. See `## Completed` above for the full account. Session/transaction semantics is still ahead, unstarted, and will get the identical design-first treatment.
43. **Resolved**: the accepted `SERVER-AUTH-DESIGN` is implemented — `SERVER-001` v0.6.0, `SERVER-001-FR-016`, every functional and timing-based acceptance criterion verified. See `## Completed` above for the full account.
44. **Resolved**: ADR-0013's design review is done — the owner reviewed `docs/design/SERVER-TRANSACTION-DESIGN.md` and accepted it as proposed, no changes requested. See `## Completed` above for the full account.
45. **Resolved**: the accepted `SERVER-TRANSACTION-DESIGN` is implemented — `SERVER-001` v0.7.0, `SERVER-001-FR-017`, every functional acceptance criterion verified over a real socket including the flagship concurrent stress test. See `## Completed` above for the full account.
46. **Resolved**: with all three of the owner's original next-direction picks done, the owner was asked "what's next" via `AskUserQuestion` and picked two options: transport encryption and a transaction throughput/latency benchmark. This item's own prior text ("none queued") is superseded by items 47/48 below.
47. **Resolved**: the transaction benchmark is done — `benches/server.rs` gained a `{domain}-txn` row set directly comparable to the existing `GetById` rows, `SERVER-001` v0.8.0 (`SERVER-001-FR-018`). No meaningful latency or throughput cost found relative to `GetById`, at this benchmark's scale, on this session's shared container. See `## Completed` above for the full account, including the numbers and the one honest, unresolved sub-question (an `Employee`-at-4-threads outlier read as noise but not confirmed as such without a real-hardware follow-up).
48. **Resolved**: the transport-encryption design proposal is written — ADR-0014, `docs/design/SERVER-TLS-DESIGN.md` — see `## Completed` above.
49. **Resolved**: ADR-0014's design review is done — the owner reviewed `docs/design/SERVER-TLS-DESIGN.md` (as revised to depend on `rusty_tls`) and accepted it, no further changes requested. See `## Completed` above for the full account.
50. **Resolved**: the accepted `SERVER-TLS-DESIGN` is implemented — `SERVER-001` v0.9.0, `SERVER-001-FR-019`, every functional acceptance criterion verified over a real socket including the byte-recording token-secrecy test. See `## Completed` above for the full account.
51. **Resolved**: the owner's original two-option "what's next" pick (transaction benchmark, transport encryption) is fully done — see items above.
52. **Resolved**: the owner asked to check whether their own `Rusty-Mill`/`rusty_*` ecosystem already had a solution before the transport-encryption design added a fresh `rustls` dependency (see ADR-0014's revision) and, separately, asked for the first external-database comparison (`EXTERNAL-DB-BENCHMARK` above) — both done, see `## Completed`.
53. **Resolved**: the owner asked how `ProductionStore`'s own persisted file compares to SQLite's/DuckDB's portable ones, which surfaced `MmapAgeStore`'s real, latent record-identity bug (see `MMAP-AGE-STORE-IDENTITY-FIX` above) — asked to fix it, with detailed benchmark testing before merging to understand the full impact. Done: a row-oriented port regressed `scan_ages` 2.2-6.1× and reversed the just-published external-database headline finding, caught before merging via that requested before/after testing; a columnar redesign recovered nearly all of it (1.1-1.4× regression) and restored the headline finding almost exactly. See `RESULTS.md`'s `### MmapAgeStore identity-keying fix` for the complete account.
54. **Resolved**: `CI-CACHE` closed `EXTERNAL-DB-BENCHMARK`'s own DuckDB CI-build-time open question — `Swatinem/rust-cache@v2` added to `.github/workflows/ci.yml`. See `## Completed` above.
55. **Resolved**: two-hop traversal against the external engines, one of `EXTERNAL-DB-BENCHMARK`'s own open questions, is now measured — `TWO-HOP-EXTERNAL-DB-BENCHMARK` (`STORAGE-013` v0.2.0). `ProductionStore` still wins every cell; the interesting result is a real, investigated Postgres join-strategy flip (`Seq Scan`+Hash Join at 1K vs. indexed Nested Loop at 100K/1M), not just a bigger-margin restatement of the one-hop finding. See `## Completed` above and `RESULTS.md`'s `### Graph traversal, two-hop`.
57. **Resolved**: three-hop traversal against the external engines is now measured — `THREE-HOP-EXTERNAL-DB-BENCHMARK` (`STORAGE-013` v0.3.0). `ProductionStore` still wins every cell, but the margin over SQLite keeps narrowing hop over hop (161× → 27× → 18× at 1,000 records); the v0.2.0 Postgres join-strategy flip reproduced identically at this depth, confirming it as a real, repeatable planning behavior. See `## Completed` above and `RESULTS.md`'s `### Graph traversal, three-hop`.
58. **Resolved, with a genuine surprise**: four-hop traversal against the external engines is now measured — `FOUR-HOP-EXTERNAL-DB-BENCHMARK` (`STORAGE-013` v0.4.0). `ProductionStore` still wins every cell, but the "SQLite's margin narrows every hop" trend from hops 1-3 broke down at hop 4 (essentially flat at 1,000 records), and Postgres overtook SQLite as the closest external engine at 1,000,000 records for the first time — a real trend-break, reported as found rather than smoothed into the earlier narrative. See `## Completed` above and `RESULTS.md`'s `### Graph traversal, four-hop`.
59. **Resolved, and the "surprise" from item 58 didn't hold**: five-hop traversal against the external engines is now measured — `FIVE-HOP-EXTERNAL-DB-BENCHMARK` (`STORAGE-013` v0.5.0). `ProductionStore` still wins every cell. The four-hop Postgres/SQLite crossover at 1,000,000 records reversed (SQLite closest again), while a different crossover appeared at 100,000 records instead — confirmed via `EXPLAIN (ANALYZE, BUFFERS)` as a genuinely volatile per-hop I/O growth effect, not a stable trend, and not smoothed over. DuckDB's own margin against `ProductionStore` remains the one part of this arc that has narrowed cleanly at every hop so far. See `## Completed` above and `RESULTS.md`'s `### Graph traversal, five-hop`.
60. **Resolved**: `ADR-0016`'s design review is done — the owner reviewed `PRODUCTION-STORE-PORTABILITY-DESIGN.md` and accepted it as proposed, no changes requested. See `## Completed` above. No code changes yet.
61. **Resolved**: `STORAGE-014` registered and `PRODUCTION-STORE-PORTABILITY` implemented on the owner's "go now" — see `## Completed` above. `ProductionStore::open_portable(path)` exists; the `.mmap` + `.records` pair is a self-contained database.
62. **Resolved, (a) of two**: the owner chose both portability follow-ups, in order ("1 then 2"). (a), the record-blob header fingerprint, is done — `RECORD-BLOB-FINGERPRINT`, `STORAGE-014` v0.2.0, see `## Completed` above; `open`'s steady-state cost at 1M went from +27% to +0.3-4%.
63. **Resolved (design accepted)**: follow-up (b) — the identical portability treatment for `GenericMmapStore`/`GenericProductionStore` — is written up as `GENERIC-STORE-PORTABILITY-DESIGN` / `ADR-0017`, and the owner accepted it as proposed. See item 64.
64. **Resolved, (b) of two**: `STORAGE-015` registered and `GENERIC-STORE-PORTABILITY` implemented — see `## Completed` above. `GenericMmapStore::open_portable(path)` and `open_order_production_stack_portable(path)` exist; the generic `.mmap` + `.records` pair is a self-contained database, matching `ProductionStore`'s. Both halves of the owner's "1 then 2" are done.
65. **Resolved — measured and closed, not built**: the design's fallback for `open`'s fingerprint cost (a per-type trait-method fingerprint) was measured at the owner's request before being built — `GENERIC-STORE-FINGERPRINT-MEASUREMENT` in `## Completed`. The shipped fingerprint is ~4% of `open` at 1M once measured in place (the Criterion +24–27% was run-to-run drift); a whole-record trait walk saves ~7 ms per million records; the only bigger saving comes from hashing less of the record, which reopens `ADR-0016`'s silently-stale-blob gap. The owner chose to close it. Re-arms only for a record type far wider than `Order`'s ~76 B serialized (large variable-length fields), and the first step then is the same in-place A/B. Remaining open items, not queued unless the owner asks: memory/performance tradeoffs, graph-traversal follow-ups, mixed-workload open questions, durability/concurrency open questions, mTLS, certificate rotation without a restart, a multi-round-trip interactive session, crash-atomicity across a batch, `EXTERNAL-DB-BENCHMARK`'s remaining open questions (a controlled-for durability comparison across all four systems, concurrent/multi-client access to the external engines), `FIVE-HOP-EXTERNAL-DB-BENCHMARK`'s own open questions (six-or-more-hop traversal — given two demonstrated reversals in the "closest engine" question already, further extrapolation isn't safe; whether `three_hop_neighbors`/`four_hop_neighbors`/`five_hop_neighbors` belong in `bench_support` alongside `two_hop_neighbors`, pending a second real call site), and `MMAP-AGE-STORE-IDENTITY-FIX`'s remaining open question (no dedicated crash-safety/multiprocess testing against `MmapAgeStore` specifically, the columnar rewrite-on-append path untested by any benchmark since nothing exercises it) — each an owner's call on priority, not decided here.
66. **Resolved, 1 of four**: the owner queued four follow-ups ("1, 2, 3, then 4"). (1), `Symmetric` edge-list portability, is done — designed and accepted (`SYMMETRIC-EDGE-PORTABILITY-DESIGN`/`ADR-0018`), then implemented as `STORAGE-016` v0.1.0 (`SYMMETRIC-EDGE-PORTABILITY`, see `## Completed` above): `Symmetric::create`/`open`/`read_portable_edges`/`open_portable` and `open_employee_production_stack_portable(path)` exist; `<path>` + `<path>.records` + `<path>.edges` is a self-contained `Employee` stack. **Next queued: (2), the blob schema tag** — recording which `R` (and, for the edge blob, which relation) a companion blob holds, the open question `STORAGE-015` and `STORAGE-016` both defer and agree to resolve together (`STORAGE-015` v0.2.0, `GENBLOB\0` version 2, and the same treatment for `GENEDGE\0`). Then (3) multi-field mmap durability (`ADR-0006`'s revisit trigger, design-first) and (4) `bincode` encoding stability across crate versions (wire protocol and blobs).
67. **Resolved (implemented), 2 of four**: (2), the blob schema tag, is written up as `BLOB-SCHEMA-TAG-DESIGN` / `ADR-0019`, accepted as proposed by the owner, and implemented as `BLOB-SCHEMA-TAG` (`STORAGE-015` v0.2.0 / `STORAGE-016` v0.2.0, PR #108 — see `## Completed` above). Proposed shape: a `SchemaTag` trait with an associated `const SCHEMA_TAG: &'static str` on `R`, hashed (FNV-1a 64) into a new 8-byte header field at offset 20 of both `GENBLOB\0` and `GENEDGE\0` (both bumped to version 2, 28-byte header), checked between the version check and the body-fingerprint check; the alternative offered at acceptance is a readable fixed-width 32-byte tag string. The implementation matches that shape exactly; the one thing it adds beyond the design's sketch is the `TAG_OFFSET`/`TAGGED_HEADER_LEN` constants being `pub(crate)` so the edge blob's tests can build version-1 and cut-inside-the-tag images without repeating the arithmetic. Then (3) multi-field mmap durability and (4) `bincode` encoding stability, as queued in item 66.
68. **Resolved (implemented), 3 of four**: (3), multi-field mmap durability — `ADR-0006`'s named revisit trigger (`GenericMmapStore` persists exactly one `ScanMarker` field in its `.mmap` file; `STORAGE-015`'s companion blob persists the rest as an immutable snapshot, which is what makes every non-`ScanMarker` field read-only through the store) — is written up as `MULTI-FIELD-MMAP-DURABILITY-DESIGN` / `ADR-0020` and the owner accepted it as proposed (docs-only so far, no code). The design weighed a multi-slot `.mmap` layout against per-field `.mmap` files against widening the blob's role and proposes per-field files via a composable `MmapScanned` layer in the existing `GMMAPST\0` format (see "Current milestone"). The options offered at acceptance were: accept as proposed (the layer, with the `SlotFile` extraction — chosen); accept with the machinery duplicated rather than extracted; prefer the multi-slot single-file layout (option 1 — a `.mmap` format change); or close as not worth building (item 22's original posture). **Implemented as `STORAGE-017` v0.1.0 in PR #114** (see "Current milestone"). (4), `bincode` encoding stability across crate versions for the wire protocol and the three blob formats — verify and document what `bincode`'s default configuration pins today, and decide whether to pin it explicitly — is written up in item 69.
69. **Accepted as proposed, 4 of four**: (4), `bincode` encoding stability — `ADR-0010`'s named limitation and item 33's open question, raised again by the three companion blobs whose bodies are `bincode` — is written up as `BINCODE-ENCODING-STABILITY-DESIGN` / `ADR-0021` (see "Current milestone" and `## Completed` above). The first half of the owner's question is answered outright: the free functions pin `DefaultOptions::new().with_fixint_encoding().allow_trailing_bytes()` — fixint at Rust width, little-endian, no limit, trailing bytes allowed — verified from the vendored 1.3.3 source and a probe, and distinct from `bincode::options()` (varint, reject trailing). The second half is proposed as yes: a `pub(crate)` `src/codec.rs` naming that configuration (with `reject_trailing_bytes()` on decode as the one offered behavioral change), all 23 call sites routed, golden byte vectors captured on the pre-change code for primitives, every `Request`/`Response` variant, and one body each of `DOGBLOB\0`/`GENBLOB\0`/`GENEDGE\0`; writer bytes unchanged, no format bump, no dependency or public API change. Options offered at acceptance: accept as proposed (reject trailing bytes); accept with `allow_trailing_bytes()` (a purely no-behavior-change pin); document only. Also flagged: the spec's home — `STORAGE-018` (recommended, with `SERVER-001` amended to cite it) or a new category. **The owner chose accept-as-proposed and `STORAGE-018`.** The implementation unit registers `STORAGE-018` v0.1.0, captures the vectors in a commit that precedes the codec, routes the sites, and resolves `ADR-0010`, `SERVER-001`, item 33, and `ADR-0019`'s trigger by pointer. **Done in PR #120** — `BINCODE-ENCODING-STABILITY`, `STORAGE-018` v0.1.0: capture commit `4ae86bd` (+12 golden tests, pre-codec), routing commit `049e4f6` (`src/codec.rs`, all 23 sites, +5 tests including the three trailing-junk cases), then this docs commit; every golden literal unchanged across the two; one finding recorded (`DOGBLOB\0` decodes before it fingerprints, so its trailing-junk case went from silently accepted to `RecordBlobUnreadable` — the accepted narrowing, nowhere else observable). See "Current milestone" and `## Completed` above. All four of the owner's queued follow-ups are now implemented. The one follow-up the implementation opened (`DOGBLOB\0` fingerprint-before-decode) was examined and closed as not warranted in `STORAGE-018` v0.1.1 (PR #122); the remaining open questions there — wire-shape evolution, `bincode` 2.x, `Uuid` at 24 bytes — each carry a trigger and no driver.
70. **Implemented** (proposed in PR #124, accepted as proposed in PR #125, implemented in PR #126 as `SERVER-001` v0.10.0 / FR-020): wire-shape evolution — the first of those three, the owner's pick — is written up as `SERVER-PROTOCOL-VERSION-DESIGN` / `ADR-0022` (see "Current milestone" and `## Completed` above): a `PROTOCOL_VERSION` constant at 2, an optional first-frame `Hello` pair appended at index 10, `min` negotiation answered before authentication, and four append-only compatibility rules; no gating state until a gated variant exists. Acceptance options were (a) as proposed (recommended), (b) hello required, (c) rule only; the owner chose (a). Implemented in PR #126 exactly as scoped: `src/server/{protocol,mod,client}.rs`, the two golden vectors, `tests/server_protocol_version.rs`, and the pointer set (`SERVER-001` open question, `STORAGE-018`'s non-goal and trigger, `ADR-0010`, `ADR-0021`, `SERVER-QUERY-LAYER-DESIGN`, item 33); one `[[test]]` line in `Cargo.toml` is the recorded deviation from the design's "`Cargo.toml` unchanged" criterion. Two independent items the round surfaced, neither in scope: `SchemaDrivenClient` has no `authenticate(token)` and so cannot be used against an auth-configured server (pre-existing); a pre-hello server drops a hello-sending client with no reply (none deployed; named, not mitigated).
71. **Implemented** (PR #128, `SERVER-001` v0.11.0 / FR-021): the first of those two — `SchemaDrivenClient` authentication (see "Current milestone" and `## Completed` above): `connect_authenticated(addr, token)` and `authenticate(&mut self, token)`, no wire or server change, two tests appended to `tests/server_auth_integration.rs`. Not a design round: a bounded, additive completion of FR-015/FR-016, per the v0.8.0 precedent. The second item (a reconnect-without-hello fallback) stays as `ADR-0022` left it.
72. **Implemented** (PR #130, `SERVER-001` v0.12.0 / FR-022): `SchemaDrivenClient` transport encryption (see "Current milestone" and `## Completed` above) — `connect_with` + `ConnectOptions` + `ClientTlsConfig`, `ClientError::Tls`, no wire or server change, three tests appended to `tests/server_tls_integration.rs`. Not a design round: a bounded, additive completion of FR-015/FR-019, the same shape as item 71. The two remaining follow-ups item 71 left — a reconnect-without-hello fallback (`ADR-0022`'s trigger, none deployed) and backfilling the thirteen legacy "this PR" Evidence cells in `ROADMAP.md`/`TRACEABILITY.md` — the backfill taken in this PR (item 73), the reconnect fallback still open.
73. **Docs-only** (PR #132): the legacy "this PR" Evidence cells backfilled — 17 rows in `ROADMAP.md` and 16 in `TRACEABILITY.md` whose Evidence/PR cell was never replaced after merge (the count in item 72 undercounted: 13 was `ROADMAP.md`'s rows through `GENERIC-STORE-FINGERPRINT-MEASUREMENT`, before the four design rows and `TRACEABILITY.md` were tallied). Each mapped by `git log -G` on its own row to the commit that added it and the merge on `main` containing that commit: the ten bootstrap rows predate the first PR and now cite commit `5bfc8c5`; the four design rows cite both their proposal PR and their acceptance PR (`GENERIC-SCHEMA-DESIGN` #12/#17, `SERVER-QUERY-LAYER-DESIGN` #33 alone, `PRODUCTION-STORE-PORTABILITY-DESIGN` #80/#82, `GENERIC-STORE-PORTABILITY-DESIGN` #88/#90); the rest cite the one PR that merged them. No cell says "this PR" on `main` any more except the row a PR is itself adding, which its backfill PR replaces — the convention every later unit has followed.
74. **Implemented** (proposed in PR #133, accepted as proposed in PR #134, implemented in PR #135 as `SERVER-001` v0.13.0 / FR-023): mutual TLS, `ADR-0014`'s first revisit trigger and the owner's second pick, written up as `SERVER-MTLS-DESIGN` / `ADR-0023` (see "Current milestone" and `## Completed` above). The design's one real decision — what a verified client certificate means — is *admission, layered under `AuthConfig`*, because the pinned `rusty_tls` exposes no way to read the admitted certificate and this crate has no X.509 parser; the richer class-from-certificate design is named as the next revisit with this one as its compatible base. One optional input on each end, nothing else changes. Options were (a) as proposed, (b) hold for class-from-certificate, (c) close as not warranted; the owner chose (a), after the throwaway probe confirmed the mechanism (see "Current milestone"). Implemented in PR #135 exactly as accepted — see "Current milestone" — with no deviation from the design and every acceptance criterion holding. Still queued after it: the transaction-session design round and the reconnect-without-hello fallback.
75. **Accepted as proposed, both parts** (proposed in PR #137, accepted in PR #138): the transaction-session / crash-atomicity design round, the owner's third pick, written up as `SERVER-TRANSACTION-SESSION-DESIGN` / `ADR-0024` / `ADR-0025` (see "Current milestone" and `## Completed` above). Two decisions, each acceptable alone: a *buffered* session (the multi-round-trip shape with no lock held between round trips — `ADR-0013`'s liveness objection dissolves rather than being managed), which is also the first version-3 protocol change `ADR-0022` named as its trigger; and a redo journal for batches, small because redo suffices under this crate's idempotent-overwrite invariant, placed at the domain adapter because that is the only layer that can replay an operation. Options per ADR were `ADR-0024`: (a) as proposed, (b) lock-held with timeout, (c) close; `ADR-0025`: (a) as proposed, opt-in, (b) always-on, (c) close; the owner chose (a) for both. Implementation follows as two units, `SERVER-001` v0.14.0 / FR-024 then v0.15.0 / FR-025. Still queued after them: the reconnect-without-hello fallback.
76. **Implemented** (PR #139, `SERVER-001` v0.14.0 / FR-024): Part A of item 75 — transaction sessions at protocol version 3 (see "Current milestone" and `## Completed` above), exactly as accepted, no deviation. The one finding worth a line: `ADR-0022`'s prediction of what the first version-3 variant would need — the negotiated version kept per connection and a branch for rule 3 — was exactly right, and cost eleven lines in `handle_connection`; the client's rule-4 gate is one comparison. Part B (`ADR-0025`, the redo journal, `SERVER-001` v0.15.0 / FR-025) is next; then the reconnect-without-hello fallback.
77. **Implemented** (PR #141, `SERVER-001` v0.15.0 / FR-025): Part B of item 75 — crash-atomic batches via an opt-in redo journal (see "Current milestone" and `## Completed` above). Two things worth a line. First, the design's file-copy replay pair worked as the crash test exactly as written: replaying a journal onto pre-batch files yields the post-batch state, replaying it onto post-batch files is a no-op, and since each slot is independent that covers every intermediate state without a crash-injection hook. Second, the one wire addition forced the protocol to version 4: `ADR-0025` said `ErrorCode::Journal` would be "the next index, version-gated like any appended code", and rule 2 makes that a bump — so `downgrade_for_version` is rule 3's first real use of "nearest older shape" (a v1–v3 connection sees `Unsupported`). Recorded as a clarification, not a deviation. The cost is real and now measured: one `fsync` per batch, in `RESULTS.md`. Next: the reconnect-without-hello fallback (item 72's last remaining follow-up).
78. **Implemented** (PR #143, `SERVER-001` v0.16.0 / FR-026): the reconnect-without-hello fallback, default-on (see "Current milestone" and `## Completed` above). The recommendation was to close it as not warranted — the design's own stance, since no pre-hello server is deployed — with opt-in as the middle option; the owner chose default-on, and the record says so plainly rather than dressing the choice up as a finding. What made it safe enough to ship that way: the heuristic answers exactly one error shape under exactly one frame, at most once, and a second failure is reported as is, so the worst case for a genuinely dying server is one extra connect, not a masked fault; `require_hello()` exists for callers who want the old EOF. With this the four-item queue from item 72 onward is empty, and `ADR-0022` has one trigger left armed: a change that cannot be expressed by appending.
79. **Accepted as proposed** (proposed in PR #145, accepted in PR #153): the journal group-commit design round, the first of the owner's next four picks ("1, 2, 3, 4"), written up as `SERVER-JOURNAL-GROUP-COMMIT-DESIGN` / `ADR-0026` (see "Current milestone" and `## Completed` above). What made it a design round rather than a one-line change: taking the `fsync` out of the exclusive section means concurrent batches, and a concurrent journal needs one guarantee the serialized one got for free — that the order the journal replays is the order the store applied — plus a checkpoint that cannot truncate an appended-but-unapplied batch. Both are in the design as requirements with tests, not as assumptions. The mechanism itself was run before it was proposed: a throwaway probe on this container's storage puts per-batch `fsync` flat at ~4.6k/s and leader/follower group commit at ~29k/s at 64 threads, the lone thread unchanged; a fixed commit delay — the knob PostgreSQL and MySQL both expose — bought group size but no throughput here, so it is offered as option (b) and recommended against. Options: (a) accept as proposed, (b) with a fixed commit delay, (c) close as not warranted; implementation, if accepted, is `SERVER-001` v0.17.0 / FR-027 as its own unit. Still queued after it: read-your-writes in sessions, class-from-certificate for mTLS, the auth audit-log design round.
80. **Accepted as proposed** (proposed in PR #147, accepted in PR #153): the session read-your-writes design round, the second of the owner's four picks, written up as `SERVER-SESSION-READ-YOUR-WRITES-DESIGN` / `ADR-0027` (see "Current milestone" and `## Completed` above). It was listed as a bounded, no-ADR change; it is not one, and the record says so: `ADR-0024` named the overlay "a real design with a cost on every read," and `SESS-FR-007` is a numbered requirement whose meaning would change under an unchanged wire shape — the one kind of change `ADR-0022`'s rules have nothing to gate. So the design makes the mode something a client asks for (an appended `BeginWith { flags }` at protocol 5) rather than something that happens to it, and limits the overlay to the one read that can be overlaid exactly (`GetById`), with the edge cases — a missing id, an absent field, a kind mismatch — defined so a write that will fail at `Commit` never produces a read that says otherwise. The finding worth a line: through the library no read at all is possible while a session is open (the `Session` borrows the client mutably), so `Session::get` is part of the design, and useful on a plain session too. Options: (a) accept as proposed, (b) with `FilterEq` adjusted too, (c) close; implementation, if accepted, takes `SERVER-001`'s next minor / FR as its own unit. Still queued after it: class-from-certificate for mTLS, the auth audit-log design round.
81. **Accepted as proposed** (proposed in PR #149, accepted in PR #153): the class-from-certificate design round, the third of the owner's four picks, written up as `SERVER-MTLS-CLASS-DESIGN` / `ADR-0028` (see "Current milestone" and `## Completed` above). Two things worth a line. First, the "X.509 reading decision" `ADR-0023` deferred turned out not to need X.509 at all: pinning the presented leaf's exact DER bytes to a class needs no parser, no dependency, and is the strongest match there is — the cost is manual rotation, which is what the PEM-file list is for, and SPKI pinning is offered as option (b) for anyone who would rather pay in X.509 code. Second, the upstream half was verified before being asked for: the pinned `rusty_tls` already has the client-side accessor and `complete_handshake` on both streams, so the server-side accessor is an eight-line mirror, run against this crate through a throwaway local `[patch]`. The probe also settled `ADR-0023`'s typed-client-error trigger for now — under TLS 1.3 the rejection arrives after the client's `Finished`, so an eager client handshake does not surface it; the server side does get a typed reason, which the audit-log round can use. The constraint: this session was not permitted to attach the upstream repository, so the `rusty_tls` PR — the implementation unit's first step — waits on that permission or on the owner applying the patch. Options: (a) accept as proposed, (b) with SPKI pinning, (c) close. Still queued after it: the auth audit-log design round.
82. **Accepted as proposed** (proposed in PR #151, accepted in PR #153): the authentication audit-log design round, the last of the owner's four picks, written up as `SERVER-AUTH-AUDIT-DESIGN` / `ADR-0029` (see "Current milestone" and `## Completed` above). Three decisions made plainly rather than assumed: *what* is recorded (the gates' decisions and nothing else — the secrecy rule is a test over every variant's line, not a comment), *where* the sink hangs (on `AuthConfig`, the policy object every gate already consults, so `serve` keeps its four parameters — `ADR-0025`'s precedent), and *what happens when the sink fails* (the server keeps serving and says so once; fail-closed is offered as option (b) because it is a real posture with a real cost). The one mechanism the server lacked — a reason for a rejected handshake — is `ADR-0028`'s eager handshake, adopted by whichever of the two lands first. Rate limiting and lockout, the other two thirds of the gap `ADR-0012` named, stay named — the log is what they would be tuned from. With this the four-pick queue is fully proposed and nothing is implemented: `ADR-0026`, `ADR-0027`, `ADR-0028`, and `ADR-0029` each waited on the owner's (a)/(b)/(c) and each got (a) in PR #153 ("a, a, a, a"). Implementation order: `ADR-0026`, `ADR-0027`, `ADR-0029`; `ADR-0028`'s crate-side unit additionally waits on the upstream `rusty_tls` PR this session could not open.
83. **Implemented** (PR #155, `SERVER-001` v0.17.0 / FR-027): the journal group commit, `ADR-0026` (see "Current milestone" and `## Completed` above), implemented exactly as accepted with no deviation. Two things worth a line. First, the design's one unverified claim — that ordered apply and a quiescent checkpoint preserve `ADR-0025`'s invariants under concurrency — is now a test rather than an argument: eight threads commit through one `CommitGroup` and the order their applies ran in is compared, element for element, with the order the journal file replays; through the server, a journal copy replayed onto pristine files equals the live state. Second, the hook the design asked for turned out to belong *before* the leader reads how far to sync, not before the `sync_data` itself — the slot a commit delay (option (b), declined) would occupy — which is what makes "three batches, one sync" literally true and, in passing, why reading the range as late as possible is the right discipline. The number: 15,322 batches/s at 64 connections where v0.15.0 measured 3,337, the lone connection unchanged. Next: `ADR-0027`, then `ADR-0029`.
84. **Implemented** (PR #157, `SERVER-001` v0.18.0 / FR-028): read-your-writes sessions, `ADR-0027` (see "Current milestone" and `## Completed` above), implemented as accepted with one clarification. The design's overlay rule — a staged write shows only if the field is in the record and the kinds match — had a hole the first integration test found: `Dog::breed` is a `Str` field the record carries, so a staged breed passed both checks and would have been shown, then refused at `Commit`. The overlay now also consults the schema's updatable tags, read once at `BeginWith` and kept with the session, which is the same reason the kind rule exists and costs one `describe()` per read-your-writes session, not per read. Recorded as a clarification rather than a deviation because it tightens the design's own stated guarantee — a write `Commit` would refuse is never shown — rather than changing it. Everything else landed as written: the appended variant and version bump per `ADR-0022`'s rules (the unknown-index probe moved for the second time), the pure overlay, `Session::get` on every session. Next: `ADR-0029` (audit log); `ADR-0028` waits on the upstream `rusty_tls` accessor.
85. **Implemented** (PR #159, `SERVER-001` v0.19.0 / FR-029): the authentication audit log, `ADR-0029` (see "Current milestone" and `## Completed` above), implemented as accepted with no deviation. Two things worth a line. First, "exactly one `Disconnected` per admitted connection" is enforced by a drop guard rather than by every `return` remembering to record — the loop has eight exit paths, and a guard makes the property structural. Second, the eager TLS handshake the design shared with `ADR-0028` landed here first; `ADR-0028`'s crate-side unit will find `CLS-FR-002` already done and only the peer-certificate read left, once the upstream accessor exists. The secrecy rule is a test, not a comment: every variant's line is checked for a known token and a known certificate string, and the event types have no field a secret could ride in. With this, three of the four accepted designs ("a, a, a, a") are implemented; the fourth waits upstream.
86. **Proposed** (PR #161): the rate-limiting / lockout design round, the second of the owner's next four picks, written up as `SERVER-AUTH-RATE-LIMIT-DESIGN` / `ADR-0030` (see "Current milestone" and `## Completed` above). The one real decision is asymmetry: the lockout is on by default because it costs a legitimate client nothing and closes the single-connection brute force on every server whether or not the operator read the docs; the per-peer budget is opt-in because it needs a window, shared state, and an answer to NAT, and those are the operator's to weigh. The second decision is silence: a throttled attempt is `Unauthenticated`, not a typed error, because the moment the limiter engages is the one thing a limiter should not announce — the same rule `SERVER-AUTH-DESIGN` set for wrong-token versus no-token. What the design refuses is worth a line too: no delays (a held thread is a denial-of-service lever), no per-token budget (a denial-of-service against the token's holders), no accept-level flood control (a different owner, named as the first revisit trigger). Options: (a) accept as proposed, (b) both opt-in with a typed error at protocol 6, (c) close. Still queued after it: stage-time validation in sessions, the access-logging design round; the upstream accessor is with a child session.
87. **Implemented** (PR #163, `SERVER-001` v0.20.0 / FR-030): stage-time validation in sessions, the third of the owner's next four picks (see "Current milestone" and `## Completed` above), as the bounded additive completion two accepted decisions already described — `ADR-0024` named the `ConnectionStore` hook, `ADR-0027` named the second `BeginWith` bit — so no new ADR, the v0.8.0 precedent. Two things worth a line. First, the versioning: a flag bit is a new *meaning* inside an existing variant, which `ADR-0022`'s rules did not literally cover; the honest reading is that a bit is introduced at a version exactly as a variant is — unknown (`Malformed`) below it, sent only after negotiating it — so `PROTOCOL_VERSION` is 6 with no new variant, and the clarification is recorded against `ADR-0022` and its design doc rather than assumed. Second, the hook's contract: `validate_op` is `validate_batch` on one operation with the journaled path's per-call existence read, so `Ok` at stage time is `Ok` at `Commit` by the same no-runtime-deletion invariant everything since `ADR-0013` rests on — and `Commit` still validates the whole batch, because that is the store's rule, not the session's. Also recorded: the upstream `rusty_tls` accessor for `ADR-0028` is PR #148 on `Rusty-Mill/rusty_mill`, opened by a child session and left for the owner to merge. Still queued: the access-logging design round.

88. **Proposed** (PR #165): the access-logging design round, the last of the owner's four next picks, written up as `SERVER-ACCESS-LOG-DESIGN` / `ADR-0031` (see "Current milestone" and `## Completed` above). The design's whole point is decoupling: `SERVER-AUTH-AUDIT-DESIGN` deliberately left "a second sink or a second event family" open rather than default to the cheaper-looking option (one more `AuditKind` variant), because folding the two together makes an operator's audit-trail decision also an access-log-volume decision, and the audit design's own framing is that most operators want the former without the latter. So this proposes the more expensive-looking option on purpose: a second module, a second trait, a second config method, in exchange for the two switches never coupling. What is logged is deliberately thin — kind and outcome shape, reusing `audit.rs`'s own `RequestKind` rather than inventing a second vocabulary — with a per-record trail and a session correlation id both named as real, larger, separately-privacy-reviewed features rather than folded in. Options: (a) accept as proposed, (b) fold into `AuditKind` instead, (c) close. With this the owner's four-item round ("1, 2, 3, 4") is fully accounted for: `ADR-0030` and `ADR-0031` await acceptance, stage-time validation is implemented, and the upstream `rusty_tls` PR #148 awaits merge.

89. **Accepted as proposed, both** (proposed in PR #161 and PR #165, accepted in PR #166): the rate-limiting/lockout and access-logging design rounds, the owner answering "accept as proposed on 2 and 4" — `ADR-0030` and `ADR-0031` each option (a), no changes requested. Both stay design-only here; implementation for each follows as its own unit, `SERVER-001`'s next minor / FR apiece. With this every one of the owner's second four-item round ("1, 2, 3, 4") is accounted for: item 87 implemented stage-time validation, both design rounds are now accepted, and the upstream `rusty_tls` accessor (PR #148 on `Rusty-Mill/rusty_mill`) is with a merge session, no longer only proposed-and-waiting.

90. **Implemented** (PR #166, `SERVER-001` v0.21.0 / FR-031): class from certificate, `ADR-0028` — the last of the owner's *first* four-item round to land, once its upstream prerequisite cleared. The upstream `TlsServerStream::peer_certificate_der` accessor merged as `Rusty-Mill/rusty_mill` PR #148 (a separately-spawned session opened it exactly to the design's verbatim patch; the owner merged it directly, without this session's involvement) and `Cargo.toml`'s `rusty_tls` rev moved to that commit — `cargo tree` unchanged in package names, so no new dependency, just five already-present transitive crates now sourced from the merged monorepo at newer versions. The crate-side change is exactly as designed: `AuthConfig` gains an exact-DER certificate-class map (`with_certificate_class`, `with_certificate_class_pem_file`, `class_for_certificate`); `handle_connection`'s TLS arm — already eager since v0.19.0, `ADR-0029`'s `AUD-FR-005`/this design's `CLS-FR-002` sharing one mechanism — now also reads the presented leaf and seeds the connection's starting class from a match, a later valid `Authenticate` still replacing it, an invalid one leaving it as it was; `is_configured()` now also considers certificate classes, so a certificates-only deployment starts an unclassed admitted leaf unauthenticated rather than `ReadWrite` (the safe direction the design named); `dog_server` reads `SERVER_AUTH_READ_ONLY_CLIENT_CERTS`/`SERVER_AUTH_READ_WRITE_CLIENT_CERTS`, refusing either without `SERVER_TLS_CLIENT_CA_PATH`. No wire, `PROTOCOL_VERSION`, `TokenClass`, `TlsConfig`, `ConnectionStore`, or store change; no deviation from the design. Three unit tests, one binary test, five integration tests. With this, both of the owner's four-item rounds are now fully implemented or (for `ADR-0030`/`ADR-0031`) accepted and queued — `ADR-0028` was the one item left waiting on something outside this session's control.

91. **Implemented** (this PR, `SERVER-001` v0.22.0 / FR-032): rate limiting and lockout, `ADR-0030` — the owner's "Start Unit 27" after accepting it. Two mechanisms, exactly as designed. A per-connection lockout — `MAX_AUTH_FAILURES = 5`, on by default, a counter never reset by a success — closes the connection after its fifth failed `Authenticate`'s response is sent, recorded `LockedOut { failures }`. An opt-in per-peer budget, `AuthConfig::with_rate_limit`, backed by a `FailureTable` (`Mutex<HashMap<IpAddr, Window>>`, monotonic `Instant`s, bounded at `MAX_TRACKED_PEERS = 4096` by expired-then-oldest eviction on insert, fail-open on a poisoned mutex) refuses an over-budget peer's `Authenticate` before any comparison, recorded `Throttled { failures }`, and still counts toward the per-connection lockout. `AuditKind` gained the two variants and, with `RequestKind`, is now `#[non_exhaustive]` — the clarification `ADR-0030` itself named against `ADR-0029`. Neither mechanism changes the wire: a throttled or locked-out attempt is `Unauthenticated`, indistinguishable from a wrong token. `dog_server` reads `SERVER_AUTH_RATE_LIMIT="<failures>/<seconds>"` through a factored, unit-tested decision table matching every other env-var wiring in that binary. Five unit tests, one binary test, three integration tests; every acceptance criterion 1–8 holds; no deviation. With this, `ADR-0030` is implemented; `ADR-0031` (the access log) is the one remaining accepted-but-unimplemented item from the owner's second four-item round.

92. **Implemented** (this PR, `SERVER-001` v0.23.0 / FR-033): per-request access log, `ADR-0031` — the owner's "Start Unit 28" after accepting it, immediately following Unit 27 on the same still-open PR. A second, independent sink family, exactly as designed: `src/server/access.rs` (new, public) defines `AccessEvent { at, peer, class, request: RequestKind, outcome: Outcome }`, `Outcome::{Ok, Err(ErrorCode)}`, `AccessSink`, `NoAccessLog` (default), `StderrAccessLog`, `FileAccessLog` — the same fail-open shapes `audit.rs` already has, deliberately not sharing a type with it so the two switches never couple. `AuthConfig::with_access_log`/`access_log()` hang it beside `audit`/`rate_limit`, `serve`'s signature unchanged. `handle_connection` records exactly one event per **dispatched request** — everything reaching the final request-dispatch match, including session-management errors — after the response is decided and outside every lock; `Hello`, `Authenticate`, and every gate-refused request stay the audit log's exclusive territory, so the two streams are disjoint by construction, proved by one integration test running both collecting sinks on the same server. `dog_server` reads `SERVER_ACCESS_LOG` (`stderr` \| path) independent of `SERVER_AUDIT_LOG`, through a factored, unit-tested `access_sink_from` matching every other env-var wiring in that binary. Three unit tests, one binary test, one integration test; every acceptance criterion 1–5 holds; no deviation. With this, both `ADR-0030` and `ADR-0031` — the owner's entire second four-item round — are implemented.

93. **Implemented** (this PR, `SERVER-001` v0.24.0 / FR-034): `AuditKind::Admitted` gains `classed_by_certificate: bool` — the owner's "Start Unit 29" after choosing it over a `ServeOptions` consolidation round. `ADR-0029`'s fourth revisit trigger (*`ADR-0028` lands — `Admitted` may gain an explicit "classed by certificate" field*), taken now that `ADR-0028` (class from certificate, FR-031) has landed, as a bounded additive completion — no new design doc or ADR, the v0.8.0/v0.16.0/v0.20.0 precedent. `true` exactly when the connection's starting class came from a matched, configured certificate (`CLS-FR-004`'s precedence match); always `false` when `initial_class` is `None`, since a classing certificate always sets it. `handle_connection` passes `certificate_class.is_some()` — the value the TLS arm already computes for the precedence match, nothing new derived. One test appended (`admitted_records_classed_by_certificate_for_a_matched_leaf_only`, driving a classed and an unclassed leaf against one collecting audit sink); every pre-existing `Admitted` literal across `src/server/audit.rs` and both integration test files updated to the new shape. No new `Request`, `Response`, `ErrorCode`, wire, `PROTOCOL_VERSION`, `Cargo.toml`, store, or `serve`-signature change.

94. **Proposed** (this PR): the `ServeOptions` consolidation round, the owner's "Start the ServeOptions consolidation round" over a smaller bounded completion — written up as `SERVER-SERVE-OPTIONS-DESIGN` / `ADR-0032`. The trigger four ADRs (`ADR-0029` ×2, `ADR-0030`, `ADR-0031`) named and each deferred, evaluated directly for the first time: `AuthConfig` now carries five independent concerns (tokens, certificate-derived classes, audit, rate limit, access log) under a name describing only the first, while `TlsConfig` has sat outside it as `serve`'s own second parameter since v0.9.0. Proposes a straight consolidation — `AuthConfig` renamed to `ServeOptions`, `TlsConfig` folded in as one more field, `serve` drops from four parameters to three — with every existing builder method's name and behavior unchanged, one new `with_tls`, and TLS construction kept its own separately-fallible step composed via `.with_tls(..)` rather than folded into one fallible `from_env`. No new capability anywhere: `SRV-FR-006` requires the change be behavior-preserving bit for bit, verified by the existing ~85-call-site test suite passing with a mechanical rename and zero assertion changes. A blast radius honestly named up front (`src/bin/dog_server.rs`, `benches/server.rs`, eight test files), but bounded and internal-only — this crate is not published, so every caller is enumerable and updated in the same unit were this accepted. One factual correction made mid-draft, recorded rather than smoothed over: `TlsConfig` does not currently derive `Clone` (checked directly, not assumed), though its fields could support it if the implementation unit judges the derive worth keeping on `ServeOptions` — `serve` itself needs no `Clone` bound either way, since it moves the value once into `Arc::new`. Options: (a) accept as proposed, (b) consolidate the five `AuthConfig` concerns but leave `TlsConfig` separate, (c) close as not yet warranted.

95. **Accepted as proposed** (proposed in PR #169, accepted in PR #170): the `ServeOptions` consolidation round, the owner answering "Accept as proposed" — `ADR-0032` option (a), no changes requested. Stays design-only here; implementation follows as its own unit, `SERVER-001`'s next minor / FR — the ~85-call-site rename `AuthConfig` → `ServeOptions`, `TlsConfig` folded in, `serve` dropping from four parameters to three, every existing builder method's name and behavior unchanged.

96. **Implemented** (this PR, `SERVER-001` v0.25.0 / FR-035): the `ServeOptions` consolidation, `ADR-0032` — the owner's "Start Unit 31: implement ADR-0032" after accepting it. A straight rename, exactly as designed: `AuthConfig` renamed `ServeOptions` throughout `src/server/mod.rs`; the type gains one field, `tls: Option<TlsConfig>`, with `with_tls`/`tls()` in the same builder shape every other opt-in concern already has; `TlsConfig` construction stays its own separately-fallible step (`ServeOptions::new`/`from_env` unchanged, still infallible), folded in via `.with_tls(..)` after the caller handles its `Result` — the same two-step pattern `dog_server.rs`'s `main` already used for `SERVER_AUDIT_LOG`/`SERVER_AUTH_RATE_LIMIT`/`SERVER_ACCESS_LOG`. `serve` drops from four parameters to three; `handle_connection` takes one `options: &ServeOptions`, reading `let tls = options.tls();` in place of its former second parameter — every downstream `match tls { .. }` unchanged under Rust's match ergonomics. `ServeOptions` drops its `Clone` derive (no call site anywhere cloned a value of this type, checked directly rather than assumed; `TlsConfig` did not implement `Clone` either, and `serve` needs no `Clone` bound — it moves the value once into `Arc::new`). Every internal caller updated mechanically across all sixteen touched files — `src/bin/dog_server.rs`, `benches/server.rs`, eight test files — with a stray "an `ServeOptions`" grammar artifact from the blind rename caught and fixed along the way. Every existing test passes with zero assertion changes (`SRV-FR-006`); every acceptance criterion 1–5 holds; no deviation. No `Cargo.toml`, wire, `PROTOCOL_VERSION`, or store change. With this, `ADR-0032` is implemented — the trigger four ADRs named and each deferred is finally closed.

97. **Docs-only** (PR #172): `benches/server.rs` re-run — the owner's "Re-run the benchmarks and update RESULTS.md" — the first re-run since the v0.17.0 group-commit pass, covering eight versions of growth (v0.18.0 read-your-writes through v0.25.0's `ServeOptions` consolidation) none of which was expected to touch the `GetById`/`Transaction` hot path. No regression found: `dog-jrnl-txn` still climbs with concurrency at the same ~4.5–4.6× ratio v0.17.0 measured, single-connection journaled latency is still one `fsync`'s worth above unjournaled at a matching ratio, and `order-txn`/`employee-txn` — previously only asserted in prose, never tabulated — land in the same band as every other row, confirming once more that the network/dispatch layer dominates at this record-count scale, not the domain or the transaction/journal machinery behind it. Run on this session's own container (not `baileyai`; scope deliberately limited to `benches/server.rs`, the one subsystem that's actually changed — the storage-layer suites `## Concurrency`/cache-miss sections require real hardware for their own claims, unchanged and not re-run). See `RESULTS.md`'s new `#### Regression check through v0.25.0` subsection. No code change.

98. **Proposed** (PR #173): the session snapshot isolation design round, the owner's "Future Growth here we come" → "Real transactions" pick from `docs/FUTURE-GROWTH.md` — written up as `SERVER-SESSION-SNAPSHOT-ISOLATION-DESIGN` / `ADR-0033`. Read literally, most of `docs/FUTURE-GROWTH.md`'s own "Transactions" framing has already closed since it was written — `Request::Transaction`/sessions give real N-operation atomicity, the journal and group commit give it crash-safety, read-your-writes lets a session see its own pending writes. What is left, and unaddressed by any accepted design: `SESS-FR-007`'s second half, deliberately left standing by `ADR-0027` — a session's reads have no protection from what another connection commits in between. Proposes optimistic read-set validation, not full MVCC: a third `BeginWith` bit records every session `GetById`'s raw committed value (before any read-your-writes overlay) into a bounded per-session read set; at `Commit`, inside the exact same exclusive section the batch's own write validation and apply already use, the adapter re-checks every tracked key and refuses the whole commit atomically on any mismatch. Real MVCC — versioned records, a snapshot pointer, garbage collection — is named explicitly as the disproportionate, `src/store/**`-touching "multi-year effort" `docs/FUTURE-GROWTH.md` itself describes, rejected this round on proportionality, not capability. Also rejected: holding a read lock for the session's lifetime (blocks every writer for as long as any session stays open — exactly what `ADR-0024` designed the whole session mechanism to avoid). Named plainly as non-goals, not glossed over: no phantom-read protection, no `FilterEq`/`ScanField` isolation. Options: (a) accept as proposed, (b) track only the single most recent read instead of a full read set, (c) close as not yet warranted.

99. **Accepted as proposed** (proposed in PR #173, accepted in PR #174): the session snapshot isolation design round, the owner answering "Accept as proposed" — `ADR-0033` option (a), no changes requested. Stays design-only here; implementation follows as its own unit, `SERVER-001`'s next minor / FR — a third `BeginWith` bit, optimistic read-set validation checked and applied atomically at `Commit`, `PROTOCOL_VERSION` moving to 7.

100. **Implemented** (this PR, `SERVER-001` v0.26.0 / FR-036): session snapshot isolation, `ADR-0033` — the owner's "Start Unit 33: implement ADR-0033" after accepting it. Exactly as designed: a third `BeginWith` bit, `SESSION_SNAPSHOT_ISOLATION = 4`, opens at protocol version 7 (`PROTOCOL_VERSION` moved to 7, the table gains row 7, `ErrorCode::Conflict` at index 10 downgraded to `Unsupported` below 7). While such a session is open, every `GetById` records the raw, pre-overlay committed value it returned into a per-connection read set (`record_read_set`, keyed by `(id, field)`, the most recent read replacing the earlier one; only a found record is tracked, `ISO-FR-005`; bounded at `MAX_TRACKED_READS = 4096`, past which a *new* key is simply not added while an already-tracked one keeps updating, `ISO-FR-004`). `ConnectionStore::apply_transaction` gains a `read_set` parameter, empty whenever the bit is off; each of the three adapters re-checks every tracked key inside the exact same `with_exclusive`/`with_journal` section its own write validation and apply already use, refusing the whole commit atomically with `(0, ErrorCode::Conflict)` on any mismatch — the same sentinel-index shape `ErrorCode::Journal` established, and verified atomic with the write side directly: a batch whose every write is itself valid still refuses whole on a stale read, including one touching a record the batch never writes at all. All three `BeginWith` bits compose independently, proved by a combined read-your-writes-plus-snapshot-isolation test on one session. Client: `SessionOptions::snapshot_isolation()`/`Session::snapshot_isolation()`, gated at version 7 like the other two options. Nine new tests (three unit tests for `record_read_set`'s repeated-key/dual-key/cap-degradation behavior, one adapter unit test for `apply_transaction`'s atomicity, five integration tests for the conflict/non-conflict/`NotFound`-untracked/composed/version-gate cases) plus two pre-existing pinned-version-literal fixes (`tests/server_schema_driven_client.rs`, and an "unknown flag" test in `tests/server_transaction_integration.rs` whose placeholder bit value — 4 — the new real bit now claims, moved to a still-unused bit); every acceptance criterion 1–8 holds; no deviation. No `Cargo.toml`, store (`src/store/**`), or `serve`-signature change. With this, `ADR-0033` is implemented — `SESS-FR-007`'s second half, left standing since `ADR-0027`, is closed.

101. **Proposed** (PR #176): the SQL `SELECT` subset design round, the owner's "Future Growth here we come" → "SQL" pick from `docs/FUTURE-GROWTH.md` — written up as `SERVER-SQL-SELECT-DESIGN` / `ADR-0034`. Read literally, that document names the full thing (a parser, a query planner, a cost-based optimizer, an execution engine) as "roughly a multi-year effort on its own" — the same scale of claim `ADR-0033`'s own round found for full MVCC, narrowed the identical way: a bounded, read-only `SELECT` subset, a real tokenizer/parser (not a renamed structured filter) living entirely client-side in `SchemaDrivenClient`, compiled to a new `Request::Query`/`Response::Rows` wire pair and a new full-table-scan primitive. Two things verified directly before proposing, not assumed: neither `DogStore` nor `src/generic/query.rs` exposes an "every id"/"every record" primitive today (`FilterEq` needs an index, `ScanField` returns values with no id attached, `GetById` answers one known id) — yet both `MmapAgeStore` and `GenericMmapStore` already hold every record in a `HashMap` keyed by id, so the new primitive (`ConnectionStore::scan_all`, `crate::generic::query`'s new `AllIds<R>` forwarded through `Reversed`/`Symmetric` exactly as every other query trait already is) is genuinely additive and touches none of `src/store/**`'s four `research`-gated backends, none of which is ever wrapped by a `ConnectionStore` adapter. Filtering/projection/`LIMIT` is one new function, applied identically across all three domains, not duplicated per adapter. The real cost named plainly, not hidden: every `Query` is an unconditional full scan — no planner, no index consultation, even where `GetById`/`FilterEq` could answer a predicate far more cheaply; that gap is explicitly the next question, not solved here. Every described field becomes queryable regardless of its `filter_eq`/`scan`/`update` capability flags, since a full scan needs no index for any of them — named as a deliberate, honest widening. No new `ErrorCode` (`UnknownField`/`Malformed` cover every rejection); not overlaid by read-your-writes, not snapshot-isolation-tracked — the same "only `GetById`" line `RYW-FR`/`ISO-FR-002` already draw, for the identical reason. Options: (a) accept as proposed, (b) skip the parser and ship only a structured compound filter (smaller, but not literally SQL), (c) close as not yet warranted.

102. **Accepted as proposed** (proposed and accepted in PR #176): the SQL `SELECT` subset design round, the owner answering "Accept as proposed" — `ADR-0034` option (a), no changes requested. Stays design-only here; implementation follows as its own unit, `SERVER-001`'s next minor / FR — a real client-side SQL `SELECT` subset (tokenizer, recursive-descent parser, `SchemaDrivenClient::query(sql)`), `Request::Query`/`Response::Rows` at protocol version 8, `ConnectionStore::scan_all` and a new `crate::generic::query::AllIds<R>` trait forwarded through `Reversed`/`Symmetric`.

103. **Implemented** (this PR, `SERVER-001` v0.27.0 / FR-037): the SQL `SELECT` subset, `ADR-0034` — the owner's "Start Unit 35: implement ADR-0034" after accepting it. Exactly as designed: `Request::Query { select, filter, limit }`/`Response::Rows { rows }` at protocol version 8 (`PROTOCOL_VERSION` moved to 8, the table gains row 8); `Selection::All`/`Fields(Vec<FieldRef>)`; `Predicate { field, op: CompareOp, value: ScanValue }`, `CompareOp::{Eq,Ne,Lt,Le,Gt,Ge}` with `is_ordering()`. `ConnectionStore::scan_all` is backed by a new `AllIds`/`AllIds<R>` trait, added to `crate::production` (mirroring `TransactionalStore`'s own small, server-facing-only shape) and `crate::generic::query` (mirroring `GetById`/`FilterEq`/`ScanField`'s own per-capability-trait pattern), forwarded through `Reversed`/`Symmetric` and — discovered mid-implementation, since `OrderProductionStack` actually wraps a third composition layer this session's earlier read had missed — `MmapScanned` too. `validate_query`/`evaluate_query` (`src/server/mod.rs`) are the one pure-function pair doing every domain's filter/project/limit work, never duplicated per adapter; every schema-described field is queryable regardless of its `filter_eq`/`scan`/`update` flags, `Dog::breed` (every flag `false`) the concrete test. No new `ErrorCode` — `UnknownField`/`Malformed` cover every rejection shape. The server never sees SQL text: `src/server/sql.rs` (new) tokenizes and parses a real recursive-descent grammar entirely client-side, against the already-discovered `DomainSchema`; `SchemaDrivenClient::query`/`Session::query` are the only entry points, gated to protocol version 8 (`ClientError::Unsupported("sql query")` with no frame sent below it). `Query` is never overlaid by read-your-writes and never snapshot-isolation read-set tracked, the same "only `GetById`" line `RYW-FR`/`ISO-FR-002` already draw. Six unit tests in `src/server/mod.rs`, eleven in `src/server/sql.rs`, two golden vectors (which empirically revealed `Option<T>`'s one-byte, not four-byte, bincode discriminant — the first `Option` on this wire), one adapter unit test in `dog.rs`, fourteen integration tests in `tests/server_sql_integration.rs` (new) covering all three domains, every comparator, `LIMIT`, every client-side error shape, the version-8 gate, and both session-isolation interactions; every acceptance criterion 1–10 holds; no deviation. No `Cargo.toml`, store (`src/store/**`), or `serve`-signature change. With this, `ADR-0034` is implemented — `docs/FUTURE-GROWTH.md`'s "SQL" item is narrowed and closed to this bounded slice, a full parser/planner/optimizer restated as the real, disproportionate, still-open answer.

104. **Proposed** (PR #178): the `GROUP BY`/aggregation design round, the owner's "Future Growth here we come" → "Aggregation / GROUP BY" pick from `docs/FUTURE-GROWTH.md` — written up as `SERVER-SQL-AGGREGATE-DESIGN` / `ADR-0035`. `docs/FUTURE-GROWTH.md` bundles this with "multi-table joins" under one bullet; this round narrows to aggregation alone, leaving joins as that document's own separate, still-open "big three" item — the identical narrowing move `ADR-0034` itself made against a larger bundled ask. Proposed shape: extends `Request::Query` (`ADR-0034`) rather than duplicating it — `GROUP BY <field>(, <field>)*` and `SELECT`-list aggregate calls (`COUNT(*)`, `SUM`/`AVG`/`MIN`/`MAX(<field>)`) parsed client-side by extending `src/server/sql.rs`'s existing grammar, compiled to a new `Request::Aggregate`/`Response::Groups` wire pair at protocol version 9, reusing `Predicate`/`CompareOp` for `WHERE` unchanged. Needs **zero new storage-adjacent primitives** — verified directly: `ConnectionStore::scan_all` (`ADR-0034`) already returns every filtered row's full field set, so grouping/reduction is one new centralized pure function (`evaluate_aggregate`, alongside `evaluate_query`) over data already in hand, not a new read path. One new `ScanValue::F64` variant carries `AVG`'s result (the wire's first fractional value); `COUNT` is restricted to `COUNT(*)` only since this schema has no `NULL` concept, so `COUNT(field)` and `COUNT(*)` are provably identical. A group whose key matches zero rows never appears (no null/zero-valued group synthesized); `GROUP BY` with zero aggregate functions is allowed (the `SELECT breed FROM dog GROUP BY breed` / effectively-`DISTINCT` case) — the natural degenerate case of the same bucketing machinery, not artificially restricted. No new `ErrorCode`; not overlaid by read-your-writes, not snapshot-isolation-tracked, the same line `RYW-FR`/`ISO-FR-002`/`SQL-FR-009` already draw. Named, not solved: `HAVING` (the most likely next slice), a query planner/cost-based optimizer (still an unindexed full scan, inherited unchanged from `ADR-0034`), `COUNT(DISTINCT field)`, multi-table joins. Options: (a) accept as proposed, (b) accept without `AVG`/`ScanValue::F64` — ship `COUNT`/`SUM`/`MIN`/`MAX` only, (c) close as not yet warranted.

105. **Accepted as proposed** (proposed in PR #178, accepted in PR #179): the `GROUP BY`/aggregation design round, the owner answering "Accept" — `ADR-0035` option (a), no changes requested. Stays design-only here; implementation follows as its own unit, `SERVER-001`'s next minor / FR — `GROUP BY`/`COUNT`/`SUM`/`AVG`/`MIN`/`MAX` on top of `Request::Query` (`ADR-0034`), a new `Request::Aggregate`/`Response::Groups` pair at protocol version 9, `ScanValue::F64` for `AVG`, zero new storage-adjacent primitives (reuses `ConnectionStore::scan_all` unchanged).
106. **Implemented** (this PR, `SERVER-001` v0.28.0 / FR-038): `GROUP BY`/aggregation, `ADR-0035` — the owner's "Start Unit 37: implement ADR-0035" after accepting it. Exactly as designed: `Request::Aggregate { group_by, filter, aggregates, limit }`/`Response::Groups { groups }` at protocol version 9 (`PROTOCOL_VERSION` moved to 9, the table gains row 9); `AggregateFn::{Count,Sum,Avg,Min,Max}`; `AggregateSpec { func, field: Option<FieldRef> }`; `AggregateGroup { key, values }`; `ScanValue::F64` for `AVG`'s result. `validate_aggregate`/`evaluate_aggregate`/`reduce`/`reduce_extreme` (`src/server/mod.rs`) do every domain's filter/bucket/reduce work once, over `ConnectionStore::scan_all` (`ADR-0034`) reused entirely unchanged — the design's zero-new-storage-primitive claim held exactly, confirmed rather than merely carried over. `group_by` empty is one implicit whole-table bucket that always exists, even holding zero rows — a real edge case this round's own implementation surfaced beyond the design's worked examples: acceptance criterion 4 requires `SELECT COUNT(*) FROM t WHERE false`-equivalent to still return one group whose `Count` is `0`, and the first cut of `evaluate_aggregate` did not (caught by this round's own new unit test, not by the design review); fixed by always seeding that one bucket, and by giving `Min`/`Max` a `schema`-typed zero fallback (rather than panicking on an empty iterator) for the one case a real observed value doesn't exist, `Sum`/`Avg` already well-defined at `0`/`0.0` with no fallback needed. `COUNT` accepts only `*`, every other function only a field, both enforced as parser-level syntax errors extending `src/server/sql.rs`'s existing grammar (`agg_call`/`group_by_clause`). `SchemaDrivenClient::query`'s return type widens from `Result<Vec<QueryRow>, ClientError>` to `Result<QueryResult, ClientError>` (`QueryResult::{Rows, Groups}`) — a deliberate, documented breaking change with no deprecation shim, the same direct-rename posture `FR-035`'s `AuthConfig`→`ServeOptions` already established, justified here by the genuine shape difference a group's lack of `RecordId` creates. `query_aggregate` performs the standard SQL functional-dependency check, the `SELECT *`-alongside-`GROUP BY` rejection, and the aggregate-field-kind check entirely client-side before any round trip, then translates `Response::Groups` back into the original `SELECT`-list column order, not the wire's own `key`/`values` split. `Aggregate` is never overlaid by read-your-writes and never snapshot-isolation read-set tracked, the same "only `GetById`" line `RYW-FR`/`ISO-FR-002`/`SQL-FR-009` already draw. Ten unit tests in `src/server/sql.rs`, five in `src/server/mod.rs` (including the empty-implicit-bucket `Min`/`Max`/`Avg` fallback specifically), two golden vectors, fifteen integration tests in `tests/server_sql_integration.rs` (now 29/29) covering every aggregate function against a hand-computed reduction, `GROUP BY` on one and two fields, the `AVG`-equals-`SUM`/`COUNT` cross-check, `WHERE`+`GROUP BY`, both zero-match shapes, `LIMIT` on a grouped result, the functional-dependency/`SELECT *`/non-numeric-field client-side rejections, the version-9 gate composed with the version-8 gate on one connection, and `Aggregate` inside a read-your-writes and a snapshot-isolation session each; every acceptance criterion 1–8 holds; no deviation. No `Cargo.toml`, store (`src/store/**`), or `serve`-signature change. With this, `ADR-0035` is implemented — `docs/FUTURE-GROWTH.md`'s aggregation/`GROUP BY`/`AVG` item is narrowed and closed to this bounded slice, multi-table joins restated as the real, separate, still-open answer.
107. **Proposed** (PR #181): the `Reminder` domain design round — the owner asked, in conversation rather than from `docs/FUTURE-GROWTH.md`, whether this crate could back `rusty_remind_me` (a separate MCP-exposed reminder/memory system). Written up as `SERVER-REMINDER-DOMAIN-DESIGN` / `ADR-0036` after a short scoping discussion found most of `rusty_remind_me`'s real surface (semantic search, schema-less memory content, an entity graph, provenance/history) genuinely out of scope for one bounded round, narrowing to the one slice that fits this crate's existing architecture with zero new engine capability: reminders themselves, a small fixed-schema record. Proposed shape: a fourth domain, `Reminder` (`title`/`due_at_unix_ms`/`status`), built on the already-accepted generic schema library — but, departing from `Order`/`Employee`'s own precedent, front-door rather than `research`-gated (the library's first real, deployable appearance outside `research`), and with `status` (not a plain number) as the durably-mutable `ScannableField` so marking a reminder done needs no SQL round trip, made safe by `Request::Query`/`Request::Aggregate` already supporting range search on any field regardless of its `filter_eq` capability. No relation of either kind — the one combination no existing adapter has, so `ReminderProductionStack` needs no `Symmetric`/`Reversed` layer at all, the simplest domain shape this library supports. A real `reminder_server` binary is proposed alongside the adapter, since a domain with no runnable server would not meet the round's own motivation. Explicitly named, not assumed: `rusty_remind_me`'s own source was not read this session — the three-field guess is inferred from its MCP tool names alone, flagged as an open question for a separate, later integration unit to verify. No wire-protocol, `PROTOCOL_VERSION`, or `ErrorCode` change proposed. Options: (a) accept as proposed / (b) accept but keep `Reminder` behind `research`, matching `Order`/`Employee` / (c) close as not warranted.
108. **Accepted as proposed** (proposed and accepted in PR #181): the `Reminder` domain design round, the owner answering "Accepted" — `ADR-0036` option (a), no changes requested. Stays design-only here; implementation follows as its own unit, `SERVER-001`'s next minor / FR — a front-door `Reminder` domain (`title`/`due_at_unix_ms`/`status`) on the generic schema library, `status` as the durably-mutable `ScannableField`, no relations, a real `reminder_server` binary.
109. **Implemented** (this PR, `SERVER-001` v0.29.0 / FR-039): the `Reminder` domain, `ADR-0036` — the owner's "Start Unit 38: implement ADR-0036" after accepting it. Exactly as designed: `Reminder { id, title, due_at_unix_ms, status }` (`src/generic/reminder.rs`, new, front-door — the generic schema library's first appearance outside its own `research`-gated reference material `order_customer`/`employee_impl`), `ReminderStatus::{Pending, Done, Snoozed, Cancelled}`. `status` — an enum, not a plain number, departing from every existing generic-schema domain's own convention — is the durably-mutable `ScannableField` (`StatusField`, its `u32` discriminant, the `server::order`-established fixed-mapping shape now reused for a `ScannableField` instead of an `IndexedField`), so marking a reminder done works via plain `UpdateField`/`Session::update`, no SQL required, made safe by `Request::Query`/`Request::Aggregate` already filtering on any field regardless of `filter_eq` capability; `due_at_unix_ms` is the equality-filterable `IndexedField` (`DueAtField`); `title` is read-only over the wire. No relation of either kind — the one combination no existing domain has — so `ReminderProductionStack = GenericMmapStore<Reminder, DueAtField, StatusField>` needs no `Symmetric`/`Reversed` composition layer at all, the simplest domain shape this library supports; `CheckpointFlush` implemented for it directly in `src/server/journal.rs`, unconditionally (matching `Reminder`'s own front-door status), delegating to `GenericMmapStore`'s already-generic `Flush`. `server::reminder::ReminderConnectionStore` (`src/server/reminder.rs`, new) implements `ConnectionStore` exactly as `OrderConnectionStore`/`EmployeeConnectionStore` already do, gated by `server` alone — matching `server::dog`'s precedent, not `server::order`/`server::employee`'s, since `Reminder` is real, deployable capability, not reference material; `parent`/`children`/`neighbors` all report `ErrorCode::Unsupported` unconditionally. `scan_field`/`update_field`/`apply_transaction`/`validate_op` on `status` validate the incoming `u32` against `status_from_u32` before ever reaching the store — `ErrorCode::Malformed` on an unrecognized discriminant, nothing applied — the one genuinely new validation path this domain needed: every existing enum-typed field has been an `IndexedField`, validated only on `filter_eq`; this is the first enum-typed `ScannableField`, validated on the update/`Transaction`/session-write path instead. `src/bin/reminder_server.rs` (new, `required-features = ["server"]`) mirrors `dog_server.rs`'s shape and every env-var-driven operational knob exactly, confirmed by a real `cargo run --bin reminder_server --features server` smoke check, not just `required-features` inspection. `dispatch`/`serve` needed no change at all — already generic over `S: ConnectionStore`, a fourth implementor is invisible to them; no new `Request`, `Response`, `ErrorCode`, `PROTOCOL_VERSION`, or wire change of any kind. Three new unit tests in `src/generic/reminder.rs` (compiled and run even under *default*, zero-feature builds, since `crate::generic::reminder` is unconditional — the `status_to_u32`/`status_from_u32` round trip including an out-of-range rejection, `create`-then-`get`/`filter_eq`, `update`-then-reopen durability), five in `src/server/reminder.rs`, nine integration tests in `tests/server_reminder_integration.rs` (new, `required-features = ["server"]` only, no `research`) covering every acceptance criterion over a real socket; every acceptance criterion 1–8 holds; no deviation. No `Cargo.toml` dependency change (`[[bin]]`/`[[test]]` entries only), no store (`src/store/**`) change, no `serve`-signature change. Ten transient new "unresolved link" doc warnings this round's own new module-header doc comments introduced (the same long-standing "bare-item link in a module's own `//!` header doesn't resolve" rustdoc quirk this codebase has repeatedly documented) were caught and fixed in place before backfill, not left standing — `cargo doc --all-features --no-deps` back at the 64-warning baseline. With this, `ADR-0036` is implemented — the owner's own question about backing `rusty_remind_me` answered with a real, deployable domain; the schema-shape assumption against `rusty_remind_me`'s real source stays this round's own named open question, left for a separate, later integration unit to verify, not assumed here.
110. **Proposed** (this PR): the `Entity` domain design round, the second in the `rusty_remind_me`-motivated line `ADR-0036` started — the owner's "Start Unit 39: entities + entity_traverse design round." Written up as `SERVER-ENTITY-DOMAIN-DESIGN` / `ADR-0037` after investigating `rusty_remind_me`'s own `entity`/`entity_upsert`/`entity_traverse` tool names against this crate's existing relation machinery, rather than assuming it would just fit. Found a real, previously-unexercised gap: `Symmetric<S, R, Marker>` (the layer providing `Neighbors`) has no forwarding impl re-exposing a *different* marker's `Neighbors` from an inner layer, unlike `Reversed` (which gained exactly that forwarding for `FR-012`, `Employee`'s directed-plus-symmetric combination) — so two independent symmetric relations on one record type is not supported today without a `FR-012`-shaped fix to `Symmetric` itself, named precisely but not proposed this round. Proposed shape: a fifth domain, `Entity` (`label`/`kind`/`mention_count`), one self-referential `SymmetricRelation` (`relates_to`) — `Dog`'s own shape, not `Employee`'s dual-relation one — front-door, `server`-gated alone, mirroring `Reminder`'s own precedent exactly. Plus one genuinely new capability this crate has never had at any layer: `SchemaDrivenClient::traverse`, bounded breadth-first graph walking built entirely client-side over the existing `Request::Neighbors` RPC (`max_depth`/`max_nodes` both caller-supplied, a cycle-safe visited set required for correctness not just efficiency) — no new `Request`/`Response`/`PROTOCOL_VERSION`/`ErrorCode` of any kind, the same "new client-side capability, zero new wire primitive" shape `ADR-0034`'s SQL parsing already established. Requirements `ENT-FR-001`–`008`, acceptance criteria 1–8, verification plan. Acceptance options: (a) as proposed / (b) accept and also fix `Symmetric`'s forwarding gap this round, delivering multiple relation types now / (c) close as not warranted.
111. **Accepted as proposed** (proposed and accepted in PR #183): the `Entity` domain design round, the owner answering "a" — `ADR-0037` option (a), no changes requested. Stays design-only here; implementation follows as its own unit, `SERVER-001`'s next minor / FR — a front-door `Entity` domain (`label`/`kind`/`mention_count`) on the generic schema library, one self-referential `SymmetricRelation` (`relates_to`), plus `SchemaDrivenClient::traverse` (bounded client-side breadth-first walking over the existing `Request::Neighbors` RPC). The `Symmetric`-forwarding gap for a second, independent relation type stays named but unfixed, per option (a).
112. **Implemented** (this PR, `SERVER-001` v0.30.0 / FR-040): the `Entity` domain, `ADR-0037` — the owner's "Start Unit 40: implement ADR-0037" after accepting it. Exactly as designed: `Entity { id, label, kind, mention_count }` (`src/generic/entity.rs`, new, front-door — this library's first `SymmetricRelation` outside `research`-gated reference material), `EntityKind::{Person, Place, Organization, Concept, Event}`. `kind` — an enum, the usual generic-schema convention, *not* inverted the way `Reminder::status` was — is the equality-filterable `IndexedField` (`KindField`), since `kind` is expected to change rarely if ever; `mention_count` is the durably-mutable `ScannableField` (`MentionCountField`), the field expected to change over an entity's lifecycle; `label` is read-only over the wire. One self-referential `SymmetricRelation`, `RelatesTo` (`relates_to`) — `Dog::littermate_of`'s own shape — so `EntityProductionStack = Symmetric<GenericMmapStore<Entity, KindField, MentionCountField>, Entity, RelatesTo>`, one composition layer; `CheckpointFlush` implemented for it directly in `src/server/journal.rs`, unconditionally, delegating to `Symmetric`'s own `Flush` impl (which itself forwards to its inner `GenericMmapStore`'s `Flush`). `server::entity::EntityConnectionStore` (`src/server/entity.rs`, new) implements `ConnectionStore` exactly as `ReminderConnectionStore` already does, gated by `server` alone; `filter_eq`/`update_field` on `kind` validate the incoming `u32` discriminant against `kind_from_u32` before ever reaching the store — `ErrorCode::Malformed` on an unrecognized value, nothing applied — the same "validate before write" shape `Reminder`'s own `status` handling established, here applied to `FilterEq` since `kind` is the `IndexedField`, not the `ScannableField`; `parent`/`children` report `ErrorCode::Unsupported` unconditionally (no `ChildOf`), `neighbors` real. `SchemaDrivenClient::traverse(&mut self, id, max_depth, max_nodes) -> Result<Vec<(RecordId, usize)>, ClientError>` (`src/server/client.rs`, new) — bounded breadth-first graph walking, built entirely client-side over the already-existing `Request::Neighbors`, no new `Request`/`Response`/`PROTOCOL_VERSION`/`ErrorCode` of any kind; both `max_depth` and `max_nodes` are caller-supplied with no crate-side default; a `HashMap<RecordId, usize>` visited-with-depth guard is required for correctness, not just efficiency (a symmetric relation trivially cycles); each id's recorded depth is its true shortest-path distance, since BFS visits ids in non-decreasing depth order; `Err(ClientError::Unsupported("traverse"))` locally, no round trip, when `relations.neighbors` is `false`; a `neighbors` failure mid-walk aborts immediately rather than returning a partial result as if complete. `src/bin/entity_server.rs` (new, `required-features = ["server"]`) mirrors `reminder_server.rs`'s shape and every env-var-driven operational knob exactly. `dispatch`/`serve` needed no change at all — already generic over `S: ConnectionStore`, a fifth implementor is invisible to them. **As proposed, option (a)**: the real gap this round's own design and `ADR-0037` named precisely — `Symmetric<S, R, Marker>` has no forwarding `Neighbors` impl for a second, independent marker the way `Reversed` does, so a second named relation type on `Entity` isn't supported without a `FR-012`-shaped fix to `Symmetric` itself — stays named but deliberately unfixed, exactly as accepted; no deviation. Four new unit tests in `src/generic/entity.rs` (compiled and run even under *default*, zero-feature builds, since `crate::generic::entity` is unconditional — the `kind_to_u32`/`kind_from_u32` round trip including an out-of-range rejection, `create`-then-`get`/`filter_eq`/`neighbors`, `update`-then-reopen durability including `neighbors`, portable-reopen matching `open`'s own fields/`neighbors`), five in `src/server/entity.rs` (`get`, `filter_eq` by `kind` including the invalid-discriminant rejection plus its `Unsupported` fields, `scan_field`/`update_field` on `mention_count`, `neighbors` reflecting `relates_to` plus `parent`/`children`'s `Unsupported`, `describe`'s reported capabilities), nine integration tests in `tests/server_entity_integration.rs` (new, `required-features = ["server"]` only, no `research`) covering every acceptance criterion over a real socket — including `traverse` over a real 4-node graph with a real cycle (correct shortest-path hop distances, the `max_depth` bound, the `max_nodes` bound cutting a walk off before `max_depth` is even reached) and `traverse`'s client-side rejection proven against a real `Reminder` server (`relations.neighbors: false`, reused as the negative fixture); every acceptance criterion 1–8 holds; no deviation. No `Cargo.toml` dependency change (`[[bin]]`/`[[test]]` entries only), no store (`src/store/**`) change, no `serve`-signature change. Full sweep green: `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` all lib tests plus `tests/server_entity_integration.rs` 9/9 (new); `cargo test` (default features) 143 lib tests (139 + 4, the `generic::entity` unit tests, run even under zero features — confirming genuine front-door status) + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after). With this, `ADR-0037` is implemented — the second `rusty_remind_me`-motivated slice delivered, exactly as accepted; the `Symmetric`-forwarding gap and whether `rusty_remind_me`'s real schema matches this document's own guess both stay this round's own named open questions, left for a later unit.
113. **Proposed** (this PR): the entity/`entity_traverse` integration verification round, the third in the `rusty_remind_me`-motivated line `ADR-0036` started — the owner's "Start Unit 41: entities + entity_traverse integration verification round." Written up as `SERVER-ENTITY-INTEGRATION-VERIFICATION-DESIGN` / `ADR-0038` after a channel neither prior round had became available this session: the `rusty-remind-me` MCP server itself is now connected, exposing its real tool schemas directly (`remind_me_entity`/`remind_me_entity_upsert`/`remind_me_entity_traverse`), closing the open question `ADR-0036` and `ADR-0037` each explicitly named and left unresolved. Findings: the traversal *shape* (bounded, bidirectional, relation-edge-based) was directionally right, but several concrete particulars were not — entities are addressed by `name`/`aliases`, not a `Uuid`; `kind` is a free-form string, not a fixed enum; there is no real `mention_count`, only linked-memory lists; no entity-enumeration tool exists at all; and, most consequentially, `remind_me_entity_traverse`'s own `relation`-label filter parameter proves multiple named relation types are real, already-exercised capability in the target system — exactly the `Symmetric`-forwarding gap `ADR-0037` named and deliberately left unfixed, now confirmed not speculative. Three options offered, the same proportionality shape prior rounds used: (a) revise `Entity`/`traverse` to match the real shape now, including the `Symmetric` fix — the complete answer but a substantial redesign, premature without confirming real integration is wanted; (b) (proposed) accept as informational, no engine change this round — `Entity`/`traverse` stand as real, tested, useful capability in their own right, simply no longer positioned as `rusty_remind_me`'s literal unmodified backing store; (c) deprecate/remove `Entity` — rejected, nothing in these findings makes it broken on its own terms. Docs-only, no source file touched.
114. **Accepted, option (a)** (proposed and accepted in PR #185): the entity/`entity_traverse` integration verification round, the owner answering "a" — `ADR-0038` option (a), (b) and (c) declined. The owner chose the complete answer over the proposed, more conservative option (b): `Entity`/`traverse` are to be revised to match `rusty_remind_me`'s real shape found in Unit 41 — `name`-keyed identity (not `Uuid`), an open `kind` string (not a fixed enum), a real `aliases` field, dropping `mention_count` for a real memory-linkage concept, and — the real, load-bearing piece — actually closing the `Symmetric`-forwarding gap `ADR-0037` twice named so labeled multi-relation traversal becomes possible, with `traverse`'s bound units matched to the real tool's `hops`/`cap` shape. This acceptance authorizes the redesign's *direction* only; the concrete mechanism (exact schema, the `Symmetric` fix's shape, migration posture for the already-shipped `Entity` domain) is real, additional design work, out of proportion to fold into the acceptance itself — follows as its own design round before any implementation, the same "design before big implementation" discipline every substantial `SERVER-001` change has followed throughout this project's history.
115. **Proposed** (this PR): the `Entity` v2 redesign round, the fourth in the `rusty_remind_me`-motivated line `ADR-0036` started — the concrete mechanism `ADR-0038`'s own acceptance deferred. Investigating it directly found two real architectural constraints neither `ADR-0038` nor Unit 41 had surfaced yet: `RecordId = Uuid` is a crate-wide invariant (`protocol.rs`'s own doc comment: "every domain this crate has ever used is `Uuid`-keyed"), not an `Entity`-local choice, so literally adopting `rusty_remind_me`'s real `name`-keyed identity would be a far larger break than "revise one domain"; and `bincode`'s positional struct encoding means the only wire-compatible way to add relation-type filtering is new, appended `Request`/`Response` variants (`ADR-0022`'s own established pattern), never new fields on `Request::Neighbors`/`DomainSchema`. Written up as `SERVER-ENTITY-V2-REDESIGN-DESIGN` / `ADR-0039`. Proposed shape: `Entity` stays `Uuid`-keyed but gains a real, equality-filterable `name` field (the closest compatible approximation to real name-based lookup); `kind` becomes an open `String` filling both `IndexedField` and `ScannableField` roles `GenericMmapStore` structurally requires (removing `mention_count`'s synthetic role entirely); a second self-referential relation, `MentionedWith` — an honestly-labeled mechanism-validation example, not a claimed real `rusty_remind_me` label — proves the `Symmetric`-forwarding fix (`ENT2-FR-003`, the `FR-012`-shaped fix `ADR-0037` twice named, now real) over a nested `Symmetric<Symmetric<..>, ..>` stack; `PROTOCOL_VERSION` moves to 10 with two new append-only variants, `Request::NeighborsByRelation`/`ListRelationKinds` (`Response::RelationKinds`), letting a relation-filtered neighbor lookup and relation-kind discovery reach the wire without touching any existing variant's byte layout; `SchemaDrivenClient::traverse` gains an optional relation filter, `None` preserving today's exact behavior. `aliases`, case-insensitive name resolution, and server-side multi-hop traversal all stay explicitly deferred, named not built. Requirements `ENT2-FR-001`–`007`, acceptance criteria 1–8, verification plan. Acceptance options: (a) accept as proposed / (b) accept but defer the wire-protocol half (ship the field/identity changes and the `Symmetric` fix in-process only, no protocol bump) / (c) close as not warranted.
116. **Accepted as proposed** (proposed and accepted in PR #185): the `Entity` v2 redesign round, the owner answering "aa" (confirmed as option (a) via clarification) — `ADR-0039` option (a), no changes requested, including `PROTOCOL_VERSION` 10's wire additions. Stays design-only here; implementation follows as its own unit, `SERVER-001`'s next minor / FR — `Entity` gains a real `name` field (`Uuid`-keyed identity unchanged), `kind` becomes an open string filling both `IndexedField`/`ScannableField` roles, a second relation `MentionedWith` proves the `Symmetric`-forwarding fix, and protocol moves to 10 with `Request::NeighborsByRelation`/`ListRelationKinds`/`Response::RelationKinds` appended.
117. **Implemented** (`SERVER-001` v0.31.0 / `SERVER-001-FR-041`, PR #185): `ADR-0039`'s implementation, the owner's "Start Unit 42: implement ADR-0039." Two real architectural blockers surfaced by direct investigation before any implementation code was written, neither assumed away, both resolved with the owner via `AskUserQuestion` rather than silently reinterpreted — the same discipline `SERVER-ENTITY-V2-REDESIGN`'s own investigation used one round earlier. **First**: `ADR-0039`'s own proposed `kind: String` filling both `IndexedField` and `ScannableField` roles does not compile — `ScannableField::ScanValue: Copy`, and `GenericMmapStore`'s mmap slot mechanism is fixed-width only by `ADR-0009`'s own design; `String` is neither. Owner picked "`kind` becomes read-only" over the two other options offered, so `kind` stays the equality-filterable `IndexedField` (open-ended now, not durably updatable — the same shape every other domain's `IndexedField` already has) and `mention_count` stays, unretired, as the `ScannableField`. **Second, more consequential**: the `Symmetric`-forwarding mechanism this line's own documents repeated since `ADR-0037` — mirroring `Reversed`'s own `Neighbors`-forwarding fix for `FR-012` — does not compile either. Verified directly with `rustc` on two isolated test files: a direct `Neighbors<R, Marker>` impl (which `Symmetric` needs for every existing single-relation domain) unconditionally conflicts (`E0119`) with any additional generic forwarding impl for a second marker, a conflict `Reversed` never faces since its own relation (`ChildOf`) is a different trait entirely from the one it forwards. This is a genuine, previously-unverified-and-wrong claim that had propagated across four prior documents (`ADR-0037`, Unit 41 Finding 6, `ADR-0038`, `ADR-0039`'s own original text) — caught here, not before, and corrected rather than perpetuated. Owner picked a new runtime-keyed multi-relation primitive over the two other options offered: `MultiSymmetric`/`MultiNeighbors` (`src/generic/{store,query}.rs`, new) key relations by a runtime `String` label instead of a compile-time `Marker`, sidestepping the conflict entirely — which also happens to match `Request::NeighborsByRelation`'s own wire shape exactly, since that field was always going to be a runtime string. `Entity` gains a second relation, `MentionedWith` (`mentioned_with`), proving the new primitive; `PROTOCOL_VERSION` moves to 10 with `Request::NeighborsByRelation`/`ListRelationKinds`/`Response::RelationKinds`, all append-only, gated entirely client-side (the same posture `Query`/`Aggregate` already established); every `ConnectionStore` adapter implements the two new methods (`Dog`/`Employee` for real over their one existing relation, `Order`/`Reminder` `Unsupported`/empty); `SchemaDrivenClient::traverse` gains an optional relation filter. **Third, smaller, real deviation, named here for the first time**: `label`'s field identifier was not renamed to `name` as the accepted text's prose described — the module's own doc comment states the field fulfills that role under its existing v1 name, but the identifier itself is unchanged. Full validation sweep green: `cargo fmt --all -- --check` clean, `cargo clippy --all-targets --all-features -- -D warnings` clean, `cargo test` (default features) 142 lib tests + 2 doctests, `cargo test --all-features` 420 lib tests plus every integration suite (including `tests/server_entity_integration.rs` 9/9, rewritten), `cargo doc --all-features --no-deps` at the 64-warning baseline (one new warning from this round's own new doc comment — a rustdoc quirk where a module's very first `//!` summary paragraph could not resolve an intra-doc link any later doc comment in the same file resolved without issue — found and fixed, not accepted). See `docs/decisions/ADR-0039-entity-v2-redesign.md`'s own implementation-log entry for the full account.


## Validation

- `cargo fmt --all --check`: clean.
- `cargo clippy --all-targets --all-features -- -D warnings`: clean.
- `cargo test --all-features` (release): 128/128 passing (120 unit + 7 `cross_backend` + 1 new `production_integration` flagship test) — up from 121/121, with `PRODUCTION-DEFAULT`'s 6 new `src/production.rs` unit tests and 1 new flagship integration test. Unchanged, still 128/128, after the `scan_ages` fix (a read-path performance change, no new tests needed).
- `cargo bench --bench workloads -- production` (release): `ProductionStore` completes all 27 cases (7 workload groups × 3 sizes, plus 9 mixed-workload cases across 3 write ratios × 3 sizes) without panics — numbers in `RESULTS.md`'s `## Production recommendation` section, **re-run after the `scan_ages` fix**: `scan_ages` dropped 12–17× at every size, mixed-workload numbers dropped proportionally. No changes to the other four backends' own numbers (not re-run this pass; unaffected by an additive variant).
- `cargo bench --bench concurrency` (release): `ProductionStore` added as a 5th variant to the existing size (1,000/100,000) × write-ratio (10/50/90%) × thread-count (1/4/32/64) sweep on `baileyai` — 24 new cases completed without panics, reported alongside the existing 4 variants' numbers, **re-run after the `scan_ages` fix**: the large, thread-count-invariant tax the first run found is gone (every cell now within 0.77–1.46× of `GlobalRwLockStore`, most at or ahead of parity).
- `cargo bench --bench durability`: unaffected by this pass — no changes to `src/durability/**` or `benches/durability.rs`. Status unchanged from the prior checkpoint.
- `cargo bench --features perf-events --bench cache_events`: unaffected by this pass. Status unchanged from the prior checkpoint.
- `cargo check --all-targets --all-features`: clean, including the new `production` module and `tests/production_integration.rs` alongside every pre-existing target.
- `cargo run --release --example memory_footprint`: unaffected by this pass; status unchanged from the prior checkpoint.
- `GENERIC-SCHEMA-DESIGN`: superseded — see `GENERIC-SCHEMA-SPIKES`/`GENERIC-SCHEMA-LIBRARY` below; the design is now real code in this repository, not scratch-crate-only.
- `GENERIC-SCHEMA-SPIKES`/`GENERIC-SCHEMA-LIBRARY` (four spike rounds plus the library-promotion pass): `cargo fmt --all --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo test --all-features` (release): 151/151 passing (up from 128/128) — 142 unit + 7 `cross_backend` + 2 flagship integration tests (`production_integration`, `generic_production_integration`), including 12 new `src/generic/{mmap_field,mmap_store,order_customer,production}.rs` unit tests. `cargo bench --bench generic_production` (release, reduced sampling: `--warm-up-time 1 --measurement-time 2 --sample-size 20`): completes all 15 cases (5 operations × 3 sizes) without panics — numbers in `RESULTS.md`'s `## Generic schema library` section, cross-checked against the `directed-relation-spike` round's and `ProductionStore`'s own established numbers, no regression found. `cargo check --all-targets --all-features`: clean. No changes to `src/production.rs`/`src/store/**`/`src/durability/**`/`src/concurrency/**` — verified by diff. No new dependency (`Cargo.toml`'s `[dependencies]` unchanged; only three new `[[bench]]` entries across the four spike rounds and this pass).
- `GENERIC-SCHEMA-WRITE-THROUGH-FIX`: `cargo fmt --all --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo test --all-features` (release): **152/152 passing** (up from 151/151) — the one new regression test, everything else unchanged. `cargo check --all-targets --all-features`: clean. Benchmark comparison (not a full re-run of `RESULTS.md`'s tables — a targeted, same-session before/after isolation specifically for this fix): `generic_get` (in-memory path) confirmed ~43–88% slower at 1K/100K/1M; `generic_scan_ages` confirmed unaffected (flat within run-to-run noise), isolating the cost to exactly the code this fix touched. `GenericProductionStore`'s own numbers (the durable path, `benches/generic_production.rs`) are untouched by this fix — `GenericMmapStore::get` wasn't modified — so not re-run. No changes to `src/generic/mmap_store.rs`, `src/generic/production.rs`, `src/production.rs`, `src/store/**`, `src/durability/**`, or `src/concurrency/**` — verified by diff. No new dependency.
- `RULE-TRACE-SPIKE` through `FUTURE-GROWTH-DOC` (PRs #19-#31, per-PR detail in `## Completed` above): each PR's own CI (`fmt`/`clippy`/`test`) was reported green at merge time in its commit message; not independently re-verified per PR by this documentation-sync pass. This pass instead re-ran the full validation suite once, fresh, against the current head (`176f5d0`) to confirm the repository's *current* state rather than trust the accumulated history: `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo test --all-features` (release): **199/199 passing** (up from 152/152 at the last full sync) — 183 lib unit (up from 152) + 7 `cross_backend` + 1 `generic_production_integration` + 4 `mmap_record_identity_keying` (new, `GENERIC-MMAP-RECORD-IDENTITY-FIX`) + 1 `production_integration` + 3 doctests (up from 2; `README-AND-RUSTDOC` added the `GenericProductionStore` example, `REQ-TRACEABILITY-SCHEMA` added `RuleDerivation`'s `compile_fail` doctest). `cargo test --release` (default, no features, per `PUBLIC-API-NARROWING`'s own claim): **63/63 passing** (61 unit + 2 doctest) with the research-gated modules absent — confirms the front-door narrowing still holds at the current head. `cargo doc --all-features --no-deps`: clean, 62 warnings (unchanged from `README-AND-RUSTDOC`'s own reported count). `cargo check --all-targets --all-features`: clean. Per-PR benchmark numbers (torn-write/schema-version-header/append-race costs, the record-identity-keying regression table) are reported as each PR's own commit message documented them, not re-run by this pass — see `## Completed` above and `RESULTS.md` for the primary source.
- `SERVER-QUERY-LAYER`: `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --features server` and `cargo test --features server,research` (release): both green (`server::{dog,order,protocol,framing}::tests::*` + `server::tests::*` + `tests/server_dog_integration.rs` (3) + `tests/server_order_integration.rs` (1, `research`-only), re-run 3× consecutively with no flakiness after the Nagle and test-isolation fixes). `cargo test --all-features` (release): **220/220 passing** (up from 199/199) — 200 lib unit (up from 183, +17 new `server::{dog,order,protocol,framing,tests}` unit tests) + 7 `cross_backend` + 1 `generic_production_integration` + 4 `mmap_record_identity_keying` + 1 `production_integration` + 3 `server_dog_integration` + 1 `server_order_integration` + 3 doctests. `cargo test --release` (default, no features): **63/63 passing**, unchanged — confirms `server` adds no default-build surface. `cargo doc --all-features --no-deps`: clean, **67 warnings** (up from 62) — all three new warnings are the same "bare-item/`Self::` link in a module's own `//!` header doesn't resolve" rustdoc quirk already present dozens of times throughout this codebase (`production.rs`'s own `[Self::create]`/`[Self::open]`, every `durability`/`generic`/`generic_spike` module doc), not a new class of issue this round introduced. No changes to `src/production.rs`/`src/generic/**`/`src/store/**`/`src/durability/**`/`src/concurrency/**` — verified by diff. No new dependency.
- `SERVER-QUERY-LAYER` v0.2.0 (schema discovery, ADR-0011): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --features server` and `cargo test --features server,research` (release): both green, including both new schema-driven tests, re-run 3× consecutively with no flakiness. `cargo test --all-features` (release): **225/225 passing** (up from 220/220) — 203 lib unit (up from 200, +3 new `server::{tests,dog::tests,order::tests}::describe_*`) + 7 `cross_backend` + 1 `generic_production_integration` + 4 `mmap_record_identity_keying` + 1 `production_integration` + 4 `server_dog_integration` (up from 3, +1 schema-driven test) + 2 `server_order_integration` (up from 1, +1 schema-driven test) + 3 doctests. `cargo test --release` (default, no features): unaffected. `cargo doc --all-features --no-deps`: clean, **67 warnings**, unchanged from the prior checkpoint. No changes to `src/production.rs`/`src/generic/**`/`src/store/**`/`src/durability/**`/`src/concurrency/**`, and no existing `Request`/`Response` variant's shape changed — verified by diff. No new dependency.
- `SERVER-QUERY-LAYER` v0.3.0 (third domain, `Employee`): `cargo fmt --all -- --check`: clean (one literal-digit-grouping clippy finding — `12_000_00`-style cents literals — caught and fixed before this reading, real salary literals rewritten to standard 3-digit grouping, e.g. `1_200_000`). `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --features server,research --test server_employee_integration` (release): green, all 3 tests. `cargo test --all-features` (release), run 3× consecutively with no flakiness: **236/236 passing** (up from 225/225) — 211 lib unit (up from 203, +8 new: 3 `generic_spike::employee_impl::tests` + 5 `server::employee::tests`) + 7 `cross_backend` + 1 `generic_production_integration` + 4 `mmap_record_identity_keying` + 1 `production_integration` + 4 `server_dog_integration` (unchanged) + 3 `server_employee_integration` (new) + 2 `server_order_integration` (unchanged) + 3 doctests. `cargo test --release` (default, no features): unaffected — confirms `server`/`research` add no default-build surface. `cargo doc --all-features --no-deps`: clean, no new warning class introduced — this round's own new unresolved-intra-doc-link warnings (`dog::DogConnectionStore`-style module-doc references) are the same long-standing rustdoc quirk already present dozens of times throughout this codebase, confirmed by a direct before/after comparison against `origin/main` (which itself already carries 54 such warnings), not a new issue this round introduced. No changes to `src/production.rs`/`src/store/**`/`src/durability/**`/`src/concurrency/**`; `src/generic/{store,production}.rs` changes limited to the single `Neighbors`-forwarding completion — verified by diff. No new dependency.

- `SERVER-QUERY-LAYER` v0.4.0 (throughput/latency benchmark): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --all-features` (release), run 3× consecutively with no flakiness: **236/236 passing**, unchanged from the prior checkpoint — `benches/server.rs` adds a bench binary, not a test. `cargo test --release` (default, no features): unaffected. `cargo doc --all-features --no-deps`: clean, no new warning introduced (this pass touches no doc-comment-bearing library item). `cargo bench --features server,research --bench server` (release): completes without panics, run twice — numbers in `RESULTS.md`'s `## Server / query layer` section. No changes to any existing source file outside `benches/server.rs`/`Cargo.toml` — verified by diff. No new dependency.
- `SERVER-QUERY-LAYER` v0.5.0 (schema-driven client library): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --release --features server,research --test server_schema_driven_client`: green, all 4 tests. `cargo test --all-features` (release), run 3× consecutively with no flakiness: **240/240 passing** (up from 236/236) — 211 lib unit (unchanged — `server::client` has no inline unit tests, since its only meaningful behavior requires a live connection, already covered end to end by the integration test) + 7 `cross_backend` + 1 `generic_production_integration` + 4 `mmap_record_identity_keying` + 1 `production_integration` + 4 `server_dog_integration` (unchanged) + 3 `server_employee_integration` (unchanged) + 2 `server_order_integration` (unchanged) + 4 `server_schema_driven_client` (new) + 3 doctests. `cargo test --release` (default, no features): **61/61 passing**, unaffected — confirms `server` adds no default-build surface. `cargo doc --all-features --no-deps`: clean, 70 warnings (up from 69) — the one new warning is the same long-standing unresolved-intra-doc-link rustdoc quirk already present throughout this codebase (a bare-item reference in `client.rs`'s own module doc comment), not a new issue class. No changes to any existing source file outside `src/server/{mod,client}.rs`/`tests/server_schema_driven_client.rs`/`Cargo.toml` — verified by diff. No new dependency.
- `SERVER-AUTH` (authentication/authorization implementation): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --release --features server --test server_auth_integration`: green, all 5 tests. `cargo test --all-features` (release), run 3× consecutively with no flakiness: **251/251 passing** (up from 240/240) — 217 lib unit (up from 211, +6 new `AuthConfig`/`Authenticate`/`dispatch`/timing tests) + every pre-existing suite unchanged + `server_auth_integration` (new, 5 tests). `cargo test --release` (default, no features): **63/63 passing**, unaffected — confirms `server` still adds no default-build surface. `cargo doc --all-features --no-deps`: clean after fixing two "links to a private item" warnings an early draft introduced (dropped the intra-doc link brackets around `handle_connection` references — this codebase's existing convention for private-item references in doc comments). No changes to any existing source file outside `src/server/{mod,protocol}.rs`, every `serve` call site (one new argument each), `src/bin/dog_server.rs`, `tests/server_auth_integration.rs` (new), and `Cargo.toml` — verified by diff. New dependency: `subtle = "2"` (constant-time token comparison), the one place this round departs from "avoid new dependencies," per ADR-0012's own explicit reasoning.
- `SERVER-TRANSACTION` (atomic multi-operation transaction implementation): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --release --features server --test server_transaction_integration`: green, all 6 tests, run 5× consecutively with no flakiness (the flagship concurrent test's own sequential-replay check is the established, proven-sound pattern this project already trusts elsewhere — an earlier, methodologically-flawed per-instant "torn read" version of this test was caught failing ~5% of the time on a *correct* implementation before being redesigned; see `## Completed` above for the full account). `cargo test --all-features` (release), run 3× consecutively with no flakiness: **260/260 passing** (up from 251/251) — 220 lib unit (up from 217, +3 new `TransactionOp`/`Transaction`/`apply_transaction` tests) + every pre-existing suite unchanged + `server_transaction_integration` (new, 6 tests). `cargo test --release` (default, no features): **63/63 passing**, unaffected — confirms `server` still adds no default-build surface. `cargo doc --all-features --no-deps`: clean, 73 warnings, unchanged from the prior checkpoint — no new warning class. No changes to any existing source file outside `src/server/{mod,protocol,dog,order,employee}.rs`, `src/production.rs`, `src/generic/production.rs`, `tests/server_transaction_integration.rs` (new), and `Cargo.toml` (new `[[test]]` entry only) — verified by diff. No new dependency.
- `SERVER-TRANSACTION-BENCHMARK` (transaction benchmark coverage): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --release --all-features`, run 3× consecutively with no flakiness: **260/260 passing, unchanged** from the `SERVER-TRANSACTION` checkpoint — this pass is genuinely bench-only, no test-suite impact. `cargo test --release` (default, no features): **63/63 passing**, unaffected. `cargo doc --all-features --no-deps`: clean, 73 warnings, unchanged from the prior checkpoint — no new warning class. `cargo bench --features server,research --bench server`, run twice on this session's container to characterize run-to-run variance: completes without panics, both `GetById` and the new `{domain}-txn` rows printed for all three domains — numbers in `RESULTS.md`'s `## Server / query layer` section, `### \`Request::Transaction\` follow-up` subsection. No changes to any existing source file outside `benches/server.rs` — verified by diff. No new dependency.
- `SERVER-TLS` (native transport encryption implementation): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --all-targets --all-features`: clean. `cargo test --release --all-features`, run 3× consecutively with no flakiness: **275/275 passing** (up from 260/260) — 230 lib unit (up from 220, +10 new `server::pem::tests::*`) + every pre-existing suite unchanged + `server_tls_integration` (new, 5 tests). `cargo test --release` (default, no features): **63/63 passing**, unaffected — confirms `server` still adds no default-build surface. `cargo doc --all-features --no-deps`: clean, 76 warnings (up from 73) — three more instances of the same already-documented "bare-item link in a module's own `//!` header doesn't resolve" rustdoc quirk this codebase already carries dozens of, not a new warning class. No changes to any existing source file outside `src/server/mod.rs`, `src/bin/dog_server.rs`, every `serve` call site (a one-argument addition each), `tests/server_tls_integration.rs` (new), `src/server/pem.rs` (new), and `Cargo.toml` (`rusty_tls`/`rcgen` dependency entries, new `[[test]]` entry) — verified by diff. One new dependency: `rusty_tls`, this crate's first git dependency.
- `EXTERNAL-DB-BENCHMARK` (external database comparison — SQLite/Postgres/DuckDB): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo test --release --all-features`: **275/275 passing**, unchanged from the prior checkpoint — bench-only change, no new tests. `cargo test --release` (default, no features): **63/63 passing**, unaffected. `cargo check --release` (default, no features): clean, ~6s — confirms the dependency-gating fix (see below) genuinely keeps `rusqlite`/`duckdb`/`postgres` out of a default build, not just claimed. `cargo doc --all-features --no-deps`: clean, 76 warnings, unchanged from the prior checkpoint (this round touches no doc-comment-bearing library item). A real `cargo bench --features research,external-db-bench --bench external_db` run (reduced sampling, `--warm-up-time 1 --measurement-time 2 --sample-size 20`) against a local Postgres 16 instance: all 36 cases (3 workloads × 3 sizes × 4 systems), no panics — numbers in `RESULTS.md`'s `## External database comparison` section. No changes to any existing source file outside `Cargo.toml`/`Cargo.lock` and this doc set — verified by diff (`git diff --stat`: `Cargo.lock`, `Cargo.toml`, `README.md`, `RESULTS.md`, `docs/PROJECT-STATUS.md`, `docs/roadmap/ROADMAP.md`, `docs/specifications/SPEC-REGISTRY.md`, `docs/traceability/TRACEABILITY.md`, plus new files `benches/external_db.rs`, `docs/decisions/ADR-0015-external-database-benchmark.md`, `docs/specifications/storage/STORAGE-013-external-database-benchmark.md` — no `src/` file touched). Two real methodology bugs found and fixed mid-round, not glossed over: Postgres's missing post-`COPY` `ANALYZE` (see `## Completed` above and `RESULTS.md`), and the dev-dependency gating bug (`[dev-dependencies]` entries without `optional = true` still compiled under a default, zero-features `cargo test` — fixed by moving `rusqlite`/`duckdb`/`postgres` to `optional = true` under `[dependencies]`, `dep:`-gated, matching `rusty_tls`'s own precedent; see ADR-0015's Consequences for the full account). Three new dependencies: `rusqlite`, `duckdb` (both `bundled`), `postgres` (`with-uuid-1`) — all `external-db-bench`-gated, off by default.
- `MMAP-AGE-STORE-IDENTITY-FIX` (record-identity-keying port + columnar redesign): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo test --release --all-features`: **283/283 passing** (up from 275/275) — 238 lib unit (up from 230, +8 new `durability::mmap_store::tests::*`) + every pre-existing suite unchanged. `cargo test --release` (default, no features): **71/71 passing** (up from 63/63, +8), unaffected otherwise — `durability` is unconditionally compiled, so the new tests run in both builds. `cargo doc --all-features --no-deps`: clean, 77 warnings (up from 76) — one more instance of the same already-documented rustdoc quirk, not a new class (every genuinely new "links to private item" warning this round's first draft introduced was fixed per this codebase's own established convention — dropping the intra-doc link brackets). Two full before/after benchmark comparisons, per the owner's own explicit request to understand the full impact before merging (git-stashed the fix, captured a same-session baseline, restored it, re-measured — both `benches/workloads.rs`'s `production`-only rows and `benches/external_db.rs`'s `production`-only rows): the row-oriented attempt regressed `scan_ages` 2.2-6.1× and reversed `EXTERNAL-DB-BENCHMARK`'s own just-published headline finding; the accepted columnar redesign cut that to 1.1-1.4× and restored the headline finding almost exactly (full tables in `RESULTS.md`'s `### MmapAgeStore identity-keying fix`). No changes to any file outside `src/durability/{mmap_store,mod}.rs` and this doc set — verified by diff. No new dependency.
- `TWO-HOP-EXTERNAL-DB-BENCHMARK` (`neighbors_two_hop` added to the external-DB comparison): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --bench external_db --features research,external-db-bench`: clean. `cargo test --release --all-features`: **283/283 passing**, unchanged from the prior checkpoint — bench-only change, no new tests. `cargo test --release` (default, no features): **71/71 passing**, unaffected. `cargo doc --all-features --no-deps`: unaffected (this round touches no doc-comment-bearing library item; `benches/external_db.rs` is a bench target, not part of `cargo doc`'s scope). A real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_two_hop` run (same reduced sampling as every other pass in this section, `--warm-up-time 1 --measurement-time 2 --sample-size 20`) against the same local Postgres instance already running this session: all 12 new cases (4 systems × 3 sizes), no panics — numbers in `RESULTS.md`'s new `### Graph traversal, two-hop` subsection. The Postgres join-strategy finding was confirmed directly via `EXPLAIN (ANALYZE, BUFFERS)` against a prepared statement on each size's own schema, not assumed from the wall-clock numbers alone. No changes to any file outside `benches/external_db.rs` and this doc set — verified by diff. No new dependency.
- `THREE-HOP-EXTERNAL-DB-BENCHMARK` (`neighbors_three_hop` added to the external-DB comparison): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --bench external_db --features research,external-db-bench`: clean. `cargo test --release --all-features`: **283/283 passing**, unchanged from the prior checkpoint — bench-only change, no new tests. `cargo test --release` (default, no features): **71/71 passing**, unaffected. `cargo doc --all-features --no-deps`: clean, 77 warnings, unchanged (the new private `three_hop_neighbors` function lives in a bench target, not the library, so `cargo doc --no-deps` never touches it). A real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_three_hop` run (same reduced sampling as every other pass in this section) against the same local Postgres instance already running this session: all 12 new cases (4 systems × 3 sizes), no panics — numbers in `RESULTS.md`'s new `### Graph traversal, three-hop` subsection. The Postgres join-strategy finding's reproduction at this depth was confirmed directly via a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass against each size's own schema, not assumed to carry over from the two-hop pass. No changes to any file outside `benches/external_db.rs` and this doc set — verified by diff. No new dependency.
- `FOUR-HOP-EXTERNAL-DB-BENCHMARK` (`neighbors_four_hop` added to the external-DB comparison): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --bench external_db --features research,external-db-bench`: clean. `cargo test --release --all-features`: **283/283 passing**, unchanged from the prior checkpoint — bench-only change, no new tests. `cargo test --release` (default, no features): **71/71 passing**, unaffected. `cargo doc --all-features --no-deps`: clean, 77 warnings, unchanged (the new private `four_hop_neighbors` function lives in a bench target, not the library). A real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_four_hop` run (same reduced sampling as every other pass in this section) against the same local Postgres instance already running this session: all 12 new cases (4 systems × 3 sizes), no panics — numbers in `RESULTS.md`'s new `### Graph traversal, four-hop` subsection. The Postgres/SQLite crossover at 1M was confirmed directly via a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass on each size's own schema (both the 1K and 1M schemas), not inferred from the wall-clock numbers alone. No changes to any file outside `benches/external_db.rs` and this doc set — verified by diff. No new dependency.
- `FIVE-HOP-EXTERNAL-DB-BENCHMARK` (`neighbors_five_hop` added to the external-DB comparison): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo check --bench external_db --features research,external-db-bench`: clean. `cargo test --release --all-features`: **283/283 passing**, unchanged from the prior checkpoint — bench-only change, no new tests. `cargo test --release` (default, no features): **71/71 passing**, unaffected. `cargo doc --all-features --no-deps`: clean, 77 warnings, unchanged (the new private `five_hop_neighbors` function lives in a bench target, not the library). A real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_five_hop` run (same reduced sampling as every other pass in this section) against the same local Postgres instance already running this session: all 12 new cases (4 systems × 3 sizes), no panics — numbers in `RESULTS.md`'s new `### Graph traversal, five-hop` subsection. The four-hop crossover's reversal was confirmed directly via a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass against all three sizes' own schemas (not just the two that mattered at four hops), establishing that Postgres's query plan itself is unchanged while the per-hop I/O growth that decides the "closest engine" question is genuinely volatile. No changes to any file outside `benches/external_db.rs` and this doc set — verified by diff. No new dependency.
- `PRODUCTION-STORE-PORTABILITY-DESIGN` (design doc + ADR-0016, no implementation): docs-only round — `cargo fmt --all -- --check`: clean (no Rust source touched). No test/clippy/doc impact: zero `src/` changes, verified by diff. Design accuracy validated by direct investigation of the current code (`src/durability/mmap_store.rs`, `src/production.rs`, `src/record.rs`, `src/durability/snapshot_full.rs`) rather than assumed from memory — confirmed `DogRecord` already derives `Serialize`/`Deserialize`, confirmed `SnapshotFullStore::open(path)` already takes only a path (the proof-of-concept this proposal's companion-file approach generalizes from), confirmed `MmapAgeStore`'s own write-to-temp-then-atomic-rename mechanism (`write_via_rename`) is real and reusable as described.
- `PRODUCTION-STORE-PORTABILITY` (`STORAGE-014` implementation): `cargo fmt --all -- --check`: clean. `cargo clippy --all-targets --all-features -- -D warnings`: clean (one round: `RecordBlob::write` is test-only, so it carries `#[cfg(test)]` rather than a dead-code allowance). `cargo test`: 83 unit + 2 doc tests pass (9 new in `record_blob.rs`, 5 new in `production.rs`; `MmapAgeStore`'s existing suite unmodified). `cargo test --all-features`: green. `cargo doc --no-deps`: clean. `git diff --stat src/durability/mmap_store.rs`: empty. Cost measurement: a throwaway release-build example (deleted, not committed) timing `create`/`open`/`open_portable` at 1K/100K/1M records before and after, median of 7 samples (3 at 1M) — table in `RESULTS.md`.
- `RECORD-BLOB-FINGERPRINT` (`STORAGE-014` v0.2.0, blob version 2): `cargo fmt --all -- --check`: clean (one round: three long `assert!` lines wrapped by `cargo fmt --all`). `cargo clippy --all-targets --all-features -- -D warnings`: clean. `cargo test --all-features`: green — 256 lib tests (12 in `record_blob.rs`, 6 portability tests in `production.rs`; `MmapAgeStore`'s suite unmodified), every integration suite passing. `cargo doc --no-deps`: no new warnings. `git diff --stat src/durability/mmap_store.rs`: still empty. Pinned fingerprint `0xd98e_f572_3749_8d59` cross-checked against an independent FNV-1a implementation (one round: the first pinned constant was a placeholder and the test reported the real value). Cost re-measured with the same throwaway release-build example (deleted, not committed), two runs, median of 7 samples (3 at 1M) — table in `RESULTS.md`.
- `GENERIC-STORE-FINGERPRINT-MEASUREMENT` (docs-only, `STORAGE-015` unchanged at v0.1.0): `cargo fmt --all -- --check` clean (no Rust source touched); zero `src/`/`benches/`/`tests/` changes, verified by diff — the scratch example used for the measurement was never committed. Measurement validity: the in-place A/B compared the same binary with one call stubbed, on the same records, in the same process order (5 samples each, medians); the isolated candidate-fingerprint timings hash the identical record set and print their hash values, and the streamed-bincode and pre-serialized-body candidates agreed bit-for-bit (`0xa9f4234a1f1c1689` both ways), so they measure the same hash over the same bytes. The Criterion drift figure is one re-run of the published group on the unchanged commit (`--warm-up-time 1 --measurement-time 2 --sample-size 20`). A later full-bench rerun at Criterion's defaults, still on unchanged code (recorded in `RESULTS.md` after the decision), reported "no change" on `open`/`create` at 100K and 1M and flagged only `open_append`/1M (+11.5%, 10-sample group) and `open`/1K (−6.3%, 0.29 ms call) — calibration of the harness's drift, not a result.
- `GENERIC-STORE-PORTABILITY` (`STORAGE-015` v0.1.0): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test` and `cargo test --all-features` green — 16 new tests (`src/generic/record_blob.rs` 9: companion-path derivation matching `ProductionStore`'s, encode/`read` round trip of every field, the streamed fingerprint equal to the one in the header, a different record set not current, a missing blob unreadable and not current, a `DOGBLOB\0` file a magic error, a wrong version a version error, a body not matching the header fingerprint rejected, a truncated body a fingerprint error not a panic; `src/generic/mmap_store.rs` 6: `create` writes the companion and `open_portable` round-trips every field in creation order, both files copied to a fresh directory reopen portably, a missing companion a typed error naming its path and a plain `open` heals it, the companion rewritten only when the record set changed, a `ProductionStore` blob at the companion path a magic error, a stray temp file not a companion and an `.mmap`-file failure leaving a valid companion untouched; `src/generic/order_customer.rs` 1: `open_order_production_stack_portable` rebuilding the full stack, `Reversed` included, and failing with a typed error naming the companion once it is removed) plus the `production.rs` doctest reopening via `open_portable`; `mmap_store.rs`'s 8 pre-existing tests, `tests/mmap_record_identity_keying.rs`, `RecordBlob`'s 12 tests, and `production.rs`'s 6 portability tests pass unmodified; `cargo doc --no-deps` carries only the 53 pre-existing unresolved-link warnings (same count on the pre-change tree, verified by stash comparison). Cost measured, not assumed: a release-build throwaway example (not kept) at 1K/100K/1M, medians of 7/7/3, three after-runs and two before-runs, plus the published Criterion `generic_production_create`/`generic_production_open` groups run `git stash`-isolated before and (twice) after in the same session — all in `RESULTS.md`'s `### GenericMmapStore file portability (STORAGE-015)` subsection, including the one cell where the two harnesses disagree (`open` at 1M) and which figure this project trusts and why.
- `SYMMETRIC-EDGE-PORTABILITY` (`STORAGE-016` v0.1.0): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test` and `cargo test --all-features` green — 23 new tests (`src/generic/edge_blob.rs` 12: path derivation, `Uuid`-pair round trip in order, the streamed fingerprint equal to the header's, a reordered or different list not current, an empty list, a missing blob unreadable and not current, a short file a header error, a `GENBLOB\0` file and a `DOGBLOB\0` file each a magic error, a wrong version a version error, a tampered body rejected, a truncated body a fingerprint error not a panic; `src/generic/store.rs` 6: `create` → `open_portable` rebuilds the same adjacency in the same order, `open` with the same edges leaves bytes and mtime alone, changed edges rewrite, reordered edges count as changed, a missing blob is a typed error from `open_portable` that `open` heals, `create` over an existing blob always rewrites; `src/generic_spike/employee_impl.rs` 5, research-gated: every query identical after a no-argument portable reopen, the three files copied elsewhere reopen, a missing `.edges` is a typed error the existing `open` heals, `open` rewrites the edge blob only when the edge list changed, a `.records` blob at the edge path is a magic error). Every pre-existing test unmodified and passing. `cargo doc --no-deps`: no new warnings. `git diff src/generic/store.rs` removes nothing (it adds the imports, a private `PortableEdges<R>` alias, the one impl block, and the test module). Two rounds of fixes: the `employee_impl.rs` `Snapshot` helper derived `Eq` while `Employee` derives only `PartialEq` (dropped `Eq`); `clippy::type_complexity` on `read_portable_edges`'s return type (factored into the `PortableEdges<R>` alias, the `PersistedSlots` precedent in `mmap_store.rs`). Cost not measured, per the design's verification plan.
- `SERVER-PROTOCOL-VERSION` (`SERVER-001` v0.10.0 / FR-020; PR #126): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 337 lib tests (333 prior + 4: `protocol.rs`'s `protocol_version_is_the_one_the_table_names` and both pinned-bytes tests extended with the `Hello` golden vectors; `mod.rs`'s `dispatch_never_routes_hello_to_a_store`, `hello_is_answered_unauthenticated_with_the_min_version`, `hello_version_zero_and_a_second_hello_are_malformed_but_not_fatal`) plus every integration suite including the new `tests/server_protocol_version.rs` (4/4: pre-auth `min` negotiation, version 0 / late hello `Malformed` with the connection open and a silent client served, `SchemaDrivenClient` negotiating 2 on all three domains, an unknown request index closing the connection with no reply); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`; three intra-doc links in the new `src/server/mod.rs` section were rewritten to `crate::server::protocol` paths so they resolve); `git diff --stat` limited to `src/server/{protocol,mod,client}.rs`, `tests/server_protocol_version.rs`, one `Cargo.toml` `[[test]]` entry, and docs — `src/server/framing.rs`, `src/codec.rs`, `benches/server.rs`, and every pre-existing test file untouched, verified by diff; both `Hello { 2 }` golden vectors derived from `crate::codec`'s rules (index 10 as a `u32` LE, then the `u32` LE field) and confirmed by the pinned-bytes tests against the real encoder.
- `SERVER-CLIENT-AUTH` (`SERVER-001` v0.11.0 / FR-021; PR #128): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 337 lib tests unchanged plus every integration suite including `tests/server_auth_integration.rs` now 7/7 (5 prior + `schema_driven_client_authenticates_at_connect_and_can_change_class_later`, `schema_driven_client_authentication_is_a_no_op_without_configured_tokens`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `git diff --stat` limited to `src/server/client.rs`, `tests/server_auth_integration.rs`, and docs — no `Cargo.toml`, protocol, or server change; `tests/server_schema_driven_client.rs` and `tests/server_protocol_version.rs` pass unmodified (every pre-existing `SchemaDrivenClient` path unchanged apart from the sharper `connect` error on an auth-configured server).
- `SERVER-CLIENT-TLS` (`SERVER-001` v0.12.0 / FR-022; PR #130): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 337 lib tests unchanged plus every integration suite including `tests/server_tls_integration.rs` now 8/8 (5 prior + `schema_driven_client_connects_over_tls`, `schema_driven_client_authenticates_over_tls`, `schema_driven_client_tls_failures_are_errors_not_hangs`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `git diff --stat` limited to `src/server/client.rs`, `tests/server_tls_integration.rs`, and docs — no `Cargo.toml`, protocol, or server change; `tests/server_schema_driven_client.rs`, `tests/server_protocol_version.rs`, and `tests/server_auth_integration.rs` pass unmodified (every pre-existing `SchemaDrivenClient` path unchanged; the internal `BufReader`/`BufWriter` pair became one stream with the same one-write-per-request behavior).
- Legacy "this PR" backfill (item 73; PR #132): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; `grep -c '| this PR |'` over `ROADMAP.md`/`TRACEABILITY.md` is 0 on the PR head; every cited PR number confirmed against `git log --merges` on `origin/main`.
- `SERVER-MTLS-DESIGN` proposal (design doc + ADR-0023, Proposed; PR #133): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; every `rusty_tls` claim (`TlsAcceptor::new_with_client_auth` and `TlsStream::new_with_client_identity` signatures, no peer-certificate accessor on `TlsServerStream`, revocation only on `TrustPolicy`) read from the pinned `9fd2e27` checkout; the `rcgen` 0.13.2 CA-issuance API (`CertificateParams::signed_by`, `IsCa`, `ExtendedKeyUsagePurpose::ClientAuth`) read from the vendored source; no code change.
- `SERVER-MTLS-DESIGN` acceptance (design doc + ADR-0023 promoted Proposed → Accepted, no implementation; PR #134): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; the throwaway probe (`tests/zz_mtls_probe.rs` + one `[[test]]` line, both discarded before commit) passed 1/1 under `--features server` with the four outcomes recorded in `ADR-0023`'s acceptance log.
- `SERVER-MTLS` (`SERVER-001` v0.13.0 / FR-023; PR #135): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 338 lib tests (337 + `tls_from_env_values_treats_a_client_ca_without_a_chain_and_key_as_an_error`) plus every integration suite including `tests/server_tls_integration.rs` now 12/12 (8 prior + `mtls_admits_a_client_whose_certificate_chains_to_the_configured_ca`, `mtls_rejects_a_client_without_a_certificate_or_with_one_from_another_ca`, `mtls_composes_with_auth_config_as_admission_then_class`, `mtls_pem_files_and_construction_errors`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `git diff --stat` limited to `src/server/mod.rs`, `src/server/client.rs`, `src/bin/dog_server.rs`, `tests/server_tls_integration.rs`, and docs — no `Cargo.toml`, wire, protocol-version, or `handle_connection` change; `tests/server_schema_driven_client.rs`, `tests/server_protocol_version.rs`, `tests/server_auth_integration.rs`, and the eight pre-existing TLS tests pass unmodified.
- `SERVER-TRANSACTION-SESSION-DESIGN` proposal (design doc + ADR-0024 + ADR-0025, Proposed; PR #137): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; every claim about the current code (variant indices 0–10 on both enums, the `Hello`/`Authenticate` intercept order and the `ReadOnly` gate, `apply_transaction`'s validate-then-apply under `with_exclusive`, the per-slot `COMMITTED` marker and `msync` `flush`, `Flush` own-then-inner across an `MmapScanned` stack, `hybrid.rs`'s never-truncate discipline, `crate::codec` and `TransactionOp`'s golden vector) read from `main` `371b421`; no code change.
- `SERVER-TRANSACTION-SESSION-DESIGN` acceptance (design doc + ADR-0024 + ADR-0025 promoted Proposed → Accepted, no implementation; PR #138): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration.
- `SERVER-TRANSACTION-SESSION` (`SERVER-001` v0.14.0 / FR-024; PR #139): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 340 lib tests (338 + `protocol.rs`'s `session_shapes_round_trip_through_the_codec` and `mod.rs`'s `dispatch_never_routes_session_requests_to_a_store`; `protocol_version_is_the_one_the_table_names` now pins 3 and both pinned-bytes tests carry the version-3 vectors) plus every integration suite — `tests/server_transaction_integration.rs` 10/10 (6 prior + `a_session_stages_writes_and_commits_them_as_one_batch`, `a_commit_failure_names_the_staged_index_and_applies_nothing`, `session_state_errors_leave_the_connection_open`, `a_read_only_connection_can_open_a_session_but_not_stage_or_commit`), `tests/server_protocol_version.rs` 5/5 (4 prior + `session_requests_are_malformed_below_protocol_version_3`; the unknown-index test moved from 11 to 14, as its own doc predicted), `tests/server_schema_driven_client.rs` 5/5 (4 prior + `sessions_stage_commit_and_roll_back_on_every_domain`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `git diff --stat` limited to `src/server/{protocol,mod,client}.rs`, the three test files, and docs — no `Cargo.toml`, adapter, store, or framing change; `benches/server.rs` untouched.
- `SERVER-ACCESS-LOG-DESIGN` proposal (design doc + ADR-0031, Proposed; PR #165): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; every claim about the current code (`handle_connection`'s dispatch path taking no observer, `AuthConfig`'s current shape and `Debug`, `audit.rs`'s `RequestKind` exhaustive over every `Request` variant) read from `main` `24690db`; no code change.
- `SERVER-SESSION-VALIDATE-ON-STAGE` (`SERVER-001` v0.20.0 / FR-030; PR #163): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 357 lib tests (356 + `dog.rs`'s `validate_op_reports_exactly_what_commit_would`; `protocol_version_is_the_one_the_table_names` pins 6) plus every integration suite including `tests/server_transaction_integration.rs` now 17/17 (15 prior + `a_validating_session_refuses_bad_writes_when_staged`, `the_validate_bit_is_unknown_below_protocol_6`; the unknown-flag probe moved from bit 2 to bit 4), `tests/server_schema_driven_client.rs` 7/7 (6 prior + `validating_sessions_report_bad_writes_at_update_through_the_client`), `tests/server_protocol_version.rs` 6/6 (the `begin_with` gate asserted in the pre-hello test); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `git diff --stat` limited to `src/server/{protocol,mod,client,dog,order,employee}.rs`, three test files, and docs — no `Cargo.toml`, `Response`/`ErrorCode`, store, or `Cargo.toml` change; every pre-existing session, journal, audit, TLS, and client suite passes unmodified apart from the two version pins (5 → 6) and the moved unknown-flag probe.
- `SERVER-AUTH-RATE-LIMIT-DESIGN` proposal (design doc + ADR-0030, Proposed; PR #161): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; every claim about the current code (the `Authenticate` arm answering `Unauthenticated` and continuing with the connection open, one constant-time comparison per configured token per attempt, `AuthConfig`'s `Clone` and the one `Arc<AuthConfig>` `serve` shares, `AuditKind`/`RequestKind` without `#[non_exhaustive]`, the audit log's `AuthenticationFailed` by peer) read from `main` `fce0762`; no code change.
- `SERVER-AUTH-RATE-LIMIT-DESIGN` and `SERVER-ACCESS-LOG-DESIGN` acceptance (design docs + `ADR-0030`/`ADR-0031` promoted Proposed → Accepted, no implementation; PR #166): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration.
- `SERVER-MTLS-CLASS` (`SERVER-001` v0.21.0 / FR-031; PR #166): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 360 lib tests (357 + `class_for_certificate_matches_by_exact_der_bytes_only`, `is_configured_is_true_with_only_a_certificate_class`, `auth_config_debug_prints_certificate_counts_not_bytes`) plus `dog_server`'s `certificate_classes_from_env_values_follows_the_documented_table` and every integration suite including `tests/server_tls_integration.rs` now 17/17 (12 prior + 5 class-from-certificate tests); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (0 baseline, 0 after); `cargo tree --features server` shows no new package name; `git diff --stat` limited to `Cargo.toml`/`Cargo.lock`, `src/server/mod.rs`, `src/bin/dog_server.rs`, `tests/server_tls_integration.rs` — no wire, `PROTOCOL_VERSION`, `TokenClass`, `TlsConfig`, or `ConnectionStore` change.
- `SERVER-AUTH-RATE-LIMIT` (`SERVER-001` v0.22.0 / FR-032; this PR): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 364 lib tests (360 + `rate_limit_parse_accepts_valid_and_rejects_malformed_input`, `failure_table_tracks_each_peer_independently`, `failure_table_forgets_a_peer_once_its_window_elapses`, `failure_table_never_exceeds_max_tracked_peers`) plus `dog_server`'s `rate_limit_from_env_value_follows_the_documented_table` and every integration suite including `tests/server_auth_integration.rs` now 13/13 (10 prior + 3 rate-limit/lockout tests); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (0 baseline, 0 after); `git diff --stat` limited to `src/server/{audit,mod}.rs`, `src/bin/dog_server.rs`, `tests/server_auth_integration.rs` — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, or `serve`-signature change.
- `SERVER-ACCESS-LOG` (`SERVER-001` v0.23.0 / FR-033; this PR): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 367 lib tests (364 + `lines_follow_the_documented_format_and_are_distinct_from_audit_lines`, `no_line_can_contain_a_marker_value`, `file_access_log_appends_and_a_failing_file_drops_without_failing`) plus `dog_server`'s 4 binary tests (3 + `access_sink_from_follows_the_documented_table`) and every integration suite including `tests/server_auth_integration.rs` now 14/14 (13 prior + `access_log_and_audit_log_streams_stay_disjoint`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (0 baseline, 0 after); `git diff --stat` limited to `src/server/access.rs` (new), `src/server/mod.rs`, `src/bin/dog_server.rs`, `tests/server_auth_integration.rs` — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, or `serve`-signature change.
- `SERVER-AUDIT-ADMITTED-CERT-FIELD` (`SERVER-001` v0.24.0 / FR-034; this PR): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 367 lib tests unchanged plus every integration suite including `tests/server_tls_integration.rs` now 18/18 (17 prior + `admitted_records_classed_by_certificate_for_a_matched_leaf_only`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (0 baseline, 0 after); `git diff --stat` limited to `src/server/{audit,mod}.rs`, `tests/server_auth_integration.rs`, `tests/server_tls_integration.rs` — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, or `serve`-signature change.
- `SERVER-SERVE-OPTIONS` (`SERVER-001` v0.25.0 / FR-035; PR #171): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 367 lib tests unchanged, every integration suite unchanged (`tests/server_tls_integration.rs` 18/18, `tests/server_auth_integration.rs` 14/14, no test added or removed anywhere); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after); `git diff --stat` across all sixteen touched files (`src/server/mod.rs`, `src/lib.rs`, `src/server/{access,audit,client,pem}.rs`, `src/bin/dog_server.rs`, `benches/server.rs`, eight test files) limited to `use`/type-name/parameter-passing lines — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, or store change; every acceptance criterion 1–5 recorded as holding in `SERVER-001`; the resolution pointer in place (the trigger `ADR-0029` first named, `ADR-0030`/`ADR-0031` each restated).
- `SERVER-SESSION-SNAPSHOT-ISOLATION` (`SERVER-001` v0.26.0 / FR-036; this PR): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 371 lib tests (367 + `mod.rs`'s `record_read_set_replaces_a_repeated_key_with_the_latest_value`/`record_read_set_keys_by_both_id_and_field`/`record_read_set_stops_adding_new_keys_past_the_cap_but_keeps_updating_old_ones` + `dog.rs`'s `apply_transaction_checks_the_read_set_atomically_with_the_write_batch`; `protocol_version_is_the_one_the_table_names` pins 7, two new `BeginWith` golden vectors, `ErrorCode::Conflict` added to the pinned-code loop) plus `tests/server_transaction_integration.rs` now 22/22 (17 prior + `snapshot_isolation_detects_a_conflicting_commit_from_another_connection`, `snapshot_isolation_commits_normally_with_no_conflicting_write`, `snapshot_isolation_does_not_track_a_not_found_read`, `snapshot_isolation_composes_with_read_your_writes`, `the_snapshot_isolation_bit_is_unknown_below_protocol_7`), plus two pre-existing pinned-version-literal fixes (`tests/server_schema_driven_client.rs`'s `server_protocol_version()` 6 → 7; `tests/server_transaction_integration.rs`'s "unknown flag" test moved from `flags: 4`, now a real bit, to `flags: 8`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after); no `Cargo.toml`, store (`src/store/**`), or `serve`-signature change; every acceptance criterion 1–8 recorded as holding in `SERVER-001`; `SESS-FR-007`'s second half and the v0.7.0 "isolation levels beyond full serialization by lock" gap resolved by pointer.
- `SERVER-SQL-SELECT` (`SERVER-001` v0.27.0 / FR-037; this PR): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 389 lib tests (371 + 18: `mod.rs`'s six new `sql`-prefixed unit tests, `sql.rs`'s eleven parser unit tests, and `dog.rs`'s one new adapter unit test; `protocol_version_is_the_one_the_table_names` pins 8, two new `Request::Query`/`Response::Rows` golden vectors added inside the existing golden-vector test) plus `tests/server_sql_integration.rs` (new) 14/14 (`select_star_with_no_where_returns_every_row_and_field`, `named_columns_project_exactly_that_subset`, `every_comparator_kind_filters_correctly`, `two_and_ed_conditions_across_two_fields`, `limit_truncates_and_omitting_it_returns_every_match`, `unknown_column_is_a_client_side_error`, `kind_mismatch_and_bad_ordering_comparator_are_client_side_errors`, `a_syntax_error_is_a_client_side_error`, `a_field_with_every_capability_flag_false_is_still_queryable`, `order_domain_query_round_trip`, `employee_domain_query_round_trip`, `query_requires_protocol_version_8`, `query_inside_a_read_your_writes_session_sees_committed_state_only`, `query_inside_a_snapshot_isolation_session_is_not_read_set_tracked`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after); no `Cargo.toml`, store (`src/store/**`), or `serve`-signature change; every acceptance criterion 1–10 recorded as holding in `SERVER-001`; `docs/FUTURE-GROWTH.md`'s "SQL" item narrowed and resolved by pointer.
- `SERVER-SQL-AGGREGATE` (`SERVER-001` v0.28.0 / FR-038; this PR): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 404 lib tests (389 + 15: `sql.rs`'s ten new grammar unit tests — `COUNT(*)`, every function with a field argument, a mixed plain-and-aggregate column list, `GROUP BY` on one/several fields composed with `WHERE`/`LIMIT`, and every new syntax error — and `mod.rs`'s five new unit tests — `validate_aggregate`'s every rejection shape, `evaluate_aggregate`'s `group_by` empty-vs-grouped and a filter matching nothing in each shape, every `AggregateFn` including the `Sum`/`Count`-vs-`Avg` cross-check, `limit`, and the empty-implicit-bucket `Min`/`Max`/`Avg` fallback specifically caught by this round's own testing, not the design review; `protocol_version_is_the_one_the_table_names` pins 9, two new `Request::Aggregate`/`Response::Groups` golden vectors added inside the existing golden-vector tests, the version-8 `Query`/`Rows` vectors byte-for-byte unchanged) plus `tests/server_sql_integration.rs` now 29/29 (14 prior + 15: `count_star_with_no_group_by_returns_one_whole_table_group`, `group_by_one_field`, `group_by_two_fields`, `every_aggregate_function_matches_a_hand_computed_reduction`, `avg_equals_sum_divided_by_count`, `where_clause_combines_with_group_by`, `a_filter_matching_nothing_for_a_key_produces_no_group_for_that_key`, `count_star_with_a_filter_matching_nothing_still_returns_one_zero_group`, `limit_on_a_grouped_result_truncates_the_group_count`, `a_plain_column_missing_from_group_by_is_a_client_side_error`, `select_star_alongside_group_by_is_a_client_side_error`, `an_aggregate_function_against_a_non_numeric_field_is_a_client_side_error`, `aggregate_requires_protocol_version_9_but_plain_select_still_runs_at_8`, `aggregate_inside_a_read_your_writes_session_sees_committed_state_only`, `aggregate_inside_a_snapshot_isolation_session_is_not_read_set_tracked`); `cargo test` (default features) 136 lib + 2 doctests unchanged; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after — one transient new "unresolved link" warning from a doc-comment intra-link this round introduced was caught and fixed, not left standing); no `Cargo.toml`, store (`src/store/**`), or `serve`-signature change; every acceptance criterion 1–8 recorded as holding in `SERVER-001`; `docs/FUTURE-GROWTH.md`'s aggregation/`GROUP BY`/`AVG` item narrowed and resolved by pointer.
- `SERVER-REMINDER-DOMAIN` (`SERVER-001` v0.29.0 / FR-039; this PR): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 412 lib tests (404 + 8: `generic/reminder.rs`'s three new unit tests — `status_to_u32_and_back_round_trips_every_variant`, `create_then_get_and_filter_eq_by_due_at`, `update_status_is_durable_across_reopen` — and `server/reminder.rs`'s five new unit tests — `get_returns_every_field`, `filter_eq_by_due_at_and_unsupported_fields`, `scan_and_update_status_only_with_discriminant_validation`, `describe_names_all_three_fields_and_reports_no_relations`, `every_relation_request_is_unsupported`) plus `tests/server_reminder_integration.rs` (new) 9/9 (`get_returns_every_field_and_group_by_status_counts_correctly`, `filter_eq_by_due_at_and_unsupported_fields`, `update_status_with_discriminant_validation_and_unsupported_fields`, `every_relation_request_is_unsupported`, `query_filters_on_due_at_by_range`, `transaction_applies_every_write`, `sessions_compose_with_reminder_the_same_as_every_other_domain`, `title_has_every_capability_flag_false_but_is_still_queryable`, `parent_lookup_type_is_unreachable_client_side`); `cargo test` (default features) 139 lib tests (136 + 3 — the `generic::reminder` unit tests, run even under zero features, confirming genuine front-door status) + 2 doctests unchanged; a real `cargo run --bin reminder_server --features server` smoke check confirmed the binary builds and actually listens, not just that `required-features` compiles; `cargo doc --all-features --no-deps` no new warnings (64 baseline, 64 after — ten transient new "unresolved link" warnings this round's own new module-header doc comments introduced, the same long-standing rustdoc quirk this codebase has repeatedly documented, were caught and fixed in place before backfill, not left standing); no `Cargo.toml` dependency change (`[[bin]]`/`[[test]]` entries only), no store (`src/store/**`) change, no `serve`-signature change; every acceptance criterion 1–8 recorded as holding in `SERVER-001`; the owner's own question about backing `rusty_remind_me` answered with a real, deployable domain, the schema-shape assumption against the real target left as this round's own named open question for a later integration unit.
- `SERVER-AUTH-AUDIT` (`SERVER-001` v0.19.0 / FR-029; PR #159): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 356 lib tests (353 + `audit.rs`'s three — the documented line format per variant, no line can carry a token or a certificate, `FileAudit` appending across opens and a read-only handle dropping without failing) plus `dog_server`'s `audit_sink_from_follows_the_documented_table` and every integration suite including `tests/server_auth_integration.rs` now 10/10 (7 prior + `every_gate_decision_is_recorded_in_order_with_the_peer`, `unauthenticated_refusals_and_open_servers_are_recorded_as_designed`, `a_file_sink_appends_one_line_per_event_through_the_server`) and `tests/server_tls_integration.rs` now 13/13 (12 prior + `handshake_outcomes_are_audited_with_a_reason`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `git diff --stat` limited to `src/server/audit.rs` (new), `src/server/mod.rs`, `src/bin/dog_server.rs`, two test files, and docs — no `Cargo.toml`, wire, `PROTOCOL_VERSION`, store, adapter, `ConnectionStore`, or `serve`-signature change; every pre-existing auth, TLS, session, journal, and client suite passes unmodified beside the appended tests.
- `SERVER-SESSION-READ-YOUR-WRITES` (`SERVER-001` v0.18.0 / FR-028; PR #157): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 353 lib tests (352 + `mod.rs`'s `overlay_staged_replaces_only_matching_updatable_fields`; the `BeginWith` golden vector, codec round trip, and dispatch check extend existing tests; `protocol_version_is_the_one_the_table_names` pins 5) plus every integration suite including `tests/server_transaction_integration.rs` now 15/15 (12 prior + `a_read_your_writes_session_sees_its_own_staged_writes_and_nobody_else_does`, `read_your_writes_never_shows_a_write_that_commit_would_refuse`, `begin_with_is_gated_by_version_and_refuses_unknown_flags`), `tests/server_schema_driven_client.rs` 6/6 (5 prior + `read_your_writes_sessions_see_their_own_staged_writes_through_the_client`), `tests/server_protocol_version.rs` 6/6 (the unknown-index probe moved to 15, the client gate asserted in the pre-hello test); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `git diff --stat` limited to `src/server/{protocol,mod,client}.rs`, three test files, and docs — no `Cargo.toml`, store, adapter, `ConnectionStore`, `dispatch`-signature, `Response`, or `ErrorCode` change; every pre-existing session, journal, TLS, auth, and client suite passes unmodified apart from the two version pins (4 → 5) and the probe index.
- `SERVER-JOURNAL-GROUP-COMMIT` (`SERVER-001` v0.17.0 / FR-027; PR #155): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 352 lib tests (347 + `journal.rs`'s four `CommitGroup` tests — ordered apply across eight threads equals journal order, a held leader syncing once for three batches, a failed sync refusing its group and the next batch succeeding, a checkpoint deferred until no later batch is appended — and `dog.rs`'s `the_fsync_never_holds_the_store_lock`) plus every integration suite including `tests/server_transaction_integration.rs` now 12/12 (11 prior + `concurrent_journaled_transactions_replay_to_the_live_state`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `cargo bench --features server,research --bench server` re-run with the `dog-jrnl-txn` rows recorded in `RESULTS.md`; `git diff --stat` limited to `src/server/{journal,dog,order,employee,mod}.rs`, `src/bin/dog_server.rs` (docs), `tests/server_transaction_integration.rs`, `RESULTS.md`, and docs — no `Cargo.toml`, journal-format, storage-format, wire, `PROTOCOL_VERSION`, `TransactionalStore`, or `ConnectionStore` change; every pre-existing journal, adapter, session, TLS, auth, and client suite passes unmodified.
- Four-design acceptance (`ADR-0026`, `ADR-0027`, `ADR-0028`, `ADR-0029` and their design docs promoted Proposed → Accepted, no implementation; PR #153): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration.
- `SERVER-AUTH-AUDIT-DESIGN` proposal (design doc + ADR-0029, Proposed; PR #151): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; every claim about the current code (the four gates in `handle_connection` and their order — `Hello`, `Authenticate`, the unauthenticated gate, the `ReadOnly` gate — `auth.check`'s constant-time comparison, `accept` then the frame loop with the handshake under the first read, `serve`'s four parameters, the binary's single startup `eprintln!`, no `log`/`tracing` in `Cargo.toml`) read from `main`; no code change.
- `SERVER-MTLS-CLASS-DESIGN` proposal (design doc + ADR-0028, Proposed; PR #149): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; every claim about the current code (`handle_connection`'s `accept` then frame loop with the handshake under the first read, `authenticated` seeded from `auth.is_configured()` alone, `AuthConfig`'s two tokens and `subtle` comparison, the `pem` module, the pinned `rusty_tls` `9fd2e27` having `TlsStream::peer_certificate_der` and `complete_handshake` on both streams but no server-side accessor, and no newer `rusty_tls` commit in the fetched history) read from `main` and the pinned checkout; the throwaway patch probe (a copied checkout with the eight-line accessor, a temporary `[patch]` and `[[test]]` in `Cargo.toml`, one test file) run under `--features server` 2/2 and every trace of it discarded before commit (`git status` clean, `Cargo.lock` restored); no code change.
- `SERVER-SESSION-READ-YOUR-WRITES-DESIGN` proposal (design doc + ADR-0027, Proposed; PR #147): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; every claim about the current code (`handle_connection`'s session intercept and `GetById` falling through to `dispatch`, `Session`'s mutable borrow and `update`/`commit`/`rollback`-only surface, no `ScanValue` kind accessor in `protocol.rs`, `Request` indices 0–13 with the unknown-index probe at 14, `PROTOCOL_VERSION = 4`, the `ReadOnly` gate refusing `UpdateField` before the session intercept) read from `main` `8edaf7e`; no code change.
- `SERVER-JOURNAL-GROUP-COMMIT-DESIGN` proposal (design doc + ADR-0026, Proposed; PR #145): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) 136 lib + 2 integration; every claim about the current code (`apply_transaction`'s validate → lock journal → `append` → apply → checkpoint all inside `with_exclusive` on all three adapters, the never-contended journal mutex, `BatchJournal`'s format and `open`, `TransactionalStore::with_exclusive` as an `RwLock` write guard, `RESULTS.md`'s FR-025 rows) read from `main` `3987f9c`; the probe (a `std`-only throwaway, never committed) run twice with both runs recorded in the design doc; no code change.
- `SERVER-CLIENT-HELLO-FALLBACK` (`SERVER-001` v0.16.0 / FR-026; PR #143): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 347 lib tests unchanged plus every integration suite including `tests/server_protocol_version.rs` now 6/6 (5 prior + `a_pre_hello_server_is_reconnected_to_without_a_hello_unless_hello_is_required`); `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (the warning set diffed against a stashed baseline: identical); `git diff --stat` limited to `src/server/client.rs`, `tests/server_protocol_version.rs`, and docs — no wire, server, `Cargo.toml`, or `PROTOCOL_VERSION` change; every pre-existing client and server suite (`tests/server_schema_driven_client.rs`, `tests/server_auth_integration.rs`, `tests/server_tls_integration.rs`, `tests/server_transaction_integration.rs`) passes unmodified.
- `SERVER-TRANSACTION-JOURNAL` (`SERVER-001` v0.15.0 / FR-025; PR #141): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 347 lib tests (340 + `journal.rs`'s four — header, replay with a torn tail dropped, foreign file/unknown version, checkpoint threshold and truncate — `dog.rs`'s `with_journal_replays_onto_pre_batch_files_and_is_a_no_op_on_post_batch_files` and `a_journaled_adapter_checkpoints_past_the_threshold`, `mod.rs`'s `journal_error_code_is_downgraded_below_version_4`; `protocol_version_is_the_one_the_table_names` now pins 4 and the `Err` vector loop carries `Journal` at 9) plus every integration suite — `tests/server_transaction_integration.rs` 11/11 (10 prior + `a_journaled_server_journals_transactions_and_session_commits_only`), every other suite unchanged apart from the two `server_protocol_version() == 4` assertions; `cargo test` (default features) 136 lib + 2 integration; `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`); `cargo bench --features server,research --bench server` completes with the new `dog-jrnl-txn` rows (numbers in `RESULTS.md`); `git diff --stat` limited to `src/server/{journal,dog,order,employee,mod,protocol}.rs`, `src/bin/dog_server.rs`, `benches/server.rs`, three test files, `RESULTS.md`, and docs — no `Cargo.toml`, store, storage-format, framing, or `ConnectionStore`-signature change.
- `SERVER-PROTOCOL-VERSION-DESIGN` acceptance (design doc + ADR-0022 promoted Proposed → Accepted, no implementation; PR #125): docs-only — `cargo fmt --all -- --check` clean; `cargo test` (default features) green as a no-regression check; no `src/` touched.
- `SERVER-PROTOCOL-VERSION-DESIGN` (design doc + ADR-0022, no implementation; proposed in PR #124): docs-only round — `cargo fmt --all -- --check`: clean (no Rust source touched); `cargo test` (default features) green as a no-regression check. No clippy/doc impact: zero `src/` changes, verified by diff. Design accuracy validated against the code as it stands at `main` `835d7a9` rather than recalled — `src/server/framing.rs` (the `u32` LE length prefix, `MAX_FRAME_BYTES` = `0x0100_0000`, so the prefix's top byte is `0x00` for every frame under 16 MiB — the fact behind the rejected per-frame option), `src/server/protocol.rs` (`Request` and `Response` each at ten variants, indices 0–9, so `Hello` lands at index 10 on both; the golden-vector tests' own note that appending keeps every line true), `src/server/mod.rs` (`handle_connection`'s `Err(_) => return` on any read/decode error — the silent drop; the `Authenticate` intercept's position ahead of the auth gate, which `Hello` mirrors), `src/server/client.rs` (`connect` sends `DescribeSchema` as its first frame; no `authenticate` method exists — the pre-existing gap noted), and `STORAGE-018`'s evolution rules (appending a variant is compatible — the guarantee the whole design rests on).
- `STORAGE-018` v0.1.1 (docs-only close of the `DOGBLOB\0` fingerprint-before-decode open question): no code or test change; `cargo test --all-features` 333 lib tests unchanged; the four documents that carried the open question (`STORAGE-018`, `SPEC-REGISTRY`, `ADR-0021`, this file) say the same thing.
- `BINCODE-ENCODING-STABILITY` (`STORAGE-018` v0.1.0): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 333 lib tests (316 prior + 12 in the capture commit: 7 `codec` golden, 2 `protocol` golden covering every `Request`/`Response` variant, 3 blob bodies; + 5 in the routing commit: trailing bytes rejected with free-function byte equality, `encode_into` ≡ `encode`, a framed `Request` with junk bytes → `FrameError::Encoding`, `GENBLOB\0` junk → fingerprint mismatch, `DOGBLOB\0` junk → `RecordBlobUnreadable`); `cargo test` (default features) 136 lib + 2 integration (the `Order`-shaped vectors and junk tests are `research`-gated with the type they use, the `Request`/`Response` vectors `server`-gated); `cargo doc --all-features --no-deps` no new warnings (63 pre-existing, the same count as `main`; three intra-doc links in the new `src/server/mod.rs` section were rewritten to `crate::server::protocol` paths so they resolve); `grep -rn "bincode::" src` → `codec.rs` plus `framing.rs:25` and `durability/mod.rs:140` only; acceptance criteria 1–8 of the spec each pinned by a named test. Golden vectors were hand-derived from the format rules (not copied from the encoder's output) and passed on the pre-codec code first, then unchanged after routing.
- `BINCODE-ENCODING-STABILITY-DESIGN` (design doc + ADR-0021, no implementation; proposed in PR #116, accepted in the acceptance PR that followed): docs-only rounds — `cargo fmt --all -- --check`: clean (no Rust source touched); `cargo test` (default features) green as a no-regression check, both rounds. No clippy/doc impact: zero `src/` changes, verified by diff. Design accuracy validated by direct investigation rather than recalled — the vendored `bincode-1.3.3` source (`src/lib.rs`'s free functions, `src/config/mod.rs`'s `DefaultOptions` and the "Options Struct vs bincode functions" table, `config/int.rs`, `config/trailing.rs`), `uuid-1.25.0`'s `serialize_bytes` path, the readme's stability promise quoted verbatim, an executed throwaway probe crate (not committed) confirming byte counts, trailing-byte behavior under both policies, and a cross-configuration decode failure, and every one of the 23 call sites with its feature gate (`grep -rn "bincode::" src`). No benchmark: nothing runs.
- `MULTI-FIELD-MMAP-DURABILITY` (`STORAGE-017` v0.1.0): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test --all-features` 316 lib tests (307 prior + 8 new in `mmap_scanned.rs` + 1 new in `order_customer.rs`, one existing extended); `cargo test` (default features) 124 lib + 2 integration; `cargo doc --no-deps` no new warnings; acceptance criteria 1–8 of the spec each pinned by a named test; Criterion before/after for `generic_production` (including the new `scan_layer` / `update_layer` groups) recorded in `RESULTS.md`.
- `MULTI-FIELD-MMAP-DURABILITY-DESIGN` (design doc + ADR-0020, no implementation): docs-only round — `cargo fmt --all -- --check`: clean (no Rust source touched); `cargo test` (default features) green as a no-regression check. No clippy/doc impact: zero `src/` changes, verified by diff. Design accuracy validated by direct investigation of the current code rather than assumed from memory — `src/generic/mmap_store.rs` (`MAGIC`/`SCHEMA_VERSION = 2`/`HEADER_LEN = 12`/`COMMITTED`, the `[id][value][COMMITTED]` slot, `slot_width`/`slot_offset`/`read_value`/`write_value`/`write_slot_into`/`append_committed_slot`/`is_committed`/`is_gapless`, `create`'s truncate-header-slots-blob order, `open`'s read-committed-by-id → reconcile (reuse / append-for-missing / ignore-stale) → remap → heal-blob order, `get`'s `set_scannable_value` patch, `scan`'s `chunks_exact` gapless fast path and sorted-positions fallback, `update`'s in-place `write_value`, `flush`'s single `msync`, and the module docs' explicit one-durable-field scope citing `ADR-0009` §4.2), `src/generic/store.rs` (`Scanned`'s `inner`/`inner_mut`/`position_index`/`cache` shape, its `GetById` patch, `Flush` forward, and `forward_scannable_pairs!`'s rotating-accumulator body generating one concrete `ScanField`/`UpdateField` pair per ordered marker pair for `Scanned` specifically — why the new layer needs the macro generalized), `src/generic/traits.rs` (`ScannableField::set_scannable_value`, `SchemaTag`), `src/generic/mmap_field.rs` (`MmapFieldValue::BYTE_WIDTH`, the `u32`/`i64`/`Uuid` impls — the fixed-width contract), `src/generic/order_customer.rs` (`OrderProductionStack = Reversed<GenericMmapStore<Order, Status, Amount>, Customer, Order, BelongsToCustomer>`, the three constructors, `Amount`/`CreatedAt`/`DiscountCents` all `i64` `ScannableField` markers, the `forward_scannable_pairs!(Order; Amount: i64, CreatedAt: i64, DiscountCents: i64)` invocation), `ADR-0006`'s revisit-trigger wording, `GENERIC-SCHEMA-DESIGN` §4.2's "N separate flat mmap-backed arrays" sketch, `ADR-0001`'s AoS/SoA single-column-scan finding, and `STORAGE-016`'s caller-supplied-path precedent. Numbers cited (25- vs. 41-byte slot stride for `Order` with one vs. three `i64` fields) are arithmetic on `BYTE_WIDTH`s, not measurements; the design asks for the measurements at implementation.
- `BLOB-SCHEMA-TAG` (`STORAGE-015` v0.2.0 / `STORAGE-016` v0.2.0, PR #108): `cargo fmt --all -- --check` clean; `cargo clippy --all-targets --all-features -- -D warnings` clean; `cargo test` (default features) and `cargo test --all-features` green — 12 new tests (5 `generic::record_blob`, 3 `generic::edge_blob`, 2 `generic::mmap_store` research-gated, 2 `generic::store`), every pre-existing unit, integration, and doc test passing unmodified apart from the `SchemaTag` impls the tests' own record types needed; `cargo doc --no-deps` warning count unchanged from `main` at the same commit (63 default / 79 `--all-features`, checked after `touch src/lib.rs` to defeat the doc cache). The design's eight acceptance criteria were checked off one by one against the test list before the docs were written. Cost not re-measured: the tag adds one 8-byte header compare per read and no work to the fingerprint pass `STORAGE-015` measured at ~4% of `open`.
- `BLOB-SCHEMA-TAG-DESIGN` (design doc + ADR-0019, no implementation): docs-only round — `cargo fmt --all -- --check`: clean (no Rust source touched). No test/clippy/doc impact: zero `src/` changes, verified by diff. Design accuracy validated by direct investigation of the current code rather than assumed from memory — `src/durability/record_blob.rs` (the shared 20-byte header, `parse_header`'s magic-then-version order and its two messages, `encode_image`, `Fnv1a64`, `HEADER_LEN = 20`), `src/generic/record_blob.rs` and `src/generic/edge_blob.rs` (both `BLOB_VERSION = 1`; both `read` paths magic → version → fingerprint → decode; both `is_current_at` reading exactly `HEADER_LEN` bytes; `EdgeBlob` generic over `Id`, not `R`), `src/generic/mmap_store.rs` (the one `impl` block at line 475 holding both the private slot helpers and `create`/`open`/`read_portable_records`/`open_portable`, and the trait impls that call those helpers — the reason the bound goes in a split block, not on the existing one), `src/generic/store.rs` (the `STORAGE-016` block's bounds), `src/generic/traits.rs` (`Record`, `SymmetricRelation`), every `Record` impl (nine plus the `production.rs` doctest's `Widget`), and every `GenericMmapStore::<…>` call site by grep (`Order` in `order_customer.rs`, the four crash-safety/multi-process binaries, `mmap_store.rs` tests, `tests/mmap_record_identity_keying.rs`; `Employee` in `employee_impl.rs`; `Widget` in the doctest — the whole in-crate `SchemaTag` impl set).
- `GENERIC-STORE-PORTABILITY-DESIGN` (design doc + ADR-0017, no implementation): docs-only round — `cargo fmt --all -- --check`: clean (no Rust source touched). No test/clippy/doc impact: zero `src/` changes, verified by diff. Design accuracy validated by direct investigation of the current code (`src/generic/mmap_store.rs`, `src/generic/order_customer.rs`, `src/generic/traits.rs`, `src/generic/store.rs`, `src/generic_spike/employee_impl.rs`, `src/durability/record_blob.rs`, `Cargo.toml`) rather than assumed from memory — confirmed the `GMMAPST\0`/schema-version-2 slot layout and `HEADER_LEN = 12`, the `HashMap`-backed record set, the `Reversed`/`Symmetric` layer constructors and what each needs from the caller, that `Order`/`Employee` carry no serde derives today (so the bound tightening is a real, named change), which helpers in `record_blob.rs` are private and would need `pub(crate)`, and that `serde`/`bincode` are already dependencies (no new dependency). Every `GenericMmapStore` call site enumerated by grep (`order_customer.rs`, `employee_impl.rs`, the `production.rs` doctest, the four crash-safety/multi-process binaries, `tests/mmap_record_identity_keying.rs`).

## Risks and decisions needed

- Repo is named `rusty_multimodal_db` on GitHub; the originating task suggested `rusty_multimodel_bench` as a less ambiguous name once the benchmark shape was clear. Recorded in the charter; no action taken since renaming a GitHub repo isn't something this session can do.
- `RESULTS.md`'s open questions now center on: the memory-vs-performance tradeoff decision (`CanonicalCachedStore`'s ~2.6× footprint over `SoaStore` at 1M — measured, not a decision), whether the graph-traversal numbers justify a real graph engine, a degree sweep, or support for an asymmetric relationship type, whether write ratios beyond 90% or bursty/correlated mixed-workload access patterns are worth benchmarking, whether LSM compaction, extending the Tier 2 variants beyond their ages-only scope, and `run_load`'s uncovered first-start path for WAL/Hybrid are worth pursuing, and whether sharded/global-`RwLock` concurrency throughput's non-monotonic behavior across the container/Windows/`baileyai` passes is a genuine machine-specific effect or would clarify on a fourth real machine — owner's call on all of these. Write-heavy-workload behavior itself is no longer an open question: `MIXED-WORKLOAD` tested it directly and found `CanonicalCachedStore` wins every configuration. `HybridStore`'s checkpoint clone cost and the reopen-parallelization size threshold are no longer open questions either — both fixed and re-benchmarked. The `scan_ages` 100K crossover is investigated to the practical limit of available tooling (L1 ruled out; last-level cache unreachable on `baileyai`'s AMD hardware via this method) and isn't planned as further work.
- Three new dependencies were added in the durability pass (`serde`+`bincode`, `memmap2`, `redb`); one more in the concurrency pass (`dashmap = "6"`). No new dependency this pass — `PRODUCTION-DEFAULT` composes existing, already-justified pieces (`RwLock<MmapAgeStore>`), per its own explicit constraint.
- `DURABILITY-TIER1`/`DURABILITY-TIER2` merged (PR #8, `251975d`; reopen-parallelization threshold fix merged separately, PR #9, `0f9937e`). `CONCURRENCY-PROTOTYPES` merged, including both real-hardware follow-ups (PR #10, `fd83b8e`). Remaining open items are the ones listed in "Next" above — owner's call on each, not decided here.
- `PRODUCTION-DEFAULT` merged (PR #11, `4d8bf0d`), including the concurrency-tax diagnosis and fix. No standing risk.
- A follow-up diagnosis session found and fixed a real, avoidable inefficiency in `MmapAgeStore::scan_ages` (`src/durability/mmap_store.rs`) — a previously-closed module from the durability round, touched only after checking in with the user per that session's own working-style instruction. This is the first code change to a prior round's closed module in this project's history; recorded here explicitly rather than folded silently into `PRODUCTION-DEFAULT`'s own commit history. No dependency, API, or durability-guarantee change — a bulk-read rewrite of two private methods, verified by the full existing test suite (128/128, unchanged) plus a re-run of both affected benchmarks.
- The physical `src/comparisons/` file reorganization was explicitly offered and explicitly declined by the user in favor of a docs-only lead this pass — not a silent scope-down; recorded in ADR-0008's Considered options and available as a future follow-up if the docs-only lead proves insufficient.
- **`GENERIC-SCHEMA-DESIGN` is resolved, not a pending decision anymore.** The task that motivated it called the schema/query abstraction "the biggest architectural decision in the project's history" and required stopping at a design document before any implementation — that review gate has since been satisfied: four validation spikes (kept as historical record in `src/generic_spike/`) tested the design against real code and real benchmarks, resolving every risk its §4 named, and the library-promotion pass promoted the validated result into `crate::generic`, a real, public library, with ADR-0009 now Accepted. `Dog`/`ProductionStore` remain completely untouched throughout — this was additive, parallel capability from start to finish, never a replacement. The one gap that promotion pass reported rather than fixed (the in-memory composition's write-through gap) is now closed too, at a documented, measured cost — see `GENERIC-SCHEMA-WRITE-THROUGH-FIX` above. The one remaining open item this arc leaves behind — mmap durability's one-field scope — is named explicitly above, not hidden, and is real but non-blocking follow-up work; nothing else in this project depends on it.
- No new dependency this pass (`GENERIC-SCHEMA-LIBRARY` reuses `memmap2`, already present from `DURABILITY-TIER2`) — `Cargo.toml`'s `[dependencies]` table is unchanged from `PRODUCTION-DEFAULT`'s checkpoint; three new `[[bench]]` entries were added across the four spike rounds and this pass (`generic_spike`, `order_relation_spike`, `generic_production`).
- **This document was 13 merged PRs stale** (last synced at `5b4300a`/PR #18; `main` had advanced to `176f5d0`/PR #31) when this pass started — `docs/roadmap/ROADMAP.md`, `docs/specifications/SPEC-REGISTRY.md`, and `docs/traceability/TRACEABILITY.md` were even further behind, unchanged since `ec36262`/PR #17. Closed this pass: every PR from #19 through #31 is now recorded in `## Completed` above with its real content, cost, and merge commit, drawn from each PR's own commit message plus a fresh, current-head re-run of the full validation suite (see `## Validation`) rather than assumed unchanged. No code was touched by this pass. The roadmap/spec-registry/traceability files were deliberately left as-is, not overlooked — see `## Next` item 29 for why none of the intervening PRs warranted a new entry there under this project's existing conventions.
- **Process gap, not yet resolved**: PR #27 (`PUBLIC-API-NARROWING`, `e2d779b`) introduced the `research` feature flag as the crate's compiled-surface boundary — a consequential, crate-wide public-API decision by any reasonable reading of `AGENTS.md`'s own ADR-cadence guidance for active major development — and shipped without an ADR. No ADR was backfilled by this pass either: writing one now, well after the fact and by a session that didn't make the original decision, risks misrepresenting when and why it was made. Recorded here as an explicit, owner-visible gap instead. If the owner wants it closed, the fix is either a real ADR describing the decision honestly as documented-after-the-fact, or an explicit owner decision that no ADR is needed for this one — not a silent retrofit.
- New dependencies since the last full sync: none. PRs #19-#31 added no new `[dependencies]` entries — `REQ-TRACEABILITY-SCHEMA`/`RULE-TRACE-SPIKE` added new `[[bench]]` targets and spike-local modules only, `PUBLIC-API-NARROWING` is a `[features]`-only change, and the mmap-hardening/multiprocess PRs reuse the existing `memmap2` dependency throughout.
- **`SERVER-QUERY-LAYER` is real, opt-in, network-listening code — off by default (the `server` feature)**. As of `SERVER-AUTH` (this checkpoint), it now *can* be configured with authentication/authorization (`AuthConfig`), but still has no transport encryption, and a server configured with no tokens is still exactly as open as before this round — the "genuinely opens a listening socket the moment it's run" risk this entry originally named is unchanged in kind, only narrower in scope. Recorded here explicitly, not just in the ADR, because this is qualitatively different from every other `research`-gated module in this crate: those are inert code until explicitly linked into a benchmark or test; `dog_server`/`serve` genuinely open a listening socket the moment they're run. Owner's call whether/when transport encryption is worth designing — not decided or attempted here.
- Two real issues were found and fixed during `SERVER-QUERY-LAYER`'s implementation, neither a design flaw: a Nagle/delayed-ACK interaction (fixed with `TCP_NODELAY`, confirmed directly — a stress test went from ~36s to ~0.04s) and a test-isolation bug (two integration tests racing on one mmap-backed temp path, fixed with a per-call unique-path counter). Both are recorded in ADR-0010's own "Acceptance and implementation" section and `## Completed` above, not hidden in commit history only.
- `SERVER-QUERY-LAYER` v0.3.0's `Employee` domain round touched `src/generic/{store,production}.rs` — the second time (after `GENERIC-SCHEMA-WRITE-THROUGH-FIX`) this project has changed a `crate::generic` file after `STORAGE-012` closed it as Implemented/Verified. Recorded here explicitly for the same reason that first change was: a real, load-bearing gap (`Reversed` never forwarding `Neighbors`) found by exercising a schema shape (`SymmetricRelation` + `ChildOf` together) no prior domain had, not a regression or a speculative change. `STORAGE-012` bumped to v0.2.0 and ADR-0009 gained an addendum to record it, matching the precedent already established rather than treated as a new decision needing its own ADR.
- New dependency this pass: none. `SERVER-QUERY-LAYER` reuses `bincode`/`serde`/`uuid`, all already present — `Cargo.toml`'s `[dependencies]` table is unchanged; one new `server` feature and its associated `[[bin]]`/`[[test]]` entries were added.
- **`SERVER-QUERY-LAYER` v0.2.0 (schema discovery) does not change the risk named above** — `DomainSchema` is discoverable data, not a new capability that widens what an unauthenticated network peer can do beyond what `Request`/`Response` already allowed; the "no auth, no encryption, localhost/trusted-network only" posture is unchanged. `ADR-0011` was written and implemented in the same delivery cycle (not staged as a separate design-only round like `ADR-0010`/`ADR-0009`) — a deliberate departure from this project's two biggest-decision precedents, reasoned through explicitly in ADR-0011's own Context rather than silently defaulting to the smaller-scope treatment. No new dependency this pass either.
- **The owner's explicit "3 then 2" instruction is now fully complete** — both the third validation domain (`Employee`, `SERVER-QUERY-LAYER` v0.3.0) and the throughput/latency benchmark (`benches/server.rs`, v0.4.0) are implemented, tested, documented, and merged, and both real-hardware follow-ups (`Beast`, `baileyai`) closed the one open question that round left behind.
- **The owner's three-direction "what's next" pick is now fully complete** — asked what's next, the owner picked all three offered directions at once: a schema-driven client library, auth/encryption, and session/transaction semantics. All three are done: the client library (bounded/additive, no new ADR — `SERVER-QUERY-LAYER` v0.5.0), auth/encryption (design plus implementation — `SERVER-001` v0.6.0, ADR-0012), and session/transaction semantics (design plus implementation — `SERVER-001` v0.7.0, ADR-0013). No further server-layer work is planned unless the owner asks for one of the remaining open items (transport encryption, a multi-round-trip interactive session, crash-atomicity across a batch) or something new.
- **`SERVER-AUTH-DESIGN` (ADR-0012) is Accepted and implemented** — the owner reviewed and approved the design as proposed, no changes requested; the implementation followed in the same session without a further owner check-in, reasoning by analogy to how `SERVER-QUERY-LAYER` was implemented right after ADR-0010's own acceptance. `subtle` is a real `Cargo.toml` dependency now, not just named in the design. Explicitly named, not glossed over: this implementation does not by itself authorize deploying a server built from this crate beyond a trusted network — it closes the access-control half of that gap, requiring an external TLS-terminating proxy/tunnel for the encryption half, with native TLS (`rustls`) deferred as a real, separate, larger future decision. See `## Completed` above and ADR-0012/`docs/design/SERVER-AUTH-DESIGN.md` for the full account.
- **`SERVER-TRANSACTION-DESIGN` (ADR-0013) is Accepted and implemented** — the owner reviewed and approved the design as proposed, no changes requested; the implementation followed in the same session, mirroring exactly how `SERVER-AUTH-DESIGN` proceeded from acceptance to implementation. `Request::Transaction` is real, opt-in-by-nature (a client that never sends it sees no change), and backed by a real, new storage-layer primitive on `ProductionStore`/`GenericProductionStore` — named up front in ADR-0013 as a real cost, not discovered mid-implementation. Explicitly named, not glossed over: this implementation delivers atomicity/isolation with respect to concurrent access only — it does not deliver crash-atomicity across a batch (a process crash mid-batch can leave a partial batch durably applied) or a multi-round-trip interactive session (every transaction is bounded by one request message, deliberately, to avoid a liveness/DoS risk this project has never accepted anywhere else). A methodologically-flawed test for the atomicity claim was caught and redesigned during this round rather than shipped — see `## Completed` above for the full account, including why. See ADR-0013/`docs/design/SERVER-TRANSACTION-DESIGN.md` for the full design record.
- **`SERVER-TLS-DESIGN` (ADR-0014) is Accepted and implemented** — the owner reviewed and approved the design as revised, no further changes requested; the implementation followed in the same session, mirroring exactly how `SERVER-AUTH`/`SERVER-TRANSACTION` each proceeded from acceptance to implementation. `rusty_tls` is a real `Cargo.toml` dependency now (this crate's first git dependency, pinned to a specific commit), not just named in the design. Explicitly named, not glossed over: this implementation reverses `SERVER-AUTH-DESIGN`'s own original rejection of native TLS — the design document's own "Considered options" section explains why that reversal is sound (the dependency-weight objection implicitly bundled TLS with an async-runtime shift that turns out not to be required) rather than a contradiction papered over. **Revised before acceptance, not silently**: the owner asked whether this owner's own `rusty_*`/`Rusty-Mill` ecosystem already had a hand-rolled or wrapped solution for the dependency this design was about to recommend. It does — `rusty_tls` (`Rusty-Mill/rusty_mill`), purpose-built so no consumer in that ecosystem depends on `rustls` directly — and the design was revised in place to depend on it instead of a fresh `rustls` dependency. **A real implementation-time finding, not anticipated in the design's own sketch**: `rusty_tls::TlsServerStream` can't be split into independent read/write halves via `TcpStream::try_clone` the way the existing plaintext path is (its `rustls::ServerConnection` state is shared, single-owner data) — resolved with a new `ReadHalf`/`WriteHalf` enum pair sharing one `Rc<RefCell<TlsServerStream<TcpStream>>>` on the TLS path, leaving the plaintext path's own `try_clone` split completely unchanged (verified: every pre-existing plaintext test passes unmodified). Also explicitly named: a real, meaningfully larger dependency addition than `subtle` was for auth — flagged plainly, not minimized, in ADR-0014's own "Consequences"; and this implementation still does not deliver mTLS (client identity is unchanged), though the mechanism for it now already exists in `rusty_tls` if that's ever revisited. See `## Completed` above and ADR-0014/`docs/design/SERVER-TLS-DESIGN.md` for the full account.
