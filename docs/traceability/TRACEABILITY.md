# Traceability

| Requirement | Roadmap | Decision/interface | Implementation | Verification | PR/release | State |
|---|---|---|---|---|---|---|
| `STORAGE-001-FR-001..005` | `GENERATOR` | `DogRecord`, `GeneratorConfig`/`generate` | `src/record.rs`, `src/generator.rs` | unit tests in `src/generator.rs` (8 tests, all passing) | commit `5bfc8c5` (bootstrap, before the first PR) | Implemented/Verified |
| `STORAGE-002-FR-001..007` | `BACKENDS` | ADR-0001, `DogStore` trait | `src/store/mod.rs`, `src/store/{aos,soa,canonical}.rs` | unit tests (18) + cross-backend tests (4), all passing | commit `5bfc8c5` (bootstrap, before the first PR) | Implemented/Verified |
| `STORAGE-003-FR-001..005` | `BENCH-SUITE`, `CACHE-MISS` | ADR-0002 | `benches/workloads.rs`, `benches/cache_events.rs`, `src/bench_support.rs` | `cargo bench` run (36/36 cases); `--features perf-events` build verified on Linux, runtime deferred to `baileyai` | commit `5bfc8c5` (bootstrap, before the first PR) | Implemented/Verified |
| `STORAGE-004-FR-001..006` | `RESULTS` | ADR-0001, ADR-0002 | `RESULTS.md` | manual acceptance review against `STORAGE-004` criteria | commit `5bfc8c5` (bootstrap, before the first PR) | Implemented/Verified |
| `STORAGE-005-FR-001..006` | `HYBRID-BACKEND` | ADR-0003, `DogStore` trait | `src/store/canonical_cached.rs` | unit tests (8, staleness test highest-priority) + cross-backend tests (5, incl. `all_backends_reflect_update_age_in_scan_ages_immediately`) + `cargo bench` 4-way run, all passing | follow-on PR | Implemented/Verified |
| `STORAGE-006-FR-001..010` | `GRAPH-TRAVERSAL` | ADR-0004, `DogStore` trait | `src/generator.rs` (`generate_littermates`), `src/store/{aos,soa,canonical,canonical_cached}.rs` (`neighbors`), `src/bench_support.rs` (`two_hop_neighbors`), `benches/workloads.rs` + `benches/cache_events.rs` | generator/backend unit tests + cross-backend tests (`all_backends_agree_on_neighbors_one_hop` highest-priority, `all_backends_agree_on_neighbors_two_hop`) + `two_hop_neighbors` unit tests + `cargo bench` 4-way run for both new workloads, all passing; cache-miss counters not producible in this session's environment (see ADR-0002) | follow-on PR | Implemented/Verified (wall-clock); cache-miss deferred |
| `STORAGE-007-FR-001..006` | `MIXED-WORKLOAD` | ADR-0003 (the write-through tax this benchmark tests under blended load) | `src/bench_support.rs` (`MixedWorkloadConfig`, `MixedWorkloadDriver`), `benches/workloads.rs` + `benches/cache_events.rs` | `src/bench_support.rs` unit tests (bounds validation incl. `NaN`, determinism, ratio-distribution statistical check, never-errors-against-its-own-dataset highest-priority, write-only-actually-mutates end-to-end check) + `cargo bench` 36-case run (3 ratios × 3 sizes × 4 backends), all passing; cache-miss counters not producible in this session's environment (see ADR-0002) | follow-on PR | Implemented/Verified (wall-clock); cache-miss deferred |
| `STORAGE-008-FR-001..010` | `DURABILITY-TIER1` | ADR-0005, shared `CanonicalCachedState` core, `DogStore` trait | `src/durability/{mod,wal_fsync,wal_buffered,snapshot_rebuild,snapshot_full,hybrid}.rs`, `src/store/mod.rs` (`StoreError::Durability`), `benches/durability.rs` | per-variant unit tests (WAL reconstruction-from-log highest-priority for variants 1/2; checkpoint-then-open-matches-fresh-store for variants 3/4; snapshot-plus-partial-replay-across-cutoff highest-priority for variant 5) + shared-core tests (WAL round-trip, torn-write recovery) + `cargo bench --bench durability` run (5 variants × 3 sizes × up to 3 metrics), all passing | follow-on PR | Implemented/Verified |
| `STORAGE-009-FR-001..006` | `DURABILITY-TIER2` | ADR-0006, `DogStore` trait | `src/durability/{mmap_store,lsm_store,embedded_store}.rs`, `benches/durability.rs` | per-variant unit tests (flush-then-reopen round-trip for mmap; newest-generation-wins-across-multiple-flushes highest-priority for LSM; reopen-sees-every-committed-write for `redb`) + `cargo bench --bench durability` run (3 variants × 3 sizes; checkpoint/flush for mmap/LSM only, `redb` excluded by design), all passing | follow-on PR | Implemented/Verified |
| `STORAGE-010-FR-001..008` | `CONCURRENCY-PROTOTYPES` | ADR-0007, new `ConcurrentStore` trait | `src/concurrency/{mod,global_rwlock,sharded,dashmap_store,actor}.rs`, `src/bench_support.rs` (`run_one_concurrent`), `benches/concurrency.rs` | per-variant unit tests (construction/read/write/scan sanity + sharded's cross-shard-reachability check) + all four variants' `concurrent_stress_matches_sequential_replay` flagship stress test (16 threads × 2,000 iterations each against a 20-id contended pool, highest priority — no lost updates/torn reads) + `cargo bench --bench concurrency` run (2 sizes × 3 ratios × 4 thread counts × 4 variants), all passing | follow-on PR | Implemented/Verified |
| `STORAGE-011-FR-001..007` | `PRODUCTION-DEFAULT` | ADR-0008, `DogStore`/`ConcurrentStore` traits, `MmapAgeStore`/`GlobalRwLockStore` (unmodified) | `src/production.rs` (new), `src/lib.rs` (lead doc comment + re-export), `benches/workloads.rs`/`benches/concurrency.rs` (additive `ProductionStore` variant), `tests/production_integration.rs` (new) | `src/production.rs` unit tests (create/open/flush round-trip, `DogStore`/`ConcurrentStore` behavior, both `From` impls, reused `run_concurrency_stress_test`) + flagship integration test (highest priority — two 16-thread × 2,000-iteration concurrent contention phases separated by a genuine drop + reopen, verified via sequential-replay linearizability and a third, post-drop reopen for persistence) + `cargo bench --bench workloads -- production` + `cargo bench --bench concurrency` run, all passing | follow-on PR | Implemented/Verified |
| `STORAGE-012-FR-001..009` | `GENERIC-SCHEMA-LIBRARY` | ADR-0009 (Accepted), promoted `crate::generic::{traits,query,store}` (unmodified from the four spikes), new `Flush`/`ScannableField::set_scannable_value` | `src/generic/{mod,traits,query,store,mmap_field,mmap_store,order_customer,production}.rs` (new/promoted), `src/generic_spike/{mod,dog_impl,order_naive,order_bench_support}.rs` (import-path updates), `src/lib.rs` (new module + doc comment), `benches/generic_production.rs` (new), `tests/generic_production_integration.rs` (new) | `src/generic/**` unit tests (12: `MmapFieldValue` round-trip, `GenericMmapStore` create/open/flush + all four query traits, `order_customer`'s full-stack + production-stack tests, `GenericProductionStore`'s lock-forwarding tests) + flagship integration test (highest priority — two 16-thread × 2,000-iteration concurrent contention phases against `GenericProductionStore<OrderProductionStack>` separated by a genuine drop + reopen, verified via sequential-replay linearizability against a plain `HashMap` reference and a third, post-drop reopen for persistence, run 5× to rule out flakiness) + `cargo bench --bench generic_production` run (5 operations × 3 sizes), cross-checked against the `directed-relation-spike` round's and `ProductionStore`'s own established numbers, all passing | PR #17 | Implemented/Verified |
| `STORAGE-012-FR-010` | `GENERIC-SCHEMA-LIBRARY`, `SERVER-QUERY-LAYER` | ADR-0009 (Acceptance and implementation addendum), `Neighbors` forwarding on `Reversed` | `src/generic/store.rs` (`Neighbors` impl on `Reversed`), `src/generic/production.rs` (`GenericProductionStore::neighbors`) | `src/generic_spike/employee_impl.rs`'s three tests (in-memory stack, durable production stack, flush-then-reopen — each exercising `Neighbors` alongside `Parent`/`Children` on the same self-referential `Employee` record), all passing | PR #39 | Implemented/Verified |

| `SERVER-001-FR-001..009` | `SERVER-QUERY-LAYER` | ADR-0010 (Accepted), `ConnectionStore` trait, `Request`/`Response` protocol | `src/server/{mod,protocol,framing,dog}.rs` (unconditional under `server`), `src/server/order.rs` (additionally behind `research`), `src/bin/dog_server.rs` | unit tests (wire-format round trip, framing round trip + oversized/truncated-frame handling, `dispatch`'s per-request-kind mapping against a fixture store, both domain adapters against a real `ProductionStore`/`GenericProductionStore`) + `tests/server_dog_integration.rs` (real socket, including the flagship concurrent-client stress test — 8 connections × 200 requests, sequential-replay linearizability) + `tests/server_order_integration.rs` (real socket, `Parent`/`Children`), all passing | PR #35 | Implemented/Verified |
| `SERVER-001-FR-010..011` | `SERVER-QUERY-LAYER` | ADR-0011 (Accepted), `DomainSchema`/`ConnectionStore::describe` | `src/server/{mod,protocol,dog,order}.rs` (`describe`/`DescribeSchema`/`Response::Schema` additions) | unit tests (`server::tests::describe_schema_returns_the_fixture_store_own_shape`, `server::{dog,order}::tests::describe_names_*`) + `tests/server_dog_integration.rs`'s `a_schema_driven_client_discovers_and_uses_the_age_field` + `tests/server_order_integration.rs`'s `a_schema_driven_client_discovers_and_uses_the_status_field` (both real-socket, schema-driven end to end — discover a field by name, then complete a real request using only the discovered tag), all passing | PR #37 | Implemented/Verified |
| `SERVER-001-FR-012..013` | `SERVER-QUERY-LAYER` | ADR-0009 (Acceptance and implementation addendum), third domain adapter `server::employee::EmployeeConnectionStore` | `src/server/employee.rs` (new), `src/generic_spike/employee_impl.rs` (new), `src/generic/{store,production}.rs` (`Neighbors` forwarding — see `STORAGE-012-FR-010`), `tests/server_employee_integration.rs` (new) | `server::employee::tests::*` (5 tests, incl. `parent_children_and_neighbors_are_all_real_for_the_first_time`) + `tests/server_employee_integration.rs`'s three tests (the all-relations-real round trip, the schema-driven test, the empty-but-real `Parent`/`Neighbors` cases), all passing | PR #39 | Implemented/Verified |
| `SERVER-001-FR-014` | `SERVER-QUERY-LAYER` | Throughput/latency benchmark, closing `SERVER-001`'s own v0.1.0 "Open questions" gap; two real-hardware follow-ups retuned the thread-count sweep and resolved the connection-count-ceiling open question | `benches/server.rs` (new; `THREAD_COUNTS` retuned `[1,4,8,16]` → `[1,4,24,48]` → `[1,4,32,64]`) | `cargo bench --features server,research --bench server` completes without panics, all three domains, single-connection latency + thread-per-connection throughput sweep — container pass (1/4/8/16), `Beast` real-hardware pass (1/4/24/48, the owner's Windows dev machine), and `baileyai` real-hardware pass (1/4/32/64, 32 real cores), each run twice to characterize its own environment's run-to-run variance — numbers in `RESULTS.md`'s `## Server / query layer` section | PR #41, #43, #44 | Implemented/Verified |
| `SERVER-001-FR-015` | `SERVER-QUERY-LAYER` | Schema-driven client library, completing ADR-0011's own schema-discovery decision rather than opening a new one | `src/server/client.rs` (new, `SchemaDrivenClient`), `tests/server_schema_driven_client.rs` (new) | `tests/server_schema_driven_client.rs`'s four tests (one per domain — `Dog`, `Order`/`Customer`, `Employee` — driving every request kind purely by discovered field name, plus the unknown-field-name client-side-error case), importing no domain-specific `FIELD_*` constant, all passing | PR #46 | Implemented/Verified |
| `SERVER-001-FR-016` | `SERVER-QUERY-LAYER` | ADR-0012 (Accepted), `docs/design/SERVER-AUTH-DESIGN.md` (Accepted) — `AuthConfig`/`TokenClass`, `Request::Authenticate`, `ErrorCode::{Unauthenticated,Unauthorized}` | `src/server/mod.rs` (`AuthConfig`/`TokenClass`, `handle_connection`/`serve` auth gating), `src/server/protocol.rs` (`Request::Authenticate`, two new `ErrorCode` variants), `src/bin/dog_server.rs` (`AuthConfig::from_env`), every other `serve` call site (`AuthConfig::default()`), `tests/server_auth_integration.rs` (new), `Cargo.toml` (`subtle` dependency) | unit tests (`server::protocol::tests::authenticate_request_and_new_error_codes_round_trip_through_bincode`, `server::tests::auth_config_*`, `server::tests::dispatch_never_routes_authenticate_to_a_store`, `server::tests::token_comparison_time_does_not_depend_on_where_the_mismatch_is` — the `AUTH-FR-006` timing measurement) + `tests/server_auth_integration.rs`'s five real-socket tests (unauthenticated rejection incl. `DescribeSchema`, wrong-token indistinguishability, `ReadOnly` read/write split, `ReadWrite` full access, no-tokens-configured backward compatibility), all passing | PR #52, #53 | Implemented/Verified |
| `STORAGE-011-FR-008`, `STORAGE-012-FR-011` | `SERVER-QUERY-LAYER` | ADR-0013 (Accepted), `docs/design/SERVER-TRANSACTION-DESIGN.md` (Accepted) — `TransactionalStore`, `GenericProductionStore::with_exclusive` | `src/production.rs` (`TransactionalStore` trait + `ProductionStore` impl), `src/generic/production.rs` (`GenericProductionStore::with_exclusive`) | `src/production.rs`'s existing test suite (unaffected — purely additive) + exercised indirectly by every `server::{dog,order,employee}::tests::*apply_transaction*` case and `tests/server_transaction_integration.rs` below, all passing | PR #57 | Implemented/Verified |
| `SERVER-001-FR-017` | `SERVER-QUERY-LAYER` | ADR-0013 (Accepted), `docs/design/SERVER-TRANSACTION-DESIGN.md` (Accepted) — `Request::Transaction`, `TransactionOp`, `Response::TransactionFailed`, `ErrorCode::RecordNotFound`, `ConnectionStore::apply_transaction` | `src/server/protocol.rs` (new request/response/error shapes), `src/server/mod.rs` (`ConnectionStore::apply_transaction`, `dispatch`'s new arm, `handle_connection`'s auth gating extended to `Transaction`), `src/server/{dog,order,employee}.rs` (`apply_transaction` per adapter, each using `TransactionalStore`/`GenericProductionStore::with_exclusive`), `tests/server_transaction_integration.rs` (new) | unit tests (`server::protocol::tests::transaction_request_and_new_shapes_round_trip_through_bincode`, `server::tests::transaction_all_pass_reports_ok`, `server::tests::transaction_reports_the_first_failing_operations_index_and_code`) + `tests/server_transaction_integration.rs`'s six real-socket tests (full success; failure at first/last operation; a malformed value; `ReadOnly` rejection before any operation is evaluated; the flagship concurrent stress test, sequential-replay linearizability against a contended id pool), all passing | PR #57 | Implemented/Verified |
| `SERVER-001-FR-018` | `SERVER-QUERY-LAYER` | Transaction benchmark coverage, completing FR-014's already-accepted benchmark scope rather than opening a new decision | `benches/server.rs` (`{domain}-txn` row set: `measure_transaction_latency`, `run_transaction_throughput`, `bench_transaction_domain`) | `cargo bench --features server,research --bench server` completes without panics, all three domains, `Request::Transaction` latency + thread-per-connection throughput sweep directly comparable to the existing `GetById` rows — this session's 4-core container, run twice to characterize run-to-run variance; no meaningful cost found relative to `GetById` — numbers in `RESULTS.md`'s `## Server / query layer` section, `### \`Request::Transaction\` follow-up` subsection | PR #59 | Implemented/Verified |
| `SERVER-001-FR-019` | `SERVER-QUERY-LAYER` | ADR-0014 (Accepted), `docs/design/SERVER-TLS-DESIGN.md` (Accepted, revised to depend on `rusty_tls`) — `TlsConfig`, `ReadHalf`/`WriteHalf` | `src/server/mod.rs` (`TlsConfig`, `TlsConfigError`, `ReadHalf`/`WriteHalf`, `serve`'s new `tls: Option<TlsConfig>` parameter, `handle_connection`'s handshake wiring), `src/server/pem.rs` (new, PEM/base64 decoder), `src/bin/dog_server.rs` (`TlsConfig::from_env`), every other `serve` call site (`None`), `tests/server_tls_integration.rs` (new), `Cargo.toml` (`rusty_tls` dependency, this spec's first git dependency; `rcgen` dev-dependency) | `src/server/pem.rs`'s unit tests (10: base64 known-vectors/whitespace-tolerance/malformed-input, PEM block decoding including multi-block chains, label-agnostic decoding, every malformed-input case) + `tests/server_tls_integration.rs`'s five real-socket tests (a full request/response round trip over TLS; `TlsConfig` composed with `AuthConfig`; a plain connection to a TLS-configured server never getting a valid response; `TlsConfig: None` reproducing plaintext behavior; the `Authenticate` token not present in the raw bytes a TLS client actually sends, via a byte-recording `TcpStream` wrapper), all passing | PR #64 | Implemented/Verified |
| `SERVER-001-FR-020` | `SERVER-PROTOCOL-VERSION` | ADR-0022 (Accepted), `docs/design/SERVER-PROTOCOL-VERSION-DESIGN.md` (Accepted) — `PROTOCOL_VERSION`, `Request::Hello`/`Response::Hello` | `src/server/protocol.rs` (`PROTOCOL_VERSION = 2`, the `Hello` variants at index 10, the compatibility rules and version table in the module docs), `src/server/mod.rs` (the first-frame `Hello` intercept, `dispatch` → `Unsupported`), `src/server/client.rs` (`connect` says hello first, `server_protocol_version()`), `tests/server_protocol_version.rs` (new), one `[[test]]` line in `Cargo.toml` | `protocol.rs`'s `Hello` golden vectors and `protocol_version_is_the_one_the_table_names`, `mod.rs`'s three `Hello` unit tests, `tests/server_protocol_version.rs` (4/4 over real sockets: pre-auth `min` negotiation, version 0 / late hello `Malformed` with the connection open, `SchemaDrivenClient` negotiating 2 on all three domains, an unknown index closing the connection) — every v0.9.1 golden vector and pre-existing suite unchanged | PR #126 | Implemented/Verified |
| `SERVER-001-FR-021` | `SERVER-CLIENT-AUTH` | `SERVER-001` v0.11.0 (bounded, additive completion of FR-015/FR-016, taken from ADR-0022's revisit trigger — no new decision) — `SchemaDrivenClient::connect_authenticated`, `SchemaDrivenClient::authenticate` | `src/server/client.rs` (`connect_authenticated(addr, token)` sending `Authenticate` between `Hello` and `DescribeSchema`; `authenticate(&mut self, token)`; `connect` on an auth-configured server reporting the server's own `Unauthenticated`) | `tests/server_auth_integration.rs`'s two appended tests (7/7: token-less `connect` and wrong-token `connect_authenticated` both `Unauthenticated`; a read token connecting, reading, refused a write; `authenticate` promoting, a rejected re-authentication changing nothing, demotion; the no-tokens no-op) — `tests/server_schema_driven_client.rs` and `tests/server_protocol_version.rs` unchanged | PR #128 | Implemented/Verified |
| `SERVER-001-FR-022` | `SERVER-CLIENT-TLS` | `SERVER-001` v0.12.0 (bounded, additive completion of FR-015/FR-019, the explicit client-side trust configuration ADR-0014's Consequences named — no new decision) — `SchemaDrivenClient::connect_with`, `ConnectOptions`, `ClientTlsConfig`, `TrustPolicy` re-export, `ClientError::Tls` | `src/server/client.rs` (`connect_with` wrapping the TCP connection in a `rusty_tls::TlsStream` before the `Hello`; the private `Transport` enum; one stream with each frame assembled before a single `write_all`) | `tests/server_tls_integration.rs`'s three appended tests (8/8: connect/negotiate/read/write/scan over TLS; composed with `AuthConfig` — plaintext `connect_authenticated` failing before the token is written, token-less TLS `Unauthenticated`, read token refused a write, `authenticate` promoting; the failure shapes — plaintext `connect` `Frame`, unparseable server name `Tls`, self-signed certificate refused under `TrustPolicy::System`) — `tests/server_schema_driven_client.rs`, `tests/server_protocol_version.rs`, `tests/server_auth_integration.rs` unchanged | PR #130 | Implemented/Verified |
| `SERVER-001-FR-023` | `SERVER-MTLS` | ADR-0023 (Accepted), `docs/design/SERVER-MTLS-DESIGN.md` (Accepted) — `TlsConfig::new_with_client_auth`, `ClientTlsConfig::with_identity` | `src/server/mod.rs` (`TlsConfig::{new_with_client_auth, from_pem_files_with_client_ca, from_env, from_env_values, requires_client_certificate}`), `src/server/client.rs` (`ClientTlsConfig::{with_identity, with_identity_pem_files, has_identity}`, the identity branch in `connect_with`, a key-redacting `Debug`), `src/bin/dog_server.rs` (doc, startup line) | `tests/server_tls_integration.rs`'s four appended mTLS tests (12/12: admitted CA-signed client raw and via `SchemaDrivenClient`; no certificate and foreign CA rejected with an error, server still serving; admission then class with and without tokens; PEM on both ends with a two-root CA file, `Io`/`Tls` construction errors) + `src/server/mod.rs`'s `tls_from_env_values_treats_a_client_ca_without_a_chain_and_key_as_an_error` — every pre-existing suite unchanged | PR #135 | Implemented/Verified |
| `SERVER-001-FR-024` | `SERVER-TRANSACTION-SESSION` | ADR-0024 (Accepted), `docs/design/SERVER-TRANSACTION-SESSION-DESIGN.md` Part A (Accepted); ADR-0022's first revisit trigger (the first version-3 variant) — `Request::{Begin, Commit, Rollback}`, `Response::Staged`, `ErrorCode::{NoSession, SessionOpen, SessionFull}`, `PROTOCOL_VERSION = 3`, `MAX_STAGED_OPS`, `SchemaDrivenClient::begin` / `Session` | `src/server/protocol.rs` (constant, seven variants, table row 3, vectors), `src/server/mod.rs` (`handle_connection`'s `negotiated`/`session` state and intercepts, the `ReadOnly` gate's `Commit`, `dispatch` → `Unsupported`, `error_message`), `src/server/client.rs` (`begin`, `Session::{update, commit, rollback}` + `Drop`, `ClientError::TransactionFailed`) | `protocol.rs`'s vectors and `session_shapes_round_trip_through_the_codec`, `mod.rs`'s `dispatch_never_routes_session_requests_to_a_store`, `tests/server_transaction_integration.rs`'s four session tests (10/10), `tests/server_protocol_version.rs`'s `session_requests_are_malformed_below_protocol_version_3` (5/5), `tests/server_schema_driven_client.rs`'s `sessions_stage_commit_and_roll_back_on_every_domain` (5/5) — every pre-existing suite and bench unchanged | PR #139 | Implemented/Verified |
| `SERVER-001-FR-025` | `SERVER-TRANSACTION-JOURNAL` | ADR-0025 (Accepted), `docs/design/SERVER-TRANSACTION-SESSION-DESIGN.md` Part B (Accepted); ADR-0013's second revisit trigger — `with_journal` on the three adapters, `BatchJournal`, `CheckpointFlush`, `JournalError`, `ErrorCode::Journal` with `PROTOCOL_VERSION = 4` | `src/server/journal.rs` (new), `src/server/{dog,order,employee}.rs` (`with_journal`, `validate_batch`/`apply_batch`, the journal steps in `apply_transaction`), `src/server/protocol.rs` (`Journal` at 9, version 4, table row 4), `src/server/mod.rs` (`downgrade_for_version`, message), `src/bin/dog_server.rs` (`SERVER_TXN_JOURNAL_PATH`), `benches/server.rs` (`dog-jrnl-txn`) | `journal.rs`'s four unit tests, `dog.rs`'s replay-pair and checkpoint tests, `mod.rs`'s downgrade test, `protocol.rs`'s `Journal` vector and version pin, `tests/server_transaction_integration.rs`'s `a_journaled_server_journals_transactions_and_session_commits_only` (11/11), the `dog-jrnl-txn` rows in `RESULTS.md` — every pre-existing suite unchanged | PR #141 | Implemented/Verified |
| `SERVER-001-FR-026` | `SERVER-CLIENT-HELLO-FALLBACK` | `SERVER-001` v0.16.0 (ADR-0022's third revisit trigger, taken at the owner's call as default-on — no new decision) — `ConnectOptions::require_hello`, the fallback in `connect_with` | `src/server/client.rs` (`connect_with`'s fallback arm, `open_transport`, `peer_closed`, `ConnectOptions::require_hello`, module docs) | `tests/server_protocol_version.rs`'s `a_pre_hello_server_is_reconnected_to_without_a_hello_unless_hello_is_required` (6/6; a pre-hello fake server on the public `dispatch`: fallback to version 1 with a read served and `begin` refused, `require_hello()` restoring the EOF, a server dying under any first frame still an error) — every pre-existing suite unchanged | this PR | Implemented/Verified |

| `STORAGE-013-FR-001..007` | `EXTERNAL-DB-BENCHMARK` | ADR-0015 (Accepted) — `benches/external_db.rs`, `external-db-bench` Cargo feature | `benches/external_db.rs` (new), `Cargo.toml` (`rusqlite`/`duckdb`/`postgres` dev-dependencies, `external-db-bench` feature, `external_db` `[[bench]]` entry) | `cargo check --bench external_db --features research,external-db-bench` clean + a real `cargo bench --features research,external-db-bench --bench external_db` run against a local Postgres instance, all three workload groups (`get`, `scan_ages`, `neighbors_one_hop`) × all three sizes × all four systems, no panics | PR #66 | Implemented/Verified |
| `STORAGE-013-FR-008` | `EXTERNAL-DB-BENCHMARK` | `STORAGE-013` v0.2.0 (completes ADR-0015's already-accepted scope, no new ADR) — `neighbors_two_hop_vs_external_db` Criterion group | `benches/external_db.rs` (`run_neighbors_two_hop_{sqlite,duckdb,postgres,production}`, `bench_neighbors_two_hop`) | `cargo check --bench external_db --features research,external-db-bench` clean + `cargo clippy --all-targets --all-features -- -D warnings` clean + a real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_two_hop` run against a local Postgres instance, all four systems × all three sizes, no panics — numbers and a real, investigated Postgres join-strategy finding in `RESULTS.md`'s `### Graph traversal, two-hop` subsection | PR #72 | Implemented/Verified |
| `STORAGE-013-FR-009` | `EXTERNAL-DB-BENCHMARK` | `STORAGE-013` v0.3.0 (completes ADR-0015's already-accepted scope, no new ADR) — `neighbors_three_hop_vs_external_db` Criterion group, private `three_hop_neighbors` helper | `benches/external_db.rs` (`three_hop_neighbors`, `run_neighbors_three_hop_{sqlite,duckdb,postgres,production}`, `bench_neighbors_three_hop`) | `cargo check --bench external_db --features research,external-db-bench` clean + `cargo clippy --all-targets --all-features -- -D warnings` clean + a real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_three_hop` run against a local Postgres instance, all four systems × all three sizes, no panics — numbers and confirmation that the v0.2.0 Postgres join-strategy finding reproduces at this depth in `RESULTS.md`'s `### Graph traversal, three-hop` subsection | PR #74 | Implemented/Verified |
| `STORAGE-013-FR-010` | `EXTERNAL-DB-BENCHMARK` | `STORAGE-013` v0.4.0 (completes ADR-0015's already-accepted scope, no new ADR) — `neighbors_four_hop_vs_external_db` Criterion group, private `four_hop_neighbors` helper | `benches/external_db.rs` (`four_hop_neighbors`, `run_neighbors_four_hop_{sqlite,duckdb,postgres,production}`, `bench_neighbors_four_hop`) | `cargo check --bench external_db --features research,external-db-bench` clean + `cargo clippy --all-targets --all-features -- -D warnings` clean + a real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_four_hop` run against a local Postgres instance, all four systems × all three sizes, no panics — numbers and the new Postgres/SQLite crossover finding (confirmed via `EXPLAIN (ANALYZE, BUFFERS)`, not assumed) in `RESULTS.md`'s `### Graph traversal, four-hop` subsection | PR #76 | Implemented/Verified |
| `STORAGE-013-FR-011` | `EXTERNAL-DB-BENCHMARK` | `STORAGE-013` v0.5.0 (completes ADR-0015's already-accepted scope, no new ADR) — `neighbors_five_hop_vs_external_db` Criterion group, private `five_hop_neighbors` helper | `benches/external_db.rs` (`five_hop_neighbors`, `run_neighbors_five_hop_{sqlite,duckdb,postgres,production}`, `bench_neighbors_five_hop`) | `cargo check --bench external_db --features research,external-db-bench` clean + `cargo clippy --all-targets --all-features -- -D warnings` clean + a real `cargo bench --features research,external-db-bench --bench external_db -- neighbors_five_hop` run against a local Postgres instance, all four systems × all three sizes, no panics — numbers and confirmation (via a fresh `EXPLAIN (ANALYZE, BUFFERS)` pass at all three sizes) that the v0.4.0 crossover reversed rather than persisted, in `RESULTS.md`'s `### Graph traversal, five-hop` subsection | PR #78 | Implemented/Verified |
| `STORAGE-014-FR-001..005` | `PRODUCTION-STORE-PORTABILITY` | ADR-0016 (Accepted) — companion record blob (`src/durability/record_blob.rs`, new), `ProductionStore::open_portable`, `DurabilityError::RecordBlobUnreadable` | `src/durability/record_blob.rs` (new: `RecordBlob`/`EncodedRecordBlob`, `companion_path`, header/version/fingerprint/decode checks, crash-safe `write`, header-only `is_current_at`; v0.2.0: `BLOB_VERSION = 2`, inline `Fnv1a64`, `RecordBlob::fingerprint`), `src/durability/mod.rs` (`pub(crate) mod record_blob`, `RecordBlobUnreadable` variant), `src/production.rs` (`create` writes the blob, `open` compares the header fingerprint and rewrites only if stale — v0.2.0; v0.1.0 byte-compared a full serialization — new `open_portable`); `src/durability/mmap_store.rs` untouched (empty diff) | `record_blob.rs`'s 12 unit tests (round trip incl. `breed`/edges, missing/short/wrong-magic/wrong-version/truncated/fingerprint-mismatched blobs all typed errors not panics, whole-file replacement consuming a stale temp file, header-only `is_current_at` incl. an age-only change reported as current, a version-1 blob reported as a version mismatch and as stale, the fingerprint pinned to a known value / order-sensitive / age-insensitive) + `production.rs`'s 6 tests (`open_portable` from the path alone, both files copied to a fresh directory, missing blob is `RecordBlobUnreadable` naming the companion path while `open` still works, `open` heals a legacy ages-only directory, `open` rewrites the blob only when the record set changed — mtime and bytes unchanged otherwise, including after an age-only change; a version-1 blob upgraded in place by `open`) + `MmapAgeStore`'s existing suite passing unmodified + `cargo test`/`cargo test --all-features`/`cargo fmt --check`/`clippy --all-targets --all-features -D warnings`/`cargo doc` clean + a release-build `create`/`open`/`open_portable` cost measurement at 1K/100K/1M in `RESULTS.md`'s `### ProductionStore file portability` subsection, re-measured for v0.2.0 in its `#### Follow-up: header fingerprint` subsection | PR #84 | Implemented/Verified |

Trace both directions: requirements reach evidence, and material code has
a requirement, defect, maintenance policy, or ADR. Update this table in
the same PR that changes implementation state.
